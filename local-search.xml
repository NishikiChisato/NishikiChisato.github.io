<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>LC 819.最常见的单词</title>
    <link href="/2022/09/23/Algorithm%20Solution/LC-819.%E6%9C%80%E5%B8%B8%E8%A7%81%E7%9A%84%E5%8D%95%E8%AF%8D/"/>
    <url>/2022/09/23/Algorithm%20Solution/LC-819.%E6%9C%80%E5%B8%B8%E8%A7%81%E7%9A%84%E5%8D%95%E8%AF%8D/</url>
    
    <content type="html"><![CDATA[<h4 id="最常见的单词"><a href="https://leetcode.cn/problems/most-common-word/">819.最常见的单词</a></h4><p>给定一个段落 (paragraph) 和一个禁用单词列表(banned)。返回出现次数最多，同时不在禁用列表中的单词。</p><p>题目保证至少有一个词不在禁用列表中，而且答案唯一。</p><p>禁用列表中的单词用小写字母表示，不含标点符号。段落中的单词不区分大小写。答案都是小写字母。</p><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs makefile"><span class="hljs-section">输入: </span><br>paragraph = <span class="hljs-string">&quot;Bob hit a ball, the hit BALL flew far after it was hit.&quot;</span><br>banned = [<span class="hljs-string">&quot;hit&quot;</span>]<br><span class="hljs-section">输出: &quot;ball&quot;</span><br><span class="hljs-section">解释: </span><br><span class="hljs-string">&quot;hit&quot;</span> 出现了3次，但它是一个禁用的单词。<br><span class="hljs-string">&quot;ball&quot;</span> 出现了2次 (同时没有其他单词出现2次)，所以它是段落里出现次数最多的，且不在禁用列表中的单词。 <br>注意，所有这些单词在段落里不区分大小写，标点符号需要忽略（即使是紧挨着单词也忽略， 比如 <span class="hljs-string">&quot;ball,&quot;</span>）， <br><span class="hljs-string">&quot;hit&quot;</span>不是最终的答案，虽然它出现次数更多，但它在禁用单词列表中。<br></code></pre></td></tr></table></figure><p>​</p><h4 id="计数-哈希表">计数 + 哈希表</h4><p>一个简单的模拟题</p><p>我们遍历得到所有的单词，将不在 <code>banned</code>数组当中的单词加入到哈希表当中，最后统计最大出现次数的单词即可</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function">string <span class="hljs-title">mostCommonWord</span><span class="hljs-params">(string paragraph, vector&lt;string&gt;&amp; banned)</span></span><br><span class="hljs-function"></span>&#123;<br>unordered_map&lt;string, <span class="hljs-type">int</span>&gt;mp;<br>string str;<br><span class="hljs-type">int</span> maxtime = INT32_MIN;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt;= paragraph.<span class="hljs-built_in">length</span>(); i++)<br>&#123;<br><span class="hljs-keyword">if</span> (i &lt; paragraph.<span class="hljs-built_in">length</span>() &amp;&amp; <span class="hljs-built_in">isalpha</span>(paragraph[i]))<br>str.<span class="hljs-built_in">push_back</span>(<span class="hljs-built_in">tolower</span>(paragraph[i]));<br><span class="hljs-keyword">else</span><br>&#123;<br><span class="hljs-keyword">if</span> (!str.<span class="hljs-built_in">empty</span>() &amp;&amp; <span class="hljs-built_in">find</span>(banned.<span class="hljs-built_in">begin</span>(), banned.<span class="hljs-built_in">end</span>(), str) == banned.<span class="hljs-built_in">end</span>())<br>&#123;<br>mp[str]++;<br>maxtime = <span class="hljs-built_in">max</span>(maxtime, mp[str]);<br>&#125;<br>str = <span class="hljs-string">&quot;&quot;</span>;<br>&#125;<br>&#125;<br>string ret;<br><span class="hljs-keyword">for</span> (unordered_map&lt;string, <span class="hljs-type">int</span>&gt;::iterator it = mp.<span class="hljs-built_in">begin</span>(); it != mp.<span class="hljs-built_in">end</span>(); it++)<br><span class="hljs-keyword">if</span> (it-&gt;second == maxtime)<br>ret = it-&gt;first;<br><span class="hljs-keyword">return</span> ret;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n+m)\)</span> ，<span class="math inline">\(n\)</span> 与 <span class="math inline">\(m\)</span> 分别是 <code>paragraph</code> 和<code>banned</code> 的长度，哈希表插入删除查找的时间复杂度均为 <span class="math inline">\(O(1)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>哈希表</tag>
      
      <tag>字符串</tag>
      
      <tag>计数</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 211.添加与搜索单词 - 数据结构设计</title>
    <link href="/2022/09/22/Algorithm%20Solution/LC-211.%E6%B7%BB%E5%8A%A0%E4%B8%8E%E6%90%9C%E7%B4%A2%E5%8D%95%E8%AF%8D---%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E8%AE%BE%E8%AE%A1/"/>
    <url>/2022/09/22/Algorithm%20Solution/LC-211.%E6%B7%BB%E5%8A%A0%E4%B8%8E%E6%90%9C%E7%B4%A2%E5%8D%95%E8%AF%8D---%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E8%AE%BE%E8%AE%A1/</url>
    
    <content type="html"><![CDATA[<h4 id="添加与搜索单词---数据结构设计"><a href="https://leetcode.cn/problems/design-add-and-search-words-data-structure/">211.添加与搜索单词 - 数据结构设计</a></h4><p>难度中等461收藏分享切换为英文接收动态反馈</p><p>请你设计一个数据结构，支持 添加新单词 和查找字符串是否与任何先前添加的字符串匹配 。</p><p>实现词典类 <code>WordDictionary</code> ：</p><ul><li><code>WordDictionary()</code> 初始化词典对象</li><li><code>void addWord(word)</code> 将 <code>word</code>添加到数据结构中，之后可以对它进行匹配</li><li><code>bool search(word)</code> 如果数据结构中存在字符串与<code>word</code> 匹配，则返回 <code>true</code> ；否则，返回<code>false</code> 。<code>word</code> 中可能包含一些 <code>'.'</code>，每个 <code>.</code> 都可以表示任何一个字母。</li></ul><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">输入：<br>[&quot;WordDictionary&quot;,&quot;addWord&quot;,&quot;addWord&quot;,&quot;addWord&quot;,&quot;search&quot;,&quot;search&quot;,&quot;search&quot;,&quot;search&quot;]<br>[[],[&quot;bad&quot;],[&quot;dad&quot;],[&quot;mad&quot;],[&quot;pad&quot;],[&quot;bad&quot;],[&quot;.ad&quot;],[&quot;b..&quot;]]<br>输出：<br>[<span class="hljs-keyword">null</span>,<span class="hljs-keyword">null</span>,<span class="hljs-keyword">null</span>,<span class="hljs-keyword">null</span>,<span class="hljs-keyword">false</span>,<span class="hljs-keyword">true</span>,<span class="hljs-keyword">true</span>,<span class="hljs-keyword">true</span>]<br><br>解释：<br>WordDictionary wordDictionary = <span class="hljs-built_in">new</span> WordDictionary();<br>wordDictionary.addWord(&quot;bad&quot;);<br>wordDictionary.addWord(&quot;dad&quot;);<br>wordDictionary.addWord(&quot;mad&quot;);<br>wordDictionary.<span class="hljs-keyword">search</span>(&quot;pad&quot;); // 返回 <span class="hljs-keyword">False</span><br>wordDictionary.<span class="hljs-keyword">search</span>(&quot;bad&quot;); // 返回 <span class="hljs-keyword">True</span><br>wordDictionary.<span class="hljs-keyword">search</span>(&quot;.ad&quot;); // 返回 <span class="hljs-keyword">True</span><br>wordDictionary.<span class="hljs-keyword">search</span>(&quot;b..&quot;); // 返回 <span class="hljs-keyword">True</span><br></code></pre></td></tr></table></figure><h4 id="模拟">模拟</h4><p>这是一道简单的 Trie 练习题，我们唯一需要注意的是，在出现<code>.</code>的时候，需要将该节点的所有孩子全部遍历一次，反之我们只需要遍历一个孩子</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">WordDictionary</span> <br>&#123;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">bool</span> isEnd;<br>vector&lt;WordDictionary*&gt;next;<br><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">dfs</span><span class="hljs-params">(WordDictionary* root, <span class="hljs-type">int</span> inx, string word)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (inx == word.<span class="hljs-built_in">length</span>())<br><span class="hljs-keyword">return</span> root-&gt;isEnd;<br><span class="hljs-type">char</span> c = word[inx];<br><span class="hljs-keyword">if</span> (c == <span class="hljs-string">&#x27;.&#x27;</span>)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; root-&gt;next.<span class="hljs-built_in">size</span>(); i++)<br><span class="hljs-keyword">if</span> (root-&gt;next[i] != <span class="hljs-literal">nullptr</span> &amp;&amp; <span class="hljs-built_in">dfs</span>(root-&gt;next[i], inx + <span class="hljs-number">1</span>, word))<br><span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br><span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>&#125;<br><span class="hljs-keyword">else</span><br>&#123;<br><span class="hljs-keyword">if</span> (root-&gt;next[c - <span class="hljs-string">&#x27;a&#x27;</span>] == <span class="hljs-literal">nullptr</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">dfs</span>(root-&gt;next[c - <span class="hljs-string">&#x27;a&#x27;</span>], inx + <span class="hljs-number">1</span>, word);<br>&#125;<br>&#125;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">WordDictionary</span>() <br>&#123;<br>isEnd = <span class="hljs-literal">false</span>;<br>next.<span class="hljs-built_in">assign</span>(<span class="hljs-number">26</span>, <span class="hljs-literal">nullptr</span>);<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">addWord</span><span class="hljs-params">(string word)</span> </span><br><span class="hljs-function"></span>&#123;<br>WordDictionary* node = <span class="hljs-keyword">this</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">char</span> c : word)<br>&#123;<br><span class="hljs-keyword">if</span> (node-&gt;next[c - <span class="hljs-string">&#x27;a&#x27;</span>] == <span class="hljs-literal">nullptr</span>)<br>node-&gt;next[c - <span class="hljs-string">&#x27;a&#x27;</span>] = <span class="hljs-keyword">new</span> <span class="hljs-built_in">WordDictionary</span>();<br>node = node-&gt;next[c - <span class="hljs-string">&#x27;a&#x27;</span>];<br>&#125;<br>node-&gt;isEnd = <span class="hljs-literal">true</span>;<br><span class="hljs-keyword">return</span>;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">search</span><span class="hljs-params">(string word)</span> </span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">dfs</span>(<span class="hljs-keyword">this</span>, <span class="hljs-number">0</span>, word);<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Trie</tag>
      
      <tag>数据结构设计</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Union-Find 算法</title>
    <link href="/2022/09/22/%E7%AE%97%E6%B3%954/Union-Find-%E7%AE%97%E6%B3%95/"/>
    <url>/2022/09/22/%E7%AE%97%E6%B3%954/Union-Find-%E7%AE%97%E6%B3%95/</url>
    
    <content type="html"><![CDATA[<h3 id="问题描述">问题描述</h3><p>问题的输入是一对整数对，其中每个整数都表示某种类型的对象，一对整数 pq 可以被理解为「p 和 q是相连的」，我们假设「相连」是一种等价关系，那么有：</p><ul><li>自反性：p 和 p 相连</li><li>对称性：p 和 q 相连，q 和 p 也相连</li><li>传递性：p 和 q 相连且 q 和 r 相连，那么 p 和 r 也相连</li></ul><p>显而易见这是「等价关系」的三条性质，我们通过「等价关系」可以将集合划分成多个「等价类」。在这里，我们定义同一等价类当中的任意两个数均是相连的。</p><p>我们的目标是过滤掉一个序列当中所有「相连」的整数对。换句话说，当我输入一个整数对p 和 q ，如果 p q相连，那么程序自动跳过并处理下一对整数对，如果二者不相连，那么程序应当输出这对整数对</p><p>在这里，为了便于讨论，我们将对象称为<strong>触点</strong>，将整数对称为<strong>连接</strong>，将各个等价类称为<strong>连通分量</strong>，除此之外，我们用<span class="math inline">\(0\)</span> 到 <span class="math inline">\(N-1\)</span> 的整数来表示 <span class="math inline">\(N\)</span> 个触点</p><p>​</p><h3 id="算法接口">算法接口</h3><p>我们设计如下的接口：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">UF</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">UF</span>(<span class="hljs-type">int</span> N);<span class="hljs-comment">//用整数表示N个触点</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Union</span><span class="hljs-params">(<span class="hljs-type">int</span> p, <span class="hljs-type">int</span> q)</span></span>;<span class="hljs-comment">//在p和q之间添加一条连接</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> p)</span></span>;<span class="hljs-comment">//p所在连通分量的标识符</span><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">connected</span><span class="hljs-params">(<span class="hljs-type">int</span> p, <span class="hljs-type">int</span> q)</span></span>;<span class="hljs-comment">//如果p和q是连通的返回true</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">count</span><span class="hljs-params">()</span></span>;<span class="hljs-comment">//连通分量的数量</span><br>    <span class="hljs-comment">//一开始我们有N个分量，将两个分量归并的Union操作会使总连通分量数减一</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>由于每个触点和分量都会用 <code>int</code>类型来表示，那么在底层设计中，我们<strong>维护一个「以触点为索引」的<code>id[]</code>数组，来表示所有的连通分量</strong>。我们使用连通分量当中某个触点的名称来表示整个连通分量，因此只要部分触点属于同一个连通分量，那么它们对应的元素值（<code>id[i]</code>）都会相同。</p><p>一开始，我们有 <span class="math inline">\(N\)</span>个触点，每个触点都单独表示了一个分量。由于 <code>id</code>数组的值会用来表示连通分量，为避免冲突，我们将 <code>id[i]</code>的值初始化成 <code>i</code> 。我们使用 <code>find</code>方法来判断触点（也就是数组索引）属于哪一个连通分量（也就是索引对应的值）。</p><p>基于 <code>find</code> 接口的声明，我们很容易得到<code>connected</code>方法的实现，它只有一条语句：<code>find(p) == find(q)</code> ，并返回一个<code>bool</code> 值</p><p>除此之外，我们还需要维护一个变量 <code>count</code>来表示当前的连通分量个数</p><p>到此为止，我们给出如下实现：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">UF</span><br>&#123;<br><span class="hljs-keyword">private</span>:<br>vector&lt;<span class="hljs-type">int</span>&gt;id;<br><span class="hljs-type">int</span> count;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">UF</span>(<span class="hljs-type">int</span> N)<span class="hljs-comment">//用整数表示N个触点</span><br>&#123;<br>count = N;<br>id.<span class="hljs-built_in">assign</span>(N, <span class="hljs-number">0</span>);<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; N; i++)<br>id[i] = i;<br>&#125;<br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">connected</span><span class="hljs-params">(<span class="hljs-type">int</span> p, <span class="hljs-type">int</span> q)</span><span class="hljs-comment">//如果p和q是连通的返回true</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">find</span>(p) == <span class="hljs-built_in">find</span>(q);<br>&#125;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">count</span><span class="hljs-params">()</span><span class="hljs-comment">//连通分量的数量</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> count;<br>&#125;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Union</span><span class="hljs-params">(<span class="hljs-type">int</span> p, <span class="hljs-type">int</span> q)</span></span>;<span class="hljs-comment">//在p和q之间添加一条连接</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> p)</span></span>;<span class="hljs-comment">//p所在连通分量的标识符</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>我们分析的重点将放在 <code>Union</code> 和 <code>find</code>的实现上面</p><p>​</p><h3 id="union-和-find-的实现">Union 和 find 的实现</h3><h4 id="quick-find-实现">quick-find 实现</h4><p>根据连通的定义，我们很容易想到：当 <code>id[p]</code> 与<code>id[q]</code> 相等时，我们说 p 和 q是连通的。也就是说，在同一个连通分量当中，所有的 <code>id[i]</code>全部都相同。在 <code>find</code> 的实现部分，只要 <code>id[p]</code> 与<code>id[q]</code> 的值相等，那么便可以返回 <code>true</code> 。在<code>Union</code>的实现部分，我们首先需要检查二者是否处于同一个连通分量，如果是则不需要做任何动作，否则我们需要将二者的值<strong>统一</strong>。为此，我们需要遍历<strong>整个</strong>数组，让<code>id[q]</code> 的值全部变为 <code>id[p]</code> 的值</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Union</span><span class="hljs-params">(<span class="hljs-type">int</span> p, <span class="hljs-type">int</span> q)</span><span class="hljs-comment">//在p和q之间添加一条连接</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> pID = <span class="hljs-built_in">find</span>(p);<br>    <span class="hljs-type">int</span> qID = <span class="hljs-built_in">find</span>(q);<br>    <span class="hljs-keyword">if</span> (pID == qID)<br>        <span class="hljs-keyword">return</span>;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; id.<span class="hljs-built_in">size</span>(); i++)<br>        <span class="hljs-keyword">if</span> (id[i] == pID)<br>            id[i] = qID;<br>    count--;<span class="hljs-comment">//不要忘了将连通分量减一</span><br>&#125;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> p)</span><span class="hljs-comment">//p所在连通分量的标识符</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">return</span> id[p];<br>&#125;<br></code></pre></td></tr></table></figure><p>​</p><h4 id="quick-find-算法分析">quick-find 算法分析</h4><p>在此，我们统计的是<strong>访问数组的次数</strong>，即读取数组与向数组写入我们都认为是访问数组</p><p>在 <code>find</code>算法实现中，我们访问数组的次数是<strong>一次</strong></p><p>在 <code>connected</code> 算法实现中，我们需要调用两次<code>find</code> 函数</p><p>在 <code>Union</code>算法实现中，我们<strong>至少需要调用两次</strong> <code>find</code>函数。在数组读取的部分，我们需要读取 <span class="math inline">\(N\)</span> 次来进行 <code>if</code>的判断；在写入数组的部分，在最坏的情况下，我们需要写入 <span class="math inline">\(N-1\)</span> 次，最好的情况下是写入一次。</p><p>因此，在当前 <code>Union</code> 的实现中，我们需要访问的次数为 <span class="math inline">\(N+3\)</span> 到 <span class="math inline">\(2N+1\)</span></p><p>我们考虑一个极端的情况，我们一开始输入了 <span class="math inline">\(N\)</span> 个点，也就是 <span class="math inline">\(N\)</span> 个连通分量，假设我们调用 <span class="math inline">\(N-1\)</span> 次 <code>Union</code>函数来使得只存在一个连通分量的时候，这时最少的访问次数为 <span class="math inline">\((N-1)(N+3) \sim N^2\)</span> （在这里，我们使用<span class="math inline">\(\sim\)</span> 来表述等价）</p><p>这个时间复杂度是平方级别的，显然，我们需要去优化一下这个算法</p><p>​</p><h4 id="quick-union-算法">quick-Union 算法</h4><p>我们采取使用 quick-find 相同的数据结构——以触点作为数组索引，但我们对<code>id[i]</code> 的值赋予不同的意义。具体来说，我们让<code>id[i]</code>的值表示同一连通分量当中某个触点的名称（也就是索引），当然，这个值也可以是自己本身。我们称这种联系为链接</p><p>在 <code>find</code>的实现中，我们从给定触点开始，通过链接找到下一个触点，不断将这个过程进行下去，最后我们一定能够找到一个指向自己的触点，即链接指向自己的触点（我们称之为根节点）。我们可以证明，这样的点必然存在</p><p>在 <code>Union</code> 的实现中，我们获取到 p 和 q的根节点的数值，我们让 p 的根节点数值等于 q 的根节点的数值，这样便实现了p 与 q 的连通</p><p>在我们初始化时，对于每个触点的值都是它本身的名称，也就是说每个触点的链接都指向自己（即每个节点都是根节点）。那么对于一个根节点而言，在执行<code>Union</code> 前后它都具有此性质，因此我们说这种点必然存在</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Union</span><span class="hljs-params">(<span class="hljs-type">int</span> p, <span class="hljs-type">int</span> q)</span><span class="hljs-comment">//在p和q之间添加一条连接</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> pID = <span class="hljs-built_in">find</span>(p);<br>    <span class="hljs-type">int</span> qID = <span class="hljs-built_in">find</span>(q);<br>    <span class="hljs-keyword">if</span> (pID == qID)<br>        <span class="hljs-keyword">return</span>;<br>    id[pID] = qID;<br>    count--;<br>&#125;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> p)</span><span class="hljs-comment">//p所在连通分量的标识符</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">while</span> (p != id[p])<br>        p = id[p];<br>    <span class="hljs-keyword">return</span> p;<br>&#125;<br></code></pre></td></tr></table></figure><p>​</p><h4 id="quick-union-算法分析">quick-Union 算法分析</h4><p>我们给出如下定义：</p><p>一棵树的<strong>大小</strong>是其节点的数量，节点的<strong>深度</strong>是它到根节点路径上的链接数，数的<strong>高度</strong>为所有节点当中的最大深度</p><p>在 <code>find</code>算法的实现中，数值读取次数为<strong>给定触点的深度加一</strong>（最后还需要访问一次来退出循环），写入次数为<strong>给定触点的深度</strong></p><p>在 <code>Union</code> 算法的实现中，只有两次调用 <code>find</code>函数的成本（如果两个触点不在同一个连通分量还需要加一）</p><p>在这里，我们再次考虑一个极端情况，输入数对为：0-1、0-2、0-3 等（即 0链接到 1，1 链接到 2，如此这般），执行 <span class="math inline">\(N-1\)</span> 次 <code>Union</code>后我们将会得到一个连通分量，我们先在来分析这个过程当中数值的访问次数</p><p>一般地，对于数对 <span class="math inline">\(0-i\)</span> ，触点 0的深度为 <span class="math inline">\(i-1\)</span> ，触点 <span class="math inline">\(i\)</span> 的深度为 0 ，那么执行一次<code>Union</code> 函数的数组访问次数为 <span class="math inline">\(2N+1\)</span> （<span class="math inline">\(find(0)=2(i-1)+1=2i-1\)</span>，<span class="math inline">\(find(i)=2*0+1=1\)</span>，<span class="math inline">\(Union(i,0)=(2i-1)+1+1=2i+1\)</span>）</p><p><span class="math inline">\(N-1\)</span> 次 <code>Union</code>函数后，数组访问次数为：<span class="math inline">\(3+5+8+\cdots+(2N-1)\sim N^2\)</span></p><p>​</p><h4 id="加权-quick-union-算法">加权 quick-Union 算法</h4><p>刚刚的连通过程我们可以认为是将<strong>两棵树的根节点之间相连</strong>，而算法的时间复杂度部分主要取决于<strong>树的高度</strong>。因此，如果我们将大树的根节点链接到小树上面，整棵树的高度必然会增加，但如果反过来则不一定</p><p>因此，我们可以用另外一个数组来记录每个连通分量的大小，我们<strong>总让小树的根节点链接到大树的根节点上</strong>，这样便可以降低时间复杂度</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">UF</span><br>&#123;<br><span class="hljs-keyword">private</span>:<br>vector&lt;<span class="hljs-type">int</span>&gt;id;<br>vector&lt;<span class="hljs-type">int</span>&gt;sz;<br><span class="hljs-type">int</span> count;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">UF</span>(<span class="hljs-type">int</span> N)<span class="hljs-comment">//用整数表示N个触点</span><br>&#123;<br>count = N;<br>id.<span class="hljs-built_in">assign</span>(N, <span class="hljs-number">0</span>);<br>sz.<span class="hljs-built_in">assign</span>(N, <span class="hljs-number">1</span>);<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; N; i++)<br>id[i] = i;<br>&#125;<br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">connected</span><span class="hljs-params">(<span class="hljs-type">int</span> p, <span class="hljs-type">int</span> q)</span><span class="hljs-comment">//如果p和q是连通的返回true</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">find</span>(p) == <span class="hljs-built_in">find</span>(q);<br>&#125;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">count</span><span class="hljs-params">()</span><span class="hljs-comment">//连通分量的数量</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> count;<br>&#125;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Union</span><span class="hljs-params">(<span class="hljs-type">int</span> p, <span class="hljs-type">int</span> q)</span><span class="hljs-comment">//在p和q之间添加一条连接</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> pID = <span class="hljs-built_in">find</span>(p);<br><span class="hljs-type">int</span> qID = <span class="hljs-built_in">find</span>(q);<br><span class="hljs-keyword">if</span> (pID == qID)<br><span class="hljs-keyword">return</span>;<br><span class="hljs-keyword">if</span> (sz[pID] &gt; sz[qID])<br>&#123;<br>id[qID] = pID;<br>sz[pID] += sz[qID];<br>&#125;<br><span class="hljs-keyword">else</span><br>&#123;<br>id[pID] = id[qID];<br>sz[qID] += sz[pID];<br>&#125;<br>count--;<br>&#125;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> p)</span><span class="hljs-comment">//p所在连通分量的标识符</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">while</span> (p != id[p])<br>p = id[p];<br><span class="hljs-keyword">return</span> p;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>关于这个算法的时间复杂度，书中只是说，<code>Union</code> 是 <span class="math inline">\(logN\)</span> ，<code>find</code> 是 <span class="math inline">\(logN\)</span></p><p>​</p><h3 id="最后">最后</h3><p>有一说一，这本书讲的实在是太好了，我是昨天晚上十点多开始看的，他一开始抛出了一个算法，然后再一步一步引领我去思考如何去优化这个算法，整个过程十分的顺畅。昨天一上头，看到了一点半，哈哈哈</p>]]></content>
    
    
    <categories>
      
      <category>算法4</category>
      
    </categories>
    
    
    <tags>
      
      <tag>图</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 2.两数相加</title>
    <link href="/2022/09/21/Algorithm%20Solution/LC-2.%E4%B8%A4%E6%95%B0%E7%9B%B8%E5%8A%A0/"/>
    <url>/2022/09/21/Algorithm%20Solution/LC-2.%E4%B8%A4%E6%95%B0%E7%9B%B8%E5%8A%A0/</url>
    
    <content type="html"><![CDATA[<h4 id="两数相加"><a href="https://leetcode.cn/problems/add-two-numbers/">2.两数相加</a></h4><p>给你两个 <strong>非空</strong>的链表，表示两个非负的整数。它们每位数字都是按照 <strong>逆序</strong>的方式存储的，并且每个节点只能存储 <strong>一位</strong> 数字。</p><p>请你将两个数相加，并以相同形式返回一个表示和的链表。</p><p>你可以假设除了数字 0 之外，这两个数都不会以 0 开头。</p><figure class="highlight inform7"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs inform7">输入：l1 = <span class="hljs-comment">[2,4,3]</span>, l2 = <span class="hljs-comment">[5,6,4]</span><br>输出：<span class="hljs-comment">[7,0,8]</span><br>解释：342 + 465 = 807.<br></code></pre></td></tr></table></figure><p>​</p><h4 id="模拟">模拟</h4><p>一般来说，链表这种问题，我们会先在前面建立一个前置节点（其节点值为 0），这样处理起来更方便</p><p>模拟没什么好说的，直接看代码就行</p><p>记得最高位的进位如果为 1 的话，需要另开一个节点</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function">ListNode* <span class="hljs-title">addTwoNumbers</span><span class="hljs-params">(ListNode* l1, ListNode* l2)</span></span><br><span class="hljs-function"></span>&#123;<br>ListNode* pre = <span class="hljs-keyword">new</span> <span class="hljs-built_in">ListNode</span>(<span class="hljs-number">0</span>), * cur = pre;<br><span class="hljs-type">int</span> carry = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">while</span> (l1 != <span class="hljs-literal">nullptr</span> || l2 != <span class="hljs-literal">nullptr</span>)<br>&#123;<br><span class="hljs-type">int</span> x = l1 ? l1-&gt;val : <span class="hljs-number">0</span>;<br><span class="hljs-type">int</span> y = l2 ? l2-&gt;val : <span class="hljs-number">0</span>;<br><span class="hljs-type">int</span> sum = x + y + carry;<br>carry = sum / <span class="hljs-number">10</span>;<br>sum %= <span class="hljs-number">10</span>;<br>cur-&gt;next = <span class="hljs-keyword">new</span> <span class="hljs-built_in">ListNode</span>(sum);<br>cur = cur-&gt;next;<br><span class="hljs-keyword">if</span> (l1)<br>l1 = l1-&gt;next;<br><span class="hljs-keyword">if</span> (l2)<br>l2 = l2-&gt;next;<br>&#125;<br><span class="hljs-keyword">if</span> (carry == <span class="hljs-number">1</span>)<br>cur-&gt;next = <span class="hljs-keyword">new</span> <span class="hljs-built_in">ListNode</span>(carry);<br><span class="hljs-keyword">return</span> pre-&gt;next;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><h4 id="递归">递归</h4><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function">ListNode* <span class="hljs-title">add</span><span class="hljs-params">(ListNode* l1, ListNode* l2, <span class="hljs-type">int</span> n)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (l1 == <span class="hljs-literal">nullptr</span> &amp;&amp; l2 == <span class="hljs-literal">nullptr</span> &amp;&amp; n == <span class="hljs-number">0</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-literal">nullptr</span>;<br>ListNode* cur = <span class="hljs-keyword">new</span> <span class="hljs-built_in">ListNode</span>(((l1 ? l1-&gt;val : <span class="hljs-number">0</span>) + (l2 ? l2-&gt;val : <span class="hljs-number">0</span>) + n) % <span class="hljs-number">10</span>);<br>cur-&gt;next = <span class="hljs-built_in">add</span>(l1 ? l1-&gt;next : <span class="hljs-literal">nullptr</span>, l2 ? l2-&gt;next : <span class="hljs-literal">nullptr</span>, ((l1 ? l1-&gt;val : <span class="hljs-number">0</span>) + (l2 ? l2-&gt;val : <span class="hljs-number">0</span>) + n) / <span class="hljs-number">10</span>);<br>        <span class="hljs-keyword">return</span> cur;<br>&#125;<br><br><span class="hljs-function">ListNode* <span class="hljs-title">addTwoNumbers</span><span class="hljs-params">(ListNode* l1, ListNode* l2)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">add</span>(l1, l2, <span class="hljs-number">0</span>);<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>链表</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 208.实现 Trie (前缀树)</title>
    <link href="/2022/09/20/Algorithm%20Solution/LC-208.%E5%AE%9E%E7%8E%B0-Trie-(%E5%89%8D%E7%BC%80%E6%A0%91)/"/>
    <url>/2022/09/20/Algorithm%20Solution/LC-208.%E5%AE%9E%E7%8E%B0-Trie-(%E5%89%8D%E7%BC%80%E6%A0%91)/</url>
    
    <content type="html"><![CDATA[<h4 id="实现-trie-前缀树"><a href="https://leetcode.cn/problems/implement-trie-prefix-tree/">208.实现 Trie (前缀树)</a></h4><p><strong><a href="https://baike.baidu.com/item/字典树/9825209?fr=aladdin">Trie</a></strong>（发音类似"try"）或者说 <strong>前缀树</strong>是一种树形数据结构，用于高效地存储和检索字符串数据集中的键。这一数据结构有相当多的应用情景，例如自动补完和拼写检查。</p><p>请你实现 Trie 类：</p><ul><li><code>Trie()</code> 初始化前缀树对象。</li><li><code>void insert(String word)</code> 向前缀树中插入字符串<code>word</code> 。</li><li><code>boolean search(String word)</code> 如果字符串<code>word</code> 在前缀树中，返回<code>true</code>（即，在检索之前已经插入）；否则，返回<code>false</code> 。</li><li><code>boolean startsWith(String prefix)</code>如果之前已经插入的字符串 <code>word</code> 的前缀之一为<code>prefix</code> ，返回 <code>true</code> ；否则，返回<code>false</code> 。</li></ul><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">输入<br>[&quot;Trie&quot;, &quot;insert&quot;, &quot;search&quot;, &quot;search&quot;, &quot;startsWith&quot;, &quot;insert&quot;, &quot;search&quot;]<br>[[], [&quot;apple&quot;], [&quot;apple&quot;], [&quot;app&quot;], [&quot;app&quot;], [&quot;app&quot;], [&quot;app&quot;]]<br>输出<br>[<span class="hljs-keyword">null</span>, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">true</span>, <span class="hljs-keyword">false</span>, <span class="hljs-keyword">true</span>, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">true</span>]<br><br>解释<br>Trie trie = <span class="hljs-built_in">new</span> Trie();<br>trie.<span class="hljs-keyword">insert</span>(&quot;apple&quot;);<br>trie.<span class="hljs-keyword">search</span>(&quot;apple&quot;);   // 返回 <span class="hljs-keyword">True</span><br>trie.<span class="hljs-keyword">search</span>(&quot;app&quot;);     // 返回 <span class="hljs-keyword">False</span><br>trie.startsWith(&quot;app&quot;); // 返回 <span class="hljs-keyword">True</span><br>trie.<span class="hljs-keyword">insert</span>(&quot;app&quot;);<br>trie.<span class="hljs-keyword">search</span>(&quot;app&quot;);     // 返回 <span class="hljs-keyword">True</span><br></code></pre></td></tr></table></figure><h4 id="多叉树实现">多叉树实现</h4><p><span class="math inline">\(Trie\)</span>树，也称为字典树或前缀树，是一种用于快速查询「某个字符串、字符前缀」是否存在的数据结构</p><p>既然是树，我们先来看看它的节点构成</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Trie</span><br>&#123;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">bool</span> isEnd;<br>vector&lt;Trie*&gt;next;<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到，不同于典型树的结构——每个节点存储数值</p><p>Trie 树的<strong>节点</strong>存储的是一个 <code>bool</code>值，表示该节点<strong>是否为某个单词的结尾</strong></p><p>Trie树的边表示的是<strong>一个字符是否存在</strong>，这一部分是采取数组来存储的</p><p>如果该字符存在，那么指针指向下一个节点，如果不存在，那么该指针为空</p><p>这个东西的实现，其实不难，难的是具体应用，我们直接给出代码</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Trie</span> &#123;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">bool</span> isEnd;<br>vector&lt;Trie*&gt;next;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Trie</span>()<br>:<span class="hljs-built_in">isEnd</span>(<span class="hljs-literal">false</span>), <span class="hljs-built_in">next</span>(<span class="hljs-number">26</span>)<br>&#123;  &#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">insert</span><span class="hljs-params">(string word)</span></span><br><span class="hljs-function"></span>&#123;<br>Trie* node = <span class="hljs-keyword">this</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">char</span> c : word)<br>&#123;<br><span class="hljs-keyword">if</span> (node-&gt;next[c - <span class="hljs-string">&#x27;a&#x27;</span>] == <span class="hljs-literal">nullptr</span>)<br>node-&gt;next[c - <span class="hljs-string">&#x27;a&#x27;</span>] = <span class="hljs-keyword">new</span> <span class="hljs-built_in">Trie</span>();<br>node = node-&gt;next[c - <span class="hljs-string">&#x27;a&#x27;</span>];<br>&#125;<br>node-&gt;isEnd = <span class="hljs-literal">true</span>;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">search</span><span class="hljs-params">(string word)</span></span><br><span class="hljs-function"></span>&#123;<br>Trie* node = <span class="hljs-keyword">this</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">char</span> c : word)<br>&#123;<br><span class="hljs-keyword">if</span> (node-&gt;next[c - <span class="hljs-string">&#x27;a&#x27;</span>] == <span class="hljs-literal">nullptr</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>node = node-&gt;next[c - <span class="hljs-string">&#x27;a&#x27;</span>];<br>&#125;<br><span class="hljs-keyword">return</span> node-&gt;isEnd;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">startsWith</span><span class="hljs-params">(string prefix)</span></span><br><span class="hljs-function"></span>&#123;<br>Trie* node = <span class="hljs-keyword">this</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">char</span> c : prefix)<br>&#123;<br><span class="hljs-keyword">if</span> (node-&gt;next[c - <span class="hljs-string">&#x27;a&#x27;</span>] == <span class="hljs-literal">nullptr</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>node = node-&gt;next[c - <span class="hljs-string">&#x27;a&#x27;</span>];<br>&#125;<br><span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(len)\)</span> ，<span class="math inline">\(len\)</span> 为字符串长度</p><p>空间复杂度：<span class="math inline">\(O(n*len)\)</span> ，<span class="math inline">\(n\)</span> 为节点个数，<span class="math inline">\(len\)</span> 为字符集大小</p><p>​</p><h4 id="二维数组实现">二维数组实现</h4><p>需要注意的是，这个代码在力扣上面无法通过！！！</p><p>我们可以用一种简单的方式来解决这个问题</p><ul><li>我们用二维数组 <code>trie</code> 来存储各个节点</li><li>用 <code>index</code> 的自增来记录当前已有的节点数量</li><li>用 <code>count</code> 来记录当前节点作为「单词结尾」的次数</li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Trie</span> <br>&#123;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> N = <span class="hljs-number">1e5</span>;<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;trie;<span class="hljs-comment">//数组，用于存储各个节点</span><br>vector&lt;<span class="hljs-type">int</span>&gt;count;<span class="hljs-comment">//记录某个节点被标记为「某个单词的终点」的次数</span><br><span class="hljs-type">int</span> index;<span class="hljs-comment">//记录目前使用的节点数</span><br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Trie</span>() <br>&#123;<br>trie.<span class="hljs-built_in">assign</span>(N, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(<span class="hljs-number">26</span>, <span class="hljs-number">0</span>));<br>count.<span class="hljs-built_in">assign</span>(N, <span class="hljs-number">0</span>);<br><span class="hljs-type">int</span> index = <span class="hljs-number">0</span>;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">insert</span><span class="hljs-params">(string word)</span> </span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> pos = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">char</span> c : word)<br>&#123;<br><span class="hljs-keyword">if</span> (trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>] == <span class="hljs-number">0</span>)<br>trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>] = ++index;<br>pos = trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>];<br>&#125;<br>count[pos]++;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">search</span><span class="hljs-params">(string word)</span> </span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> pos = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">char</span> c : word)<br>&#123;<br><span class="hljs-keyword">if</span> (trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>] == <span class="hljs-number">0</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>pos = trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>];<br>&#125;<br><span class="hljs-keyword">return</span> count[pos] != <span class="hljs-number">0</span>;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">startsWith</span><span class="hljs-params">(string prefix)</span> </span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> pos = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">char</span> c : prefix)<br>&#123;<br><span class="hljs-keyword">if</span> (trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>] == <span class="hljs-number">0</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>pos = trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>];<br>&#125;<br><span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>​</p><h4 id="二维数组时间优化">二维数组（时间优化）</h4><p>显然，如果我们每定义一个 Trie对象，都需要一个很大的二维数组的开销</p><p>我们可以将这个二维数组转为静态，在每次执行构造函数的时候对以往的二维数组进行一次清理，这样便可以减小开销</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Trie</span> <br>&#123;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">static</span> <span class="hljs-type">const</span> <span class="hljs-type">int</span> N = <span class="hljs-number">100009</span>;<br><span class="hljs-type">static</span> vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">trie</span>(N, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(<span class="hljs-number">26</span>, <span class="hljs-number">0</span>));<span class="hljs-comment">//数组，用于存储各个节点</span><br><span class="hljs-type">static</span> vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">count</span>(N, <span class="hljs-number">0</span>);<span class="hljs-comment">//记录某个节点被标记为「某个单词的终点」的次数</span><br><span class="hljs-type">static</span> <span class="hljs-type">int</span> index = <span class="hljs-number">0</span>;<span class="hljs-comment">//记录目前使用的节点数</span><br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Trie</span>() <br>&#123;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = index; i &gt;= <span class="hljs-number">0</span>; i--)<br>        &#123;<br>            trie[i].<span class="hljs-built_in">assign</span>(<span class="hljs-number">26</span>, <span class="hljs-number">0</span>);<br>        &#125;<br>        count.<span class="hljs-built_in">assign</span>(N, <span class="hljs-number">0</span>);<br>        index = <span class="hljs-number">0</span>;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">insert</span><span class="hljs-params">(string word)</span> </span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> pos = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">char</span> c : word)<br>&#123;<br><span class="hljs-keyword">if</span> (trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>] == <span class="hljs-number">0</span>)<br>trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>] = ++index;<br>pos = trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>];<br>&#125;<br>count[pos]++;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">search</span><span class="hljs-params">(string word)</span> </span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> pos = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">char</span> c : word)<br>&#123;<br><span class="hljs-keyword">if</span> (trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>] == <span class="hljs-number">0</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>pos = trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>];<br>&#125;<br><span class="hljs-keyword">return</span> count[pos] != <span class="hljs-number">0</span>;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">startsWith</span><span class="hljs-params">(string prefix)</span> </span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> pos = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">char</span> c : prefix)<br>&#123;<br><span class="hljs-keyword">if</span> (trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>] == <span class="hljs-number">0</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>pos = trie[pos][c - <span class="hljs-string">&#x27;a&#x27;</span>];<br>&#125;<br><span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Trie</tag>
      
      <tag>数据结构设计</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 494.目标和</title>
    <link href="/2022/09/20/Algorithm%20Solution/LC-494.%E7%9B%AE%E6%A0%87%E5%92%8C/"/>
    <url>/2022/09/20/Algorithm%20Solution/LC-494.%E7%9B%AE%E6%A0%87%E5%92%8C/</url>
    
    <content type="html"><![CDATA[<h4 id="目标和"><a href="https://leetcode.cn/problems/target-sum/">494.目标和</a></h4><p>给你一个整数数组 <code>nums</code> 和一个整数 <code>target</code>。</p><p>向数组中的每个整数前添加 <code>'+'</code> 或 <code>'-'</code>，然后串联起所有整数，可以构造一个 <strong>表达式</strong> ：</p><ul><li>例如，<code>nums = [2, 1]</code> ，可以在 <code>2</code> 之前添加<code>'+'</code> ，在 <code>1</code> 之前添加 <code>'-'</code>，然后串联起来得到表达式 <code>"+2-1"</code> 。</li></ul><p>返回可以通过上述方法构造的、运算结果等于 <code>target</code> 的不同<strong>表达式</strong> 的数目。</p><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs tap">输入：nums = [1,1,1,1,1], target = 3<br>输出：5<br>解释：一共有<span class="hljs-number"> 5 </span>种方法让最终目标和为<span class="hljs-number"> 3 </span>。<br>-1 +<span class="hljs-number"> 1 </span>+<span class="hljs-number"> 1 </span>+<span class="hljs-number"> 1 </span>+<span class="hljs-number"> 1 </span>= 3<br>+1 -<span class="hljs-number"> 1 </span>+<span class="hljs-number"> 1 </span>+<span class="hljs-number"> 1 </span>+<span class="hljs-number"> 1 </span>= 3<br>+1 +<span class="hljs-number"> 1 </span>-<span class="hljs-number"> 1 </span>+<span class="hljs-number"> 1 </span>+<span class="hljs-number"> 1 </span>= 3<br>+1 +<span class="hljs-number"> 1 </span>+<span class="hljs-number"> 1 </span>-<span class="hljs-number"> 1 </span>+<span class="hljs-number"> 1 </span>= 3<br>+1 +<span class="hljs-number"> 1 </span>+<span class="hljs-number"> 1 </span>+<span class="hljs-number"> 1 </span>-<span class="hljs-number"> 1 </span>= 3<br></code></pre></td></tr></table></figure><h4 id="dfs">DFS</h4><p>这个数据量才 20 ，并且每个数据只有 + 或者 -两种操作，直接暴力搜索就完事</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">dfs</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> target, <span class="hljs-type">int</span> pos, <span class="hljs-type">int</span> cur)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (pos == nums.<span class="hljs-built_in">size</span>())<br><span class="hljs-keyword">return</span> cur == target ? <span class="hljs-number">1</span> : <span class="hljs-number">0</span>;<br><span class="hljs-type">int</span> x = <span class="hljs-built_in">dfs</span>(nums, target, pos + <span class="hljs-number">1</span>, cur + nums[pos]);<br><span class="hljs-type">int</span> y = <span class="hljs-built_in">dfs</span>(nums, target, pos + <span class="hljs-number">1</span>, cur - nums[pos]);<br><span class="hljs-keyword">return</span> x + y;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">findTargetSumWays</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> target)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">dfs</span>(nums, target, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>);<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(2^n)\)</span></p><p>空间复杂度：<span class="math inline">\(O(2^n)\)</span></p><h4 id="dfs-记忆化搜索">DFS + 记忆化搜索</h4><p>由于 <code>taeget</code>可能为负数，因此我们并不能直接用数组来去重，这里可以考虑用<code>map</code> 来去重</p><p>去重的时候我们需要并不能直接将 <code>cur</code> 来作为去重对象</p><p>我们去重的目的是避免重复计算，而如何判断此次计算是重复的就是我们需要思考的问题</p><p>在传统的动态规划自顶向下的 DFS 的去重当中，归根结底是对 DP数组下标的去重，因为如果这个下标对应的状态以及算过了，那么就不需要再算了</p><p>这里也是同理，我们并不能直接将 <code>cur</code>作为去重对象，原因在于，可能会存在其他的情况相加也等于<code>cur</code></p><p>我们观察 DFS函数的参数，我们需要<strong>依靠参数的部分来完成去重</strong></p><p>因为只要两次递归的参数一样，我们就一定能够确定后一次一定是重复的</p><p>参数部分是变量的只有 <code>pos</code> 和 <code>cur</code></p><p>那么我们可以构造一个字符串，用一个连接符来连接这两个数，这样便构造出了一个可以用于去重的「下标」</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>map&lt;string, <span class="hljs-type">int</span>&gt;cache;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">dfs</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> target, <span class="hljs-type">int</span> pos, <span class="hljs-type">int</span> cur)</span></span><br><span class="hljs-function"></span>&#123;<br>string str = <span class="hljs-built_in">to_string</span>(pos) + <span class="hljs-string">&quot;-&quot;</span> + <span class="hljs-built_in">to_string</span>(cur);<br><span class="hljs-keyword">if</span> (cache.<span class="hljs-built_in">find</span>(str) != cache.<span class="hljs-built_in">end</span>())<br><span class="hljs-keyword">return</span> cache[str];<br><span class="hljs-keyword">if</span> (pos == nums.<span class="hljs-built_in">size</span>())<br>&#123;<br>cache.<span class="hljs-built_in">insert</span>(<span class="hljs-built_in">pair</span>&lt;string, <span class="hljs-type">int</span>&gt;(str, cur == target ? <span class="hljs-number">1</span> : <span class="hljs-number">0</span>));<br><span class="hljs-keyword">return</span> cache[str];<br>&#125;<br><br><span class="hljs-type">int</span> x = <span class="hljs-built_in">dfs</span>(nums, target, pos + <span class="hljs-number">1</span>, cur + nums[pos]);<br><span class="hljs-type">int</span> y = <span class="hljs-built_in">dfs</span>(nums, target, pos + <span class="hljs-number">1</span>, cur - nums[pos]);<br>cache.<span class="hljs-built_in">insert</span>(<span class="hljs-built_in">pair</span>&lt;string, <span class="hljs-type">int</span>&gt;(str, x + y));<br><span class="hljs-keyword">return</span> cache[str];<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">findTargetSumWays</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> target)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">dfs</span>(nums, target, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>);<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n*\sum_{i=0}^{n}abs(nums[i]))\)</span>，后者<code>nums[i]</code> 当中任意个数的和 ，因为 <code>pos</code>的取值为 <span class="math inline">\(n\)</span> ，而 <code>cur</code>的取值为 <span class="math inline">\(\sum_{i=0}^{n}abs(nums[i])\)</span></p><p>空间复杂度：<span class="math inline">\(O(n*\sum_{i=0}^{n}abs(nums[i]))\)</span> 同上</p><h4 id="dp-数组">DP 数组</h4><p>我们 <code>dp</code> 数组的定义为：考虑前 <code>i</code>个数，恰好凑出 <code>j</code> 的所有不同表达式数目</p><p>显然，<code>dp[i][j]</code>的值只能从两个状态转移过来：<code>dp[i-1][j-nums[i-1]]</code> 与<code>dp[i-1][j+nums[i-1]]</code> 。从前一个数加上第 <code>i</code>个数与从后一个数减去第 <code>i</code> 个数</p><p>这道题的关键点在于：<code>j-nums[i-1]</code>可以是负数，但我们数组是不允许访问负数的</p><p>答案很简单，我们做一个偏移即可，那么这个偏移量的大小是什么</p><p>我们看一种最极限的情况，就是 <code>nums</code>数组当中所有值相加恰好等于 <code>target</code> 的情况</p><p>在这种情况下，我们的偏移量至少是 <code>nums</code>数组当中所有元素的绝对值之和</p><p>而对于其他情况，我们可以明确的是 ，我们让 <code>nums</code>数组当中所有数按照上面的方式进行加和，就一定不会出现问题</p><p>偏移量绝对不是 <code>2 * target</code></p><p>具体看下面代码：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">findTargetSumWays</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> target)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = nums.<span class="hljs-built_in">size</span>();<br><span class="hljs-type">int</span> sum = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i : nums)<br>sum += <span class="hljs-built_in">abs</span>(i);<br>        <span class="hljs-keyword">if</span>(sum &lt; <span class="hljs-built_in">abs</span>(target))<br>            <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(n + <span class="hljs-number">1</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(<span class="hljs-number">2</span> * sum + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>));<br>dp[<span class="hljs-number">0</span>][sum] = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= n; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = -sum; j &lt;= sum; j++)<br>&#123;<br><span class="hljs-keyword">if</span> (j - nums[i - <span class="hljs-number">1</span>] + sum &gt;= <span class="hljs-number">0</span>)<br>dp[i][j + sum] += dp[i - <span class="hljs-number">1</span>][j - nums[i - <span class="hljs-number">1</span>] + sum];<br><span class="hljs-keyword">if</span> (j + nums[i - <span class="hljs-number">1</span>] + sum &lt;= <span class="hljs-number">2</span> * sum)<br>dp[i][j + sum] += dp[i - <span class="hljs-number">1</span>][j + nums[i - <span class="hljs-number">1</span>] + sum];<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[n][sum + target]; <br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 879.盈利计划</title>
    <link href="/2022/09/20/Algorithm%20Solution/LC-879.%E7%9B%88%E5%88%A9%E8%AE%A1%E5%88%92/"/>
    <url>/2022/09/20/Algorithm%20Solution/LC-879.%E7%9B%88%E5%88%A9%E8%AE%A1%E5%88%92/</url>
    
    <content type="html"><![CDATA[<h4 id="盈利计划"><a href="https://leetcode.cn/problems/profitable-schemes/">879.盈利计划</a></h4><p>集团里有 <code>n</code>名员工，他们可以完成各种各样的工作创造利润。</p><p>第 <code>i</code> 种工作会产生 <code>profit[i]</code> 的利润，它要求<code>group[i]</code>名成员共同参与。如果成员参与了其中一项工作，就不能参与另一项工作。</p><p>工作的任何至少产生 <code>minProfit</code> 利润的子集称为<strong>盈利计划</strong> 。并且工作的成员总数最多为 <code>n</code>。</p><p>有多少种计划可以选择？因为答案很大，所以 <strong>返回结果模</strong><code>10^9 + 7</code> <strong>的值</strong>。</p><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs tap">输入：n = 5, minProfit = 3, group = [2,2], profit = [2,3]<br>输出：2<br>解释：至少产生<span class="hljs-number"> 3 </span>的利润，该集团可以完成工作<span class="hljs-number"> 0 </span>和工作<span class="hljs-number"> 1 </span>，或仅完成工作<span class="hljs-number"> 1 </span>。<br>总的来说，有两种计划。<br></code></pre></td></tr></table></figure><h4 id="三维-dp">三维 DP</h4><p>这里问的是有多少种<strong>可能的计划</strong>，基本上就是明说要用 DP了</p><p>我们来看看这里的「变量」有哪些？</p><p>所谓「变量」，指的是会影响<strong>「可能计划」</strong>的选择</p><p>显然，考虑工作的个数一定是一个</p><p>当然，还有参与成员个数以及产生的利润</p><p>由于需要产生超过 <code>minProfit</code>的才能称为盈利计划，因此我们可以这么定义：</p><p>考虑前 <code>i</code> 个工作，在使用员工数为 <code>j</code>，至少盈利 <code>k</code> 的情况下所有可能的选择个数为<code>dp[i][j][k]</code></p><p>显然，当我们不选择第 <code>i</code> 个工作时，有 <span class="math display">\[dp[i][j][k]=dp[i-1][j][k]\]</span> 当我们选择第 <code>i</code> 个工作时，有： <span class="math display">\[dp[i][j][k]=dp[i-1][j-group[i-1]][k-profit[i-1]]\]</span> 我们回顾 <code>dp</code>数组的定义，对于员工数，是不允许出现负数的。但对于至少盈利数<code>k</code> 而言，并没有规定其不能为负</p><p>也就是说，<code>k</code> 的值是允许为负数的</p><p>这里就有一个问题是，我们的 <code>k</code> 一般会从 0开始遍历，<strong>我们不会遍历负数那一边</strong></p><p>与此同时，就算我们真的想去遍历负数那一边，我们会发现一个问题是：<strong>每个工作产生的利润一定是非负数</strong>，也就是说，直接去遍历是没有意义的</p><p>那么该怎么办？</p><p>我们考虑为什么 <code>k - profit[i-1]</code> 会等于负数</p><p>我们 <code>dp</code> 数组的定义是至少在盈利为 <code>k</code>时，怎么怎么样</p><p>如果 <code>k - profit[i-1]</code>为负数的话，等同于至少盈利为负数的情况下，怎么怎么样</p><p>但由于我们每个工作的利润一定是一个非负数，不可能出现负数</p><p>这就说明，在到达该状态之前，我们一定没有做任何工作，不然不可能出现至少利润为负数的情况</p><p>没有做任何工作，也就相当于是<strong>当前利润至少为 0</strong></p><p>所以，我们可以将这个负数与 0 做一个等价映射，即： <span class="math display">\[dp[i][j][k]=dp[i-1][j-group[i-1]][max(k-profit[i-1],0)]\]</span> 完整代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-type">int</span> mod = <span class="hljs-number">1e9</span> + <span class="hljs-number">7</span>;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">profitableSchemes</span><span class="hljs-params">(<span class="hljs-type">int</span> n, <span class="hljs-type">int</span> minProfit, vector&lt;<span class="hljs-type">int</span>&gt;&amp; group, vector&lt;<span class="hljs-type">int</span>&gt;&amp; profit)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> m = group.<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&gt;<span class="hljs-built_in">dp</span>(m + <span class="hljs-number">1</span>, vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;(n + <span class="hljs-number">1</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(minProfit + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>)));<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt;= n; j++)<br>dp[<span class="hljs-number">0</span>][j][<span class="hljs-number">0</span>] = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= m; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt;= n; j++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> k = <span class="hljs-number">0</span>; k &lt;= minProfit; k++)<br>&#123;<br><span class="hljs-type">int</span> x = dp[i - <span class="hljs-number">1</span>][j][k];<br><span class="hljs-type">int</span> y = (j &gt;= group[i - <span class="hljs-number">1</span>]) ? dp[i - <span class="hljs-number">1</span>][j - group[i - <span class="hljs-number">1</span>]][<span class="hljs-built_in">max</span>(k - profit[i - <span class="hljs-number">1</span>], <span class="hljs-number">0</span>)] : <span class="hljs-number">0</span>;<br>dp[i][j][k] = (x + y + dp[i][j][k]) % mod;<br>&#125;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[m][n][minProfit];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^3)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n^3)\)</span></p><h4 id="二维空间压缩">二维空间压缩</h4><p>我们从后往前遍历，便可以实现空间的压缩</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-type">int</span> mod = <span class="hljs-number">1e9</span> + <span class="hljs-number">7</span>;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">profitableSchemes</span><span class="hljs-params">(<span class="hljs-type">int</span> n, <span class="hljs-type">int</span> minProfit, vector&lt;<span class="hljs-type">int</span>&gt;&amp; group, vector&lt;<span class="hljs-type">int</span>&gt;&amp; profit)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> m = group.<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(n + <span class="hljs-number">1</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(minProfit + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>));<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt;= n; i++)<br>dp[i][<span class="hljs-number">0</span>] = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= m; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = n; j &gt;= group[i - <span class="hljs-number">1</span>]; j--)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> k = minProfit; k &gt;= <span class="hljs-number">0</span>; k--)<br>&#123;<br><span class="hljs-type">int</span> x = dp[j][k];<br><span class="hljs-type">int</span> y = dp[j - group[i - <span class="hljs-number">1</span>]][<span class="hljs-built_in">max</span>(k - profit[i - <span class="hljs-number">1</span>], <span class="hljs-number">0</span>)];<br>dp[j][k] = (x + y) % mod;<br>&#125;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[n][minProfit];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^3)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n^2)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 474.一和零</title>
    <link href="/2022/09/19/Algorithm%20Solution/LC-474.%E4%B8%80%E5%92%8C%E9%9B%B6/"/>
    <url>/2022/09/19/Algorithm%20Solution/LC-474.%E4%B8%80%E5%92%8C%E9%9B%B6/</url>
    
    <content type="html"><![CDATA[<h4 id="一和零"><a href="https://leetcode.cn/problems/ones-and-zeroes/">474.一和零</a></h4><p>给你一个二进制字符串数组 <code>strs</code> 和两个整数 <code>m</code>和 <code>n</code> 。</p><p>请你找出并返回 <code>strs</code> 的最大子集的长度，该子集中<strong>最多</strong> 有 <code>m</code> 个 <code>0</code> 和<code>n</code> 个 <code>1</code> 。</p><p>如果 <code>x</code> 的所有元素也是 <code>y</code> 的元素，集合<code>x</code> 是集合 <code>y</code> 的 <strong>子集</strong> 。</p><figure class="highlight excel"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs excel">输入：strs = [<span class="hljs-string">&quot;10&quot;</span>, <span class="hljs-string">&quot;0001&quot;</span>, <span class="hljs-string">&quot;111001&quot;</span>, <span class="hljs-string">&quot;1&quot;</span>, <span class="hljs-string">&quot;0&quot;</span>], m = <span class="hljs-number">5</span>, <span class="hljs-built_in">n</span> = <span class="hljs-number">3</span><br>输出：<span class="hljs-number">4</span><br>解释：最多有 <span class="hljs-number">5</span> 个 <span class="hljs-number">0</span> 和 <span class="hljs-number">3</span> 个 <span class="hljs-number">1</span> 的最大子集是 &#123;<span class="hljs-string">&quot;10&quot;</span>,<span class="hljs-string">&quot;0001&quot;</span>,<span class="hljs-string">&quot;1&quot;</span>,<span class="hljs-string">&quot;0&quot;</span>&#125; ，因此答案是 <span class="hljs-number">4</span> 。<br>其他满足题意但较小的子集包括 &#123;<span class="hljs-string">&quot;0001&quot;</span>,<span class="hljs-string">&quot;1&quot;</span>&#125; 和 &#123;<span class="hljs-string">&quot;10&quot;</span>,<span class="hljs-string">&quot;1&quot;</span>,<span class="hljs-string">&quot;0&quot;</span>&#125; 。&#123;<span class="hljs-string">&quot;111001&quot;</span>&#125; 不满足题意，因为它含 <span class="hljs-number">4</span> 个 <span class="hljs-number">1</span> ，大于 <span class="hljs-built_in">n</span> 的值 <span class="hljs-number">3</span> 。<br></code></pre></td></tr></table></figure><h4 id="朴素三维-dp">朴素三维 DP</h4><p>这道题是一个多维背包的问题</p><p>做多了会有这种感觉：题目叫我们求最大值，一般都会往动态规划那个方向去靠</p><p>这道题里面，变量有三个：可选择的字符串，0 的个数，1 的个数</p><p>我们定义 <code>dp[i][j][k]</code> 为：考虑前 <code>i</code>个字符串，在 0 的个数为 <code>j</code> ，1 的个数为 <code>k</code>的情况下的最大子集的长度</p><p>我们用一个 <code>cnt</code> 数组来额外记录原数组当中每个字符串当中 0和 1 的数量</p><p>那么显然： <span class="math display">\[dp[i][j][k]=max(dp[i-1][j][k],dp[i-1][j-cnt[i-1][0]][k-cnt[i-1][1]])\]</span> 代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">findMaxForm</span><span class="hljs-params">(vector&lt;string&gt;&amp; strs, <span class="hljs-type">int</span> m, <span class="hljs-type">int</span> n)</span></span><br><span class="hljs-function"></span>&#123;<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;cnt;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; strs.<span class="hljs-built_in">size</span>(); i++)<br>&#123;<br><span class="hljs-type">int</span> zero = <span class="hljs-number">0</span>, one = <span class="hljs-number">0</span>;<br>string str = strs[i];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; str.<span class="hljs-built_in">length</span>(); i++)<br>&#123;<br><span class="hljs-keyword">if</span> (str[i] == <span class="hljs-string">&#x27;0&#x27;</span>)<br>zero++;<br><span class="hljs-keyword">else</span> one++;<br>&#125;<br>cnt.<span class="hljs-built_in">push_back</span>(&#123; zero, one &#125;);<br>&#125;<br><span class="hljs-type">int</span> len = strs.<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&gt;<span class="hljs-built_in">dp</span>(len + <span class="hljs-number">1</span>, vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;(m + <span class="hljs-number">1</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(n + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>)));<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= len; i++)<br>&#123;<br><span class="hljs-type">int</span> zero = cnt[i - <span class="hljs-number">1</span>][<span class="hljs-number">0</span>], one = cnt[i - <span class="hljs-number">1</span>][<span class="hljs-number">1</span>];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt;= m; j++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> k = <span class="hljs-number">0</span>; k &lt;= n; k++)<br>&#123;<br><span class="hljs-type">int</span> x = dp[i - <span class="hljs-number">1</span>][j][k];<br><span class="hljs-type">int</span> y = (j &gt;= zero &amp;&amp; k &gt;= one) ? dp[i - <span class="hljs-number">1</span>][j - zero][k - one] + <span class="hljs-number">1</span> : <span class="hljs-number">0</span>;<br>dp[i][j][k] = <span class="hljs-built_in">max</span>(x, y);<br>&#125;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[len][m][n];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^3)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n^3)\)</span></p><h4 id="二维空间压缩">二维空间压缩</h4><p>我们从后往前遍历，虽然有两层循环，但不影响，统一从后往前</p><p>这个不难，直接给代码</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">findMaxForm</span><span class="hljs-params">(<span class="hljs-type">const</span> vector&lt;string&gt;&amp; strs, <span class="hljs-type">int</span> m, <span class="hljs-type">int</span> n)</span></span><br><span class="hljs-function"></span>&#123;<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;cnt;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; strs.<span class="hljs-built_in">size</span>(); i++)<br>&#123;<br><span class="hljs-type">int</span> zero = <span class="hljs-number">0</span>, one = <span class="hljs-number">0</span>;<br>string str = strs[i];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; str.<span class="hljs-built_in">length</span>(); i++)<br>&#123;<br><span class="hljs-keyword">if</span> (str[i] == <span class="hljs-string">&#x27;0&#x27;</span>)<br>zero++;<br><span class="hljs-keyword">else</span> one++;<br>&#125;<br>cnt.<span class="hljs-built_in">push_back</span>(&#123; zero, one &#125;);<br>&#125;<br><span class="hljs-type">int</span> len = strs.<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(m + <span class="hljs-number">1</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(n + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>));<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= len; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = m; j &gt;= <span class="hljs-number">0</span>; j--)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> k = n; k &gt;= <span class="hljs-number">0</span>; k--)<br>&#123;<br><span class="hljs-type">int</span> zero = cnt[i - <span class="hljs-number">1</span>][<span class="hljs-number">0</span>], one = cnt[i - <span class="hljs-number">1</span>][<span class="hljs-number">1</span>];<br><span class="hljs-type">int</span> x = dp[j][k];<br><span class="hljs-type">int</span> y = (j &gt;= zero &amp;&amp; k &gt;= one) ? dp[j - zero][k - one] + <span class="hljs-number">1</span> : <span class="hljs-number">0</span>;<br>dp[j][k] = <span class="hljs-built_in">max</span>(x, y);<br>&#125;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[m][n];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^3)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n^2)\)</span></p><h4 id="最后">最后</h4><p>需要说明的是，我们在遍历 <code>dp</code>数组的时候，是「按需遍历」</p><p>意思是指，只有这个值是有意义的，我才遍历</p><p>用第一个三维的来举例就是</p><p>显然选择前 0 个并没有什么意义，因此 <code>i</code> 从 1 开始遍历</p><p>除此之外，这里也没有涉及到 Base Case的情况，所以不需要对数组进行初始化</p><p>对于内层的 <code>j</code> 和 <code>k</code>，由于不会有空串的出现，因此二者均从 1 开始遍历</p><p>当然，<code>j</code> 需要遍历 <code>m</code> 次，<code>k</code>需要遍历 <code>n</code> 次</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>大二上 第二周 2022.9.12 - 2022.9.18</title>
    <link href="/2022/09/18/%E6%97%A5%E5%B8%B8%E6%80%BB%E7%BB%93/%E5%A4%A7%E4%BA%8C%E4%B8%8A-%E7%AC%AC%E4%BA%8C%E5%91%A8-2022.9.12---2022.9.18/"/>
    <url>/2022/09/18/%E6%97%A5%E5%B8%B8%E6%80%BB%E7%BB%93/%E5%A4%A7%E4%BA%8C%E4%B8%8A-%E7%AC%AC%E4%BA%8C%E5%91%A8-2022.9.12---2022.9.18/</url>
    
    <content type="html"><![CDATA[<p>我感觉，我其实可以一个星期肝六天，然后周天休息</p><p>emmmmm</p><p>我觉得，还是有点摆吧</p><p>我还是希望周末两天都能去图书馆</p><p>虽然我觉得我现在做的很好，但我觉得，我还能做的更好</p><p>希望往后的每个星期，都能完完整整地学完七天吧，主要是，时间不太够啊</p><p>说一下这个星期干了什么吧</p><p>CSAPP 看到 192 页了，这个速度我觉得已经很快了</p><p>这里有个问题是，第二章我是想做笔记的，但。。。。</p><p>实在是不知道要怎么做</p><p>我感觉，它都只是描述一下那个过程是什么样子，一些指令的意思</p><p>我总不能，把每个指令给记下来吧</p><p>所以这个部分，我也很无奈，只能是去看过去，记不了什么笔记</p><p>麻了。。。。</p><p>然后是算法方面</p><p>三叶姐姐的背包合集，我已经看到多维背包了</p><p>这个速度真的是超出我的意料，我原本以为是只能一天看一篇的</p><p>事实上，我一天能看三篇（双手捂嘴</p><p>然后这里特别值得一说的是，那个多重背包的单调队列优化</p><p>我花了三个晚上才把它看明白</p><p>那三个晚上真的是什么事情都没做，就搁那对着电脑发呆，想，想，想</p><p>好在，最后想出来了</p><p>开心，哈哈哈</p><p>三叶姐姐的背包合集还有七篇，我应该下一个星期就能全部看完</p><p>哇，大成功！！！鼓掌鼓掌</p><p>然后韩顺平 Linux 基本操作的视频，我也打算下个星期把它全部干掉</p><p>当然是跳着看的，我不可能全部看完</p><p>我想去搞点项目玩玩</p><p>接着是往后的规划</p><p>CSAPP 肯定还得接着看，这个保守估计得持续到第八周，这个时间是死的</p><p>然后算法的部分，我打算看完三叶姐姐的背包合集之后，去把那个数据结构合集和图论给看完</p><p>算法部分的规划差不多就是这样</p><p>我其实想去把那个力扣每日打卡给写一下的（毕竟我想拿那个徽章（（</p><p>可能，这个想法得到十一月才能实现吧</p><p>还有就是，我想十月份的时候参加一下三叶姐姐的那个打卡</p><p>精神支柱了属于是（（（</p><p>哈哈哈，今天就这些吧</p><p>下个星期继续努力吧</p>]]></content>
    
    
    <categories>
      
      <category>日常总结</category>
      
    </categories>
    
    
    <tags>
      
      <tag>周总结</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 1155.掷骰子的N种方法</title>
    <link href="/2022/09/17/Algorithm%20Solution/LC-1155.%E6%8E%B7%E9%AA%B0%E5%AD%90%E7%9A%84N%E7%A7%8D%E6%96%B9%E6%B3%95/"/>
    <url>/2022/09/17/Algorithm%20Solution/LC-1155.%E6%8E%B7%E9%AA%B0%E5%AD%90%E7%9A%84N%E7%A7%8D%E6%96%B9%E6%B3%95/</url>
    
    <content type="html"><![CDATA[<h4 id="掷骰子的n种方法"><a href="https://leetcode.cn/problems/number-of-dice-rolls-with-target-sum/">1155.掷骰子的N种方法</a></h4><p>这里有 <code>n</code> 个一样的骰子，每个骰子上都有 <code>k</code>个面，分别标号为 <code>1</code> 到 <code>k</code> 。</p><p>给定三个整数 <code>n</code> , <code>k</code> 和 <code>target</code>，返回可能的方式(从总共 <code>kn</code>种方式中)滚动骰子的数量，使正面朝上的数字之和等于 <code>target</code>。</p><p>答案可能很大，你需要对 <code>109 + 7</code> <strong>取模</strong>。</p><figure class="highlight aspectj"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs aspectj">输入：n = <span class="hljs-number">1</span>, k = <span class="hljs-number">6</span>, <span class="hljs-keyword">target</span> = <span class="hljs-number">3</span><br>输出：<span class="hljs-number">1</span><br>解释：你扔一个有<span class="hljs-number">6</span>张脸的骰子。<br>得到<span class="hljs-number">3</span>的和只有一种方法。<br></code></pre></td></tr></table></figure><p>​</p><h4 id="朴素二维">朴素二维</h4><p>这道题其实是一个分组背包的模型，会做分组背包的话，这道题基本没有问题。嘛，就算不会也能做</p><p>我们定义 <code>dp[i][j]</code> 为：考虑前 <code>i</code>个骰子，当正面之和<strong>恰好</strong>为 <code>j</code>时的所有可能</p><p>显然，<code>dp[0][0] = 1</code></p><p>现在，我们考虑哪些状态可以转移到 <code>dp[i][j]</code></p><p>回顾 <code>dp</code> 数组的定义，我们既然是考虑 <code>dp[i][j]</code>的值，那么就说明前 <code>i-1</code>个骰子是<strong>已经被我们考虑完毕的</strong>，这一点很关键</p><p>那么，我们假设第 <code>i</code> 个骰子的值为 <code>var</code> ，有：<code>dp[i][j] = dp[i - 1][j - var]</code></p><p>状态 <code>i</code> 与状态 <code>i-1</code> 之间的差值就是第<code>i</code> 个骰子的数值嘛</p><p>由于，第 <code>i</code> 个骰子的数值可能有 <code>k</code>个，这对应了 <code>k</code> 个状态</p><p>这 <code>k</code> 个状态都可以转移到 <code>dp[i][j]</code>，因此我们需要将这 <code>k</code> 个状态的所有值全部加起来，这个总和就是<code>dp[i][j]</code> 的值，即： <span class="math display">\[dp[i][j]=\sum^{n}_{i=1}dp[i-1][j-var]\]</span></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-type">int</span> mod = <span class="hljs-number">1e9</span> + <span class="hljs-number">7</span>;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">numRollsToTarget</span><span class="hljs-params">(<span class="hljs-type">int</span> n, <span class="hljs-type">int</span> k, <span class="hljs-type">int</span> target)</span></span><br><span class="hljs-function"></span>&#123;<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(n + <span class="hljs-number">1</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(target + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>));<br>dp[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>] = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= n; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt;= target; j++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> var = <span class="hljs-number">1</span>; var &lt;= k; var++)<br>&#123;<br><span class="hljs-keyword">if</span> (j - var &gt;= <span class="hljs-number">0</span>)<br>dp[i][j] = (dp[i][j] + dp[i - <span class="hljs-number">1</span>][j - var]) % mod;<br>&#125;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[n][target];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n*k*target)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n*target)\)</span></p><p>​</p><h4 id="一维压缩">一维压缩</h4><p>我们直接用一维数组来表示原二维数组当中的某一行，即：</p><p>一维数组 <code>dp[j]</code> 表示二维数组的第 <code>dp[i]</code>行的所有值</p><p>因此，未对 <code>dp[j]</code> 赋值前，<code>dp[j]</code>里面的数组表示的是二维数组 <code>d[i-1]</code> 这一行的值</p><p>对它赋值后，它就表示第 <code>dp[i]</code> 行的值</p><p>需要注意的是，我们没错都需要对 <code>dp[j]</code> 进行清零</p><p>这是因为在原二维数组当中，<code>dp[i]</code> 行的数值总是为 0</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-type">int</span> mod = <span class="hljs-number">1e9</span> + <span class="hljs-number">7</span>;<br>    <span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">numRollsToTarget</span><span class="hljs-params">(<span class="hljs-type">int</span> n, <span class="hljs-type">int</span> k, <span class="hljs-type">int</span> target)</span> </span><br><span class="hljs-function">    </span>&#123;<br>        vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">dp</span>(target + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>);<br>        dp[<span class="hljs-number">0</span>] = <span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++)<br>        &#123;<br>            <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j = target; j &gt;= <span class="hljs-number">0</span>; j--)<br>            &#123;<br>                dp[j] = <span class="hljs-number">0</span>;<span class="hljs-comment">//需要归零</span><br>                <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> t = <span class="hljs-number">1</span>; t &lt;= k; t++)<br>                    <span class="hljs-keyword">if</span>(j - t &gt;= <span class="hljs-number">0</span>)<br>                    &#123;<br>                        dp[j] += dp[j - t];<br>                        dp[j] %= mod;<br>                    &#125;<br>            &#125;<br>        &#125;<br>        <span class="hljs-keyword">return</span> dp[target];<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n*k*target)\)</span></p><p>空间复杂度：<span class="math inline">\(O(target)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>动规 分组背包</title>
    <link href="/2022/09/17/Algorithm%20Solution/%E5%8A%A8%E8%A7%84-%E5%88%86%E7%BB%84%E8%83%8C%E5%8C%85/"/>
    <url>/2022/09/17/Algorithm%20Solution/%E5%8A%A8%E8%A7%84-%E5%88%86%E7%BB%84%E8%83%8C%E5%8C%85/</url>
    
    <content type="html"><![CDATA[<h3 id="分组背包">分组背包</h3><p>问题描述：</p><p>给定 <strong><code>N</code> 个物品组</strong>和一个容量为<code>V</code> 的背包，第 <code>i</code> 个物品组共有 <code>s[i]</code>个物品，第 <code>i</code> 组第 <code>j</code> 件物品体积为<code>v[i][j]</code> 、价值为 <code>w[i][j]</code></p><p>每组有若干个物品，<strong>同一组内的物品最多只能选择一个</strong>，求解将哪些物品装入背包使得背包价值最大</p><p>​</p><h3 id="空间优化">空间优化</h3><p>这里我们直接给出空间优化的解法</p><p>我们考虑第 <span class="math inline">\(i\)</span>组当中的物品，由于同一组当中的物品只能选择一个，因此我们有：</p><p>当我不选择物品时，有：<span class="math inline">\(dp[i][j]=dp[i-1][j]\)</span></p><p>当我选择第 <span class="math inline">\(0\)</span> 个物品时，有：<span class="math inline">\(dp[i][j] = dp[i-1][j-v[i][0]]+w[i][0]\)</span></p><p>当我选择第 <span class="math inline">\(1\)</span> 个物品时，有：<span class="math inline">\(dp[i][j]=dp[i-1][j-v[i][1]]+w[i][1]\)</span></p><p>当我选择第 <span class="math inline">\(2\)</span> 个物品时，有：<span class="math inline">\(dp[i][j]=dp[i-1][j-v[i][2]]+w[i][2]\)</span></p><p><span class="math inline">\(\cdots\)</span></p><p>当我选择第 <span class="math inline">\(k\)</span> 个物品时，有：<span class="math inline">\(dp[i][j]=dp[i-1][j-v[i][k]]+w[i][k]\)</span></p><p>由于我们需要求最大值，因此最终结果就是求上面所有值的最大值，即：<span class="math display">\[dp[i][j]=max(dp[i-1][j],dp[i-1][j-v[i][k]]+w[i][k]), \text{其中}\ 0\le k\le s[i]\]</span>当我们将空间进行压缩时，由于需要上一层的数据，因此需要从后往前进行遍历</p><p>完整代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">back</span><span class="hljs-params">(<span class="hljs-type">int</span> N, <span class="hljs-type">int</span> V, vector&lt;<span class="hljs-type">int</span>&gt;&amp; s, vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&amp; v, vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&amp; w)</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">dp</span>(V + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>);<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; N; i++)<br>    &#123;<br>        <span class="hljs-type">int</span> si = s[i];<br>        vector&lt;<span class="hljs-type">int</span>&gt;vi = v[i];<br>        vector&lt;<span class="hljs-type">int</span>&gt;wi = w[i];<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = V; j &gt;= <span class="hljs-number">0</span>; j--)<br>        &#123;<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> k = <span class="hljs-number">0</span>; k &lt; si; k++)<br>                <span class="hljs-keyword">if</span> (j - vi[k] &gt;= <span class="hljs-number">0</span>)<br>                    dp[j] = <span class="hljs-built_in">max</span>(dp[j], dp[j - vi[k]] + wi[k]);<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> dp[V];<br>&#125;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n*\sum^{n-1}_{i=0}s[i])\)</span> ，实际上就是<span class="math inline">\(O(n^3)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>动规 混合背包问题</title>
    <link href="/2022/09/17/Algorithm%20Solution/%E5%8A%A8%E8%A7%84-%E6%B7%B7%E5%90%88%E8%83%8C%E5%8C%85%E9%97%AE%E9%A2%98/"/>
    <url>/2022/09/17/Algorithm%20Solution/%E5%8A%A8%E8%A7%84-%E6%B7%B7%E5%90%88%E8%83%8C%E5%8C%85%E9%97%AE%E9%A2%98/</url>
    
    <content type="html"><![CDATA[<h3 id="混合背包">混合背包</h3><p>前面我们详细分析了三种背包问题，分别是 <a href="https://nishikichisato.github.io/2022/08/02/Algorithm%20Solution/动规-0-1背包问题/">0-1背包问题</a>、 <a href="https://nishikichisato.github.io/2022/09/12/Algorithm%20Solution/动规-完全背包问题/">完全背包问题</a>、 <a href="https://nishikichisato.github.io/2022/09/14/Algorithm%20Solution/动规-多重背包问题/">多重背包问题</a>、 <a href="https://nishikichisato.github.io/2022/09/16/Algorithm%20Solution/多重背包之单调队列优化/">多重背包之单调队列优化</a></p><p>在状态的定义上，三种背包都有「不超过」和「恰好」两种定义，关于这一点我们之前做的题也见过了</p><ul><li>对于「不超过」的定义，<code>dp[0][j] = 0</code> 均为有效值<ul><li>这表示，考虑前 0 个物品，那么背包容量必然不可能超过 0，因此均为有效值</li></ul></li><li>对于「恰好」的定义，只有 <code>dp[0][0] = 0</code> 才是有效值<ul><li>显然，只有当什么都不选的时候，才恰好满足背包空间为 0 ，因此只有<code>dp[0][0] = 0</code> 才是有效值</li></ul></li></ul><p>我们来看一道将上面所有背包混合起来的问题</p><p>问题描述：</p><p>有 <code>N</code> 种物品和一个最多能装 <code>V</code> 的背包，第<code>i</code> 件物品的价值是 <code>w[i]</code> 、体积是<code>v[i]</code> 、数量是 <code>s[i]</code> ，有：</p><ul><li>当 <code>s[i] &gt; 0</code> 时，<code>s[i]</code>的值就是该物品可用的次数</li><li>当 <code>s[i] = 0</code> 时，该物品可以用无数次</li><li>当 <code>s[i] = -1</code> 时，该物品只能用一次</li></ul><p>​</p><h4 id="空间优化">空间优化</h4><p>这里就是将上面三种背包全部综合了一下，其实也不难</p><p>我们知道，0-1背包和完全背包只是遍历的顺序不一样而已，前者从前向后，后者从后向前</p><p>而多重背包可用通过二进制优化，最终的处理方案跟 0-1 背包完全一样</p><p>由于最后的处理只在遍历顺序上有不同，那么我们先对所有物品进行二进制处理，最后转换成0-1 背包就行</p><p>实际上很简单，这个东西，我直接给代码吧，前面三个会，这个压根就没有思维难度</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">back</span><span class="hljs-params">(<span class="hljs-type">int</span> N, <span class="hljs-type">int</span> V, vector&lt;<span class="hljs-type">int</span>&gt;&amp; s, vector&lt;<span class="hljs-type">int</span>&gt;&amp; v, vector&lt;<span class="hljs-type">int</span>&gt;&amp; w)</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;<span class="hljs-type">int</span>&gt;worth;<br>    vector&lt;<span class="hljs-type">int</span>&gt;volume;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; N; i++)<br>    &#123;<br>        <span class="hljs-type">int</span> elem = s[i];<br>        <span class="hljs-keyword">if</span> (elem &gt; <span class="hljs-number">0</span>)<span class="hljs-comment">//多重背包</span><br>        &#123;<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">1</span>; j &lt; elem; j *= <span class="hljs-number">2</span>)<br>            &#123;<br>                elem -= j;<br>                worth.<span class="hljs-built_in">push_back</span>(j * w[i]);<br>                volume.<span class="hljs-built_in">push_back</span>(j * v[i]);<br>            &#125;<br>            <span class="hljs-keyword">if</span> (elem &gt; <span class="hljs-number">0</span>)<br>            &#123;<br>                worth.<span class="hljs-built_in">push_back</span>(elem * w[i]);<br>                volume.<span class="hljs-built_in">push_back</span>(elem * v[i]);<br>            &#125;<br>        &#125;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (elem == <span class="hljs-number">0</span>)<span class="hljs-comment">//完全背包</span><br>        &#123;<br>            worth.<span class="hljs-built_in">push_back</span>(-w[i]);<span class="hljs-comment">//用负值标记</span><br>            volume.<span class="hljs-built_in">push_back</span>(v[i]);<br>        &#125;<br>        <span class="hljs-keyword">else</span><br>        &#123;<br>            worth.<span class="hljs-built_in">push_back</span>(w[i]);<br>            volume.<span class="hljs-built_in">push_back</span>(v[i]);<br>        &#125;<br>    &#125;<br>    vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">dp</span>(V + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>);<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; N; i++)<br>    &#123;<br>        <span class="hljs-type">int</span> vi = v[i];<br>        <span class="hljs-type">int</span> wi = w[i];<br>        <span class="hljs-keyword">if</span> (wi &lt; <span class="hljs-number">0</span>)<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; V; j++)<span class="hljs-comment">//完全背包的处理</span><br>                dp[j] = <span class="hljs-built_in">max</span>(dp[j], dp[j - vi] - wi);<br>        <span class="hljs-keyword">else</span><br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = V; j &gt;= vi; j--)<span class="hljs-comment">//0-1背包和多重背包的处理</span><br>                dp[j] = <span class="hljs-built_in">max</span>(dp[j], dp[j - vi] + wi);<br>    &#125;<br>    <span class="hljs-keyword">return</span> dp[V];<br>&#125;<br></code></pre></td></tr></table></figure><p>这东西时空复杂度没法算，所以我们直接忽视它</p><p>这里说一下为什么里面可以用 <code>if</code> 来分开</p><p>你想嘛，<code>dp[j]</code>表示每一层，那么对于同一层而言，<code>dp[j]</code>的值表示的是<strong>选择物品多少而对应的价值</strong></p><p>因此在遍历内层循环的时候，我们可用通过这个物品的价值正负（经过预先处理），得知该物品可用选择的次数</p><p>再根据选择的次数按照不同的背包来进行处理，0-1背包就从后往前，完全背包就从前往后遍历</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Linux 基础操作</title>
    <link href="/2022/09/17/Linux/Linux-%E5%9F%BA%E7%A1%80%E6%93%8D%E4%BD%9C/"/>
    <url>/2022/09/17/Linux/Linux-%E5%9F%BA%E7%A1%80%E6%93%8D%E4%BD%9C/</url>
    
    <content type="html"><![CDATA[<h2 id="关机重启">关机重启</h2><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs awk">shutdown -h now <span class="hljs-regexp">//</span>立刻关机<br>shutdown -h <span class="hljs-number">1</span>   <span class="hljs-regexp">//</span><span class="hljs-number">1</span> 分钟后关机<br>shutdown -r now <span class="hljs-regexp">//</span>现在重启<br>halt            <span class="hljs-regexp">//</span>关机，与上面一样<br>reboot          <span class="hljs-regexp">//</span>现在重启，与上面一样<br>sync            <span class="hljs-regexp">//</span>将内存的数据同步到磁盘<br></code></pre></td></tr></table></figure><h2 id="用户登录和注销">用户登录和注销</h2><h3 id="登录">登录</h3><p>一般最好不要用 root登录，因为这东西的权限最大。为避免操作失误，尽量少用。</p><p>登录的时候可以用普通用户登录，然后再用</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs awk">su - 用户名<br><span class="hljs-regexp">//</span>例如 su - root<br></code></pre></td></tr></table></figure><p>命令来切换成系统管理员身份</p><h3 id="注销">注销</h3><p>相应地，可以用</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">logout</span><br></code></pre></td></tr></table></figure><p>来注销用户</p><p>需要说明的是，这个命令不能在图像界面使用。并且在最后一个用户的时候使用的话会直接<strong>退出</strong>系统</p><h2 id="用户管理">用户管理</h2><h3 id="添加用户">添加用户</h3><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">useradd</span> 用户名<br></code></pre></td></tr></table></figure><p>这一条指令会自动在 <code>home</code> 目录下面创建默认的家目录</p><p>家目录的文件名默认为用户名称，如果需要进行更改，可以输入：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">useradd</span> -d 文件名 用户名<br></code></pre></td></tr></table></figure><p>在 Ubuntu下直接用这一个命令<strong>只会创建用户而不会创建家目录</strong>，需要写：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">useradd</span> -m 用户名<br></code></pre></td></tr></table></figure><p>相应的，我们可以修改用户密码：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">passwd</span> 用户名<br></code></pre></td></tr></table></figure><p>如果不在后面加用户名的话，会默认修改当前用户的密码</p><h3 id="删除用户">删除用户</h3><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">userdel</span> 用户名<br></code></pre></td></tr></table></figure><p>上面这个是基本指令，但实际上的删除用户有两种情况：</p><ul><li>保留家目录</li><li>不保留家目录</li></ul><p>对于前者，我们直接输入</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">userdel</span> 用户名<br></code></pre></td></tr></table></figure><p>就行，这个是默认保留家目录</p><p>对于后者，我们需要输入</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">userdel</span> -r 用户名<br></code></pre></td></tr></table></figure><p>这样就会将家目录一起删除</p><h3 id="查询用户">查询用户</h3><figure class="highlight applescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs applescript"><span class="hljs-built_in">id</span> 用户名<br></code></pre></td></tr></table></figure><h3 id="切换用户">切换用户</h3><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">su</span> - 用户名<br></code></pre></td></tr></table></figure><p>从权限高的用户切换到权限低的用户，不需要输入密码，反之需要</p><p>如果需要返回原来的用户，可以输入</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs awk"><span class="hljs-keyword">exit</span><br><span class="hljs-regexp">//</span>或者<br>logout<br></code></pre></td></tr></table></figure><h3 id="查看当前用户信息">查看当前用户信息</h3><p>如果需要查看目前正在登录的用户信息，可以输入</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs awk">whoami<br><span class="hljs-regexp">//</span>或者<br>who am i<br></code></pre></td></tr></table></figure><h3 id="用户组">用户组</h3><p>在 Linux中，如果多个用户拥有相同的权限，除了一个一个设置权限外还有一种方法是：<strong>将所以的用户拉到一个组，对这个组赋予权限</strong></p><p>因此我们说，用户组的作用是允许<strong>系统对有相同权限的多个用户进行统一管理</strong></p><h4 id="创建组">创建组</h4><p>一般的命令是这个：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">groupadd</span> 组名<br></code></pre></td></tr></table></figure><p>当然，我们也给定一个参数来指明这个组的 <code>group id</code>，即：</p><figure class="highlight applescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs applescript">groupadd -g 组<span class="hljs-built_in">id</span> 组名<br></code></pre></td></tr></table></figure><h4 id="删除组">删除组</h4><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">groupdel</span> 组名<br></code></pre></td></tr></table></figure><h4 id="将用户添加到组">将用户添加到组</h4><p>我们可以在添加一个用户的时候直接将其加入到一个组里面，即：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">useradd</span> -g 用户组 用户名<br></code></pre></td></tr></table></figure><p>需要说明的是，如果在添加用户的时候没有指定组，那么会<strong>默认创建一个与该用户名相同的组</strong></p><h4 id="修改用户的组">修改用户的组</h4><p>我们可以将一个用户的组进行修改，即：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">usermod</span> -g 用户组 用户名<br></code></pre></td></tr></table></figure><h4 id="用户和组的相关文件">用户和组的相关文件</h4><ul><li>/etc/passwd 文件</li></ul><p>用户（ user ）的配置文件，记录用户的各种信息</p><p>每行含义：用户 ：口令 ：用户标识号 ：组标识号 ：注释性描述 ：主目录：登陆Shell</p><p>解释：</p><ol type="1"><li>口令一般加密，只会显示 X</li><li>用户标识号是用户的 uid</li><li>组标识号是组的 uid</li><li>Shell 可以理解成一个将控制台指令翻译成 Linux内核可以理解的代码的工具，一般使用 bash</li></ol><ul><li>/etc/shadow 文件</li></ul><p>用户口令的配置文件（这东西看不懂的）</p><p>每行含义：登录名 ：加密口令 ：最后一次修改时间 ：最小时间间隔：警告时间 ：不活动时间 ：失效时间 ：标志</p><p>说明：</p><ol type="1"><li>这里的加密口令是加密过的，如果没有设置密码的话这里为空</li><li>后面的部分全部都是一些特殊的数字，看不懂</li></ol><ul><li>/etc/group 文件</li></ul><p>组的配置文件，记录 Linux 包含的组的信息</p><p>每行含义：组名 ：口令 ：组标识号 ：组内用户列表</p><p>说明：</p><ol type="1"><li>那个组内用户列表是看不到的，被隐藏起来了</li></ol><h2 id="运行级别">运行级别</h2><h4 id="说明">说明</h4><p>运行级别说明：</p><p>0 ：关机</p><p>1 ：单用户（可以用于找回丢失的密码）</p><p>2 ：多用户状态但没有网络服务（几乎不用）</p><p>3 ：多用户状态有网络服务（用的最多）</p><p>4 ：系统未使用保留给用户（这个没用）</p><p>5 ：图形界面（用的最多）</p><p>6 ：系统重启，默认运行级别不能是 6 ，否则系统无法正常启动</p><p>常用的运行级别是 3 和 5 ，当然也可以制定默认运行级别</p><h4 id="查看默认运行级别">查看默认运行级别</h4><figure class="highlight dsconfig"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs dsconfig"><span class="hljs-string">systemctl</span> <span class="hljs-built_in">get-default</span> 运行级别<br></code></pre></td></tr></table></figure><p>这里需要说明的是：</p><p><code>multi-user.target</code> 相当于运行级别 3，也就是多用户有网络服务</p><p><code>graphical.target</code> 相当于运行级别 5 ，即图形化界面</p><p>还有就是，后面那个运行级别需要写英文，不能写数字</p><h2 id="帮助指令">帮助指令</h2><ul><li>man 获得帮助信息</li></ul><figure class="highlight inform7"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs inform7"><span class="hljs-keyword">man</span> 命令或配置文件<br></code></pre></td></tr></table></figure><p>举个例子，我们需要查看 ls 命令的帮助信息，可以用</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs powershell"><span class="hljs-built_in">man</span> <span class="hljs-built_in">ls</span><br></code></pre></td></tr></table></figure><p>补充：Linux 下以 <code>.</code> 开头的文件表示隐藏文件</p><ul><li>help 命令</li></ul><p>这个可以用于获取 shell 内置命令的帮助信息</p><h2 id="文件目录指令">文件目录指令</h2><h3 id="pwd-指令">pwd 指令</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">pwd</span><br></code></pre></td></tr></table></figure><p>显示当前工作目录的<strong>绝对路径</strong></p><h3 id="ls-指令">ls 指令</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ls</span><br></code></pre></td></tr></table></figure><p>列出当前目录下的文件信息</p><p>参数：</p><p><code>-a</code> ：显示当前目录的所有文件和目录，包括隐藏的</p><p><code>-l</code> ：以列表的方式显示信息</p><h3 id="cd-指令">cd 指令</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> 目录<br></code></pre></td></tr></table></figure><p>切换到指定目录</p><p><code>cd ~</code> 或者 <code>cd</code>这两个都是回到自己的家目录。比如，如果是 root 用户，则会回到 root的家目录 <code>/root</code></p><p><code>cd..</code> 回到当前目录的上一级目录</p><h3 id="mkdir-指令">mkdir 指令</h3><p>实际上是 <code>make directory</code>，<strong>创建目录</strong>的意思，不是创建文件</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs arduino">mkdir [选项] 要创建的目录<br></code></pre></td></tr></table></figure><p>常用选项：</p><p><code>-p</code> ：创建多级目录</p><p><code>mkdir</code>默认只能创建一级目录，需要创建多级的话需要加参数</p><p>还有一个就是这东西如果只写目录名的话那么默认在当前目录下创建目录</p><h3 id="rmdir-指令">rmdir 指令</h3><p><code>remove directory</code>，删除<strong>空目录</strong>。这个指令只能删除空目录，如果目录当中有内容则不能用</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs arduino">rmdir [选项] 目录名<br></code></pre></td></tr></table></figure><p>如果要删除非空目录，则用 <code>rm -rf 目录名</code> 。后面的<code>r</code> 代表 <code>recursion</code> ，递归的；<code>f</code> 代表<code>force</code> ，强制的</p><h3 id="touch-指令">touch 指令</h3><p>创建<strong>空文件</strong>，啥都不写的那种</p><p><code>touch</code> 是创建文件，<code>mkdir</code> 是创建目录</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">touch</span> 文件名<br></code></pre></td></tr></table></figure><p>比如，可以写 <code>touch main.cpp</code> 这种</p><h3 id="cp-指令">cp 指令</h3><p><code>copy</code> 的意思，拷贝文件到指定目录</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cp</span> [选项] <span class="hljs-built_in">source</span> destination<br></code></pre></td></tr></table></figure><p><code>-r</code> 递归地拷贝<strong>整个目录</strong></p><p>这个指令默认是拷贝文件，如果是想要拷贝目录的话需要加上<code>-r</code></p><p>需要说明的是，在复制的时候会提示是否去覆盖原文件。如果文件很多的话一个一个确认会很麻烦，因此我们可以输入一个强制覆盖的指令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">\<span class="hljs-built_in">cp</span> -r <span class="hljs-built_in">source</span> destination<br></code></pre></td></tr></table></figure><p>这样的话就不会有提示了</p><h3 id="rm-指令">rm 指令</h3><p>可以用于移除<strong>文件或目录</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">rm</span> [选项] 要删除的文件或目录<br></code></pre></td></tr></table></figure><p>选项：</p><p><code>-r</code> ：递归删除整个文件夹</p><p><code>-f</code> ：强制删除不提示</p><p>注意，如果是删文件的话就<strong>不要带参数</strong></p><h3 id="mv-指令">mv 指令</h3><p>移动文件与目录或者重命名</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs awk">mv oldName newName <span class="hljs-regexp">//</span>重命名<br>mv source destination <span class="hljs-regexp">//</span>移动文件<br></code></pre></td></tr></table></figure><p>这个指令可以移动文件<strong>并</strong>重命名。实际上，对于重命名的情况，只要<code>oldName</code> 和 <code>newName</code>处于同一个目录下就是重命名，否则就是移动文件</p><h3 id="cat-指令">cat 指令</h3><p>查看文件内容，这个指令是<strong>只能查看不能修改</strong>，而<code>vim</code> 指令则是<strong>既可以查看也可以修改</strong>。因此<code>cat</code> 相比于 <code>vim</code> 更安全</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> [选项] 要查看的文件<br></code></pre></td></tr></table></figure><p>常用选项：</p><p><code>-n</code> ：显示行号</p><p>管道命令：<strong>将前一个命令的结果交给后一个命令进行处理</strong></p><p><code>cat</code> 指令是可以跟管道命令一起使用的，如下：</p><figure class="highlight stata"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stata"><span class="hljs-keyword">cat</span> -<span class="hljs-keyword">n</span> 文件名 | <span class="hljs-keyword">more</span><br></code></pre></td></tr></table></figure><p><code>more</code> 其实是一个指令，并不是一个参赛</p><h3 id="more-指令">more 指令</h3><figure class="highlight dos"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs dos"><span class="hljs-built_in">more</span> 要查看的文件<br></code></pre></td></tr></table></figure><p><code>more</code> 指令是一个基于 <code>vi</code>编辑器的文本过滤器，它以全屏的形式显示文本文件的内容</p><table><thead><tr class="header"><th style="text-align: center;">操作</th><th style="text-align: center;">功能</th></tr></thead><tbody><tr class="odd"><td style="text-align: center;">空格</td><td style="text-align: center;">向下翻页</td></tr><tr class="even"><td style="text-align: center;">Enter</td><td style="text-align: center;">向下一行</td></tr><tr class="odd"><td style="text-align: center;">q</td><td style="text-align: center;">退出</td></tr><tr class="even"><td style="text-align: center;">Ctrl + F</td><td style="text-align: center;">向下滚动一屏</td></tr><tr class="odd"><td style="text-align: center;">Ctrl + B</td><td style="text-align: center;">返回上一屏</td></tr><tr class="even"><td style="text-align: center;">=</td><td style="text-align: center;">输出当前行号</td></tr><tr class="odd"><td style="text-align: center;">:f</td><td style="text-align: center;">输出文件名和当前行号</td></tr></tbody></table><h3 id="less-指令">less 指令</h3><p><code>less</code> 的功能与 <code>more</code> 类似，但功能相比于<code>more</code> 更加强大。</p><p><code>vim</code>用来查看文件是会将整个文件全部加载到内存当中，<code>less</code>只会加载需要查看的部分，是一种动态加载。</p><table><thead><tr class="header"><th style="text-align: center;">操作</th><th style="text-align: left;">功能</th></tr></thead><tbody><tr class="odd"><td style="text-align: center;">空格</td><td style="text-align: left;">向下翻页</td></tr><tr class="even"><td style="text-align: center;">PageDown</td><td style="text-align: left;">向下翻页</td></tr><tr class="odd"><td style="text-align: center;">PageUp</td><td style="text-align: left;">向上翻页</td></tr><tr class="even"><td style="text-align: center;">/ 字符串</td><td style="text-align: left;">查找；n 向下查找，N 向上查找</td></tr><tr class="odd"><td style="text-align: center;">? 字符串</td><td style="text-align: left;">查找，方式与上面的相反</td></tr><tr class="even"><td style="text-align: center;">q</td><td style="text-align: left;">退出</td></tr></tbody></table><h3 id="echo-指令">echo 指令</h3><p>输出内容到控制台</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs css">echo <span class="hljs-selector-attr">[选项]</span> <span class="hljs-selector-attr">[输出内容]</span><br></code></pre></td></tr></table></figure><p>例如我们可以输出环境变量 <code>$HOSTNAME</code> 到控制台，即<code>echo $HOSTNAME</code></p><p>或者直接输出 <code>Hello World</code> ，即<code>echo "Hello World"</code></p><h3 id="head-指令">head 指令</h3><p>用于显示<strong>文件开头</strong>部分内容，默认情况下是显示前 10行的内容</p><figure class="highlight stan"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs stan"><span class="hljs-built_in">head</span> 文件<br><span class="hljs-built_in">head</span> -n <span class="hljs-number">5</span> 文件 <span class="hljs-comment">//显示 5 行，可以是任意数</span><br></code></pre></td></tr></table></figure><h3 id="tail-指令">tail 指令</h3><p>用于显示<strong>文件末尾</strong>部分内容，默认是最后 10 行的内容</p><figure class="highlight stan"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs stan"><span class="hljs-built_in">tail</span> 文件<br><span class="hljs-built_in">tail</span> -n <span class="hljs-number">5</span> 文件 <span class="hljs-comment">//显示最后 5 行，可以是任意数</span><br><span class="hljs-built_in">tail</span> -f 文件   <span class="hljs-comment">//实时追踪该文档的所有更新</span><br></code></pre></td></tr></table></figure><p>第三条指令会占用终端来实时监控该文件的更新</p><h3 id="指令和-指令">&gt; 指令和 &gt;&gt; 指令</h3><p><code>&gt;</code>是输出重定向，就是将原来输出的地方重定向到另外一个地方</p><p><code>&gt;</code> 和 <code>&gt;&gt;</code>唯一的区别是，前者会先将原文件的内容<strong>全部清空</strong>，然后在输入，也就是覆盖写入；后者是在原文件末尾进行<strong>追加</strong>，并不会覆盖</p><p>例如，我们可以 <code>ls -l &gt; 文件名</code> 或者<code>ls -l &gt;&gt; 文件名</code></p><p>需要注意的是，<code>cat 文件1 &gt; 文件2</code>这相当于是将文件1的内容覆盖到文件2当中去。</p><p>要理解的话很简单，<code>cat</code> 指令是显示文件内容，然后用<code>&gt;</code>重定向到另一个文件，就相当于是用第二个文件来显示第一个文件的内容，并且是<strong>覆盖写入</strong>，所以就是将文件1覆盖文件2</p><h3 id="ln-指令">ln 指令</h3><p>创建一个软链接，就相当于 <code>win</code> 里面的快捷方式</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ln</span> -s 原文件或目录 软链接名<br></code></pre></td></tr></table></figure><p>需要注意的是，使用 <code>pwd</code>查看软连接文件路径时，会<strong>显示软连接文件的路径</strong>，<strong>不会显示原文件的路径</strong></p><h3 id="history-指令">history 指令</h3><p><strong>查看</strong>已经执行过的历史命令，也可以<strong>执行</strong>历史命令</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs awk">history     <span class="hljs-regexp">//</span>查询所有的历史命令<br>hestory <span class="hljs-number">10</span>  <span class="hljs-regexp">//</span>显示最近执行的 <span class="hljs-number">10</span> 条命令<br>!<span class="hljs-number">10</span>         <span class="hljs-regexp">//</span>执行历史编号为 <span class="hljs-number">10</span> 的命令<br></code></pre></td></tr></table></figure><h2 id="时间日期指令">时间日期指令</h2><p>这个很简单，我们直接过一下就行</p><h3 id="date-指令">date 指令</h3><p>用于查看时间</p><figure class="highlight perl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs perl">date    //显示所有<br>date <span class="hljs-string">&quot;+%Y %m %d %H %M %S&quot;</span> //这里的参数可以自定义，但 + 一定要有<br></code></pre></td></tr></table></figure><p><code>date</code> 还可以用于设置日期</p><figure class="highlight applescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs applescript"><span class="hljs-built_in">date</span> -s 字符串时间<br></code></pre></td></tr></table></figure><p>例如，可以写 <code>date -s "2022-9-2 10:02:30"</code></p><h3 id="cal-指令">cal 指令</h3><p>用于查看日历</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">cal</span><span class="hljs-meta"> [选项]</span><br></code></pre></td></tr></table></figure><p>不加 选项 ，显示本月月历，加了之后可以显示那一年整年的日历</p><h2 id="搜索查找">搜索查找</h2><h3 id="find-指令">find 指令</h3><p>从指定目录向下递归遍历各个子目录，将满足条件的文件或目录显示在终端</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs css">find <span class="hljs-selector-attr">[搜索文件]</span> <span class="hljs-selector-attr">[选项]</span><br></code></pre></td></tr></table></figure><p>选项说明：</p><table><thead><tr class="header"><th style="text-align: center;">选项</th><th style="text-align: left;">功能</th></tr></thead><tbody><tr class="odd"><td style="text-align: center;">-name 文件名</td><td style="text-align: left;">按指定文件名查找文件</td></tr><tr class="even"><td style="text-align: center;">-user 用户名</td><td style="text-align: left;">查找属于指定用户名所有的文件</td></tr><tr class="odd"><td style="text-align: center;">-size 大小</td><td style="text-align: left;">按文件大小进行查询，+ 表示大于，-表示小于，单位有 K, M, G</td></tr></tbody></table><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">find</span> / +<span class="hljs-number">200</span>M  //在根目录下查找大约 <span class="hljs-number">200</span>M 的文件<br><span class="hljs-attribute">find</span> / <span class="hljs-number">200</span>M   //在根目录下查找等于 <span class="hljs-number">200</span>M 的文件<br></code></pre></td></tr></table></figure><h3 id="locate-指令">locate 指令</h3><p><code>locate</code>可以快速定位文件路径，因为这是基于数据库进行的查找，不需要遍历整个文件系统。为了保证查询结果的准确度，管理员必须定期更新<code>locate</code> 时刻</p><figure class="highlight gauss"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs gauss"><span class="hljs-keyword">locate</span> 搜索文件<br></code></pre></td></tr></table></figure><p>需要注意的是，由于 <code>locate</code>指令基于数据库查询，所以在第一次运行前，需要执行<code>updatedb</code></p><h3 id="which-指令">which 指令</h3><p>可以查看某个指令在哪个目录下，具体用法为：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">which</span> 指令<br></code></pre></td></tr></table></figure><p>例如，<code>which ls</code> 就是查询 <code>ls</code>指令所在的目录</p><h3 id="grep-指令">grep 指令</h3><p><code>grep</code>是过滤查找，也就是按照关键字查找的意思，通常会跟管道命令 <code>|</code>结合起来使用</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs css">grep <span class="hljs-selector-attr">[选项]</span> 查找内容 查找文件<br></code></pre></td></tr></table></figure><p>选项：</p><table><thead><tr class="header"><th style="text-align: center;">选项</th><th style="text-align: center;">功能</th></tr></thead><tbody><tr class="odd"><td style="text-align: center;">-n</td><td style="text-align: center;">显示行号</td></tr><tr class="even"><td style="text-align: center;">-i</td><td style="text-align: center;">忽略字母大小写</td></tr></tbody></table><p>例如，我们可以写 <code>cat 文件名 | grep -n 查找关键字</code>，查找关键字需要用字符串</p><p>相应地，我也可以直接 <code>grep -n 查找关键字 文件名</code></p><h2 id="文件压缩和解压">文件压缩和解压</h2><h3 id="gzip-和-gunzip-指令">gzip 和 gunzip 指令</h3><p><code>gzip</code>用于压缩<strong>文件</strong>，压缩后文件的后缀<strong>只能</strong>为<code>.gz</code></p><p><code>gunzip</code> 用于解压文件，解压后缀为 <code>.gz</code>的文件</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs awk">gzip 文件      <span class="hljs-regexp">//</span>得到 文件.gz<br>gunzip 文件.gz <span class="hljs-regexp">//</span>得到 文件<br></code></pre></td></tr></table></figure><h3 id="zip-和-unzip-指令">zip 和 unzip 指令</h3><p><code>zip</code>用于<strong>压缩文件或目录</strong>，<code>unzip</code> 用于解压</p><p>上面的 <code>gzip</code> 只能用于压缩文件，目录是不能压缩的</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-built_in">zip</span> [选项] 压缩文件名.<span class="hljs-built_in">zip</span> 待压缩文件<br>unzip [选项] 压缩文件名.<span class="hljs-built_in">zip</span><br></code></pre></td></tr></table></figure><p><code>zip</code> 常用选项：</p><p><code>-r</code> ：递归地进行压缩，用于压缩目录</p><p><code>unzip</code> 常用选项</p><p><code>-d</code> ：指定解压后文件存放的<strong>目录</strong></p><h3 id="tar-指令">tar 指令</h3><p><code>tar</code>既可以用于压缩也可以用于解压，其对象既可以是文件也可以是目录，最后打包得到的是<code>文件名.tar.gz</code> 文件</p><p>至于如何确定是压缩还是解压，通过选项来确定</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stylus">tar <span class="hljs-selector-attr">[选项]</span> xxx<span class="hljs-selector-class">.tar</span><span class="hljs-selector-class">.gz</span> 待打包文件<br></code></pre></td></tr></table></figure><table><thead><tr class="header"><th style="text-align: center;">选项</th><th style="text-align: center;">功能</th></tr></thead><tbody><tr class="odd"><td style="text-align: center;">-c</td><td style="text-align: center;">产生 .tar 打包文件</td></tr><tr class="even"><td style="text-align: center;">-v</td><td style="text-align: center;">显示详细信息</td></tr><tr class="odd"><td style="text-align: center;">-f</td><td style="text-align: center;">指定压缩后的文件名</td></tr><tr class="even"><td style="text-align: center;">-z</td><td style="text-align: center;">打包同时压缩</td></tr><tr class="odd"><td style="text-align: center;">-x</td><td style="text-align: center;">解包</td></tr></tbody></table><p>压缩文件可以写：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stylus">tar -zcvf xxx<span class="hljs-selector-class">.tar</span><span class="hljs-selector-class">.gz</span> 待压缩文件<br></code></pre></td></tr></table></figure><p>后面可以写多个文件</p><p>解压可以写：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">tar</span> -zxvf xxx.tar.gz<br></code></pre></td></tr></table></figure><p>解压到指定目录：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stylus">tar -zxvf xxx<span class="hljs-selector-class">.tar</span><span class="hljs-selector-class">.gz</span> -C 目录名<br></code></pre></td></tr></table></figure><h2 id="组管理和权限管理">组管理和权限管理</h2><p>Linux 中，每个用户都会所在一个组，反过来，组可以更方便地管理用户</p><p>Linux 中的每个文件均有三个概念：所有者，所在组，其他组</p><ul><li>所有者是指该文件的创建用户是谁</li><li>所在组是指该文件的创建用户所在的组</li><li>其他组是指除了该文件所在的组以外的其他组</li></ul><h3 id="chown-指令">chown 指令</h3><p><code>change own</code>改变所有者，该指令可以修改一个<strong>文件或目录</strong>的所有者</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chown</span> 所有者 文件名<br></code></pre></td></tr></table></figure><p>当然也可以同时更改一个<strong>文件或目录</strong>的所有者和所在组</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chown</span> 所有者:所在组 文件名<br></code></pre></td></tr></table></figure><p>例如：<code>chown chisato:chisato test</code></p><p>如果需要对<strong>整个目录</strong>的所有者或所在组进行修改的话，可以加<code>-R</code> 选项来让操作递归下去</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chown</span> -R 所有者 文件名<br></code></pre></td></tr></table></figure><h3 id="chgrp-指令">chgrp 指令</h3><p><code>change group</code>修改所在组，该指令可以修改一个<strong>文件或目录</strong>的所在组</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chgrp</span> 组名 文件名<br></code></pre></td></tr></table></figure><p>如果需要更改一个目录当中的所有的文件，可以加上 <code>-R</code>参数，即：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chgrp</span> -R 组名 目录名<br></code></pre></td></tr></table></figure><h3 id="修改用户所在组">修改用户所在组</h3><p>在添加用户时，我们可以指定用户所在组，即：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">useradd</span> -g 组名 用户名<br></code></pre></td></tr></table></figure><p>除此之外，我们可以用 <code>root</code> 权限更改某个用户所在组</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">usermod</span> -g 新组名 用户名<br></code></pre></td></tr></table></figure><p>除此之外，<code>usermod</code> 还可以修改用户的初始登录目录，即：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">usermod</span> -d 目录名 用户名<br></code></pre></td></tr></table></figure><p>需要注意的是，用户必须要有能够进入该目录的权限，否则会修改失败</p><h3 id="权限">权限</h3><p>用 <code>ls -l</code> 指令显示内容后，第一列（一共 10个字母）表示该文件或目录的权限</p><p>0 - 9 位的详细说明：</p><ol type="1"><li><p>第 0 位表示文件类型</p><ul><li><p><code>l</code> 是链接，对应 <code>windows</code>里的快捷方式</p></li><li><p><code>d</code> 是目录，也就是文件夹</p></li><li><p><code>c</code> 是字符设备文件、鼠标、键盘</p></li><li><p><code>b</code> 是块设备，比如硬盘</p></li><li><p><code>-</code> 表示该文件就是一个普通文件</p></li></ul></li><li><p>第 1 - 3 位为该文件<strong>所有者</strong>对该文件的权限</p></li><li><p>第 4 - 6位为该文件<strong>所在组</strong>中的用户对该文件的权限</p></li><li><p>第 7 - 9位为该文件<strong>其他组</strong>中的用户对该文件的权限</p></li></ol><p>第 1 - 9 位均是描述权限，权限有三种：<code>r</code> 、<code>w</code>、<code>x</code></p><p><code>r</code> 表示 <code>read</code> ，可读</p><p><code>w</code> 表示 <code>write</code> ，可写</p><p><code>x</code> 表示 <code>execute</code> ，可执行</p><p><code>rwx</code> 作用到文件与作用到目录是不一样的，具体如下：</p><ul><li><code>rwx</code> 作用到文件<ul><li><code>r</code> 表示可读：允许对该文件进行读取、查看</li><li><code>w</code>表示可写：允许对该文件进行修改，但<strong>不能删除</strong>。如果要删除文件需要对该文件所在的目录有<code>w</code> 的权限</li><li><code>e</code> 表示可执行：允许该文件被执行</li></ul></li><li><code>rwx</code> 作用到目录<ul><li><code>r</code> 表示可读：允许读取、查看该目录的内容</li><li><code>w</code>表示可写：允许对该目录内的文件进行创建、删除的操作，还允许对该目录进行重命名</li><li><code>x</code> 表是可执行：允许用户进入到这个目录。如果没有<code>x</code> 权限则用户无法进入该目录</li></ul></li></ul><p>Linux 中还可以用数字来表示权限，如：<code>r = 4</code>、<code>w = 2</code> 、<code>x = 1</code> ，因此<code>rwx = 7</code></p><h3 id="chmod-指令">chmod 指令</h3><p>通过 <code>chmod</code> 指令，可以修改文件或者目录的权限</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chmod</span> [选项] 文件名<br></code></pre></td></tr></table></figure><p>有两种方式可以对权限进行修改</p><ul><li>用 <code>+</code> 、<code>-</code> 、<code>=</code> 进行修改<ul><li><code>u</code> 表示 <code>user</code> ，即该文件的所有者</li><li><code>g</code> 表示 <code>group</code> ，即该文件的所在组</li><li><code>o</code> 表示 <code>other</code> ，即该文件的其他组</li><li><code>a</code> 表示 <code>all</code> ，即全部</li><li><code>+</code> 表示添加该权限，<code>-</code>表示去掉该权限，<code>=</code> 就是直接赋值</li></ul></li></ul><p>例如：</p><p>对所有者赋予 <code>rwx</code> ，所在组赋予 <code>rw</code>，其他组赋予 <code>x</code></p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs routeros">chmod <span class="hljs-attribute">u</span>=rwx,g=rw,o=x 文件名<br></code></pre></td></tr></table></figure><p>对所有人去除 <code>x</code></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chmod</span> a-x 文件名<br></code></pre></td></tr></table></figure><p>这里的<strong>各个参数之间是不允许打空格的</strong></p><ul><li>用数字变更权限<ul><li><code>r</code> 用 4 表示，<code>w</code> 用 2 表示，<code>x</code>用 1 表示</li><li>可以通过数字的加法来实现权限的更改</li></ul></li></ul><p>对所有者赋予 <code>rwx</code> ，所在组赋予 <code>rw</code>，其他组赋予 <code>x</code></p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">chmod</span> <span class="hljs-number">761</span> 文件名<br></code></pre></td></tr></table></figure><p>同样<strong>不能打空格</strong></p><h3 id="关于目录的-rwx-的讨论">关于目录的 rwx 的讨论</h3><p><code>x</code> 表示可以进入到该目录，可以执行 <code>cd</code></p><p><code>r</code> 表示可以读取该目录的文件，可以执行 <code>ls</code></p><p><code>w</code> 表示可以对该目录当中的文件进行增加或修改</p><h2 id="crond-任务调度">crond 任务调度</h2><p>如果我们希望系统<strong>自动定时</strong>帮我们去做一些任务，可以通过<code>crontab</code> 进行定时任务设置</p><p>任务调度：指系统在某个时间执行特点的命令或程序</p><p>任务调度分为系统工作和个别用户工作，前者是指某些工作必须要周而复始地执行，例如病毒扫描；后者是指个别用户希望执行某些程序，例如数据库备份</p><h3 id="crontab-指令">crontab 指令</h3><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">crontab</span><span class="hljs-meta"> [选项]</span><br></code></pre></td></tr></table></figure><p>常用选项：</p><p><code>-e</code> ：编辑 <code>crontab</code> 定时任务</p><p><code>-l</code> ：查询 <code>crontab</code> 任务</p><p><code>-r</code> ：删除当前用户所有 <code>crontab</code> 任务</p><p>这里关于时间的设置我们需要重点记一下</p><p>一般我们会这么写：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs awk">crontab -e<br>* * * * * ls -l <span class="hljs-regexp">/home/</span> &gt; <span class="hljs-regexp">/home/</span>log.txt<br></code></pre></td></tr></table></figure><p>后面是一个重定向，我们重点看前面那 5 个 <code>*</code> 号</p><p>第一个 * ，表示一小时当中的第几分钟</p><p>第二个 * ，表示一天当中的第几小时</p><p>第三个 * ，表示一个月当中的第几天</p><p>第四个 * ，表示一年当中的第几月</p><p>第五个 * ，表示一周当中的星期几，范围：0 - 7（0 与 7均表示星期天）</p><p>特殊符号说明：</p><p><code>*</code> 表示任何时间</p><p><code>,</code> 表示不连续的时间</p><p><code>-</code> 表示时间范围</p><p><code>*/n</code> 表示多久执行一次</p><h2 id="硬盘分区">硬盘分区</h2><p>Linux的文件系统中，最上层的是一个根目录，其他目录是在根目录之下可以通过根目录来进行访问的存在。但这仅仅是文件系统的结构，我们还需要将硬盘的存储空间与每个目录之间联系起来</p><p>Linux采用了一种称为『载入』的处理办法。硬盘当中可以对空间进行划分，得到一个一个分区，Linux可以将分区直接『挂载』到对应的目录下，这样便可以通过目录来访问对应的硬盘空间了</p><h3 id="硬盘说明">硬盘说明</h3><p>Linux 分 IDE 硬盘和 SCSI 硬盘，目前基本上都是后者</p><p>对于 IDE 硬盘，标识符为 hdx~ ；对于 SCSI 硬盘，标识符为 sdx~</p><p>这里我们介绍 SCSI ，IDE 同理</p><ul><li>sd 用来表示分区所在设备的类型（指 SCSI 硬盘）</li><li>x 为盘号（a 为基本盘，b为基本从属盘，c 为辅助主盘，d为辅助从属盘）</li><li>~ 表示分区，前四个分区用数字 1 到 4表示，它们表示主分区或拓展分区，从第 5 开始是逻辑分区</li></ul><h3 id="lsblk-指令">lsblk 指令</h3><p>我们可以用 <code>lsblk</code> 或者 <code>lsblk -l</code>来查看<strong>分区情况</strong>，即：</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">lsblk</span><br><span class="hljs-attribute">lsblk -l</span><br></code></pre></td></tr></table></figure><h3 id="df--h-指令">df -h 指令</h3><p>查询系统整体<strong>硬盘</strong>使用情况</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">df</span> -h<br></code></pre></td></tr></table></figure><p>当分区使用率到达百分之八十以上，需要去删除文件或者拓展分区</p><h3 id="du--h-指令">du -h 指令</h3><p>用于查询指定目录的硬盘占用情况，默认为当前目录</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">du</span> [选项] 目录名<br></code></pre></td></tr></table></figure><p>常用选项：</p><ul><li><code>-s</code> ：指定目录占用大小汇总</li><li><code>-h</code> ：带计量单位</li><li><code>-a</code> ：含文件</li><li><code>--max-depth=</code> ：子目录列举深度，建议写 1</li><li><code>-c</code> ：列出明细的同时，增加汇总值</li></ul><h3 id="磁盘查询常用指令">磁盘查询常用指令</h3><p>统计 <code>/home</code> 目录下文件的个数：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ls</span> -l | grep <span class="hljs-string">&quot;^-&quot;</span> | <span class="hljs-built_in">wc</span> -l<br></code></pre></td></tr></table></figure><p>统计 <code>/home</code> 目录下文件夹的个数：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ls</span> -l | grep <span class="hljs-string">&quot;^d&quot;</span> | <span class="hljs-built_in">wc</span> -l<br></code></pre></td></tr></table></figure><p>统计 <code>/home</code> 目录下的文件个数，包括子文件夹内的</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ls</span> -Rl | grep <span class="hljs-string">&quot;^-&quot;</span> | <span class="hljs-built_in">wc</span> -l<br></code></pre></td></tr></table></figure><p><code>ls</code> 那里的 <code>R</code> 表示递归列举</p><h2 id="进程">进程</h2><p>在 Linux 中，每个执行的程序都被称为一个进程，每个进程都会有一个PID</p><p>进程的可能以两种方式进行存在：前台和后台</p><p>前台程序就是目前用户在屏幕上自己操作的，后台进程是屏幕上无法看到但实际在运行的</p><p>一般系统服务会以后台的形式常住在系统中，直到关机才结束</p><h3 id="ps-指令">ps 指令</h3><p>ps指令是用于查看目前系统中，有哪些正在执行，以及它们执行的状况。可以不加参数执行</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">ps</span><span class="hljs-meta"> [选项]</span><br></code></pre></td></tr></table></figure><p><code>-a</code> ：显示当前终端所有进程信息</p><p><code>-u</code> ：以用户的格式显示进程信息</p><p><code>-x</code> ：显示后台进程运行的参数</p><p><code>-e</code> ：显示所有进程</p><p><code>-f</code> ：全格式</p><p>指令说明：</p><p>USER ：用户名称</p><p>PID ：进程号</p><p>%CPU ：进程占用 CPU 的百分比</p><p>%MEM ：进程占用物理内存的百分比</p><p>VSZ ：进程占用虚拟内存的大小</p><p>RSS ：进程占用物理内存的大小</p><p>TTY ：终端名称</p><p>STAT ：进程状态</p><ul><li>S 睡眠，s 表示该进程三会话的先导进程</li><li>N 表示进程拥有比普通优先级更低的优先级</li><li>R 正在运行</li><li>D 短期等待</li><li>Z 僵死进程</li><li>T 被跟踪或者被停止</li></ul><h3 id="kill-和-killall-指令">kill 和 killall 指令</h3><p><code>kill</code> 和 <code>killall</code>均可以用来终止进程，基本语法为：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">kill</span> [选项] 进程号<br>killall 进程名称<br></code></pre></td></tr></table></figure><p><code>killall</code>会终止掉一个进程及其所有子进程，<code>kill</code> 只会终止调当前进程</p><p>有的时候，<code>kill</code> 作用在某些进程上会失效，原因在于 Linux系统认为这是重要的进程，会自动忽略调该指令。如果需要强制停止可以加上<code>-9</code> 这个选项</p><h3 id="pstree-指令">pstree 指令</h3><p>以树的形式显示进程信息，看的更加直观</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">pstree</span><span class="hljs-meta"> [选项]</span><br></code></pre></td></tr></table></figure><p>常用选项：</p><p><code>-p</code> ：显示进程的 PID</p><p><code>-u</code> ：显示进程的所属用户</p><h2 id="服务">服务</h2><p>服务的本质就是进程，通常是在后台运行，来监听某个端口等待其他程序的请求（比如：mysqld,sshd, 防火墙），因此我们也称服务为守护进程</p><h3 id="service-指令">service 指令</h3><p>我们可以用 <code>service</code> 指令来管理服务，指令如下：</p><figure class="highlight coq"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs coq">service 服务名 [start | <span class="hljs-type">stop</span> | <span class="hljs-type">restart</span> | <span class="hljs-type">reload</span> | <span class="hljs-type">status</span>]<br></code></pre></td></tr></table></figure><p>我们可以在 <code>/etc/init.d</code> 当中查看 <code>service</code>管理的服务名称，即：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">ls -l <span class="hljs-regexp">/etc/i</span>nit.d/<br></code></pre></td></tr></table></figure><p>开机流程说明：</p><p><code>开机</code> &gt;&gt; <code>BIOS</code> &gt;&gt;<code>/boot</code> &gt;&gt; <code>systemd进程 1</code> &gt;&gt;<code>运行级别</code> &gt;&gt; <code>运行级别对应的服务</code></p><h3 id="chkconfig-指令">chkconfig 指令</h3><p>全部是 <code>check config</code>，用于设置服务在各个运行级别的自启动或者不自启动</p><p>基本语法：</p><figure class="highlight awk"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs awk">chkconfig --list  <span class="hljs-regexp">//</span>查看服务<br></code></pre></td></tr></table></figure><p>修改某个服务在 5 运行级别下的自启动</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">chkconfig</span> --level <span class="hljs-number">5</span> 服务名 <span class="hljs-literal">on</span>/<span class="hljs-literal">off</span><br></code></pre></td></tr></table></figure><p><code>chkconfig</code> 设置完毕后需要重启机器才能生效</p><p>需要注意的是，这个指令在 Ubuntu 下使用不了</p><h3 id="systemctl-指令">systemctl 指令</h3><p>该指令管理的服务在 <code>/usr/lib/systemd/system</code> 查看</p><p>管理服务指令：</p><figure class="highlight coq"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs coq">systemctl [start | <span class="hljs-type">stop</span> | <span class="hljs-type">restart</span> | <span class="hljs-type">status</span>]<br></code></pre></td></tr></table></figure><p>查看服务开机启动状态</p><figure class="highlight delphi"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs delphi">systemctl list-<span class="hljs-keyword">unit</span>-files<br></code></pre></td></tr></table></figure><p>设置服务开机自启动</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs routeros">systemctl <span class="hljs-built_in">enable</span> 服务名<br></code></pre></td></tr></table></figure><p>关闭服务开机启动</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs routeros">systemctl <span class="hljs-built_in">disable</span> 服务名<br></code></pre></td></tr></table></figure><p>查询某个服务是否自启动</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">systemctl <span class="hljs-keyword">is</span>-<span class="hljs-keyword">enable</span> 服务名<br></code></pre></td></tr></table></figure><h2 id="shell-编程">Shell 编程</h2><p>Shell 是一个命令行解释器，它介于外层应用程序和 Linux内核之间。它为用户提供了一个向 Linux内核发送请求以便运行程序的界面系统级程序</p><ul><li>脚本格式要求</li></ul><p>脚本需要以 <code>#!/bin/bash</code> 开头</p><p>脚本需要有可执行权限</p><p><code>shell</code> 脚本一般以 <code>.sh</code> 来结尾</p><ul><li>执行方式</li></ul><p>输入绝对路径或相对路径（需要有执行权限）</p><p>sh + 脚本（不需要执行权限）</p><h3 id="shell-变量">Shell 变量</h3><p>Linux 当中的 Shell 变量分为系统变量和用户自定义变量</p><p>系统变量为：$HOME, $PWD, $SHELL, $USER 等</p><p>可以用 <code>set</code> 来显示当前 Shell 当中的所有变量</p><p>​</p><p>定义变量：变量名=值（不能加空格）</p><p>撤销变量：unset 变量</p><p>声明静态变量：readonly 变量，静态变量不能 <code>unset</code></p><p>变量名一般习惯大写</p><p>Shell里面的变量没有类型之分，因此可以将指令的结果赋值给变量，具体操作为：<code>A='date'</code>或者 <code>A=$(date)</code> 需要用反引号或在 $来指明，否则系统会认为这是一个字符串</p><p>​</p><p>设置环境变量</p><p>在 <code>/etc/profile</code> 文件中添加<code>export 变量名=变量值</code></p><p>之后用 <code>source /etc/profile</code>更新一下文件，这样便新建了一个环境变量</p><p>这个环境变量可以在所有的 Shell 脚本当中使用</p><p>​</p><p>Shell 脚本当中的多行注释的写法</p><figure class="highlight erlang-repl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs erlang-repl">:&lt;&lt;!<br>内容<br>!<br></code></pre></td></tr></table></figure><p>​</p><p>位置参数变量</p><p>在我们用命令行执行 Shell脚本时，可以向里面传入参数，这个参数也就是位置参数，基本语法如下：</p><p><code>$n</code> ：<code>n</code> 为数字，<code>$1-9</code>表示第一到第九个参数，十以上的参数需要用 <code>$&#123;10&#125;</code> 这样表示</p><p><code>$*</code>：拿到命令行当中的所有参数，会将所有传入的参数当成一个整体</p><p><code>$@</code> ：同样也是拿到所有参数，不过会将每个参数区分对待</p><p><code>$#</code> ：表示命令行当中参数的个数</p>]]></content>
    
    
    <categories>
      
      <category>Linux</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Linux</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>多重背包之单调队列优化</title>
    <link href="/2022/09/16/Algorithm%20Solution/%E5%A4%9A%E9%87%8D%E8%83%8C%E5%8C%85%E4%B9%8B%E5%8D%95%E8%B0%83%E9%98%9F%E5%88%97%E4%BC%98%E5%8C%96/"/>
    <url>/2022/09/16/Algorithm%20Solution/%E5%A4%9A%E9%87%8D%E8%83%8C%E5%8C%85%E4%B9%8B%E5%8D%95%E8%B0%83%E9%98%9F%E5%88%97%E4%BC%98%E5%8C%96/</url>
    
    <content type="html"><![CDATA[<h3 id="问题描述">问题描述</h3><p>有 <span class="math inline">\(N\)</span> 种物品和一个容量是 <span class="math inline">\(V\)</span> 的背包，每种物品数量有限</p><p>第 <span class="math inline">\(i\)</span> 件物品的体积为 <span class="math inline">\(v[i]\)</span> ，价值为 <span class="math inline">\(w[i]\)</span> ，数量为 <span class="math inline">\(s[i]\)</span></p><p>求解将哪些物品装入背包使得总价值最大</p><p>​</p><h3 id="前言">前言</h3><p>前天，我晚上坐在图书馆像往常一样打开了三叶姐姐背包合集当中的多重背包，毕竟学到这个了嘛</p><p>第一篇，就是最朴素的解法，因为有完全背包的处理经验，所以我觉得很简单，大概看了一下问题描述就去敲代码了，最后的代码在思路上基本一致，我也就过了。毕竟简单，差不多花了十分钟左右吧</p><p>然后第二篇，二进制优化。说实话我初步看的时候卡了一下，她举了个例子，说是10 里面的数可以拆成 1 2 4 3这四个数的不同排列组合，看到这个地方我其实有点懵，但我还是继续去看代码</p><p>关于代码的循环部分为什么要不断乘 2，这个问题一开始确实是卡到我了。但后来我从时间复杂度的角度想到，这个算法是将时间复杂度降到<span class="math inline">\(O(n^2logn)\)</span> ，也就是用 <span class="math inline">\(logn\)</span> 个数去替代掉原先的 <span class="math inline">\(n\)</span> 个数，而 <span class="math inline">\(logn\)</span> 就是 2 的次幂，因此不断乘 2的原因也就出来了</p><p>至于那个例子当中的 3 ，这属于是 10 不断减去 2的次幂得到的余数。至此，代码思路我已经完全理解了，后面就是简单的 0-1背包。到这里为止，我差不多花了三四十分钟吧，还算是比较简单</p><p>然后，那天晚上我以为，我能把最终 <span class="math inline">\(O(n^2)\)</span>的优化看完（事实证明我还是太天真了</p><p>这个算法，我对它的印象就是？？？？？？？？？？？？？？</p><p>什么玩意儿？？？？？？？？</p><p>取余？？？？单调队列？？？？？？</p><p>然后我想着，去看看代码，说不定就会了呢，毕竟二进制优化我就是看代码看明白的</p><p>然后，我看到那个那个循环变量初值的设置，我？？？？？？？？？</p><p>往下看，那个循环的判断，我。。。。我怕不是个智障（别骂了别骂了，qwq</p><p>那天晚上就是在死磕那道题，在必应上面各种找，最后也是什么都没有看明白</p><p>转机发生在昨天</p><p>我尝试去做了一下单调队列的经典问题之后，大概理解了这玩意的思想</p><p>我去 Acwing上面找了两个题解，对着看，然后自己在那里想，最后拿着纸笔一步一步从数学的角度来推，最后才大概理解了</p><p>不得不说，昨天那三个多小时的专注，我只想说，好爽啊！一回神，三个小时没了（下次还敢（（</p><p>然后今天凌晨的时候把代码敲了一下，有一些细节方面的问题，但我的理解是没错的</p><p>今天晚上把整个思路整理了一下，写出来这篇博客</p><p>姑且记录一下这三天的心路历程，太TM艰辛了（唉，人都想麻了，终于会了</p><p>我不敢保证我的思路就一定没有错误，但我觉得它是逻辑自洽的（确信，哈哈哈</p><p>嘛，想说的就这些</p><p>​</p><h3 id="单调队列优化">单调队列优化</h3><p>关于这个问题的朴素解法和二进制优化，我们在前面已经说过了，这两个没有什么思维难度，我们主要看这个</p><p>我们 <span class="math inline">\(dp\)</span> 数组的定义是：考虑前<span class="math inline">\(i\)</span> 个物品，在背包容量不超过 <span class="math inline">\(j\)</span> 的情况下的最大价值为 <span class="math inline">\(f(i,j)\)</span> （我们这里为了便于讨论，用 <span class="math inline">\(f\)</span> 替代 <span class="math inline">\(dp\)</span> ）</p><p>原先朴素解法的时间复杂度为 <span class="math inline">\(O(n^3)\)</span> ，在 <span class="math inline">\(f\)</span> 定义不变的情况下，由于存在 <span class="math inline">\(O(n^2)\)</span> 个状态，因此时间复杂度最小都是<span class="math inline">\(O(n^2)\)</span></p><p>这里多出来的 <span class="math inline">\(O(n)\)</span>是因为需要对每个物品遍历 <span class="math inline">\(s[i]\)</span>次，也就是总状态为 <span class="math inline">\(O(n^3)\)</span></p><p>二进制优化的点在于，减少对每个物品的遍历次数，将遍历 <span class="math inline">\(s[i]\)</span> 次降为遍历 <span class="math inline">\(log\ s[i]\)</span>次，这里是基于对物品个数的优化</p><p>我们现在考虑对状态进行分组</p><p>一般地，设第 <span class="math inline">\(i\)</span> 件物品的体积为<span class="math inline">\(vi\)</span> ，价值为 <span class="math inline">\(wi\)</span> ，个数为 <span class="math inline">\(si\)</span></p><p><span class="math inline">\(f(i,j)\)</span> 的状态只能由 <span class="math inline">\(f(i,j-vi) ,\ f(i,j-2*vi) ,\ f(i,j-3*vi),\ \cdots,\f(i,j-si*vi)\)</span></p><p>观察上式，不难发现： <span class="math inline">\(j-si*vi\)</span>会得到一个余数，<strong>而对于同一个余数内的所有状态而言，它们只能由前面跟该状态余数相同的状态转移过来</strong></p><p>所以，我们便可以通过「同余」这一点来进行分组，我们只需要单独处理组内的状态，再将所有组合起来就得到了所有状态</p><p>更一般地，我们有： <span class="math display">\[f(i,j)=max(f(i,j),\ f(i,j-v)+w,\ f(i, j-2v)+2w,\ \cdots,\ f(i,j-s*v)+s*w)\]</span> 考虑与完全背包一样的处理方法，将 <span class="math inline">\(j\)</span> 向后减一位，有： <span class="math display">\[f(i,j-v)=max(f(i,j-v),\ f(i,j-2v)+w,\ f(i,j-3v)+2w,\ \cdots,\f(i,j-(s+1)*v)+w)\]</span> 当我们将这个过程重复下去，有： <span class="math display">\[\left\{\begin{array}{l}f(i,j)=max(f(i,j),\ f(i,j-v)+w,\ f(i, j-2v)+2w,\ \cdots,\ f(i,j-s*v)+s*w)\\f(i,j-v)=max(f(i,j-v),\ f(i,j-2v)+w,\ f(i,j-3v)+2w,\ \cdots,\f(i,j-(s+1)*v)+w)\\f(i,j-2v)=max(f(i,j-2v),\ f(i,j-3v)+w,\ f(i,j-4v)+2w),\ \cdots,\f(i,j-(s+2)*v)+w)\\\cdots \\f(i,r+s*v)=max({\color{violet}f(i,r+s*v),\ f(i,r+(s-1)*v)+w,\ \cdots,\f(i,r)+s*w})\\f(i,r+2v)=max(f(i,r+2v),\ f(i,r+v)+w,\ f(i,r)+2w)\\f(i,r+v)=max(f(i,r+v),\ f(i,r)+w)\\f(i,r)=f(i,r)\end{array}\right.\]</span> 其中 <span class="math inline">\(r=j \bmod v\)</span></p><p>如此这般，我们便实现了对 <span class="math inline">\(j\)</span>的余数进行分组</p><p>观察上面这个过程，会有如下两个结论：</p><ul><li>自顶向下看，长度会先不变后减小<ul><li>在长度不变时，<strong>改变的是该长度窗口内的状态</strong></li></ul></li><li>自底向上看，长度会先增大，在达到 <span class="math inline">\((s+1)\)</span> 时不再改变<ul><li>由于该种物品只有 <span class="math inline">\(s\)</span>个，因此窗口内可以达到的最大状态为 <span class="math inline">\((s+1)\)</span> （<span class="math inline">\(v\)</span> 前面的系数从 <span class="math inline">\(0\)</span> 增长到 <span class="math inline">\(s\)</span> ，因此为 <span class="math inline">\((s+1)\)</span>）</li></ul></li></ul><p>​</p><p>到此为止，我们将所有的状态按照<strong>同余</strong>进行了分组，并且对于某个状态<span class="math inline">\(j\)</span>，其值只能由同组当中的其他状态转移而来</p><p>并且，这里的「其他状态」是有<strong>数量限制</strong>的，这便构成了一个滑动窗口</p><p>对于状态 <span class="math inline">\(f(i,j)\)</span>的值，我们需要找出该滑动窗口当中的<strong>最大值</strong>来进行更新</p><p>在滑动窗口中找最大值，这就是经典的单调队列问题了</p><p>​</p><p>具体操作如下：</p><p>我们将二维 <span class="math inline">\(dp[i][j]\)</span>数组压缩成一维 <span class="math inline">\(dp[j]\)</span>，其<strong>定义不变</strong></p><p>原本进行状态压缩的时候，<strong>循环是不需要变动的</strong>，这也就导致了未对<code>dp</code> 数组赋值之前，其值表示前一行的数值</p><p>但这里，我们仅仅是外层循环不变（因为我们还是需要对 <span class="math inline">\(N\)</span>种物品进行遍历），第二层循环需要通过<strong>枚举余数来进行分组</strong></p><p>由于第二层循环不再是<strong>枚举背包剩余容量</strong>，因此我们需要一个<strong>辅助数值来记录上一次<code>dp</code> 数组当中的数值</strong></p><p>第二层枚举余数的时候，每次自增 1 ，每枚举一次，就相当于是一个组</p><p>对于每个组，我们都需要一个单调队列，并且组的个数一定是小于<code>v[i]</code> 的（因为这是对除以 <code>v[i]</code>得到的余数进行枚举，最大值只能是 <code>v[i]-1</code>）</p><p>在最内层，我们需要对同一组当中的所有状态进行枚举（添加到单调队列当中），因此这一层每次自增的大小为<code>v[i]</code></p><p>可以看到，虽然循环有三层，但里面两层都是在考虑物品恒定的情况下枚举所有的状态，这里一共的时间复杂度是<span class="math inline">\(O(n)\)</span> （这里相当于原本二维<code>dp</code> 函数当中枚举某一行）</p><p>所有总共的时间复杂度为 <span class="math inline">\(O(n^2)\)</span></p><p>​</p><p>代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">back</span><span class="hljs-params">(<span class="hljs-type">int</span> N, <span class="hljs-type">int</span> V, vector&lt;<span class="hljs-type">int</span>&gt;&amp; s, vector&lt;<span class="hljs-type">int</span>&gt;&amp; v, vector&lt;<span class="hljs-type">int</span>&gt;&amp; w)</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">dp</span>(V + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>);<br>    vector&lt;<span class="hljs-type">int</span>&gt;pre;<span class="hljs-comment">//辅助队列，对上一次dp数组的值进行保留</span><br>    vector&lt;<span class="hljs-type">int</span>&gt;q;<span class="hljs-comment">//当前队列，对窗口内的状态进行处理</span><br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; N; i++)<br>    &#123;<br>        pre = dp;<span class="hljs-comment">//将dp数组当中所有状态的值赋值给pre</span><br><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; v[i]; j++)<span class="hljs-comment">//枚举余数</span><br>        &#123;<br>            <span class="hljs-type">int</span> tail = <span class="hljs-number">-1</span>, head = <span class="hljs-number">0</span>;<span class="hljs-comment">//对于每个组，都需要一个单调队列</span><br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> k = j; k &lt;= V; k += v[i])<span class="hljs-comment">//枚举状态，每次自增一个v</span><br>            &#123;<br>                <span class="hljs-comment">//删去不在窗口内的元素，单调队列删除元素是在队头</span><br>                <span class="hljs-keyword">while</span> (head &lt;= tail &amp;&amp; q[head] &lt; k - s[i] * v[i])<br>                    head++;<br>                <span class="hljs-comment">//保证单调队列的单调性，从队尾删除</span><br>                <span class="hljs-keyword">while</span> (head &lt;= tail &amp;&amp; pre[q[tail]] - (q[tail] - j) / v[i] * w[i] &lt;= q[k] - (k - j) / v[i] * w[i])<br>                    tail--;<br>                <span class="hljs-comment">//给dp数组进行赋值，用前面已经保存的dp数组，即pre数组</span><br>                <span class="hljs-keyword">if</span> (head &lt;= tail)<br>                    dp[k] = <span class="hljs-built_in">max</span>(dp[k], pre[q[head]] + (k - q[head]) / v[i] * w[i]);<br>                <span class="hljs-comment">//将状态插入队列</span><br>                q[++tail] = k;<br>            &#125;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> dp[V];<br>&#125;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span></p><p>​</p><p>这里我们详细说明一下最内层当中四行代码的意义，其余部分在上面均有解释，在此不过多说明</p><ul><li><code>while (head &lt;= tail &amp;&amp; q[head] &lt; k - s[i] * v[i])</code></li></ul><p>由于我们是一个滑动窗口，并且该窗口大小恒定，那么必然会有尾端的状态离开窗口</p><p>回顾上面的推导，窗口内的状态是从 <span class="math inline">\(f(i,j)\)</span> 到 <span class="math inline">\(f(i,j-s*v)\)</span> ，因此当队列头部的状态下标小于<span class="math inline">\(k - s * v\)</span> 的话（<strong>这里的<span class="math inline">\(k\)</span> 是该组当中的所有状态，即 <span class="math inline">\(k=t*v+j\)</span> ，其中 <span class="math inline">\(0\le t\les\)</span></strong>），需要将该元素出队</p><p>由于这是个单调队列，需要维护队列内部元素的单调性，因此出队的元素只能是最大值并且从队头出队</p><ul><li><code>while (head &lt;= tail &amp;&amp; pre[q[tail]] - (q[tail] - j) / v[i] * w[i] &lt;= q[k] - (k - j) / v[i] * w[i])</code></li></ul><p>这一条是为了保证队列的单调性，当出现一个元素比队尾元素大时，需要将小于该元素的队中元素从队尾排出</p><p><code>pre[q[tail]]</code> 表示在 <code>dp</code>数组当中处于「队尾」元素的状态的值，那么为什么要减去一个值呢？</p><p>观察上面的式子，每一条式子当中的对应项总是差 <span class="math inline">\(t*w\)</span> 。反过来说，我们从 <span class="math inline">\(f(i,j)\)</span> 转移到 <span class="math inline">\(f(i,j+v)\)</span> 与 <span class="math inline">\(f(i,j+2v)\)</span> 所需要加上的 <span class="math inline">\(w\)</span>是不一样的，而这里我们又是从前往后推，因此需要减去对应的 <span class="math inline">\(w\)</span></p><p>这里的 <span class="math inline">\(t*w\)</span> 很容易求出，由于<span class="math inline">\(q[tail],\ k\)</span> 均是类似于 <span class="math inline">\(t*v+j\)</span> 的形式，因此拿它们减去 <span class="math inline">\(j\)</span> 再除以 <span class="math inline">\(v\)</span> 乘上 <span class="math inline">\(w\)</span> 就是对应的 <span class="math inline">\(t*w\)</span> 的值</p><ul><li><code>dp[k] = max(dp[k], pre[q[head]] + (k - q[head]) / v[i] * w[i])</code></li></ul><p>这里我们是对 <code>dp[k]</code>这个状态进行赋值，其值主要来自于队列的队头</p><p>由于我们是从队头来推 <code>dp[k]</code> ，因此我们需要加上一个 <span class="math inline">\(t*w\)</span></p><p>我们知道，<span class="math inline">\(k\)</span>所代表的状态一定在队头所表示的状态的后面，因此 <span class="math inline">\(k\)</span> 一定大于队头状态 <span class="math inline">\(q[head]\)</span> ，后续处理跟上面一致</p><ul><li><code>q[++tail] = k</code></li></ul><p>这里我们是将各个不同的<strong>「状态」</strong>入队。这里的「状态」，指的是<strong>背包不同的体积</strong></p><p>由于我们的队列初始状态是 <code>tail = -1, head = 0</code>，因此入队是将 <code>tail++</code> ，队头出队是 <code>head++</code>，队尾出队是 <code>tail--</code> ，并且队列不为空的条件是<code>head &lt;= tail</code> ，初始时将二者设置相反也有利于判空</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>动规 多重背包问题</title>
    <link href="/2022/09/14/Algorithm%20Solution/%E5%8A%A8%E8%A7%84-%E5%A4%9A%E9%87%8D%E8%83%8C%E5%8C%85%E9%97%AE%E9%A2%98/"/>
    <url>/2022/09/14/Algorithm%20Solution/%E5%8A%A8%E8%A7%84-%E5%A4%9A%E9%87%8D%E8%83%8C%E5%8C%85%E9%97%AE%E9%A2%98/</url>
    
    <content type="html"><![CDATA[<h3 id="多重背包">多重背包</h3><p>问题描述：</p><p>有 <code>N</code> 种物品和一个最多能背重量为 <code>W</code>的背包。第 <code>i</code> 件物品的重量是 <code>w[i]</code>，得到的价值是 <code>v[i]</code> ，<strong>可以选择的次数是<code>s[i]</code></strong></p><p>求解将哪些物品装入背包里使得物品价值总和最大</p><p>我们看到， 0-1背包是每件物品<strong>只能选择一次</strong>，完全背包是每一件物品可以<strong>选择无数次</strong>，而多重背包是每一件物品可以<strong>选择有限次</strong></p><p>这三者的区别也就在对物品的选择次数上面了，直接套用前面的定义，我们给出<code>dp</code> 数组的定义：</p><p><code>dp[i][j]</code> 表示考虑前 <code>i</code>个物品，在背包容量不超过 <code>j</code> 的情况下所得到的最大价值</p><p>不失一般性地，我们考虑对第 <code>i</code>件物品进行选择（<code>i</code> 从 1 开始遍历）</p><p>选择 0 次第 <code>i</code> 件物品，有：<span class="math inline">\(dp[i][j]=dp[i-1][j]\)</span></p><p>选择 1 次第 <code>i</code> 件物品，有：<span class="math inline">\(dp[i][j]=dp[i-1][j-w[i-1]]+v[i-1]\)</span></p><p>选择 2 次第 <code>i</code> 件物品，有：<span class="math inline">\(dp[i][j]=dp[i-1][j-2*w[i-1]]+2*v[i-1]\)</span></p><p><span class="math inline">\(\cdots\)</span></p><p>选择 <span class="math inline">\(s[i-1]\)</span> 次第 <code>i</code>件物品，有：<span class="math inline">\(dp[i][j]=dp[i-1][j-s[i-1]*w[i-1]]+s[i-1]*v[i-1]\)</span></p><p>因此状态转移方程为：</p><p><span class="math inline">\(dp[i][j]=max(dp[i-1][j],dp[i-1][j-k*w[i-1]]+k*v[i-1])\)</span>其中 <span class="math inline">\(0\le k \le s[i-1],\ 0\le k*w[i-1]\lej\)</span></p><p>下面给出完整代码：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">back</span><span class="hljs-params">(<span class="hljs-type">int</span> N, <span class="hljs-type">int</span> W, vector&lt;<span class="hljs-type">int</span>&gt;&amp; v, vector&lt;<span class="hljs-type">int</span>&gt;&amp; w, vector&lt;<span class="hljs-type">int</span>&gt;&amp; s)</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(N + <span class="hljs-number">1</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(W + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>));<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt;= W; j++)<br>    &#123;<br>        <span class="hljs-type">int</span> maxselect = <span class="hljs-built_in">min</span>(j / w[j], s[j]);<br>        dp[<span class="hljs-number">0</span>][j] = maxselect * v[j];<br>    &#125;<br><br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= N; i++)<br>    &#123;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt;= W; j++)<br>        &#123;<br>            <span class="hljs-type">int</span> x = dp[i - <span class="hljs-number">1</span>][j];<span class="hljs-comment">//不选择物品</span><br>            <span class="hljs-type">int</span> y = <span class="hljs-number">0</span>;<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> k = <span class="hljs-number">1</span>; k &lt;= s[i]; k++)<br>            &#123;<br>                <span class="hljs-keyword">if</span> (j &lt; k * s[i - <span class="hljs-number">1</span>])<br>                    <span class="hljs-keyword">break</span>;<br>                y = <span class="hljs-built_in">max</span>(y, dp[i - <span class="hljs-number">1</span>][j - k * s[i - <span class="hljs-number">1</span>]] + k * v[i - <span class="hljs-number">1</span>]);<br>            &#125;<br>            dp[i][j] = <span class="hljs-built_in">max</span>(x, y);<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> dp[N][W];<br>&#125;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^3)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n^2)\)</span></p><h3 id="一维空间优化">一维空间优化</h3><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">back</span><span class="hljs-params">(<span class="hljs-type">int</span> N, <span class="hljs-type">int</span> W, vector&lt;<span class="hljs-type">int</span>&gt;&amp; v, vector&lt;<span class="hljs-type">int</span>&gt;&amp; w, vector&lt;<span class="hljs-type">int</span>&gt;&amp; s)</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">dp</span>(W + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>);<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt;= N; i++)<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = W; j &gt;= w[i]; j--)<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> k = <span class="hljs-number">1</span>; k &lt;= s[i] &amp;&amp; j &gt;= k * w[i]; k++)<br>                dp[j] = <span class="hljs-built_in">max</span>(dp[j], dp[j - k * w[i]] + k * v[i]);<br>    <span class="hljs-keyword">return</span> dp[W];<br>&#125;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^3)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span></p><p>对于多重背包而言，时间复杂度是无法降下来的。也就是说，如果数据量超过<span class="math inline">\(10^2\)</span> ，就会超时</p><h3 id="二进制优化">二进制优化</h3><p>由于多重背包当中的每个物品是有数量的（在背包容量允许的情况下），因此我们可以将所有的物品全部列举出来，将其转换成0-1 背包来进行处理</p><p>但我们只需要稍微想一想就能发现，这种方式在时间复杂度上不会有任何的优化，而且空间复杂度还增大了</p><p>因为 <code>dp</code> 的状态一共有 <span class="math inline">\(n^2\)</span>个，并且我还需要去对于每个物品我还需要去遍历 <span class="math inline">\(s[i]\)</span> ，因此在时间复杂度上是没有变化的</p><p>空间复杂度就很简单了，需要一个更大的数组来装这么多数嘛</p><p>在我们不改变 <code>dp</code>数组的定义的时候，唯一可以优化的地方是减少对每个数的遍历次数</p><p>原先需要对某个数遍历 <span class="math inline">\(n\)</span>次，现在如果我只需要遍历 <span class="math inline">\(log\ n\)</span>次就能解决，那么总时间复杂度就能从 <span class="math inline">\(n^3\)</span> 降为 <span class="math inline">\(n^2log\ n\)</span></p><p>具体的，对于第 <code>i</code> 个数，我最大可以选择 <span class="math inline">\(s[i]\)</span> 次，那么我需要对这 <span class="math inline">\(s[i]\)</span> 个数逐个遍历一遍才能得到最终结果</p><p>现在，如果我们想要用遍历 <span class="math inline">\(log\s[i]\)</span> 就能解决问题的话，我们就必须用这 <span class="math inline">\(log\ s[i]\)</span> 个数的组合来表示出所有 <span class="math inline">\(s[i]\)</span> 的数</p><p>举个例子，假如 <span class="math inline">\(s[i]=10\)</span>，也就是说 <span class="math inline">\(k\)</span> 需要从 1 开始遍历到10</p><p>但对于 10 这个数，我们可以用 1、2、4、3 这四个数的组合来表示出所有 1到 10 的数</p><p>前面的 1、2、4 ，是需要以 2 的倍数进行增长。后面的 3 是余数，可以通过10 - 1 - 2 - 4 得到</p><p>这样，我们便可以用 <span class="math inline">\(log\ s[i]\)</span>个数来表示 <span class="math inline">\(s[i]\)</span> 了</p><p>需要注意的是，<span class="math inline">\(v[i]\)</span> 与 <span class="math inline">\(w[i]\)</span>也需要同步进行增大，处理完只后就完全是 0 -1 背包了</p><p>完整代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">back</span><span class="hljs-params">(<span class="hljs-type">int</span> N, <span class="hljs-type">int</span> W, vector&lt;<span class="hljs-type">int</span>&gt;&amp; v, vector&lt;<span class="hljs-type">int</span>&gt;&amp; w, vector&lt;<span class="hljs-type">int</span>&gt;&amp; s)</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;<span class="hljs-type">int</span>&gt;value;<br>    vector&lt;<span class="hljs-type">int</span>&gt;weight;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; N; i++)<br>    &#123;<br>        <span class="hljs-type">int</span> val = s[i];<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> k = <span class="hljs-number">1</span>; k &lt; val; k *= <span class="hljs-number">2</span>)<br>        &#123;<br>            val -= k;<br>            value.<span class="hljs-built_in">push_back</span>(k * v[i]);<br>            weight.<span class="hljs-built_in">push_back</span>(k * w[i]);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (val &gt; <span class="hljs-number">0</span>)<span class="hljs-comment">//对余下的数进行处理</span><br>        &#123;<br>            value.<span class="hljs-built_in">push_back</span>(val * v[i]);<br>            weight.<span class="hljs-built_in">push_back</span>(val * w[i]);<br>        &#125;<br>    &#125;<br>    vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">dp</span>(W + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>);<span class="hljs-comment">//转 0-1 背包</span><br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt;= N; i++)<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = W; j &gt;= weight[i]; j--)<br>            dp[j] = <span class="hljs-built_in">max</span>(dp[j], dp[j - weight[i]] + value[i]);<br>    <span class="hljs-keyword">return</span> dp[W];<br>&#125;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2*logn)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span></p><p>该算法适用于数据量为 <span class="math inline">\(10^3\)</span>的情况</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 518.零钱兑换 II</title>
    <link href="/2022/09/13/Algorithm%20Solution/LC-518.%E9%9B%B6%E9%92%B1%E5%85%91%E6%8D%A2-II/"/>
    <url>/2022/09/13/Algorithm%20Solution/LC-518.%E9%9B%B6%E9%92%B1%E5%85%91%E6%8D%A2-II/</url>
    
    <content type="html"><![CDATA[<h4 id="零钱兑换-ii"><a href="https://leetcode.cn/problems/coin-change-2/">518. 零钱兑换II</a></h4><p>给你一个整数数组 <code>coins</code> 表示不同面额的硬币，另给一个整数<code>amount</code> 表示总金额。</p><p>请你计算并返回可以凑成总金额的硬币组合数。如果任何硬币组合都无法凑出总金额，返回<code>0</code> 。</p><p>假设每一种面额的硬币有无限个。</p><p>题目数据保证结果符合 32 位带符号整数。</p><figure class="highlight subunit"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs subunit">输入：amount = 5, coins = [1, 2, 5]<br>输出：4<br>解释：有四种方式可以凑成总金额：<br>5=5<br>5=2<span class="hljs-string">+2</span><span class="hljs-string">+1</span><br>5=2<span class="hljs-string">+1</span><span class="hljs-string">+1</span><span class="hljs-string">+1</span><br>5=1<span class="hljs-string">+1</span><span class="hljs-string">+1</span><span class="hljs-string">+1</span><span class="hljs-string">+1</span><br></code></pre></td></tr></table></figure><h4 id="dp-数组">DP 数组</h4><p>这个就是我们之前做过的完全背包的问题，只要把完全背包的原理理解了，这种题就是秒杀</p><p>题目要我们返回所有的可能，我们知道 <code>dp[j]</code> 的状态只会从<code>dp[j]</code> 和 <code>dp[j-val]</code>当中转移过来，我们直接把二者相加即可</p><p>这道题唯一要注意的就是 Base Case</p><p>当我们考虑前 0 个元素时，若要凑出 0 ，总共会有 1 种可能性</p><p>考虑前 0 个，相当于什么都不选，这时我当然可以凑出 0 啊，所以是 1种</p><p>往后的，这道题就没有什么思维难度了</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">change</span><span class="hljs-params">(<span class="hljs-type">int</span> amount, vector&lt;<span class="hljs-type">int</span>&gt;&amp; coins)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = coins.<span class="hljs-built_in">size</span>();<br>vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">dp</span>(amount + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>);<br>        dp[<span class="hljs-number">0</span>] = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= n; i++)<br>&#123;<br><span class="hljs-type">int</span> val = coins[i - <span class="hljs-number">1</span>];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = val; j &lt;= amount; j++)<br>&#123;<br>dp[j] = dp[j] + dp[j - val];<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[amount];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 322.零钱兑换</title>
    <link href="/2022/09/13/Algorithm%20Solution/LC-322.%E9%9B%B6%E9%92%B1%E5%85%91%E6%8D%A2/"/>
    <url>/2022/09/13/Algorithm%20Solution/LC-322.%E9%9B%B6%E9%92%B1%E5%85%91%E6%8D%A2/</url>
    
    <content type="html"><![CDATA[<h4 id="零钱兑换"><a href="https://leetcode.cn/problems/coin-change/">322. 零钱兑换</a></h4><p>给你一个整数数组 <code>coins</code>，表示不同面额的硬币；以及一个整数 <code>amount</code>，表示总金额。</p><p>计算并返回可以凑成总金额所需的 <strong>最少的硬币个数</strong>。如果没有任何一种硬币组合能组成总金额，返回 <code>-1</code> 。</p><p>你可以认为每种硬币的数量是无限的。</p><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs tap">输入：coins = [1, 2, 5], amount = 11<br>输出：3 <br>解释：11 =<span class="hljs-number"> 5 </span>+<span class="hljs-number"> 5 </span>+ 1<br></code></pre></td></tr></table></figure><h4 id="dp-数组">DP 数组</h4><p>考虑到由于每一个硬币的选择可以是无数次的，因此我们可以很轻易地想到完全背包模型，直接套就行</p><p>我们考虑前 <code>i</code> 个物品，假设凑出 <code>j</code>的最小硬币个数为 <code>dp[i][j]</code></p><p>我们有：<code>dp[i][j] = min(dp[i-1][j], dp[i][j-coins[i-1]] + 1)</code>（假定我们的 <code>i</code> 从 1 开始遍历）</p><p>空间压缩后的版本为：<code>dp[j] = min(dp[j], dp[j-coins[i-1]] + 1)</code></p><p>接着我们考虑 <code>dp[j]</code> 当中的 Base Case</p><p>由于这就等同于 <code>dp[i][j]</code> 当中的第 0 行，相当于：</p><p>考虑前 0 个数，凑出 <code>j</code> 的最小硬币个数</p><p>显然，<code>dp[0][0] = 0</code>而其他的均无意义，我们这里将其设为一个较大值 <code>INF</code></p><p>放到 <code>dp[j]</code> 当中就是，<code>dp[0] = 0</code> ，其余的都是<code>INF</code></p><p>总体代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-type">int</span> INF = <span class="hljs-number">0x3f3f3f3f</span>;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">coinChange</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; coins, <span class="hljs-type">int</span> amount)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = coins.<span class="hljs-built_in">size</span>();<br>vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">dp</span>(amount + <span class="hljs-number">1</span>, INF);<br>dp[<span class="hljs-number">0</span>] = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= n; i++)<br>&#123;<br><span class="hljs-type">int</span> val = coins[i - <span class="hljs-number">1</span>];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = val; j &lt;= amount; j++)<br>&#123;<br>dp[j] = <span class="hljs-built_in">min</span>(dp[j], dp[j - val] + <span class="hljs-number">1</span>);<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[amount] == INF ? <span class="hljs-number">-1</span> : dp[amount];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>动规 完全背包问题</title>
    <link href="/2022/09/12/Algorithm%20Solution/%E5%8A%A8%E8%A7%84-%E5%AE%8C%E5%85%A8%E8%83%8C%E5%8C%85%E9%97%AE%E9%A2%98/"/>
    <url>/2022/09/12/Algorithm%20Solution/%E5%8A%A8%E8%A7%84-%E5%AE%8C%E5%85%A8%E8%83%8C%E5%8C%85%E9%97%AE%E9%A2%98/</url>
    
    <content type="html"><![CDATA[<h3 id="完全背包">完全背包</h3><p>问题描述：</p><p>有 <strong><code>N</code> 种物品</strong>和一个最多能背重量为<code>W</code> 的背包。第 <code>i</code> 件物品的重量是<code>weight[i]</code> ，得到的价值是 <code>value[i]</code></p><p><strong>每件物品可以用无数次</strong>，求解将哪些物品装入背包里使得物品价值总和最大</p><p>在 0-1背包的基础上，只需要添加「<strong>物品可以被无限次选择」</strong>这个条件，就成了完全背包</p><p>关于 0-1 背包，我们在 <a href="https://nishikichisato.github.io/2022/08/02/Algorithm%20Solution/动规-0-1背包问题/">动规0-1背包问题</a> 这里有过详细的说明，这里不再过多赘述</p><p>我们知道，0-1 背包的状态转移方程为：</p><p><span class="math inline">\(dp[i][j] = max(dp[i-1][j],\dp[i-1][j-weight[i]]+value[i])\)</span></p><p>我们说，完全背包就是在 0-1背包的基础上，加上了<strong>「物品可以被无限次选择」</strong>这一条件</p><p>仿照 0-1 背包的分析思路，我们不难得到：</p><p>对于第 <code>i</code> 件物品</p><p>我们选择 0 次，有：<span class="math inline">\(dp[i][j] =dp[i-1][j]\)</span></p><p>我们选择 1 次，有：<span class="math inline">\(dp[i][j]=dp[i-1][j-weight[i]]+value[i]\)</span></p><p>我们选择 2 次，有：<span class="math inline">\(dp[i][j]=dp[i-1][j-2*weight[i]]+2*value[i]\)</span></p><p><span class="math inline">\(\cdots\)</span></p><p>不失一般性地，我们对第 <code>i</code> 件物品选择 <span class="math inline">\(k\)</span> 次，显然：</p><p><span class="math inline">\(dp[i][j]=max(dp[i-1][j],dp[i-1][j-k*weight[i]]+k*value[i])\)</span>，其中 <span class="math inline">\(k*weight[i]\le j\)</span></p><p>我们对上式进行展开，有：</p><p><span class="math inline">\(dp[i][j]=max(dp[i-1][j],\ {\color{violet}dp[i-1][j-w[i]]+v[i],\ dp[i-1][j-2*w[i]]+2*v[i],\\cdots\  })\)</span></p><p>这里我们用 <span class="math inline">\(w\)</span> 来替换 <span class="math inline">\(weight\)</span> ，用 <span class="math inline">\(v\)</span> 来替换 <span class="math inline">\(value\)</span></p><p>当我们忽略第一维变化的时候，我们发现，<span class="math inline">\(j\)</span>的变化是<strong>先相等，后减小</strong></p><p>也就是说，对于上面粉色部分，我们很容易得出如下结论：</p><p><span class="math inline">\(dp[i][j-w[i-1]]=max({\color{violet}dp[i-1][j-w[i]],\dp[i-1][j-2*w[i]]+v[i],\ \cdots})\)</span></p><p>显然，<strong>两个式子相同颜色部分相差一个恒定值</strong>：<span class="math inline">\(v[i]\)</span></p><p>我们将两式合并，有：</p><p><span class="math inline">\(dp[i][j] =max(dp[i-1][j],dp[i][j-w[i]]+v[i])\)</span></p><p>因此，如果我需要求 <span class="math inline">\(dp[i][j]\)</span>的值，只需要求出 <span class="math inline">\(dp[i-1][j]\)</span> 与<span class="math inline">\(dp[i][j-w[i]]+v[i]\)</span>之间的较大值即可</p><p>​</p><p>简单对比一下：</p><p>0-1背包：<code>dp[i][j] = max(dp[i-1][j], dp[i-1][j-w[i]] + v[i])</code></p><p>完全背包：<code>dp[i][j] = max(dp[i-1][j], dp[i][j-w[i]] + v[i])</code></p><p>所以这区别。。。。真不大</p><p>下面是完全背包的代码（直接套 0-1 背包就行）：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">back</span><span class="hljs-params">(<span class="hljs-type">int</span> N, <span class="hljs-type">int</span> W, vector&lt;<span class="hljs-type">int</span>&gt;&amp; weight, vector&lt;<span class="hljs-type">int</span>&gt;&amp; value)</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(N + <span class="hljs-number">1</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(W + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>));<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt;= N; i++)<br>    &#123;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">1</span>; j &lt;= W; j++)<br>        &#123;<br>            <span class="hljs-keyword">if</span> (j &gt; weight[i - <span class="hljs-number">1</span>])<span class="hljs-comment">//背包装得下</span><br>                dp[i][j] = <span class="hljs-built_in">max</span>(dp[i][j], <br>                               dp[i][j - weight[i]] + value[i]);<br>        <span class="hljs-keyword">else</span>                  <span class="hljs-comment">//背包装不下，等价于不装物品的情况</span><br>                dp[i][j] = dp[i - <span class="hljs-number">1</span>][j];<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> dp[N][W];<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="空间优化">空间优化</h3><p>0-1背包我们给出了一维数组的压缩方法，这里我们给出完全背包的压缩方法</p><p>对于前者而言，我们是从后往前遍历，因为我们不希望赋盖掉<code>dp[i-1][j]</code> 和 <code>dp[i-1][j-w[i]]</code> 这两个值</p><p>而我们来看完全背包的状态转移方程：<code>dp[i][j] = max(dp[i-1][j], dp[i][j-w[i]] + v[i])</code></p><p><code>dp[i][j]</code> 的值取决于 <code>dp[i-1][j]</code> 和<code>dp[i][j-w[i]]</code></p><p>对于前者，就是 <code>dp[j]</code> 没赋值之前的值</p><p>对于后者，它就<strong>是 <code>dp[j]</code>这一行已经赋值的数值</strong>，因此我们直接采取从前往后遍历即可</p><p>完整代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">back</span><span class="hljs-params">(<span class="hljs-type">int</span> N, <span class="hljs-type">int</span> W, vector&lt;<span class="hljs-type">int</span>&gt;&amp; weight, vector&lt;<span class="hljs-type">int</span>&gt;&amp; value)</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">dp</span>(W + <span class="hljs-number">1</span>);<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt;= N; i++)<br>    &#123;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">1</span>; j &lt;= W; j++)<br>        &#123;<br>            <span class="hljs-keyword">if</span> (j &gt; weight[i])<span class="hljs-comment">//背包装得下</span><br>                dp[j] = <span class="hljs-built_in">max</span>(dp[j], dp[j - weight[i]] + value[i]);<br>        <span class="hljs-keyword">else</span>                  <span class="hljs-comment">//背包装不下，等价于不装物品的情况</span><br>                dp[j] = dp[j];<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> dp[W];<br>&#125;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>CSAPP 第三章 程序的机器级表示</title>
    <link href="/2022/09/12/CSAPP/CSAPP-%E7%AC%AC%E4%B8%89%E7%AB%A0-%E7%A8%8B%E5%BA%8F%E7%9A%84%E6%9C%BA%E5%99%A8%E7%BA%A7%E8%A1%A8%E7%A4%BA/"/>
    <url>/2022/09/12/CSAPP/CSAPP-%E7%AC%AC%E4%B8%89%E7%AB%A0-%E7%A8%8B%E5%BA%8F%E7%9A%84%E6%9C%BA%E5%99%A8%E7%BA%A7%E8%A1%A8%E7%A4%BA/</url>
    
    <content type="html"><![CDATA[<h2 id="数据格式">数据格式</h2><p>我们采取 Intel 的标准，字（word）用来表示 16 位数据类型，因此 32位也被称为 双字（double word），64 位被称为 四字（quad word）</p><table><thead><tr class="header"><th style="text-align: center;">C声明</th><th style="text-align: center;">Intel 数据类型</th><th style="text-align: center;">汇编代码后缀</th><th style="text-align: center;">大小（字节）</th></tr></thead><tbody><tr class="odd"><td style="text-align: center;">char</td><td style="text-align: center;">字节</td><td style="text-align: center;">b</td><td style="text-align: center;">1</td></tr><tr class="even"><td style="text-align: center;">short</td><td style="text-align: center;">字</td><td style="text-align: center;">w</td><td style="text-align: center;">2</td></tr><tr class="odd"><td style="text-align: center;">int</td><td style="text-align: center;">双字</td><td style="text-align: center;">l</td><td style="text-align: center;">4</td></tr><tr class="even"><td style="text-align: center;">long</td><td style="text-align: center;">四字</td><td style="text-align: center;">q</td><td style="text-align: center;">8</td></tr><tr class="odd"><td style="text-align: center;">char*</td><td style="text-align: center;">四字</td><td style="text-align: center;">q</td><td style="text-align: center;">8</td></tr><tr class="even"><td style="text-align: center;">float</td><td style="text-align: center;">单精度</td><td style="text-align: center;">s</td><td style="text-align: center;">4</td></tr><tr class="odd"><td style="text-align: center;">double</td><td style="text-align: center;">双精度</td><td style="text-align: center;">l</td><td style="text-align: center;">8</td></tr></tbody></table><p>汇编指令都会有一个字符后缀，用来表示数据的大小。例如：movb（传送字节）、movw（传送字）、movl（传送双字）、movq（传送四字）</p><p>后缀 l 用来表示双字，这是因为 32 位数也被看成 长字（long word）</p><p>我们观察上表可以得出，后缀 l同时表示了双字和双精度，但这并不会起冲突，因为浮点数的运算是一套完全不同的指令和寄存器</p><p>这里我们可以看出，所有的操作数都以一个统一的格式给出，每一种类型都对应一个后缀，这一点也为后续的数据传送提供了便利</p><h2 id="访问信息">访问信息</h2><p>一个 x86-64 的中央处理单元（CPU）包含一组 16 个存储 64位值的通用目的寄存器，这些寄存器用来存储整数数据和指针。具体名称和功能去看原书120 页</p><h3 id="操作数指示符">操作数指示符</h3><p>大多数指令有一个或者多个操作数，操作数是指一个操作当中需要使用的<strong>源数据值</strong>和放置结果的<strong>目的地址</strong></p><p>源数据值可以以常数的形式给出，也可以通过访问寄存器或内存来读取。目的地址可以是寄存器也可以是内存。因此，操作数可以被划分为三种类型：</p><ul><li>立即数（immediate），用来<strong>直接给出常数值</strong></li><li>寄存器，通过访问某个寄存器得到其内容，当然是允许间接访问的</li><li>内存引用，会根据计算出来的地址来访问某个特定的内存，也允许间接访问</li></ul><p>具体的访问操作看原书 121页，这个地方不难，把后面的练习做一下就能搞明白</p>]]></content>
    
    
    <categories>
      
      <category>CSAPP</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CSAPP</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>大二上 第一周 2022.9.5 - 2022.9.11</title>
    <link href="/2022/09/11/%E6%97%A5%E5%B8%B8%E6%80%BB%E7%BB%93/%E5%A4%A7%E4%BA%8C%E4%B8%8A-%E7%AC%AC%E4%B8%80%E5%91%A8-2022.9.5---2022.9.11/"/>
    <url>/2022/09/11/%E6%97%A5%E5%B8%B8%E6%80%BB%E7%BB%93/%E5%A4%A7%E4%BA%8C%E4%B8%8A-%E7%AC%AC%E4%B8%80%E5%91%A8-2022.9.5---2022.9.11/</url>
    
    <content type="html"><![CDATA[<p>这一周下面，我虽然学习还是很认真的，但还是暴露了很多的问题</p><p>一个就是我看 CSAPP 这本书看的很慢，但我觉得这个问题很难得到解决</p><p>到现在为止，我是看完了第二章，开第三章了</p><p>我争取在第八周结束的时候把这本书看完吧</p><p>其实也就是每天平均看十页，不算多，但很耗时间</p><p>现在我估计我只会在大物课和选修课上面看，其他的课得听一下啊</p><p>算法的话，还是跟之前一样，晚上去图书馆的时候集中去看 CSAPP 和算法</p><p>我现在已经把三叶姐的路径合集全部看完了，除了最好一道题，那一道我感觉好难，现在可能搞不定的那种</p><p>我打算先开背包合集，等看到一半的时候再回来看那一道，现在我觉得我是真的没有能力解决的那种</p><p>然后关于力扣那个每日一题的打卡，我是真的好想每天都去打啊，但是是真的没有时间，哭了~~</p><p>可能九月和十月都打不了吧，我现在想着把背包合集全部刷完再去每日打卡</p><p>关于背包合集的速度，我的打算是上学的那五天就一共看三篇半，周末两条看三篇半，差不多一个星期看七篇的程度</p><p>诶，这么算好像三个星期就可以看完诶</p><p>不过这是理想情况啦，这个东西理解起来真没这么简单</p><p>嘛，觉得时间不够动态调整便是</p><p>然后是韩顺平的 Linux基本操作的视频，这个就是基础操作，我打算两周速通</p><p>就每天晚上九点从图书馆回到宿舍跑完步洗完澡吃完东西之后的那段时间里面解决它，两周应该是够的</p><p>搞完这个之后我打算去看牛客的 Linux网络编程，我是想着能去搞一点项目实战一下的</p><p>虽然现在计网还没怎么学，不过这东西也每个线性顺序嘛，感兴趣学就完事了</p><p>姑且就先这样吧，我觉得现在的安排还是挺合理的</p><p>书的话我是很难做到两本同时看的，太难受了</p><p>每天有算法，有视频，有书，足够了</p><p>哦对了，忘记评价一下这个星期的学习了</p><p>总的来说还是很不错的，昨天中秋节，白天学了一天，晚上跟同学出去干 K寄</p><p>今天周末，打了一场力扣周赛，就会做两道题，然后就没有别的了</p><p>还可以的，其实</p>]]></content>
    
    
    <categories>
      
      <category>日常总结</category>
      
    </categories>
    
    
    <tags>
      
      <tag>周总结</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 1575.统计所有可行路径</title>
    <link href="/2022/09/08/Algorithm%20Solution/LC-1575.%E7%BB%9F%E8%AE%A1%E6%89%80%E6%9C%89%E5%8F%AF%E8%A1%8C%E8%B7%AF%E5%BE%84/"/>
    <url>/2022/09/08/Algorithm%20Solution/LC-1575.%E7%BB%9F%E8%AE%A1%E6%89%80%E6%9C%89%E5%8F%AF%E8%A1%8C%E8%B7%AF%E5%BE%84/</url>
    
    <content type="html"><![CDATA[<h4 id="统计所有可行路径"><a href="https://leetcode.cn/problems/count-all-possible-routes/">1575.统计所有可行路径</a></h4><p>给你一个 <strong>互不相同</strong> 的整数数组，其中<code>locations[i]</code> 表示第 <code>i</code> 个城市的位置。同时给你<code>start</code>，<code>finish</code> 和 <code>fuel</code>分别表示出发城市、目的地城市和你初始拥有的汽油总量</p><p>每一步中，如果你在城市 <code>i</code> ，你可以选择任意一个城市<code>j</code> ，满足 <code>j != i</code> 且<code>0 &lt;= j &lt; locations.length</code> ，并移动到城市<code>j</code> 。从城市 <code>i</code> 移动到 <code>j</code>消耗的汽油量为<code>|locations[i] - locations[j]|</code>，<code>|x|</code> 表示<code>x</code> 的绝对值。</p><p>请注意， <code>fuel</code> 任何时刻都 <strong>不能</strong>为负，且你 <strong>可以</strong> 经过任意城市超过一次（包括<code>start</code> 和 <code>finish</code> ）。</p><p>请你返回从 <code>start</code> 到 <code>finish</code>所有可能路径的数目。</p><p>由于答案可能很大， 请将它对 <code>10^9 + 7</code> 取余后返回。</p><figure class="highlight clean"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs clean">输入：locations = [<span class="hljs-number">2</span>,<span class="hljs-number">3</span>,<span class="hljs-number">6</span>,<span class="hljs-number">8</span>,<span class="hljs-number">4</span>], start = <span class="hljs-number">1</span>, finish = <span class="hljs-number">3</span>, fuel = <span class="hljs-number">5</span><br>输出：<span class="hljs-number">4</span><br>解释：以下为所有可能路径，每一条都用了 <span class="hljs-number">5</span> 单位的汽油：<br><span class="hljs-number">1</span> -&gt; <span class="hljs-number">3</span><br><span class="hljs-number">1</span> -&gt; <span class="hljs-number">2</span> -&gt; <span class="hljs-number">3</span><br><span class="hljs-number">1</span> -&gt; <span class="hljs-number">4</span> -&gt; <span class="hljs-number">3</span><br><span class="hljs-number">1</span> -&gt; <span class="hljs-number">4</span> -&gt; <span class="hljs-number">2</span> -&gt; <span class="hljs-number">3</span><br></code></pre></td></tr></table></figure><p>​</p><h4 id="dfs-记忆化搜索">DFS + 记忆化搜索</h4><p>我们先讨论一下什么时候会用到 DFS</p><p>DFS ，也就是暴力搜索，通过<strong>以 <code>n</code>叉树的形式</strong>将所有可能出现的情况全部列举出来（必然会有重复），进而得到最终的答案</p><p>既然涉及到 <code>n</code> 叉树，那就肯定会涉及到递归与循环</p><p>因此，DFS 有两个特定：</p><ul><li>由于使用递归加循环，因此可以暴力穷举出<strong>仅仅通过嵌套<code>for</code> 循环无法实现的穷举</strong></li><li>在不剪枝的情况下，时间复杂度为<strong>指数级</strong>（一般来讲是<code>n</code> 叉数，高度为 <code>m</code>）</li></ul><p>所有，在不进行剪枝的情况下，使用 DFS 的数据量级<strong>不能超过30</strong></p><p>对 DFS进行剪枝的操作，叫做「记忆化搜索」，也就是我们之前一直使用的「备忘录」</p><p>回到这道题，我们需要给出所有从 <code>start</code> 到<code>finish</code> 的路径</p><p>我们从起点开始考虑这个问题。假设我当前处于 <code>start</code>处，我有 <code>n-1</code> 种选择去走下一步（姑且不考虑油量的问题）</p><p>当我走下一步之后，又会有 <code>n-1</code>种选择（因为一个点可以多次到达）</p><p>所以我们可以确定一点：这道题很难通过嵌套 <code>for</code>循环得到最终的结果，也就是说，我们需要想到用 DFS 来暴力枚举所有可能</p><p>即 DFS函数的返回值就是<strong>当前位置到终点的所有路径和</strong></p><p>​</p><p>关于 DFS 的实现，我们需要注意以下三个问题：</p><ul><li><p>确定「参数」的含义</p></li><li><p>确定递归函数的出口（也就是 Base Case）</p></li><li><p>编写「最小递归单元」的处理逻辑</p></li></ul><p>我们先看第一个，容易发现，我们每次移动的时候会发生改变的位置有两个，一个是当前位置<code>pos</code> ，另一个是当前油量 <code>fuel</code></p><p>显然，这两个就需要加入到参数部分</p><p>除此之外，我们还需要确定是否到达终点 <code>end</code> 以及输入数组<code>lc</code> （location）</p><p>接着我们看递归的出口部分，也就是去找 Base Case</p><p>在这个部分，我们需要确定的是：<strong>什么情况下，此次递归是有效或者无效的</strong></p><p>有效的情况非常简单，只要当前位置 <code>pos</code> 到达<code>end</code> ，我们就让路径数加 1</p><p>无效的情况稍微复杂一点</p><p>一个很容易想到的是，当前油量 <code>fuel</code> 为负数，直接返回 0</p><p>还有一个是，当前油量 <code>fuel</code>不为负数，但无法去到任何地方，这个也需要返回 0</p><p>至于「最小递归单元」的处理问题，由于我们是需要对<strong>当前位置以外的所有位置进行遍历</strong>，因此我们将遍历得到的结果求和返回即可</p><p>​</p><p>然后，我们来看看「记忆化搜索」的部分</p><p>从上面的分析当中得知，我们目前有两个量（<code>pos</code> 和<code>fuel</code>）是会发生改变的</p><p>而我们 DFS函数的返回值的意义为：从当前位置出发到达终点的所有路径和</p><p>因此我们设置一共缓存数组 <code>cache</code> ，其定义为：从当前位置<code>pos</code> 在拥有油量为 <code>fuel</code>的情况下到达终点的所有路径和为 <code>cache[pos][fuel]</code></p><p>​</p><p>总体代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;cache;<span class="hljs-comment">//处于pos位置且油量为fuel, 到达end的路径数</span><br><span class="hljs-type">int</span> mod = <span class="hljs-number">1000000007</span>;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">dfs</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; lc, <span class="hljs-type">int</span> pos, <span class="hljs-type">int</span> end, <span class="hljs-type">int</span> fuel)</span><span class="hljs-comment">//pos fuel 为当前位置与油量, end 为终点</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (cache[pos][fuel] != <span class="hljs-number">-1</span>)<br><span class="hljs-keyword">return</span> cache[pos][fuel];<br><span class="hljs-keyword">if</span> (pos != end &amp;&amp; fuel &lt; <span class="hljs-number">0</span>)<br>&#123;<br>cache[pos][fuel] = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br><br><span class="hljs-type">int</span> n = lc.<span class="hljs-built_in">size</span>();<br><span class="hljs-type">bool</span> gonext = <span class="hljs-literal">false</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++)<br><span class="hljs-keyword">if</span> (i != pos)<br>&#123;<br><span class="hljs-type">int</span> need = <span class="hljs-built_in">abs</span>(lc[i] - lc[pos]);<br><span class="hljs-keyword">if</span> (fuel &gt;= need)<br>&#123;<br>gonext = <span class="hljs-literal">true</span>;<br><span class="hljs-keyword">break</span>;<br>&#125;<br>&#125;<br><span class="hljs-keyword">if</span> (fuel &gt; <span class="hljs-number">0</span> &amp;&amp; !gonext)<br>&#123;<br>cache[pos][fuel] = pos == end ? <span class="hljs-number">1</span> : <span class="hljs-number">0</span>;<br><span class="hljs-keyword">return</span> cache[pos][fuel];<br>&#125;<br><br><span class="hljs-type">int</span> sum = pos == end ? <span class="hljs-number">1</span> : <span class="hljs-number">0</span>;<span class="hljs-comment">//如果当前就是end, 那么我们需要返回1</span><br><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++)<br>&#123;<br><span class="hljs-keyword">if</span> (pos != i)<br>&#123;<br><span class="hljs-type">int</span> need = <span class="hljs-built_in">abs</span>(lc[i] - lc[pos]);<br><span class="hljs-keyword">if</span> (fuel &gt;= need)<br>&#123;<br>sum += <span class="hljs-built_in">dfs</span>(lc, i, end, fuel - need);<br>sum %= mod;<br>&#125;<br>&#125;<br>&#125;<br>cache[pos][fuel] = sum;<br><span class="hljs-keyword">return</span> sum;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">countRoutes</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; locations, <span class="hljs-type">int</span> start, <span class="hljs-type">int</span> finish, <span class="hljs-type">int</span> fuel)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = locations.<span class="hljs-built_in">size</span>();<br>cache.<span class="hljs-built_in">assign</span>(n, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(fuel + <span class="hljs-number">1</span>, <span class="hljs-number">-1</span>));<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">dfs</span>(locations, start, finish, fuel);<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2*fuel)\)</span>，最坏的情况下，我们有 <span class="math inline">\(n*fuel\)</span>个状态需要去遍历（填满 <span class="math inline">\(fuel\)</span>数组），对每个状态而言又需要去遍历一次 <span class="math inline">\(locations\)</span> 数组</p><p>空间复杂度：<span class="math inline">\(O(n^2*fuel)\)</span>，由于是树型结构，因此有多少个状态就需要多少的空间</p><p>​</p><h4 id="dfs-记忆化搜索base-case-优化">DFS + 记忆化搜索（Base Case优化）</h4><p>这里是可以在 Base Case 的部分进行优化的</p><p>思考一个问题：如果下一步无法到达终点，还有机会到达吗</p><p>假设当前油量为 <span class="math inline">\(fuel\)</span>，到达终点所需油量为两地位置差的绝对值，即： <span class="math inline">\(need=|lc[end]-lc[pos]|\)</span></p><p>此时有 <span class="math inline">\(fuel&lt;need\)</span></p><p>当我每次移动到一个新位置时，所花费油量均有 <span class="math inline">\(cost&gt;0\)</span></p><p>因此到达新位置的油量为 <span class="math inline">\(fuel&#39;=fuel-cost&lt;fuel\)</span>，此时到达终点所需油量为 <span class="math inline">\(need&#39;=lc[end]-lc[new]=need-cost\)</span>（题目当中有说任意两个地点的值不相同）</p><p>即：<span class="math inline">\(\Delta fuel = \Delta need\)</span>，也就是说，只要当前位置无法一步到达终点，走其他路也无法到达</p><p>完整代码如下：</p><p>​</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;cache;<span class="hljs-comment">//处于pos位置且油量为fuel, 到达end的路径数</span><br><span class="hljs-type">int</span> mod = <span class="hljs-number">1000000007</span>;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">dfs</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; lc, <span class="hljs-type">int</span> pos, <span class="hljs-type">int</span> end, <span class="hljs-type">int</span> fuel)</span><span class="hljs-comment">//pos fuel 为当前位置与油量, end 为终点</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (cache[pos][fuel] != <span class="hljs-number">-1</span>)<br><span class="hljs-keyword">return</span> cache[pos][fuel];<br><br><span class="hljs-type">int</span> n = lc.<span class="hljs-built_in">size</span>();<br><br><span class="hljs-type">int</span> need = <span class="hljs-built_in">abs</span>(lc[pos] - lc[end]);<span class="hljs-comment">//改动在这里</span><br><span class="hljs-keyword">if</span> (fuel &lt; need)<br>&#123;<br>cache[pos][fuel] = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br><br><span class="hljs-type">int</span> sum = pos == end ? <span class="hljs-number">1</span> : <span class="hljs-number">0</span>;<br><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++)<br>&#123;<br><span class="hljs-keyword">if</span> (pos != i)<br>&#123;<br><span class="hljs-type">int</span> need = <span class="hljs-built_in">abs</span>(lc[i] - lc[pos]);<br><span class="hljs-keyword">if</span> (fuel &gt;= need)<br>&#123;<br>sum += <span class="hljs-built_in">dfs</span>(lc, i, end, fuel - need);<br>sum %= mod;<br>&#125;<br>&#125;<br>&#125;<br>cache[pos][fuel] = sum;<br><span class="hljs-keyword">return</span> sum;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">countRoutes</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; locations, <span class="hljs-type">int</span> start, <span class="hljs-type">int</span> finish, <span class="hljs-type">int</span> fuel)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = locations.<span class="hljs-built_in">size</span>();<br>cache.<span class="hljs-built_in">assign</span>(n, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(fuel + <span class="hljs-number">1</span>, <span class="hljs-number">-1</span>));<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">dfs</span>(locations, start, finish, fuel);<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时空复杂度与上面一致</p><p>​</p><h4 id="dp-数组">DP 数组</h4><p>其实上面记忆化搜索的模块是为了引出下面这个用 <code>DP</code>数组实现的方案</p><p>这道题就属于路径规划里面那种没办法直接猜出 <code>DP</code>数组的定义的情况</p><p>毕竟这玩意的定义，全靠经验</p><p>而算法题是，只要这道题我没做过类似的题目，那么我大概率这道题做不出来</p><p>所有，我们需要一种可以得到 <code>DP</code>数组定义的方法，这个方法就是记忆化搜索</p><p>当然，用 <code>DP</code> 数组可以解的题目，记忆化搜索都可以解</p><p>我们不是说去用记忆化搜索去解题（用也没关系啦），而是借用这种思考方式来引出<code>DP</code> 数组的定义</p><p>根据上面的分析，我们目前手里有两个变量：当前位置 <span class="math inline">\(pos\)</span> 和剩余油量 <span class="math inline">\(fuel\)</span> ，那么我们可以仿照上面<code>dfs</code> 函数的定义，来写一个 <code>DP</code> 数组的定义</p><p>当前位置为 <code>pos</code> 且剩余油量为 <code>fuel</code>时，到达终点的所有路径和为 <code>dp[pos][fuel]</code></p><p>然后，对于当前位置 <code>pos</code> 而言，我们需要去遍历剩下的<code>n-1</code> 个位置</p><p>并且有一点需要注意的是，在我还没有去遍历的时候，我的初始油量都是<code>fuel</code></p><p>这里就类似于 <code>dfs</code> 函数里面的 <code>for</code>循环，每次进入递归的时候，<code>fuel</code> 都是不变的，改变的是<code>need</code></p><p>因此，我们在遍历这个二维数组的时候，需要将<strong>第二维的遍历放在外层循环</strong></p><p>然后便是 Base Case的处理，这里我们需要处理的只有一种情况，就是只要当前处于<code>finish</code> ，不管当前油量是多少，将数组全部初始化为 1 ，即<code>dp[finish][j] = 1</code></p><p>完整代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-type">int</span> mod = <span class="hljs-number">1000000007</span>;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">countRoutes</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; locations, <span class="hljs-type">int</span> start, <span class="hljs-type">int</span> finish, <span class="hljs-type">int</span> fuel)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = locations.<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(n, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(fuel + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>));<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; fuel + <span class="hljs-number">1</span>; j++)<br>dp[finish][j] = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> cur = <span class="hljs-number">0</span>; cur &lt; fuel + <span class="hljs-number">1</span>; cur++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> k = <span class="hljs-number">0</span>; k &lt; n; k++)<br>&#123;<br><span class="hljs-keyword">if</span> (k == i)<br><span class="hljs-keyword">continue</span>;<br><span class="hljs-type">int</span> need = <span class="hljs-built_in">abs</span>(locations[i] - locations[k]);<br><span class="hljs-keyword">if</span> (cur &gt;= need)<br>&#123;<br>dp[i][cur] += dp[k][cur - need];<br>dp[i][cur] %= mod;<br>&#125;<br>&#125;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[start][fuel];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时空复杂度并不会发生变化</p><p>​</p><h4 id="总结">总结</h4><p>对于那种没有做过的路径问题，我们可以先尝试着去想想对应的记忆化搜索要怎么处理，然后便可以很容易的照搬得出出<code>dp</code> 的定义了</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>路径问题</tag>
      
      <tag>DFS</tag>
      
      <tag>记忆化搜索</tag>
      
      <tag>模板</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 1289.下降路径最小和 II</title>
    <link href="/2022/09/07/Algorithm%20Solution/LC-1289.%E4%B8%8B%E9%99%8D%E8%B7%AF%E5%BE%84%E6%9C%80%E5%B0%8F%E5%92%8C-II/"/>
    <url>/2022/09/07/Algorithm%20Solution/LC-1289.%E4%B8%8B%E9%99%8D%E8%B7%AF%E5%BE%84%E6%9C%80%E5%B0%8F%E5%92%8C-II/</url>
    
    <content type="html"><![CDATA[<h4 id="下降路径最小和-ii"><a href="https://leetcode.cn/problems/minimum-falling-path-sum-ii/">1289.下降路径最小和 II</a></h4><p>给你一个 <code>n x n</code> 整数矩阵 <code>arr</code> ，请你返回<strong>非零偏移下降路径</strong> 数字和的最小值。</p><p><strong>非零偏移下降路径</strong> 定义为：从 <code>arr</code>数组中的每一行选择一个数字，且按顺序选出来的数字中，相邻数字不在原数组的同一列。</p><figure class="highlight inform7"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs inform7">输入：arr = <span class="hljs-comment">[<span class="hljs-comment">[1,2,3]</span>,<span class="hljs-comment">[4,5,6]</span>,<span class="hljs-comment">[7,8,9]</span>]</span><br>输出：13<br>解释：<br>所有非零偏移下降路径包括：<br><span class="hljs-comment">[1,5,9]</span>, <span class="hljs-comment">[1,5,7]</span>, <span class="hljs-comment">[1,6,7]</span>, <span class="hljs-comment">[1,6,8]</span>,<br><span class="hljs-comment">[2,4,8]</span>, <span class="hljs-comment">[2,4,9]</span>, <span class="hljs-comment">[2,6,7]</span>, <span class="hljs-comment">[2,6,8]</span>,<br><span class="hljs-comment">[3,4,8]</span>, <span class="hljs-comment">[3,4,9]</span>, <span class="hljs-comment">[3,5,7]</span>, <span class="hljs-comment">[3,5,9]</span><br>下降路径中数字和最小的是 <span class="hljs-comment">[1,5,7]</span> ，所以答案是 13 。<br></code></pre></td></tr></table></figure><h4 id="dp-数组朴素解法">DP 数组（朴素解法）</h4><p>对于 <code>dp[i][j]</code> 而言，其值由上一行当中除掉<code>dp[i - 1][j]</code> 之外的所有值的最小值</p><p>这种解法很简单，时间复杂度是 <span class="math inline">\(O(n^3)\)</span> ，这里的数量级是 <span class="math inline">\(10^2\)</span> ，一共是 <span class="math inline">\(10^6\)</span> ，小于 <span class="math inline">\(10^7\)</span> ，可以过</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">minFallingPathSum</span><span class="hljs-params">(vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&amp; grid)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = grid.<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(n, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(n, <span class="hljs-number">0</span>));<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; n; j++)<br>dp[<span class="hljs-number">0</span>][j] = grid[<span class="hljs-number">0</span>][j];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt; n; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; n; j++)<br>&#123;<br>dp[i][j] = INT32_MAX;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> k = <span class="hljs-number">0</span>; k &lt; n; k++)<br>&#123;<br><span class="hljs-keyword">if</span> (k == j)<br><span class="hljs-keyword">continue</span>;<br>dp[i][j] = <span class="hljs-built_in">min</span>(dp[i][j], dp[i - <span class="hljs-number">1</span>][k]);<br>&#125;<br>dp[i][j] += grid[i][j];<br>&#125;<br>&#125;<br><span class="hljs-type">int</span> ret = INT32_MAX;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> m : dp[n - <span class="hljs-number">1</span>])<br>ret = <span class="hljs-built_in">min</span>(ret, m);<br><span class="hljs-keyword">return</span> ret;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^3)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n^2)\)</span></p><h4 id="dp-数组时间优化">DP 数组（时间优化）</h4><p>对于这道题，<code>dp</code> 数组有 <span class="math inline">\(n^2\)</span>个状态，这个部分是不可能得到优化的，唯一可以入手的地方是<strong>不对前一行进行遍历</strong></p><p>我们原本是想， <code>dp[i][j]</code> 的值是上一行当中除掉<code>dp[i][j - 1]</code>的最小值，这么想的话必然需要进行多一次循环，因为我需要全部遍历一遍上一行的所有值</p><p>但是，我们从最小值的角度来想，不管怎么样，<code>dp[i][j]</code>的值一定是上一行的最小值，只不过这个最小值可能会在<code>dp[i - 1][j]</code> ，也可能不会在</p><p>对于不在的情况，我们直接将 <code>i - 1</code> 行的最小值赋给<code>dp[i][j]</code> 即可</p><p>对于在的情况，我们将 <code>i - 1</code>行的<strong>次小值</strong>赋值给 <code>dp[i][j]</code> 即可</p><p>这样，我们只需要记录上一行的两个值，就可以推出<code>dp[i][j]</code></p><p>我们定义两个下标，<code>fir</code> 用于记录最小值，<code>sec</code>用于记录次小值，初始状态先让二者为 -1</p><p>然后我们先对二者做一个判断，只要二者为 -1 就对它们赋值</p><p>之后就是常规的递推，<code>fir</code> 对 <code>sec</code>赋值，<code>j</code> 对 <code>fir</code> 赋值</p><p>全部代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">minFallingPathSum</span><span class="hljs-params">(vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&amp; grid)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = grid.<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(n, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(n, <span class="hljs-number">0</span>));<br><span class="hljs-type">int</span> fir = <span class="hljs-number">-1</span>, sec = <span class="hljs-number">-1</span>;<span class="hljs-comment">//fir记录最小值下标, sec记录次小值下标</span><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; n; j++)<br>&#123;<br><span class="hljs-type">int</span> val = grid[<span class="hljs-number">0</span>][j];<br>dp[<span class="hljs-number">0</span>][j] = val;<br><br><span class="hljs-keyword">if</span> (val &lt; (fir == <span class="hljs-number">-1</span> ? INT32_MAX : dp[<span class="hljs-number">0</span>][fir]))<span class="hljs-comment">//这么写可以保证两个数都会被赋值</span><br>&#123;<br>sec = fir;<br>fir = j;<br>&#125;<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (val &lt; (sec == <span class="hljs-number">-1</span> ? INT32_MAX : dp[<span class="hljs-number">0</span>][sec]))<br>sec = j;<br><br>&#125;<br><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt; n; i++)<br>&#123;<br><span class="hljs-type">int</span> tfir = <span class="hljs-number">-1</span>, tsec = <span class="hljs-number">-1</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; n; j++)<br>&#123;<br><span class="hljs-type">int</span> val = grid[i][j];<br><span class="hljs-comment">//我们已经预先求出了上一行的最小值和次小值，这里应该先赋值</span><br><span class="hljs-keyword">if</span> (j != fir)<br>dp[i][j] = dp[i - <span class="hljs-number">1</span>][fir] + val;<br><span class="hljs-keyword">else</span> dp[i][j] = dp[i - <span class="hljs-number">1</span>][sec] + val;<br><br><span class="hljs-comment">//下面对dp[i]行求最小值和次小值</span><br><span class="hljs-keyword">if</span> (dp[i][j] &lt; (tfir == <span class="hljs-number">-1</span> ? INT32_MAX : dp[i][tfir]))<br>&#123;<br>tsec = tfir;<br>tfir = j;<br>&#125;<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (dp[i][j] &lt; (tsec == <span class="hljs-number">-1</span> ? INT32_MAX : dp[i][tsec]))<br>tsec = j;<br>&#125;<br>fir = tfir;<br>sec = tsec;<br>&#125;<br><span class="hljs-type">int</span> ret = INT32_MAX;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> m : dp[n - <span class="hljs-number">1</span>])<br>ret = <span class="hljs-built_in">min</span>(ret, m);<br><span class="hljs-keyword">return</span> ret;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n^2)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>路径问题</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 931.下降路径最小和</title>
    <link href="/2022/09/07/Algorithm%20Solution/LC-931.%E4%B8%8B%E9%99%8D%E8%B7%AF%E5%BE%84%E6%9C%80%E5%B0%8F%E5%92%8C/"/>
    <url>/2022/09/07/Algorithm%20Solution/LC-931.%E4%B8%8B%E9%99%8D%E8%B7%AF%E5%BE%84%E6%9C%80%E5%B0%8F%E5%92%8C/</url>
    
    <content type="html"><![CDATA[<h4 id="下降路径最小和"><a href="https://leetcode.cn/problems/minimum-falling-path-sum/">931.下降路径最小和</a></h4><p>给你一个 <code>n x n</code> 的 <strong>方形</strong> 整数数组<code>matrix</code> ，请你找出并返回通过 <code>matrix</code>的<strong>下降路径</strong> 的 <strong>最小和</strong> 。</p><p><strong>下降路径</strong>可以从第一行中的任何元素开始，并从每一行中选择一个元素。在下一行选择的元素和当前行所选元素最多相隔一列（即位于正下方或者沿对角线向左或者向右的第一个元素）。具体来说，位置<code>(row, col)</code> 的下一个元素应当是<code>(row + 1, col - 1)</code>、<code>(row + 1, col)</code> 或者<code>(row + 1, col + 1)</code> 。</p><h4 id="dp-数组">DP 数组</h4><p>还是按照原先的思路，我们定义 <code>dp[i][j]</code> 为到达点 <span class="math inline">\((i,\ j)\)</span> 的最短距离</p><p>我们直接来看对于状态转移方程的讨论</p><p>容易看出，对于点 <span class="math inline">\((i,\ j)\)</span></p><p>只要 <code>j != n - 1</code> ，那么都可以从右上和上方转移过来</p><p>只要 <code>j != 0</code> ，那么都可以从左上和上方转移过来</p><p>基于此我们便可以写出状态转移方程，然后取三个数的最小值赋给<code>dp[i][j]</code>即可</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">minFallingPathSum</span><span class="hljs-params">(vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&amp; matrix)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = matrix.<span class="hljs-built_in">size</span>(), m = matrix[<span class="hljs-number">0</span>].<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(n, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(m, <span class="hljs-number">0</span>));<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; m; j++)<br>dp[<span class="hljs-number">0</span>][j] = matrix[<span class="hljs-number">0</span>][j];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt; n; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; m; j++)<br>&#123;<br>dp[i][j] = dp[i - <span class="hljs-number">1</span>][j] + matrix[i][j];<br><span class="hljs-keyword">if</span> (j &gt; <span class="hljs-number">0</span>)<br>dp[i][j] = <span class="hljs-built_in">min</span>(dp[i][j], dp[i - <span class="hljs-number">1</span>][j - <span class="hljs-number">1</span>] + matrix[i][j]);<br><span class="hljs-keyword">if</span> (j &lt; m - <span class="hljs-number">1</span>)<br>dp[i][j] = <span class="hljs-built_in">min</span>(dp[i][j], dp[i - <span class="hljs-number">1</span>][j + <span class="hljs-number">1</span>] + matrix[i][j]);<br>&#125;<br>&#125;<br><span class="hljs-type">int</span> ret = INT32_MAX;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> n : dp[n - <span class="hljs-number">1</span>])<br>ret = <span class="hljs-built_in">min</span>(ret, n);<br><span class="hljs-keyword">return</span> ret;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>这里有一个细节，如果状态转移方程我这么写：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp">dp[i][j] = dp[i - <span class="hljs-number">1</span>][j];<br><span class="hljs-keyword">if</span> (j &gt; <span class="hljs-number">0</span>)<br>    dp[i][j] = <span class="hljs-built_in">min</span>(dp[i][j], dp[i - <span class="hljs-number">1</span>][j - <span class="hljs-number">1</span>]) + matrix[i][j];<br><span class="hljs-keyword">if</span> (j &lt; m - <span class="hljs-number">1</span>)<br>    dp[i][j] = <span class="hljs-built_in">min</span>(dp[i][j], dp[i - <span class="hljs-number">1</span>][j + <span class="hljs-number">1</span>]) + matrix[i][j];<br></code></pre></td></tr></table></figure><p>看上去好像每错，事实上我第一次就是这么写的，我自己看了半天还没发现，无语。。。</p><p>事实上，这么写会导致 <code>matrix[i][j]</code>被加了两次，外面只需要它加一次就行，这么写是错的</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>路径问题</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>DP 数组空间压缩</title>
    <link href="/2022/09/06/Algorithm%20Solution/DP-%E6%95%B0%E7%BB%84%E7%A9%BA%E9%97%B4%E5%8E%8B%E7%BC%A9/"/>
    <url>/2022/09/06/Algorithm%20Solution/DP-%E6%95%B0%E7%BB%84%E7%A9%BA%E9%97%B4%E5%8E%8B%E7%BC%A9/</url>
    
    <content type="html"><![CDATA[<p>我们在 <a href="https://nishikichisato.github.io/2022/09/05/Algorithm%20Solution/120.-三角形最小路径和/">120.三角形最小路径和</a> 这里说过一种 <code>dp</code>数组压缩的方式：滚动数组压缩</p><p>其实质是将所有的偶数位全部投影到 <code>dp[0]</code>，所有奇数位全部投影到 <code>dp[1]</code></p><p>在实现上并无具体难度，只是单纯地将第一维改为<code>dp[i &amp; 1]</code> 就行</p><p>不过这么做的前提是 <code>dp[i][j]</code>的值只依赖其相邻位置，不过也正是因为这一点才允许空间压缩，所有这里也没什么好说的</p><p>我们来看另一种空间压缩，这里以 <a href="https://nishikichisato.github.io/2022/09/04/Algorithm%20Solution/LC-516.最长回文子序列/">LC516.最长回文子序列</a> 为例，我们将代码拿过来：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">longestPalindromeSubseq</span><span class="hljs-params">(string s)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> n = s.<span class="hljs-built_in">length</span>();<br>    vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(n, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(n, <span class="hljs-number">0</span>));<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>, j = <span class="hljs-number">0</span>; i &lt; n &amp;&amp; j &lt; n; i++, j++)<br>        dp[i][j] = <span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = n - <span class="hljs-number">1</span>; i &gt;= <span class="hljs-number">0</span>; i--)<br>    &#123;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = i + <span class="hljs-number">1</span>; j &lt; n; j++)<br>        &#123;<br>            <span class="hljs-keyword">if</span> (s[i] == s[j])<br>                dp[i][j] = <span class="hljs-number">2</span> + dp[i + <span class="hljs-number">1</span>][j - <span class="hljs-number">1</span>];<br>            <span class="hljs-keyword">else</span> dp[i][j] = <span class="hljs-built_in">max</span>(dp[i + <span class="hljs-number">1</span>][j], dp[i][j - <span class="hljs-number">1</span>]);<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> dp[<span class="hljs-number">0</span>][n - <span class="hljs-number">1</span>];<br>&#125;<br></code></pre></td></tr></table></figure><p>我们来考虑一个问题：我们真的需要 <span class="math inline">\(n^2\)</span> 的空间来存储 <code>dp</code>数组吗？</p><p>事实上，对于 <code>dp[i][j]</code>的值，我们真正需要用到的数据<strong>只有两行</strong>：<code>dp[i]</code>行与 <code>dp[i - 1]</code> 行</p><p>也就是说，前面从 0 到 <code>i - 2</code> 行的数据都可以被压缩</p><p>接着再考虑一个问题：我们对 <code>dp</code>数组的推导主要是通过赋值来实现，如果只用一维 <code>dp</code>的话，那么赋值前的 <code>dp</code> 数组里面的数值有什么含义？</p><p>显然，我们当前是需要对 <code>dp</code> 数组进行更新的，也就是求出<code>dp[i]</code>的所有值，那么没赋值前的值就是上一次循环结束后遗留下来的值，也就是<code>dp[i - 1]</code></p><p>到此为止，我们问题已经解决一大半了</p><p>接着考虑状态转移，我们说 <code>dp[i][j]</code> 的值是由<code>dp[i + 1][j - 1]</code> 、<code>dp[i][j - 1]</code> 和<code>dp[i + 1][j]</code></p><p>我们<strong>从行的视角来考虑</strong>，<code>dp[i + 1][j]</code> 是<code>dp[i][j]</code> 未赋值前的值，我们已知</p><p><code>dp[i - 1][j]</code> 是已经先于 <code>dp[i][j]</code>赋值的，就在它前面，我们也已知</p><p>现在有问题的只有 <code>dp[i + 1][j - 1]</code> ，这个值是在对<code>dp[i - 1][j]</code>赋值的时候掩盖掉的，也就是说我们需要将这个值保留下来才行</p><p>在<strong>内层循环里面但还没开始</strong>前，我们需要用临时变量<code>tmp</code> 把 <code>dp[j]</code>的值记录下来，并在此次<strong>内层循环结束</strong>之后赋值给<code>pre</code> （仍在内层循环内）</p><p><code>pre</code> 会在<strong>每次进入内层循环之前</strong>就初始化成0 ，然后让 <code>pre</code> 替代 <code>dp[i + 1][j - 1]</code> 即可</p><p>需要注意的是，我们的<strong>循环是不需要改变的</strong>！！！这一点很重要</p><p>完整代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">longestPalindromeSubseq</span><span class="hljs-params">(string s)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = s.<span class="hljs-built_in">length</span>();<br>vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">dp</span>(n, <span class="hljs-number">1</span>);<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = n - <span class="hljs-number">1</span>; i &gt;= <span class="hljs-number">0</span>; i--)<br>&#123;<br><span class="hljs-type">int</span> pre = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = i + <span class="hljs-number">1</span>; j &lt; n; j++)<br>&#123;<br><span class="hljs-type">int</span> tmp = dp[j];<br><span class="hljs-keyword">if</span> (s[i] == s[j])<br>dp[j] = <span class="hljs-number">2</span> + pre;<br><span class="hljs-keyword">else</span> dp[j] = <span class="hljs-built_in">max</span>(dp[j], dp[j - <span class="hljs-number">1</span>]);<br>pre = tmp;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[n - <span class="hljs-number">1</span>];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span></p><p>最后我们来讨论一下这种一维压缩之后的初始化问题</p><p>我们看到，从 <code>dp[i][j]</code> 压缩到 <code>dp[j]</code>之后，我们的循环依旧是两层</p><p><strong>只不过是用 <code>dp[j]</code> 去表示 <code>dp[i][j]</code>的每一层而已</strong></p><p>也就是说， <strong><code>dp[j]</code> 的初始化情况就等同于<code>dp[i][j]</code> 当中第 0 层的情况</strong></p><p>其实把上面理解了，这里也就不难了</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>总结</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>关于子序列问题的总结</title>
    <link href="/2022/09/06/Algorithm%20Solution/%E5%85%B3%E4%BA%8E%E5%AD%90%E5%BA%8F%E5%88%97%E9%97%AE%E9%A2%98%E7%9A%84%E6%80%BB%E7%BB%93/"/>
    <url>/2022/09/06/Algorithm%20Solution/%E5%85%B3%E4%BA%8E%E5%AD%90%E5%BA%8F%E5%88%97%E9%97%AE%E9%A2%98%E7%9A%84%E6%80%BB%E7%BB%93/</url>
    
    <content type="html"><![CDATA[<p>前面我们写了两道子序列的题目，分别是 <a href="https://nishikichisato.github.io/2022/09/04/Algorithm%20Solution/LC-516.最长回文子序列/">LC516.最长回文子序列</a> 和 <a href="https://nishikichisato.github.io/2022/09/04/Algorithm%20Solution/LC-1143.最长公共子序列/">LC1143.最长公共子序列</a></p><p>这两道题所用的 <code>dp</code> 数组均是二维 <code>dp</code>，其定义分别为：</p><p>若有两数组 <code>arr1</code> 与数组 <code>arr2</code> ，在区间<code>arr1[0..i]</code> 和区间 <code>arr[0..j]</code> 中，我们定义<code>dp[i][j]</code> 为所求子序列长度</p><p>若只有一数组 <code>arr</code> ，在区间 <code>arr[i..j]</code>，我们定义 <code>dp[i][j]</code> 为所求子序列长度</p><p>二者的遍历方式因题目而异，在这里只讨论定义</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>总结</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 120.三角形最小路径和</title>
    <link href="/2022/09/05/Algorithm%20Solution/LC-120.%E4%B8%89%E8%A7%92%E5%BD%A2%E6%9C%80%E5%B0%8F%E8%B7%AF%E5%BE%84%E5%92%8C/"/>
    <url>/2022/09/05/Algorithm%20Solution/LC-120.%E4%B8%89%E8%A7%92%E5%BD%A2%E6%9C%80%E5%B0%8F%E8%B7%AF%E5%BE%84%E5%92%8C/</url>
    
    <content type="html"><![CDATA[<h4 id="三角形最小路径和"><a href="https://leetcode.cn/problems/triangle/">120.三角形最小路径和</a></h4><p>给定一个三角形 <code>triangle</code> ，找出自顶向下的最小路径和。</p><p>每一步只能移动到下一行中相邻的结点上。相邻的结点 在这里指的是 下标 与上一层结点下标 相同或者等于 上一层结点下标 + 1的两个结点。也就是说，如果正位于当前行的下标 i，那么下一步可以移动到下一行的下标 i 或 i + 1 。</p><figure class="highlight inform7"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs inform7">输入：triangle = <span class="hljs-comment">[<span class="hljs-comment">[2]</span>,<span class="hljs-comment">[3,4]</span>,<span class="hljs-comment">[6,5,7]</span>,<span class="hljs-comment">[4,1,8,3]</span>]</span><br>输出：11<br>解释：如下面简图所示：<br>   2<br>  3 4<br> 6 5 7<br>4 1 8 3<br>自顶向下的最小路径和为 11（即，2 + 3 + 5 + 1 = 11）。<br></code></pre></td></tr></table></figure><h4 id="dp-数组">DP 数组</h4><p>前面做了那么多道的最短路径，现在这种题目已经是轻车熟路了</p><p>我们定义 <code>dp[i][j]</code> 表示到达点 <span class="math inline">\((i,\ j)\)</span> 所需的最短路径和</p><p>在这道题里面，我们需要找出自顶向下的最短路径和，也就是我们需要找出<code>dp</code> 数组最后一行当中的最小值，即 <code>dp[n-1][j]</code>的最小值</p><p>然后我们来考虑一下状态转移方程。这种路径问题，做多了真的是有感觉</p><p>我们考虑<strong>其它点应该如何转移到点</strong> <span class="math inline">\((i,\ j)\)</span> 这个问题，即对于所求<code>dp[i][j]</code> ，我们有且仅有如下两种方式转移过来</p><ul><li>正上方</li><li>左上方</li></ul><p>接着我们考虑这两种情况的限制条件，不难想到：</p><p>对于位置第 <code>j = 0</code>列，<strong>只可以</strong>正上方转移过来</p><p>对于位置 <code>j == i</code>，<strong>只可以</strong>从左上方转移过来</p><p>我们用区间的角度来理解这两句话就是：</p><p>若 <code>j == 0</code> ，则只能从正上方转移</p><p>若 <code>0 &lt; j &lt; i</code> ，则既可以从正上也可以从左上转移</p><p>若 <code>j == i</code> ，即只能从左上转移</p><p>实际上，有这三句就已经足够我们写转移方程了，但这里还可以继续讨论</p><p>刚刚我们是从区间也就是 <code>j</code>的取值来考虑所得到的结果，对于这个结果，我们从<strong>「可以从哪个方向转移过来」</strong>来考虑<code>j</code> 的取值</p><p>若<strong>一定能</strong>从正上方转移，则 <code>j &lt; i</code>，只需要把只能从左上方转移过来的条件排除在外即可，其转移方程为<code>dp[i][j] = dp[i - 1][j]</code></p><p>若<strong>一定能</strong>从左上方转移过来，则 <code>j &gt; 0</code>，同理，把只能从正上方转移过来的情况排除，其转移方程为<code>dp[i][j] = dp[i - 1][j - 1]</code></p><p>讨论到这里，最主要的就已经结束了，还有一个细节：</p><p>上面这两种情况是相互独立的，也就是分别由两个 <code>if</code>来进行判断</p><p>我们最终的 <code>dp[i][j]</code>的取值需要取二者的较小值（如果二者均成立）</p><p>我原先的想法是，分别用两个变量记录下这两个数（没有成立的那个用最大值标记），然后在对<code>dp[i][j]</code> 赋值的时候对这两个变量取较小值即可</p><p>实际上还有更简单的办法：先对 <code>dp[i][j]</code>赋最大值，然后分别于那两个转移过来的值作比较即可，这样一定可以取到最小值</p><p>就算那两个 <code>if</code>都不成立，在下一层当中也会直接忽略掉这个值（因为它是最大值）</p><p>具体代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">minimumTotal</span><span class="hljs-params">(vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&amp; triangle)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = triangle.<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(n, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(n, <span class="hljs-number">0</span>));<br>dp[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>] = triangle[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt; n; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt;= i; j++)<br>&#123;<br>dp[i][j] = INT32_MAX;<br><span class="hljs-type">int</span> val = triangle[i][j];<br><span class="hljs-keyword">if</span> (j &lt; i)<br>dp[i][j] = <span class="hljs-built_in">min</span>(dp[i][j], dp[i - <span class="hljs-number">1</span>][j] + val);<br><span class="hljs-keyword">if</span> (j &gt; <span class="hljs-number">0</span>)<br>dp[i][j] = <span class="hljs-built_in">min</span>(dp[i][j], dp[i - <span class="hljs-number">1</span>][j - <span class="hljs-number">1</span>] + val);<br>&#125;<br>&#125;<br><span class="hljs-type">int</span> ret = INT32_MAX;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> m : dp[n - <span class="hljs-number">1</span>])<span class="hljs-comment">//求最小值</span><br>ret = <span class="hljs-built_in">min</span>(ret, m);<br><span class="hljs-keyword">return</span> ret;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n^2)\)</span></p><h4 id="dp-数组-滚动数组压缩">DP 数组 + 滚动数组压缩</h4><p>滚动数组的状态压缩没有任何的思维难度，至少看起来是这样</p><p>我们直接将<strong>第一维</strong>从 <code>i</code> 改为<code>2</code></p><p>然后将所有的 <code>dp[i]</code> 全部改为 <code>dp[i % 2]</code> 或者<code>dp[i &amp; 1]</code> ，这里推荐用后者，毕竟可移植性好</p><p>最起码，从做法上看，没有任何难度，但我还是解释一下</p><p>我们单单看 <code>dp</code> 数组的第一维，<code>dp[i]</code>的取值<strong>只依靠</strong><code>dp[i - 1]</code>，也就是该位置前面那一个的值（记住，我们只看第一维）</p><p>那么，对于原 <code>dp</code>数组当中的<strong>某一行</strong>，我把所有<strong>奇数索引</strong>对应的值投影到<code>dp[1]</code> ，所有<strong>偶数索引</strong>对应的值投影到<code>dp[0]</code> ，由于每一次对 <code>dp[i]</code>的赋值只依靠前一个的值，那么我们让 <code>dp[0]</code> 和<code>dp[1]</code> 的值不断更新即可，这也就是滚动数组嘛</p><p>根本没有任何的思维难度。。。。。。</p><p>具体代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">minimumTotal</span><span class="hljs-params">(vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&amp; triangle)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = triangle.<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(<span class="hljs-number">2</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(n, <span class="hljs-number">0</span>));<br>dp[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>] = triangle[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt; n; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt;= i; j++)<br>&#123;<br>dp[i &amp; <span class="hljs-number">1</span>][j] = INT32_MAX;<br><span class="hljs-type">int</span> val = triangle[i][j];<br><span class="hljs-keyword">if</span> (j &lt; i)<br>dp[i &amp; <span class="hljs-number">1</span>][j] = <span class="hljs-built_in">min</span>(dp[i &amp; <span class="hljs-number">1</span>][j], dp[(i - <span class="hljs-number">1</span>) &amp; <span class="hljs-number">1</span>][j] + val);<br><span class="hljs-keyword">if</span> (j &gt; <span class="hljs-number">0</span>)<br>dp[i &amp; <span class="hljs-number">1</span>][j] = <span class="hljs-built_in">min</span>(dp[i &amp; <span class="hljs-number">1</span>][j], dp[(i - <span class="hljs-number">1</span>) &amp; <span class="hljs-number">1</span>][j - <span class="hljs-number">1</span>] + val);<br>&#125;<br>&#125;<br><span class="hljs-type">int</span> ret = INT32_MAX;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> m : dp[(n - <span class="hljs-number">1</span>) &amp; <span class="hljs-number">1</span>])<br>ret = <span class="hljs-built_in">min</span>(ret, m);<br><span class="hljs-keyword">return</span> ret;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>路径问题</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>大二上 初始周 2022.8.31 - 2022.9.4</title>
    <link href="/2022/09/04/%E6%97%A5%E5%B8%B8%E6%80%BB%E7%BB%93/%E5%A4%A7%E4%BA%8C%E4%B8%8A-%E5%88%9D%E5%A7%8B%E5%91%A8-2022.8.31---2022.9.4/"/>
    <url>/2022/09/04/%E6%97%A5%E5%B8%B8%E6%80%BB%E7%BB%93/%E5%A4%A7%E4%BA%8C%E4%B8%8A-%E5%88%9D%E5%A7%8B%E5%91%A8-2022.8.31---2022.9.4/</url>
    
    <content type="html"><![CDATA[<p>这五天学的还可以，说实话学的挺多的，我是十分满意</p><p>C++ Primer Plus差不多快看完了，还剩下最后一章，我们下个星期的周末找点时间把它干掉</p><p>然后那个 Effective C++我就暂时算它完结了吧，到目前为止只看了百分之八十五，都是暑假期间看的，现在不想看</p><p>我现在还开始看 CSAPP，这本书说实话，写的很好，感觉每天给两个小时，应该不到两个月就可以看完，我打算七周把它结束</p><p>CSAPP 看完之后我就可以开始看 计算机网络自顶向下了，这个稍微薄一点，差不多 500 页吧，之后再来 5 周应该可以解决</p><p>也就是说，最理想的情况是，我能在 12 周就把 CSAPP 和 计网自顶向下看完</p><p>说实话，这个效率已经很高了</p><p>嘛，我慢慢学，一定可以</p><p>计划里面我是将 14周也预留出来看的，但去有点不太敢，毕竟我平时基本不会学专业课，一个月是有点赶的说实话</p><p>嘛，14 周的周末到时候会空出来给我复习专业课</p><p>那我们 14 周也就只有上学的时间是可以利用的了</p><p>最后那两周，看看 操作系统导论 吧，这本书我争取这个寒假把它看完</p><p>当然寒假肯定不可能只看完这一本就是了（）</p><p>然后说说算法部分吧</p><p>我打算花 4周的时间把动态规划给全部搞定，我们就看三叶姐的那些文章，主要是路径问题和背包啦</p><p>我算过，我每天晚上差不多可以有三个半小时，一天看完一篇应该问题不太大，实在看不懂就先放着，过几天再看嘛</p><p>感觉不好说，路径那边有 11 篇文章，背包有 21篇，这。。。。一个月看不完啊</p><p>那我们给 6 周吧，这个时间其实是很充裕了，应该没有问题</p><p>然后就是那个 Linux 基本操作，这个。。。。我目前差个 100 P ，emmmm</p><p>我每天晚上都不一定能看两个，大头估计得留着周末看，我们也给 6周吧，跟动态规划那边一样</p><p>周末具体要干什么得看情况来定，不过我应该会去看这些吧，因为我平时上学就是看这些的，如果周末去看其他的就会断断续续，怪难受的</p><p>差不多就这样</p><p>我难得觉得我这一周学习还行</p><p>这种学习的感觉还是挺舒服的，我喜欢，哈哈哈哈</p><p>嗯，就这样</p>]]></content>
    
    
    <categories>
      
      <category>日常总结</category>
      
    </categories>
    
    
    <tags>
      
      <tag>周总结</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 516.最长回文子序列</title>
    <link href="/2022/09/04/Algorithm%20Solution/LC-516.%E6%9C%80%E9%95%BF%E5%9B%9E%E6%96%87%E5%AD%90%E5%BA%8F%E5%88%97/"/>
    <url>/2022/09/04/Algorithm%20Solution/LC-516.%E6%9C%80%E9%95%BF%E5%9B%9E%E6%96%87%E5%AD%90%E5%BA%8F%E5%88%97/</url>
    
    <content type="html"><![CDATA[<h4 id="最长回文子序列"><a href="https://leetcode.cn/problems/longest-palindromic-subsequence/">516.最长回文子序列</a></h4><p>给你一个字符串 <code>s</code>，找出其中最长的回文子序列，并返回该序列的长度。</p><p>子序列定义为：不改变剩余字符顺序的情况下，删除某些字符或者不删除任何字符形成的一个序列。</p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs abnf">输入：s <span class="hljs-operator">=</span> <span class="hljs-string">&quot;bbbab&quot;</span><br>输出：<span class="hljs-number">4</span><br>解释：一个可能的最长回文子序列为 <span class="hljs-string">&quot;bbbb&quot;</span> 。<br></code></pre></td></tr></table></figure><h4 id="dp-数组">DP 数组</h4><p>关于前面<a href="https://nishikichisato.github.io/2022/09/04/Algorithm%20Solution/LC-1143.最长公共子序列/">LC1143.最长公共子序列</a>的问题中，我们是按照 <code>s1[0..i]</code>这样分析的</p><p>也就是说，我们其实是<strong>默认检索区间的左端点的不动性</strong>，这在遍历子序列是不会有什么问题的。因为我们只是对一个区间进行检索，看这里面是否存在<code>LCS</code> 而已</p><p>但这道题不能这么做，或者说这么做问题会很复杂</p><p>我们同样是采取细化问题的思路，那么我们如何让目前已有的回文字符串更长一点？</p><p>是不是只需要检索这个已有子串<strong>向外延申出去的第一个字符是否相等</strong>就行，从这里就可以看出来，我们不能采用区间左端点不动的方式</p><p>于是乎，我们给出以下 <code>dp</code> 数组的定义：</p><p><code>dp[i][j]</code> 的定义为：区间 <code>s[i..j]</code>当中最长的回文子序列</p><p>然后，我们来看看初始情况</p><p>这里我们默认 <code>i &lt;= j</code> ，也就是说 <code>i &gt; j</code>的部分（矩阵左下角）全部为 0</p><p>当 <code>i == j</code> 时，也就是此时只有一个字符，此时回文串长度为 1，因此此矩阵的对角线部分全部为 1</p><p>上面是初始情况的分析</p><p>刚刚我们说到，我们需要<strong>检索已检索区间向外延申的第一个字符是否相同</strong>，因此在这在情况下，<code>dp[i][j]</code>由<code>dp[i + 1][j - 1]</code> 推出，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(s[i] == s[j])<br>    dp[i][j] = dp[i + <span class="hljs-number">1</span>][j - <span class="hljs-number">1</span>] + <span class="hljs-number">2</span>;<span class="hljs-comment">//长度加 2</span><br></code></pre></td></tr></table></figure><p>接着我们看这两个字符不相同的情况</p><p>由于我们的 <code>dp</code> 函数的定义是区间 <code>s[i..j]</code>的最长回文子序列，因此我们只需要将左右两个字符分别添加到已检索区间内，然后再取二者的较大值即可，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">dp[i][j] = <span class="hljs-built_in">max</span>(dp[i + <span class="hljs-number">1</span>][j], dp[i][j - <span class="hljs-number">1</span>]);<br></code></pre></td></tr></table></figure><p>需要注意的是，我们只是<strong>扩大了检索区间</strong>，并不是扩大了最长子序列的长度</p><p>到此为止，基本思路已经分析完毕，但还没有结束</p><p>我们目前需要对矩阵区域的右上角进行遍历，然后我们的<code>dp[i][j]</code> 是由 <code>dp[i - 1][j - 1]</code>、<code>dp[i + 1][j]</code> 、<code>dp[i][j - 1]</code>这三个点推出，也就是说，我们需要从矩阵的最下面开始遍历，然后再逐渐往上</p><p>外层循环 <code>i</code> 的开始点为 <code>n - 1</code>，内层循环的开始点为 <code>i + 1</code>（这里画图很容易理解），之后开始循环即可</p><p>完整代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">longestPalindromeSubseq</span><span class="hljs-params">(string s)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = s.<span class="hljs-built_in">length</span>();<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(n, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(n, <span class="hljs-number">0</span>));<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>, j = <span class="hljs-number">0</span>; i &lt; n &amp;&amp; j &lt; n; i++, j++)<br>dp[i][j] = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = n - <span class="hljs-number">1</span>; i &gt;= <span class="hljs-number">0</span>; i--)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = i + <span class="hljs-number">1</span>; j &lt; n; j++)<br>&#123;<br><span class="hljs-keyword">if</span> (s[i] == s[j])<br>dp[i][j] = <span class="hljs-number">2</span> + dp[i + <span class="hljs-number">1</span>][j - <span class="hljs-number">1</span>];<br><span class="hljs-keyword">else</span> dp[i][j] = <span class="hljs-built_in">max</span>(dp[i + <span class="hljs-number">1</span>][j], dp[i][j - <span class="hljs-number">1</span>]);<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[<span class="hljs-number">0</span>][n - <span class="hljs-number">1</span>];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span>，有 <span class="math inline">\(n^2\)</span> 个状态</p><p>空间复杂度：<span class="math inline">\(O(n^2)\)</span></p><h4 id="总结">总结</h4><p>前文<a href="https://nishikichisato.github.io/2022/09/04/Algorithm%20Solution/LC-1143.最长公共子序列/">LC1143.最长公共子序列</a>中，我们见到了二维 <code>dp</code>数组的一种定义</p><p>这里我们见到了二维 <code>dp</code> 数组的另一种定义：</p><p>在子序列 <code>s[i..j]</code> 中， 所要求的子序列长度为<code>dp[i][j]</code></p><p>至于遍历方式，这一点根据题目的意思来</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>模板题目</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 1143.最长公共子序列</title>
    <link href="/2022/09/04/Algorithm%20Solution/LC-1143.%E6%9C%80%E9%95%BF%E5%85%AC%E5%85%B1%E5%AD%90%E5%BA%8F%E5%88%97/"/>
    <url>/2022/09/04/Algorithm%20Solution/LC-1143.%E6%9C%80%E9%95%BF%E5%85%AC%E5%85%B1%E5%AD%90%E5%BA%8F%E5%88%97/</url>
    
    <content type="html"><![CDATA[<h4 id="最长公共子序列"><a href="https://leetcode.cn/problems/longest-common-subsequence/">1143.最长公共子序列</a></h4><p>给定两个字符串 <code>text1</code> 和<code>text2</code>，返回这两个字符串的最长 <strong>公共子序列</strong>的长度。如果不存在 <strong>公共子序列</strong> ，返回 <code>0</code>。</p><p>一个字符串的 <strong>子序列</strong>是指这样一个新的字符串：它是由原字符串在不改变字符的相对顺序的情况下删除某些字符（也可以不删除任何字符）后组成的新字符串。</p><ul><li>例如，<code>"ace"</code> 是 <code>"abcde"</code> 的子序列，但<code>"aec"</code> 不是 <code>"abcde"</code> 的子序列。</li></ul><p>两个字符串的 <strong>公共子序列</strong>是这两个字符串所共同拥有的子序列。</p><figure class="highlight nix"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs nix">输入：<span class="hljs-attr">text1</span> = <span class="hljs-string">&quot;abcde&quot;</span>, <span class="hljs-attr">text2</span> = <span class="hljs-string">&quot;ace&quot;</span> <br>输出：<span class="hljs-number">3</span>  <br>解释：最长公共子序列是 <span class="hljs-string">&quot;ace&quot;</span> ，它的长度为 <span class="hljs-number">3</span> 。<br></code></pre></td></tr></table></figure><h4 id="dp-函数">dp 函数</h4><p>如果是对两个字符串直接整个求 <code>LCS</code>的话，这个过程会根本没有思路</p><p>因此我们需要将这个问题细化一下，我们去求<strong>指定区间的<code>LCS</code> </strong>，想到这里，<code>dp</code>函数的定义就已经呼之欲出了：</p><p><code>int dp(string&amp; s1, int i, string&amp; s2, int j)</code>的定义为区间 <code>s1[i, s1.length]</code> 和区间<code>s2[j, s2.length]</code> 的最长公共子序列</p><p>这个地方从后面开始遍历跟从前面开始遍历是一样的，我们用后往前的</p><p>此时，起始遍历点为 <code>dp(s1.length - 1, s2.length - 1)</code></p><p>显然，当两个字符串中有一个遍历完了，我们就需要结束递归，因此</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(i == <span class="hljs-number">-1</span> || j == <span class="hljs-number">-1</span>)<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br></code></pre></td></tr></table></figure><p>现在有了定义之后，我们发现求一个区间的最长公共子序列还是很困难，我们需要继续将问题细化</p><p>最长公共子序列，说到底也是由一个一个字符所组成的，也就是说只要<code>s1[i] == s2[j]</code> ，那么当前字符就一定在 <code>LCS</code>内，因此我们需要返回<code>return 1 + dp(s1, i - 1, s2, j - 1)</code></p><p>想到这里其实就已经结束了，剩下的我们需要考虑<code>s1[i] != s2[j]</code> 的情况</p><p>我们还是从字符是否在 <code>LCS</code>内来开始讨论，对于这种两个字符不相等的情况，不外乎三种可能：</p><ul><li><code>s1</code> 不在，<code>s2</code> 在</li><li><code>s1</code> 在，<code>s2</code> 不在</li><li><code>s1</code> 与 <code>s2</code> 均不在</li></ul><p>由于我们需要求的是最长公共子序列，因此这三种情况我们需要求一个最大值，即：<code>max(dp(i-1, j), dp(i, j-1), dp(i-1, j-1))</code>，这三个与上面的三个依次对应</p><p>因此我们最终的代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">dp</span><span class="hljs-params">(string&amp; s1, <span class="hljs-type">int</span> i, string&amp; s2, <span class="hljs-type">int</span> j)</span><span class="hljs-comment">//s1[i,...],s2[j,...]当中的最长公共子序列</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (i == <span class="hljs-number">-1</span> || j == <span class="hljs-number">-1</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br><span class="hljs-keyword">if</span> (s1[i] == s2[j])<br><span class="hljs-keyword">return</span> <span class="hljs-number">1</span> + <span class="hljs-built_in">dp</span>(s1, i - <span class="hljs-number">1</span>, s2, j - <span class="hljs-number">1</span>);<br><span class="hljs-keyword">else</span><br><span class="hljs-keyword">return</span> <span class="hljs-built_in">max</span>(<span class="hljs-built_in">dp</span>(s1, i - <span class="hljs-number">1</span>, s2, j - <span class="hljs-number">1</span>), <span class="hljs-built_in">max</span>(<span class="hljs-built_in">dp</span>(s1, i - <span class="hljs-number">1</span>, s2, j), <span class="hljs-built_in">dp</span>(s1, i, s2, j - <span class="hljs-number">1</span>)));<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">longestCommonSubsequence</span><span class="hljs-params">(string text1, string text2)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">dp</span>(text1, text1.<span class="hljs-built_in">length</span>() - <span class="hljs-number">1</span>, text2, text2.<span class="hljs-built_in">length</span>() - <span class="hljs-number">1</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>这个代码在 LC上面会因为超时而过不了，我们需要加一个备忘录来进行去重</p><h4 id="dp-函数-备忘录">dp 函数 + 备忘录</h4><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;memo;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">dp</span><span class="hljs-params">(string&amp; s1, <span class="hljs-type">int</span> i, string&amp; s2, <span class="hljs-type">int</span> j)</span><span class="hljs-comment">//s1[i,...],s2[j,...]当中的最长公共子序列</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (i == <span class="hljs-number">-1</span> || j == <span class="hljs-number">-1</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br><span class="hljs-keyword">if</span> (memo[i][j] != <span class="hljs-number">-1</span>)<br><span class="hljs-keyword">return</span> memo[i][j];<br><span class="hljs-keyword">if</span> (s1[i] == s2[j])<br>memo[i][j] = <span class="hljs-number">1</span> + <span class="hljs-built_in">dp</span>(s1, i - <span class="hljs-number">1</span>, s2, j - <span class="hljs-number">1</span>);<br><span class="hljs-keyword">else</span><br>memo[i][j] =  <span class="hljs-built_in">max</span>(<span class="hljs-built_in">dp</span>(s1, i - <span class="hljs-number">1</span>, s2, j - <span class="hljs-number">1</span>), <span class="hljs-built_in">max</span>(<span class="hljs-built_in">dp</span>(s1, i - <span class="hljs-number">1</span>, s2, j), <span class="hljs-built_in">dp</span>(s1, i, s2, j - <span class="hljs-number">1</span>)));<br><span class="hljs-keyword">return</span> memo[i][j];<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">longestCommonSubsequence</span><span class="hljs-params">(string text1, string text2)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = text1.<span class="hljs-built_in">length</span>(), m = text2.<span class="hljs-built_in">length</span>();<br>memo.<span class="hljs-built_in">assign</span>(n, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(m, <span class="hljs-number">-1</span>));<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">dp</span>(text1, text1.<span class="hljs-built_in">length</span>() - <span class="hljs-number">1</span>, text2, text2.<span class="hljs-built_in">length</span>() - <span class="hljs-number">1</span>);<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(m * n)\)</span></p><p>空间复杂度：<span class="math inline">\(O(m * n)\)</span></p><h4 id="dp-数组">dp 数组</h4><p>用 <code>dp</code>数组来解，同样是从前往后与从后往前都一样，我们采取从前往后</p><p>由前面的分析我们可以确定，我们需要从 <code>dp[i - 1][j]</code> 或者是<code>dp[i][j - 1]</code> 来推出 <code>dp[i][j]</code></p><p>如果我们采取字符串索引与数组索引一一对应的形式，那么势必会导致字符串的索引越界，因此我们需要让字符串进行一下偏移，让<strong>字符串的0 索引对应数组的 1索引</strong>，之后我们再在这个基础上面考虑初始情况</p><p><strong>在有索引偏移的情况下，在 <code>dp</code>数组当中的的初始情况就是去对 <code>dp[i][0]</code> 与<code>dp[0][j]</code> 进行赋初值</strong></p><p>由于我们将字符串整体向右偏移了一位，因此数组的 0索引在字符串这边没有任何的意义，因此也不需要对数组进行初始化</p><p>在对字符串进行访问时，只需要记住其索引是 <code>i - 1</code> 或者<code>j - 1</code> ，剩下的就没有问题了</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">longestCommonSubsequence</span><span class="hljs-params">(string text1, string text2)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = text1.<span class="hljs-built_in">length</span>(), m = text2.<span class="hljs-built_in">length</span>();<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(n + <span class="hljs-number">1</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(m + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>));<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= n; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">1</span>; j &lt;= m; j++)<br>&#123;<br><span class="hljs-keyword">if</span> (text1[i - <span class="hljs-number">1</span>] == text2[j - <span class="hljs-number">1</span>])<br>dp[i][j] = <span class="hljs-number">1</span> + dp[i - <span class="hljs-number">1</span>][j - <span class="hljs-number">1</span>];<br><span class="hljs-keyword">else</span><br>dp[i][j] = <span class="hljs-built_in">max</span>(dp[i - <span class="hljs-number">1</span>][j - <span class="hljs-number">1</span>], <span class="hljs-built_in">max</span>(dp[i - <span class="hljs-number">1</span>][j], dp[i][j - <span class="hljs-number">1</span>]));<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[n][m];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n * m)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n * m)\)</span></p><h4 id="总结">总结</h4><p>这是一道涉及<strong>两个</strong>字符串/数组的场景，我们采用二维的<code>dp</code> 数组来解决这个问题，其相关定义为：</p><p>在数组 <code>arr1</code> 和数组 <code>arr2</code> 中，子数组<code>arr1[0..i]</code> 和子数组 <code>arr2[0..j]</code>中，所求子序列长度为 <code>dp[i][j]</code></p><p>当涉及具体迭代时，采用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i : n)<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j : m)<br>        <span class="hljs-keyword">if</span>(arr1[i] == arr2[j])<br>            dp[i][j] = 特殊情况<br>        <span class="hljs-keyword">else</span><br>            dp[i][j] = 最值情况<br></code></pre></td></tr></table></figure><h4 id="最后">最后</h4><p>感觉之前做题完全没有一个清晰的思路，拿到一道新题完全下不了手的那种</p><p>感觉这种情况就跟这道题最开始是一样的，对整个问题进行分析是无从下手的，应该去将问题<strong>细化</strong>，直到最后可以分析出来为止</p><p>所以，我想将这道题作为我细化题目的一个最小单位，其他的问题可以由这道题延申拓展出来，或者说我做的其他题可以细化到这道题上面，这就是我的目的</p><p>嘛，这种「最小单位」肯定是需要不断做题积累总结的，只能说是慢慢来吧</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>LCS</tag>
      
      <tag>模板题目</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>CSAPP 第二章 信息的表示和处理</title>
    <link href="/2022/09/03/CSAPP/CSAPP-%E7%AC%AC%E4%BA%8C%E7%AB%A0-%E4%BF%A1%E6%81%AF%E7%9A%84%E8%A1%A8%E7%A4%BA%E5%92%8C%E5%A4%84%E7%90%86/"/>
    <url>/2022/09/03/CSAPP/CSAPP-%E7%AC%AC%E4%BA%8C%E7%AB%A0-%E4%BF%A1%E6%81%AF%E7%9A%84%E8%A1%A8%E7%A4%BA%E5%92%8C%E5%A4%84%E7%90%86/</url>
    
    <content type="html"><![CDATA[<h2 id="信息存储">信息存储</h2><p>大多数计算机使用 8位的字节作为<strong>最小的可寻址的内存单位</strong>，而不是直接访问内存当中单独的位（或者说比特）。机器级程序会将内存视为一个非常的字节数组，这个内存称为<em>虚拟内存</em>。内存当中的<strong>每个字节</strong>都用唯一的数字来进行标识，这称为该字节的<em>地址</em>，所有可能的地址集合就是<em>虚拟地址空间</em>。</p><p>这里的虚拟地址空间只是给机器级程序的一个<strong>概念性</strong>的映射，具体的底层实现由硬件来处理。</p><h3 id="寻址和字节顺序">寻址和字节顺序</h3><p>我们知道，一个对象肯定不可能只占一个字节，那么对于多字节的程序对象，我们必须建立两个规则：这个对象的地址是什么，以及在内存当中该如何存储这些字节。</p><p>多字节对象大多被存储为<strong>连续</strong>的字节序列，该对象的地址就是所使用的连续的字节序列当中<strong>最小</strong>的地址。例如，一个<code>int</code> 类型的变量占 4 个字节，分别是 0x100、0x101、0x102、0x103，那么在这里面，0x100 就是这个变量的地址。</p><p>我们考虑一个有 <strong><span class="math inline">\(\omega\)</span>位</strong>的整数，其位表示为 <span class="math inline">\([\x_{\omega-1},\ x_{\omega-2},\ \cdots,x_1\ ,x_0\ ]\)</span> 。我们假设<span class="math inline">\(\omega\)</span> 是 8 的倍数（不够的位数补 0） ，那么该整数的最高<strong>字节</strong>为 <span class="math inline">\([\ x_{\omega-1},\ x_{\omega-2},\ \cdots,\x_{\omega-7},\ x_{\omega-8}\ ]\)</span> ，最低位<strong>字节</strong>为<span class="math inline">\([\ x_7,\ x_6,\ \cdots,\ x_1,\ x_0\]\)</span>。如果一台机器将<strong>低位有效字节存储低地址</strong>处（0x100处），那么该机器为<strong>小端存储</strong>，如果将<strong>高位有效字节存储在低地址</strong>处，那么为大端<strong>存储</strong>。</p><h3 id="布尔运算">布尔运算</h3><p>我们给出四种最基本的布尔运算结果：</p><table><thead><tr class="header"><th style="text-align: center;">~</th><th style="text-align: center;"></th><th style="text-align: center;"></th><th style="text-align: center;">&amp;</th><th style="text-align: center;">1</th><th style="text-align: center;">0</th><th style="text-align: center;"></th><th style="text-align: center;">|</th><th style="text-align: center;">1</th><th style="text-align: center;">0</th><th style="text-align: center;"></th><th style="text-align: center;">^</th><th style="text-align: center;">1</th><th style="text-align: center;">0</th></tr></thead><tbody><tr class="odd"><td style="text-align: center;">0</td><td style="text-align: center;">1</td><td style="text-align: center;"></td><td style="text-align: center;">1</td><td style="text-align: center;">1</td><td style="text-align: center;">0</td><td style="text-align: center;"></td><td style="text-align: center;">1</td><td style="text-align: center;">1</td><td style="text-align: center;">1</td><td style="text-align: center;"></td><td style="text-align: center;">1</td><td style="text-align: center;">0</td><td style="text-align: center;">1</td></tr><tr class="even"><td style="text-align: center;">1</td><td style="text-align: center;">0</td><td style="text-align: center;"></td><td style="text-align: center;">0</td><td style="text-align: center;">0</td><td style="text-align: center;">0</td><td style="text-align: center;"></td><td style="text-align: center;">0</td><td style="text-align: center;">1</td><td style="text-align: center;">0</td><td style="text-align: center;"></td><td style="text-align: center;">0</td><td style="text-align: center;">1</td><td style="text-align: center;">0</td></tr></tbody></table><p>我们着重说一下异或，只有当 <span class="math inline">\(P\)</span>、<span class="math inline">\(Q\)</span> 不同时为真时，<span class="math inline">\(P \oplus Q\)</span> 才为真。</p><p>整数运算的一个属性是每一个数都存在一个加法逆元 <span class="math inline">\(-x\)</span> ，使得 <span class="math inline">\(x\+ \ (-x)\ =\ 0\)</span>。在布尔运算里面也有相同的属性，这里面的加法就是异或 <span class="math inline">\(\oplus\)</span> 。</p><p>对任意值 <span class="math inline">\(a\)</span> 而言，有 <span class="math inline">\(a \ \oplus \ a \ =\ 0\)</span> 、<span class="math inline">\(a\ \oplus \ 0 \ =\ a\)</span> 。</p><p>并且异或类似于集合当中的补集，举个例子就是：</p><p><span class="math inline">\(0x11223344\ \oplus\ 0\ =\0x11223344\)</span> 、<span class="math inline">\(0x87654321\ \oplus \0xffffffff\ =\ 0x789abcde\)</span></p><p>即 8 + 7 = 15 、9 + 6 = 15 、1 + e =15，所以说异或具有补集的性质。</p><h3 id="逻辑运算与移位操作">逻辑运算与移位操作</h3><p>上面的布尔运算属于<strong>位级运算</strong>的范畴，跟逻辑运算完全不一样</p><p>逻辑运算认为所有的非零参数都表示 TRUE ，而 0 表示 FALSE，因此便有了下面的这些运算结果：</p><table><thead><tr class="header"><th style="text-align: center;">!0x41</th><th style="text-align: center;">0x00</th></tr></thead><tbody><tr class="odd"><td style="text-align: center;">!!0x41</td><td style="text-align: center;">0x01</td></tr><tr class="even"><td style="text-align: center;">0x41 &amp;&amp; 0x42</td><td style="text-align: center;">0x01</td></tr><tr class="odd"><td style="text-align: center;">0x41 || 0x42</td><td style="text-align: center;">0x01</td></tr></tbody></table><p>这里的移位操作我们专指 C 语言</p><p>对于一个位表示为 <span class="math inline">\([\ x_{\omega-1},\x_{\omega-2},\ \cdots,\ x_1,\ x_0\ ]\)</span>的数，如果我们对其<strong>左移</strong> <span class="math inline">\(k\)</span> 位，其结果为$[ x_{-k-1}, x_{-k-2}, , x_0, 0, , 0 ] $ 。就结果而言，最右端补 <span class="math inline">\(k\)</span> 个 0是显然的。但如果是右移情况会有那么一点不一样</p><p>右移分为两种：逻辑右移和算术右移。对于逻辑右移，左边会补 <span class="math inline">\(k\)</span> 个 0 ；对于算术右移，左端会补 <span class="math inline">\(k\)</span> 个最高有效位的值（最高位是 1 就全补 1，最高位是 0 就全补 0 ）</p><h2 id="整数表示">整数表示</h2><p>关于定义的数学描述，建议去看原书第 42 页，这里不过多赘述</p><h3 id="无符号数的编码">无符号数的编码</h3><p>假设一个整数数据类型有 <span class="math inline">\(\omega\)</span>位。我们可以将位向量写成 <span class="math inline">\(\vec{x}\)</span>，用来表示整个向量，或者将位向量写成 <span class="math inline">\([\x_{\omega-1},\ x_{\omega-2},\ \cdots,\ x_1,\ x_0\ ]\)</span>，来表示这个向量当中的每一位。这样，我们便获得了一个整数的二进制表示</p><p>在这个编码中，每个 <span class="math inline">\(x_i\)</span> 既可以是0 也可以是 1</p><p>基于此，我们便得到了一个无符号数的编码定义：</p><p>对于向量 <span class="math inline">\(\vec{x}\ =\ [\ x_{\omega-1},\x_{\omega-2},\ \cdots,\ x_1,\ x_0\ ]\)</span> 而言，有：</p><p><span class="math inline">\(B2U_\omega\ \doteq\ \sum_{i\ =\0}^{\omega\ -\ 1}x_i2^i\)</span></p><h3 id="补码数的编码">补码数的编码</h3><p>下面，我们给出补码的定义：</p><p>对于向量 <span class="math inline">\(\vec{x}\ =\ [\ x_{\omega-1},\x_{\omega-2},\ \cdots,\ x_1,\ x_0\ ]\)</span> 而言，有：</p><p><span class="math inline">\(B2T_\omega\ \doteq\-x_{\omega-1}2^{\omega-1}\ +\ \sum_{i\ =\ 0}^{\omega\ -\2}x_i2^i\)</span></p><p><strong>对于有符号数而言，一般都是采用补码编码</strong></p><p>关于补码具体的数值，可以去看原书 46 页。我们在此需要说明一点是：<span class="math inline">\(|TMin_\omega|\  =\ |TMax_\omega|\ +\ 1\)</span>，原因在于补码将数划分为负数和非负数，而 0需要算在非负数里面，因此就造成二者绝对值少 1 的情况</p><p>下面我们给出有符号数的其他两种表示方法</p><p><strong>反码</strong>（Ones' Complement）：<span class="math inline">\(B2O_\omega\ \doteq\ -(x_{\omega-1}2^{\omega-1}\ -\1)\ +\ \sum_{i\ =\ 0}^{\omega\ -\ 2}x_i2^i\)</span></p><p>所以说反码加 1 等于补码</p><p><strong>原码</strong>（Sign-Magnitude）：<span class="math inline">\(B2S_\omega\ \doteq\ (-1)^{x_{\omega-1}}\ \cdot\(\sum_{i\ =\ 0}^{\omega\ -\ 2}x_i2^i)\)</span></p><p>不难看出，原码是将最高位当作符号位，并不表示大小</p><p>原码有一个问题是会将 <span class="math inline">\([00 \cdots0]\)</span> 与 <span class="math inline">\([10 \cdots0]\)</span>都解释为 0</p><h3 id="拓展一个数字的位表示">拓展一个数字的位表示</h3><p>对于无符号数的转换，只需要简单地在开头添加 0 ，这也被称为零拓展</p><p>无符号数的零拓展：</p><p>定义宽度为 <span class="math inline">\(\omega\)</span> 的位向量 <span class="math inline">\(\vec u\ =\ [\ u_{\omega-1},\ u_{\omega-2},\\cdots,\ u_1,\ u_0\ ]\)</span> 和宽度为 <span class="math inline">\(\omega&#39;\)</span> 的位向量 <span class="math inline">\(u&#39;\ =\ [\ {\color{violet}0},\{\color{violet}0},\ \cdots,\ {\color{violet}0},\ u_{\omega-1},\u_{\omega-2},\ \cdots,\ u_1,\ u_0\ ]\)</span> ，其中 <span class="math inline">\(\omega&#39;\ &gt;\ \omega\)</span> ，有：<span class="math inline">\(B2U_{\omega}(\vec u)\ =\ B2U_{\omega&#39;}(\vecu&#39;)\)</span></p><p>补码数的符号拓展：</p><p>定义宽度为 <span class="math inline">\(\omega\)</span> 的位向量 <span class="math inline">\(\vec u\ =\ [\ u_{\omega-1},\ u_{\omega-2},\\cdots,\ u_1,\ u_0\ ]\)</span> 和宽度为 <span class="math inline">\(\omega&#39;\)</span> 的位向量 <span class="math inline">\(u&#39;\ =\ [\ {\color{violet}u_{\omega-1}},\{\color{violet}u_{\omega-1}},\ \cdots,\ {\color{violet}u_{\omega-1}},\u_{\omega-1},\ u_{\omega-2},\ \cdots,\ u_1,\ u_0\ ]\)</span> ，其中<span class="math inline">\(\omega&#39;\ &gt;\ \omega\)</span>，有：</p><p><span class="math inline">\(B2T_{\omega}(\vec u)\ =\B2T_{\omega&#39;}(\vec u&#39;)\)</span></p><p>这个的证明很妙，去看原书第 55 页</p><h3 id="截断数字">截断数字</h3><p>截断无符号数：</p><p>设 <span class="math inline">\(\vec x\ =\ [\ x_{\omega-1},\x_{\omega-2},\ \cdots,\ x_0\ ]\)</span> ，而 <span class="math inline">\(\vec x&#39;\)</span> 为将其截断<strong>为</strong><span class="math inline">\(k\)</span> 位后的结果，即 <span class="math inline">\(\vec x&#39;\ =\ [\ x_{k-1},\ x_{k-2},\ \cdots,\x_1,\ x_0\ ]\)</span> 。我们令 <span class="math inline">\(x\ =\B2U_{\omega}(\vec x)\)</span> ，<span class="math inline">\(x&#39;\ =\B2U_{k}(\vec x&#39;)\)</span> ，有：</p><p><span class="math inline">\(x&#39;\ =\ x\ mod\ 2^k\)</span></p><p>这个的证明也很秒，这里简单说一下</p><p>由于无符号值是通过加法一个一个加起来的，而我们模 <span class="math inline">\(k\)</span> 的话，位数小于 <span class="math inline">\(k\)</span> 的还是原来的数，位数大于等于 <span class="math inline">\(k\)</span> 的全部变成零</p><p>太妙了</p><p>截断补码数值：</p><p>设 <span class="math inline">\(\vec x\ =\ [\ x_{\omega-1},\x_{\omega-2},\ \cdots,\ x_0\ ]\)</span> ，而 <span class="math inline">\(\vec x&#39;\)</span> 为将其截断<strong>为</strong><span class="math inline">\(k\)</span> 位后的结果，即 <span class="math inline">\(\vec x&#39;\ =\ [\ x_{k-1},\ x_{k-2},\ \cdots,\x_1,\ x_0\ ]\)</span> 。我们令 <span class="math inline">\(x\ =\B2U_{\omega}(\vec x)\)</span> ，<span class="math inline">\(x&#39;\ =\B2T_{k}(\vec x&#39;)\)</span> ，有：</p><p><span class="math inline">\(x&#39;\ =\ U2T_k(x\ mod\2^k)\)</span></p><p>简单来说，对于补码的截断，我们<strong>先对其做无符号截断，再从补码的角度看待剩下的位向量</strong>即可（括号里面是无符号截断，外面的<span class="math inline">\(U2T\)</span> 是从补码的角度进行翻译）</p><h2 id="整数运算">整数运算</h2><p>需要说明的是，我们在这里只会给出整数运算的定义以及具体实现，关于其底层数学证明，我是智障，我看不懂</p><h3 id="无符号加法">无符号加法</h3><p>我们先给出其数学表达式：</p><p>我们定义符号 <span class="math inline">\(+^{u}_{\omega}\)</span>表示对两个 <span class="math inline">\(\omega\)</span>位的无符号数进行加法，则：</p><p>对满足 <span class="math inline">\(0\le\ x,\ y\&lt;2^{\omega}\)</span> 的 <span class="math inline">\(x\)</span> 和<span class="math inline">\(y\)</span> ，有：</p><p><span class="math inline">\(x\ +^{u}_{\omega}\ y\ =\ \begin{cases} x\+\ y,&amp;x\ +\ y\ &lt;\ 2^{\omega}\\ x\ +\ y\ -2^{\omega},&amp;\2^{\omega}\ \le\ x\ +\ y\ &lt;\ 2^{\omega+1} \end{cases}\)</span></p><p>对于其位运算，我们直接将二者相加，再对其进行<strong>截断</strong>即可</p><p>我们给出位运算表达式，其中 <span class="math inline">\(x\)</span> 与<span class="math inline">\(y\)</span>均为<strong>无符号数</strong>，有：</p><p><span class="math inline">\(x+^u_{\omega}y=(x+y)\ mod\2^{\omega}\)</span></p><h4 id="关于无符号数加法溢出的检测">关于无符号数加法溢出的检测</h4><p>设 <span class="math inline">\(0\le x,\ y\le UMax_{\omega}\)</span>，我们令 <span class="math inline">\(s=x+^{u}_{\omega}y\)</span>，当且仅当 <span class="math inline">\(s &lt; x\)</span> （或者等价条件<span class="math inline">\(s&lt;y\)</span>）时，加法溢出</p><p>这个证明很简单，原书 62 页</p><h3 id="无符号的非">无符号的非</h3><p>无符号的非的意思是，在保证为 <span class="math inline">\(\omega\)</span> 位的情况下，求一个数 <span class="math inline">\(x\)</span> ，使得 <span class="math inline">\(x+a=0\)</span> ，其中 <span class="math inline">\(a\)</span> 为已知数</p><p>对满足 <span class="math inline">\(0\le x&lt;2^{\omega}\)</span>的任意 <span class="math inline">\(x\)</span> ，其 <span class="math inline">\(\omega\)</span> 位的无符号逆元 <span class="math inline">\(-_{\omega}^{u}\ x\)</span> 由下式给出：</p>$-^{u}_{} x=<span class="math display">\[\begin{cases}x,&amp;x=0\\2^{\omega}-x,&amp;x&gt;0 \end{cases}\]</span><p>$</p><h3 id="补码加法">补码加法</h3><p>数学表达式为：</p><p>对满足 <span class="math inline">\(-2^{\omega-1} \le x,\ y\le2^{\omega-1}-1\)</span> 的整数 <span class="math inline">\(x\)</span> 和<span class="math inline">\(y\)</span> ，有：</p><p><span class="math inline">\(x+^{t}_{\omega}y=\begin{cases}x+y-2^{\omega},&amp;2^{\omega-1}\lex+y&amp;\text{正溢出}\\x+y,&amp;-2^{\omega-1}\lex+y&lt;2^{\omega-1}&amp;\text{正常}\\x+y+2^{\omega},&amp;x+y&lt;-2^{\omega-1}&amp;\text{负溢出}\end{cases}\)</span></p><p>对于其位运算的具体操作：我们先将二者做无符号加法并将其截断，最后将结果转换成补码</p><p>我们给出如下位运算表达式，其中 <span class="math inline">\(x\)</span>与 <span class="math inline">\(y\)</span>均为<strong>补码数</strong>，有</p><p><span class="math inline">\(x+^t_{\omega}y=U2T_{\omega}(T2U(x)_{\omega}+^u_{\omega}T2U_{\omega}(y))\)</span></p><h4 id="补码加法溢出的检测">补码加法溢出的检测</h4><p>设 <span class="math inline">\(TMin_{\omega}\le x,\ y\leTMax_{\omega}\)</span> ，令 <span class="math inline">\(s=x+^{t}_{\omega}y\)</span></p><p>注意到，当发生正溢出时，<span class="math inline">\(s&lt;0\)</span>；当发生负溢出时，<span class="math inline">\(s&gt;0\)</span>，因此：</p><p>当 <span class="math inline">\(x&gt;0,\ y&gt;0,\ s\le 0\)</span> 或者<span class="math inline">\(x&lt;0,\ y&lt;0,\ s\ge 0\)</span>时，补码加法发生溢出</p><p>C++代码为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">tadd</span><span class="hljs-params">(<span class="hljs-type">int</span> x, <span class="hljs-type">int</span> y)</span><span class="hljs-comment">//overflow return 0 , else return 1</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> sum = x + y;<br>    <span class="hljs-keyword">return</span> !(x &gt; <span class="hljs-number">0</span> &amp;&amp; y &gt; <span class="hljs-number">0</span> &amp;&amp; sum &lt;= <span class="hljs-number">0</span>) &amp;&amp; !(x &lt; <span class="hljs-number">0</span> &amp;&amp; y &lt; <span class="hljs-number">0</span> &amp;&amp; sum &gt;= <span class="hljs-number">0</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p>这里补充一点：</p><p>对于等式：<span class="math inline">\((x+y)-(x+y)=0\)</span>而言，<strong>不管加法是否溢出，该式子都成立</strong></p><p>我们将其移位，得到：<span class="math inline">\((x+y)-x=y\)</span>无论<strong>加法是否溢出，这都成立</strong></p><p>所有，如果我们需要检测补码加法是否溢出时，<strong>不能</strong>这么写：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">tadd</span><span class="hljs-params">(<span class="hljs-type">int</span> x, <span class="hljs-type">int</span> y)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> sum = x + y;<br>    <span class="hljs-keyword">return</span> (sum - x == y) &amp;&amp; (sum - y == x);<br>&#125;<br></code></pre></td></tr></table></figure><p>这写的，我能笑一天的那种</p><h3 id="补码的非">补码的非</h3><p>原理同无符号的非，求一个数 <span class="math inline">\(x\)</span>在补码的条件下与已知数 <span class="math inline">\(a\)</span>相加得零的值，即：<span class="math inline">\(x+a=0\)</span></p><p>对满足 <span class="math inline">\(TMin_{\omega} \le x \leTMax_{\omega}\)</span> 的 <span class="math inline">\(x\)</span>，其补码 <span class="math inline">\(-^{t}_{\omega}x\)</span>由下式给出：</p><p><span class="math inline">\(-^{t}_{\omega}x=\begin{cases}TMin_{\omega},&amp;x=TMin_{\omega}\\-x,&amp;x&gt;TMin_{\omega}\end{cases}\)</span></p><p>无论是无符号的非，还是补码的非，实际上都是为了将减法运算转换为加法</p><p>就比如，考虑两个补码位向量 <span class="math inline">\(\vecu\)</span> 和 <span class="math inline">\(\vec v\)</span>，当我们试图计算 <span class="math inline">\(B2T(\vecu-^{t}_{\omega}\vec v)\)</span> 时（这里我们以补码视角看待最终结果）</p><p>由于我们定义了补码的非，上式实际上等价为：</p><p><span class="math inline">\(B2T_{\omega}(\vec u-^{t}_{\omega}\vecv)=B2T_{\omega}(\vec u+^{t}_{\omega}(-^{t}_{\omega}x))\)</span></p><p>由于 <span class="math inline">\(\vec u\)</span> 和 <span class="math inline">\(-^t_{\omega}x\)</span>均可以<strong>单独算出一个值</strong>，因此这便成了两个补码数的加法，按照加法原则相加即可</p><h4 id="补码非位级的快速计算">补码非位级的快速计算</h4><p>考虑一个问题：假设我已知一个补码的位级表示，如何快速求出其非的位级表示</p><p>一个显而易见的结果是：先对其每一位取反，再将结果加 1 ，即：</p><p><span class="math inline">\(-x=(\sim x)+1\)</span></p><p>这是最朴素的做法，其实还有一种更加方便的</p><p>考虑一个位向量 <span class="math inline">\(\vec x\ =\ [\x_{\omega-1},\ x_{\omega-2},\ \cdots,\ x_0\ ]\)</span>。假设从右边数第一个为 1 的数是第 <span class="math inline">\(k\)</span>位，即：<span class="math inline">\(\vec x\ =\ [\ x_{\omega-1},\x_{\omega-2},\ \cdots,\ x_0\ ]\ =\ [\ x_{\omega-1},\ x_{\omega-2},\\cdots,\ 1,\ 0,\ 0,\ \cdots,\ 0]\)</span></p><p>那么，我们只需要从第 <span class="math inline">\(k+1\)</span>位起将左边的位全部取反</p><p>例如：<span class="math inline">\([1101]=-3,\ [0011]=3\)</span></p><h3 id="无符号乘法">无符号乘法</h3><p>无符号乘法可以表述为：将两个数先相乘再对其结果做 <span class="math inline">\(\omega\)</span> 位数截断，数学定义如下：</p><p>设 <span class="math inline">\(0\le x,\ y\le UMax_{\omega}\)</span>，有：</p><p><span class="math inline">\(x*^{u}_{\omega}y=(x*y)\ mod\2^{\omega}\)</span></p><h3 id="补码乘法">补码乘法</h3><p>补码乘法相当于先将两个数做无符号乘法，再从补码的角度去看待结果，其数学定义如下：</p><p>设 <span class="math inline">\(TMin_{\omega}\le x,\ y\leTMax_{\omega}\)</span> ，有：</p><p><span class="math inline">\(x*^{t}_{\omega}y=U2T_{\omega}((x*y)\ mod\2^{\omega})\)</span></p><h3 id="乘以常数">乘以常数</h3><p>我们首先来讨论乘以 <span class="math inline">\(2^k\)</span>的情况</p><p>设无符号数 <span class="math inline">\(x\)</span> 和 无符号数 <span class="math inline">\(k\)</span> ，运算 <span class="math inline">\(x&lt;&lt;k\)</span> 的值等价于 <span class="math inline">\(x*^u_{\omega}2^k\)</span>（这里会自动做位数截断）</p><p>设补码数 <span class="math inline">\(x\)</span> 和无符号数 <span class="math inline">\(k\)</span> ，运算 <span class="math inline">\(x&lt;&lt;k\)</span> 的值等价于 <span class="math inline">\(x*^t_{\omega}2^k\)</span></p><p>也就是说，对于无符号数与补码数而言，乘以 <span class="math inline">\(2^k\)</span> ，相当于将其对应的位向量左移 <span class="math inline">\(k\)</span> 位</p><p>接着，我们来讨论乘以任意数 <span class="math inline">\(K\)</span>的情况</p><p>在给出具体分析之前，我们首先要知道一点：CPU计算加法、减法、移位的速度远快于直接计算乘法除法等运算，因此我们需要尽可能将乘以任意数<span class="math inline">\(K\)</span> 转化成加法、减法、移位的运算</p><p>对于任意数 <span class="math inline">\(K\)</span>，我们可以很轻易地给出其位向量 <span class="math inline">\(\veck=[k_{\omega-1},\ k_{\omega-2},\cdots,\ k_1,\ k_0]\)</span>，也就是说，<span class="math inline">\(K\)</span> 可以表示为</p><p><span class="math inline">\(K=\sum^{\omega-1}_{i=0}k_i2^k\)</span>，那么我们便可以通过移位来描述这个过程：</p><p><span class="math inline">\(x*K=x*\sum^{\omega-1}_{i=0}k_i2^k=\sum^{\omega-1}_{i=0}(x&lt;&lt;k_i)\)</span></p><p>举个例子：</p><p><span class="math inline">\(K=[1010]\)</span> ，那么 <span class="math inline">\(x*K=(x&lt;&lt;3)+(x&lt;&lt;1)\)</span></p><p><span class="math inline">\(K=[1011]\)</span> ，那么 <span class="math inline">\(x*K=(x&lt;&lt;3)+(x&lt;&lt;1)+(x&lt;&lt;0)=(x&lt;&lt;3)+(x&lt;&lt;1)+x\)</span></p><p>我们用更一般化的语言来描述这个过程，考虑一组从位位置 <span class="math inline">\(n\)</span> 到位位置 <span class="math inline">\(m\)</span> 的一组<strong>连续</strong>的 1 （<span class="math inline">\(14=(0\cdots 0)(111)(0)\)</span>，在这里，<span class="math inline">\(n=1,\ m=1\)</span>）</p><p>那么，<span class="math inline">\(x*K\)</span>可以有下面两种表述：</p><p><span class="math inline">\(x*K=(x&lt;&lt;n)+(x&lt;&lt;(n-1))+\cdots+(x&lt;&lt;m)\)</span></p><p><span class="math inline">\(x*K=(x&lt;&lt;(n+1))-(x&lt;&lt;m)\)</span></p><p>到此为止，我们给出了乘以任意数的数学表述</p><h3 id="除以-2-的幂">除以 2 的幂</h3><p>我们定义 <span class="math inline">\(\lfloor x \rfloor\)</span> 表示<span class="math inline">\(x-1\le \lfloor x \rfloor&lt;x\)</span>，<span class="math inline">\(\lceil x \rceil\)</span> 表示 <span class="math inline">\(x-1&lt;\lceil x \rceil \le x\)</span>，我们有如下结论：</p><p>对于正数，除法为向下舍入，即 <span class="math inline">\(x&gt;0,\y&gt;0\)</span> ，有：<span class="math inline">\(x/y=\lfloorx/y\rfloor\)</span></p><p>对于负数，除法为向上舍入，即 <span class="math inline">\(x&gt;0,\y&lt;0\)</span> ，有：<span class="math inline">\(x/y=\lceilx/y\rceil\)</span></p><p>这里的证明，去看原书 72 页</p><p>设无符号数值 <span class="math inline">\(x\)</span> 和无符号数值<span class="math inline">\(k\)</span> ，运算 <span class="math inline">\(x&gt;&gt;k\)</span> 的值等价于 <span class="math inline">\(\lfloor x/2^k \rfloor\)</span></p><p>对于补码数值 <span class="math inline">\(x\)</span> 和无符号数值<span class="math inline">\(k\)</span> ，运算 <span class="math inline">\((x+(1&lt;&lt;k)-1)&gt;&gt;k\)</span> 的值等价于<span class="math inline">\(\lceil x/2^k \rceil\)</span></p><p>这里的证明，去看原书 73 页</p><p>例如，假设 <span class="math inline">\(k=4\)</span> ，那么 <span class="math inline">\(x/2^4=(x+(1&lt;&lt;4)-1)&gt;&gt;4=(x+15)&gt;&gt;4\)</span></p><p>除以 2的幂，我们可以用移位来解决，但除以任意数的话，并不能通过这种方式来实现</p>]]></content>
    
    
    <categories>
      
      <category>CSAPP</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CASPP</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 64.最小路径和</title>
    <link href="/2022/09/03/Algorithm%20Solution/LC-64.%E6%9C%80%E5%B0%8F%E8%B7%AF%E5%BE%84%E5%92%8C/"/>
    <url>/2022/09/03/Algorithm%20Solution/LC-64.%E6%9C%80%E5%B0%8F%E8%B7%AF%E5%BE%84%E5%92%8C/</url>
    
    <content type="html"><![CDATA[<h4 id="最小路径和"><a href="https://leetcode.cn/problems/minimum-path-sum/">64.最小路径和</a></h4><p>给定一个包含非负整数的 <code>*m* x *n*</code> 网格 <code>grid</code>，请找出一条从左上角到右下角的路径，使得路径上的数字总和为最小。</p><p><strong>说明：</strong>每次只能向下或者向右移动一步。</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs lua">输入：grid = <span class="hljs-string">[[1,3,1],[1,5,1],[4,2,1]]</span><br>输出：<span class="hljs-number">7</span><br>解释：因为路径 <span class="hljs-number">1</span>→<span class="hljs-number">3</span>→<span class="hljs-number">1</span>→<span class="hljs-number">1</span>→<span class="hljs-number">1</span> 的总和最小。<br></code></pre></td></tr></table></figure><h4 id="dp">DP</h4><p>这题目跟前面的 <a href="https://leetcode.cn/problems/unique-paths-ii/">63. 不同路径 II</a>基本不差吧，思路是一样的</p><p>我们定义 <span class="math inline">\(dp[i][j]\)</span>表示机器人到达点 <span class="math inline">\((i,j)\)</span>所需的<strong>最小</strong>路径和</p><p>机器人站在某一点处不外乎有三种选择：</p><ul><li>该位置只能向下走，则 <span class="math inline">\(dp[i][j] = dp[i -1][j] + grid[i][j]\)</span> ，我们求的是路径和，一定要加上 <span class="math inline">\(grid\)</span> 的值</li><li>该位置只能向右走，则 <span class="math inline">\(dp[i][j] = dp[i][j- 1] + grid[i][j]\)</span></li><li>该位置即能向下也能向右，则 <span class="math inline">\(dp[i][j] =min(dp[i - 1][j], dp[i][j - 1]) + grid[i][j]\)</span></li></ul><p>这三点明确之后，代码就很简单了</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">minPathSum</span><span class="hljs-params">(vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&amp; grid)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> m = grid.<span class="hljs-built_in">size</span>(), n = grid[<span class="hljs-number">0</span>].<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(m, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(n, <span class="hljs-number">0</span>));<br>dp[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>] = grid[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>];<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; m; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; n; j++)<br>&#123;<br><span class="hljs-keyword">if</span> (i &gt; <span class="hljs-number">0</span> &amp;&amp; j &gt; <span class="hljs-number">0</span>)<br>dp[i][j] = <span class="hljs-built_in">min</span>(dp[i - <span class="hljs-number">1</span>][j], dp[i][j - <span class="hljs-number">1</span>]) + grid[i][j];<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (i &gt; <span class="hljs-number">0</span>)<br>dp[i][j] = dp[i - <span class="hljs-number">1</span>][j] + grid[i][j];<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (j &gt; <span class="hljs-number">0</span>)<br>dp[i][j] = dp[i][j - <span class="hljs-number">1</span>] + grid[i][j];<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[m - <span class="hljs-number">1</span>][n - <span class="hljs-number">1</span>];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(m * n)\)</span></p><p>空间复杂度：<span class="math inline">\(O(m * n)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>路径问题</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>RTTI</title>
    <link href="/2022/09/03/C++%20Primer%20Plus/RTTI/"/>
    <url>/2022/09/03/C++%20Primer%20Plus/RTTI/</url>
    
    <content type="html"><![CDATA[<h3 id="概述">概述</h3><p>RTTI 是运行阶段类型识别（Runtime TypeIdentification）的简称，其目的是为程序<strong>在运行阶段确定对象的类型</strong>提供一种标准</p><p>也就是说，RTTI的用途就是为了<strong>在程序运行期间确定对象的类型</strong></p><p><strong>RTTI只能用于包含虚函数的类层次结构</strong>，原因在于只有一个继承体系当中含义虚函数，将派生类对象赋值给基类指针这件事才有意义（话说回来，没有虚函数直接把派生类对象赋值给基类指针真的好蠢啊，怎么会有人写出这种代码的）</p><h3 id="rtti-工作原理">RTTI 工作原理</h3><p>C++ 有 3 个支持 RTTI 的元素</p><ul><li><code>dynamic_cast</code>运算符，用于确定两个指针之间的转型是否正确。正确的话返回指针地址，否则返回空指针</li><li><code>typeid</code> 运算符返回一个指出对象的类型的值</li><li><code>type_info</code> 结构存储了有关特定类型的信息</li></ul><h4 id="dynamic_cast-运算符">dynamic_cast 运算符</h4><p>考虑下面这样一个类层次结构</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">base</span> &#123;&#125;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">derived</span> : <span class="hljs-keyword">public</span> base &#123;&#125;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">last</span> : <span class="hljs-keyword">public</span> derived &#123;&#125;<br></code></pre></td></tr></table></figure><p>然后，我们创建三个指针</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp">base* pb = <span class="hljs-keyword">new</span> base;<br>base* pd = <span class="hljs-keyword">new</span> derived;<br>base* pl = <span class="hljs-keyword">new</span> last;<br></code></pre></td></tr></table></figure><p>基类指针指向派生类对象，这很合理</p><p>接着，我们来看转型</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp">last* p1 = (last*)pl;    <span class="hljs-comment">//1</span><br>last* p2 = (last*)pb;    <span class="hljs-comment">//2</span><br>derived* p3 = (last*)p1; <span class="hljs-comment">//3</span><br></code></pre></td></tr></table></figure><p>在讨论之前，我们需要明确一点：与讨论「指针指向的对象的类型」相比，讨论「指针转型是否安全」更有意义。</p><p>需要额外说明的是，只有那些<strong>指针类型与对象类型（或者对象的直接或间接基类的类型）相同</strong>的类型转换才是安全的</p><p>说人话就是，指针本身的类型要么与其指向对象的类型一致，要么其对象的类型是指针本身类型的派生类。除这两种情况以外的所有，转型都不安全</p><p>对于上面这个问题我们一个一个看</p><p>对于第一个，先将 <code>pl</code> 指针的类型从 <code>base*</code> 转成<code>last*</code> ，然后再对 <code>p1</code> 进行赋值，而<code>p1</code> 所指的对象就是原先 <code>pl</code> 所指的对象，即<code>last</code> 类型，这与 <code>p1</code>的类型相一致，这么做是安全的</p><p>对于第二个，同样是先将指针的类型进行转换，最后是 <code>last*</code>类型的 <code>p2</code> 指针指向 <code>derived</code>对象，出现了派生类指针指向基类对象，这么做是错误的</p><p>对于第三个，是先将 <code>pl</code> 指针转成 <code>last*</code>类型，然后再对 <code>derived*</code>类型的指针进行赋值。这么做并没有问题，因为派生类指针给基类指针赋值是正确的。最终的结果是<code>derived*</code> 类型的指针指向一个 <code>last</code>对象，这么做也是安全的</p><p>确认一个转型是否安全可以用 <code>dynamic_cast</code>关键字，不过这东西速度很慢，最好不要用</p><p>这个关键字的语法是：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-built_in">dynamic_cast</span>&lt;Type*&gt;(pr);<span class="hljs-comment">//这里的 pr 是指针</span><br></code></pre></td></tr></table></figure><p>如果对象 <code>*pr</code> 的类型是 <code>Type</code> 或者从<code>Type</code> 直接或间接派生过来，那么上面的语句将会把<code>pr</code> 转成 <code>Type*</code> 类型，否则将会返回<code>nullptr</code></p><p>额外说明一点，这个语句是将指针的类型转换成某个特定的类型，往后的下一步往往是对指针进行赋值，所以应该这么写：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Type* pt = <span class="hljs-built_in">dynamic_cast</span>&lt;Type*&gt;(pr);<br></code></pre></td></tr></table></figure><p>除了指针，这个关键字还可以对引用使用，不过在引用里面没有类似于空指针的引用值，所以在转型失败的时候会抛出一个<code>bad_cast</code> 异常。这个异常由 <code>expression</code>类派生而来，它在头文件 <code>typeinfo</code> 当中定义</p><p>具体用法如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">try</span><br>&#123;<br>Type&amp; pt = <span class="hljs-built_in">dynamic_cast</span>&lt;Type&amp;&gt;(pr);<br>&#125;<br><span class="hljs-built_in">catch</span> (bad_cast&amp;)<br>&#123;<br>...<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="typeid-运算符和-type_info-类">typeid 运算符和 type_info 类</h4><p><code>typeid</code>运算符可以确定两个<strong>对象</strong>（不是指针）是否为同一种类型，它接收两种类型的参数：</p><ul><li>类名</li><li>运算结果为对象的表达式</li></ul><p><code>typeid</code> 运算符会返回一个对 <code>type_info</code>对象的引用，这个对象重载了 <code>==</code> 和 <code>!=</code>，使得可以直接作比较，即</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(<span class="hljs-built_in">typeid</span>(Type) == <span class="hljs-built_in">typeid</span>(*pt))<br>&#123;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>如果 <code>pt</code> 是一个空指针，那么会引发 <code>bad_typeid</code>异常，用 <code>catch</code> 捕获就行</p><p><code>typeid</code> 当中还有一个方法 <code>name</code>，可以用字符串来显示类型</p><p>RTTI 确实会有一些效率方面的问题，但如果用同类型的多个<code>if else</code> 和 <code>typeid</code>来进行替代的话，效率会更低，所以还是建议使用<code>dynamic_cast</code></p>]]></content>
    
    
    <categories>
      
      <category>C++ Primer Plus</category>
      
    </categories>
    
    
    <tags>
      
      <tag>C++ Primer Plus</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>CSAPP 第一章 计算机系统漫游（本书碎碎念）</title>
    <link href="/2022/09/02/CSAPP/CSAPP%20%E7%AC%AC%E4%B8%80%E7%AB%A0%20%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E6%BC%AB%E6%B8%B8%EF%BC%88%E6%9C%AC%E4%B9%A6%E7%A2%8E%E7%A2%8E%E5%BF%B5%EF%BC%89/"/>
    <url>/2022/09/02/CSAPP/CSAPP%20%E7%AC%AC%E4%B8%80%E7%AB%A0%20%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E6%BC%AB%E6%B8%B8%EF%BC%88%E6%9C%AC%E4%B9%A6%E7%A2%8E%E7%A2%8E%E5%BF%B5%EF%BC%89/</url>
    
    <content type="html"><![CDATA[<p>源程序是由值 0 和 1 组成的位序列（也成为比特序列）。8 个位被组织成 1组，称之为字节。每个字节用于表示程序中的某些文本字符。</p><p>由 ASCII字符构成的文件成为文本文件，所有其他文件都称为二进制文件。</p><p>操作系统内核是应用程序和硬件之间的媒介。它提供三种基本的抽象：</p><ol type="1"><li>文件是对 I/O 设备的抽象</li><li>虚拟内存是对主存和磁盘的抽象</li><li>进程是处理器、主存、I/O 设备的抽象</li></ol><p>下面是碎碎念时间</p><p>今晚把第一章看完了，不多，也就 20 页</p><p>这里面全部都是概述性的东西，我也实在是记不了什么笔记，只想大概写一些我看完之后的感受</p><p>说实话，我看完之后对这本书期待挺高的</p><p>我之前一直在想，到底是用 <code>swich case</code> 的速度快还是用一堆<code>if else</code>的速度快；调用函数的系统开销到底有多大；<code>while</code> 和<code>for</code> 这两个循环到底有啥区别；除此之外还有很多</p><p>这些问题并不涉及到很深的部分，但以我目前所学的知识无法解答，然后我看到这本书会对这些问题进行解答</p><p>说实话，看到这个部分的时候我对这本书就有一点点的兴趣了</p><p>希望，希望不要给我划大饼</p><p>具体就这些吧，往后的慢慢再看，我争取两个月看完这本书。当然，笔记和每一章的总结不会少，这种书单单只做笔记不做总结的话，我感觉跟没看似的</p>]]></content>
    
    
    <categories>
      
      <category>CSAPP</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CSAPP</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 63.不同路径II</title>
    <link href="/2022/09/02/Algorithm%20Solution/LC-63.%E4%B8%8D%E5%90%8C%E8%B7%AF%E5%BE%84II/"/>
    <url>/2022/09/02/Algorithm%20Solution/LC-63.%E4%B8%8D%E5%90%8C%E8%B7%AF%E5%BE%84II/</url>
    
    <content type="html"><![CDATA[<h4 id="不同路径-ii"><a href="https://leetcode.cn/problems/unique-paths-ii/">63. 不同路径II</a></h4><p>一个机器人位于一个 <code>m x n</code> 网格的左上角（起始点在下图中标记为 “Start” ）。</p><p>机器人每次只能向下或者向右移动一步。机器人试图达到网格的右下角（在下图中标记为“Finish”）。</p><p>现在考虑网格中有障碍物。那么从左上角到右下角将会有多少条不同的路径？</p><p>网格中的障碍物和空位置分别用 <code>1</code> 和 <code>0</code>来表示。</p><figure class="highlight clean"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs clean">输入：obstacleGrid = [[<span class="hljs-number">0</span>,<span class="hljs-number">0</span>,<span class="hljs-number">0</span>],[<span class="hljs-number">0</span>,<span class="hljs-number">1</span>,<span class="hljs-number">0</span>],[<span class="hljs-number">0</span>,<span class="hljs-number">0</span>,<span class="hljs-number">0</span>]]<br>输出：<span class="hljs-number">2</span><br>解释：<span class="hljs-number">3</span>x3 网格的正中间有一个障碍物。<br>从左上角到右下角一共有 <span class="hljs-number">2</span> 条不同的路径：<br><span class="hljs-number">1.</span> 向右 -&gt; 向右 -&gt; 向下 -&gt; 向下<br><span class="hljs-number">2.</span> 向下 -&gt; 向下 -&gt; 向右 -&gt; 向右<br></code></pre></td></tr></table></figure><h4 id="dp">DP</h4><p>这个题，就是在 <a href="https://leetcode.cn/problems/unique-paths/">62. 不同路径</a>的基础上修改了一点点，基本思路一模一样</p><p>我们唯一需要注意的是，<code>obstacleGrid[i][j] == 1</code>的时候，<code>dp[i][j]</code> 必须为 0 ，也就是默认值</p><p>这一步并不需要额外设置，我们只需要在最前面加一个判断<code>obstacleGrid[i][j] == 0</code> 就行</p><p>具体代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">uniquePathsWithObstacles</span><span class="hljs-params">(vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;&amp; obstacleGrid)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> m = obstacleGrid.<span class="hljs-built_in">size</span>(), n = obstacleGrid[<span class="hljs-number">0</span>].<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(m, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(n, <span class="hljs-number">0</span>));<br>dp[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>] = obstacleGrid[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>] == <span class="hljs-number">1</span> ? <span class="hljs-number">0</span> : <span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; m; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; n; j++)<br>&#123;<br><span class="hljs-keyword">if</span> (obstacleGrid[i][j] == <span class="hljs-number">0</span>)<br>&#123;<br><span class="hljs-keyword">if</span> (i &gt; <span class="hljs-number">0</span> &amp;&amp; j &gt; <span class="hljs-number">0</span>)<br>dp[i][j] = dp[i - <span class="hljs-number">1</span>][j] + dp[i][j - <span class="hljs-number">1</span>];<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (i &gt; <span class="hljs-number">0</span>)<br>dp[i][j] = dp[i - <span class="hljs-number">1</span>][j];<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (j &gt; <span class="hljs-number">0</span>)<br>dp[i][j] = dp[i][j - <span class="hljs-number">1</span>];<br>&#125;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[m - <span class="hljs-number">1</span>][n - <span class="hljs-number">1</span>];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n^2)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>路径问题</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 687.最长同值路径</title>
    <link href="/2022/09/02/Algorithm%20Solution/LC-687.%E6%9C%80%E9%95%BF%E5%90%8C%E5%80%BC%E8%B7%AF%E5%BE%84/"/>
    <url>/2022/09/02/Algorithm%20Solution/LC-687.%E6%9C%80%E9%95%BF%E5%90%8C%E5%80%BC%E8%B7%AF%E5%BE%84/</url>
    
    <content type="html"><![CDATA[<h4 id="最长同值路径"><a href="https://leetcode.cn/problems/longest-univalue-path/">687.最长同值路径</a></h4><p>给定一个二叉树的 <code>root</code> ，返回 <em>最长的路径的长度</em>，这个路径中的 <em>每个节点具有相同值</em> 。这条路径可以经过也可以不经过根节点。</p><p><strong>两个节点之间的路径长度</strong> 由它们之间的边数表示。</p><figure class="highlight accesslog"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs accesslog">输入：root = <span class="hljs-string">[5,4,5,1,1,5]</span><br>输出：<span class="hljs-number">2</span><br></code></pre></td></tr></table></figure><h4 id="dfs">DFS</h4><p>对于二叉树中任意一个节点 <code>root</code>，我们想要找到包含它的最长同值路径，我们就必须要知道「<code>root-&gt;left</code>与 <code>root-&gt;right</code> 其本身为<strong>起点</strong>」的路径长度<code>ans</code> ，并将这两个 <code>ans</code> 相加</p><p>进一步，我们需要知道这两个 <code>ans</code>的<strong>较大值</strong>才能确定<strong>以该节点为起点</strong>的路径长度</p><p>根据这一点，我们可以很容易确定这里需要用<strong>后序遍历</strong></p><p>然后我们再维护一个变量 <code>cur</code> ，用来记录包含此节点<code>root</code> 的最长路径的值</p><p>如此这般，取最大的 <code>cur</code> 作为最后输出即可</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-type">int</span> curmax = <span class="hljs-number">0</span>;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">traveral</span><span class="hljs-params">(TreeNode* root)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (root == <span class="hljs-literal">nullptr</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br><span class="hljs-type">int</span> left = <span class="hljs-built_in">traveral</span>(root-&gt;left);<br><span class="hljs-type">int</span> right = <span class="hljs-built_in">traveral</span>(root-&gt;right);<br><span class="hljs-type">int</span> ans = <span class="hljs-number">0</span>, cur = <span class="hljs-number">0</span>;     <span class="hljs-comment">//ans 表示其左右孩子以它们本身为起点的最长路径</span><br>                          <span class="hljs-comment">//cur 表示包含当前节点的最长路径</span><br>          <span class="hljs-comment">//ans 与 cur 必须初始化为 0 ，因为我们是对这个节点进行讨论</span><br><span class="hljs-keyword">if</span> (root-&gt;left != <span class="hljs-literal">nullptr</span> &amp;&amp; root-&gt;left-&gt;val == root-&gt;val)<br>&#123;<br>ans = left + <span class="hljs-number">1</span>;<span class="hljs-comment">//因为相同的缘故，所以要加 1</span><br>cur += ans;<br>&#125;<br><span class="hljs-keyword">if</span> (root-&gt;right != <span class="hljs-literal">nullptr</span> &amp;&amp; root-&gt;right-&gt;val == root-&gt;val)<br>&#123;<br>ans = <span class="hljs-built_in">max</span>(ans, right + <span class="hljs-number">1</span>);<span class="hljs-comment">//对 ans 取较大值，后面将其返回给上一层</span><br>cur += right + <span class="hljs-number">1</span>;<br>&#125;<br>curmax = curmax &gt; cur ? curmax : cur;<span class="hljs-comment">//取 cur 最大值</span><br><span class="hljs-keyword">return</span> ans;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">longestUnivaluePath</span><span class="hljs-params">(TreeNode* root)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-built_in">traveral</span>(root);<br><span class="hljs-keyword">return</span> curmax;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n)\)</span>，仅仅是遍历这棵二叉树</p><p>空间复杂度：<span class="math inline">\(O(1)\)</span>，申请了常数量级的空间</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>二叉树</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 62.不同路径</title>
    <link href="/2022/09/01/Algorithm%20Solution/LC-62.%E4%B8%8D%E5%90%8C%E8%B7%AF%E5%BE%84/"/>
    <url>/2022/09/01/Algorithm%20Solution/LC-62.%E4%B8%8D%E5%90%8C%E8%B7%AF%E5%BE%84/</url>
    
    <content type="html"><![CDATA[<h4 id="不同路径"><a href="https://leetcode.cn/problems/unique-paths/">62. 不同路径</a></h4><p>一个机器人位于一个 <code>m x n</code> 网格的左上角（起始点在下图中标记为 “Start” ）。</p><p>机器人每次只能向下或者向右移动一步。机器人试图达到网格的右下角（在下图中标记为“Finish” ）。</p><p>问总共有多少条不同的路径？</p><figure class="highlight clean"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs clean">输入：m = <span class="hljs-number">3</span>, n = <span class="hljs-number">2</span><br>输出：<span class="hljs-number">3</span><br>解释：<br>从左上角开始，总共有 <span class="hljs-number">3</span> 条路径可以到达右下角。<br><span class="hljs-number">1.</span> 向右 -&gt; 向下 -&gt; 向下<br><span class="hljs-number">2.</span> 向下 -&gt; 向下 -&gt; 向右<br><span class="hljs-number">3.</span> 向下 -&gt; 向右 -&gt; 向下<br></code></pre></td></tr></table></figure><h4 id="dp">DP</h4><p>我们定义 <span class="math inline">\(f[i][j]\)</span> 表示到达点<span class="math inline">\((i, j)\)</span> 的所有路径数。显然有，<span class="math inline">\(f[0][0] = 1\)</span> ，并且最终的答案为 <span class="math inline">\(f[m - 1][n - 1]\)</span></p><p>这个机器人每次只能「向下」、「向右」移动一次。显然，我们可以得出机器人在某个状态的所有移动可能</p><ol type="1"><li>当前位置<strong>只能</strong>「向下」移动，那么 <span class="math inline">\(f[i][j] = f[i - 1][j]\)</span> ，只要<code>i &gt; 0</code> 就可以满足这种情况</li><li>当前位置<strong>只能</strong>「向右」移动，那么 <span class="math inline">\(f[i][j] = f[i][j - 1]\)</span> ，只要<code>j &gt; 0</code> 就可以满足这种情况</li><li>当前位置<strong>既可以</strong>「向下」<strong>也可以</strong>「向右」移动，那么<span class="math inline">\(f[i][j] = f[i - 1][j] + f[i][j - 1]\)</span>，这需要 <code>i &gt; 0, j &gt; 0</code></li></ol><p>因此总代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">uniquePaths</span><span class="hljs-params">(<span class="hljs-type">int</span> m, <span class="hljs-type">int</span> n)</span></span><br><span class="hljs-function"></span>&#123;<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(m, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(n, <span class="hljs-number">0</span>));<br>dp[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>] = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; m; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; n; j++)<br>&#123;<br><span class="hljs-keyword">if</span> (i &gt; <span class="hljs-number">0</span> &amp;&amp; j &gt; <span class="hljs-number">0</span>)<br>dp[i][j] = dp[i - <span class="hljs-number">1</span>][j] + dp[i][j - <span class="hljs-number">1</span>];<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (i &gt; <span class="hljs-number">0</span>)<br>dp[i][j] = dp[i - <span class="hljs-number">1</span>][j];<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (j &gt; <span class="hljs-number">0</span>)<br>dp[i][j] = dp[i][j - <span class="hljs-number">1</span>];<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[m - <span class="hljs-number">1</span>][n - <span class="hljs-number">1</span>];<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(m * n)\)</span></p><p>空间复杂度：<span class="math inline">\(O(m * n)\)</span></p><h5 id="总结">总结：</h5><p>由于 <code>dp</code> 是一个递推的过程，因此可以通过数据量来判断<code>dp</code> 数组的维数。一般来说，当数据量为 <span class="math inline">\(10^5 \sim 10^6\)</span> 时是一维 <code>dp</code>，当数据量为 <span class="math inline">\(10^2 \sim 10^4\)</span>时是二维 <code>dp</code></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>路径问题</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 1475.商品折扣后的最终价格</title>
    <link href="/2022/09/01/Algorithm%20Solution/LC-1475.%E5%95%86%E5%93%81%E6%8A%98%E6%89%A3%E5%90%8E%E7%9A%84%E6%9C%80%E7%BB%88%E4%BB%B7%E6%A0%BC/"/>
    <url>/2022/09/01/Algorithm%20Solution/LC-1475.%E5%95%86%E5%93%81%E6%8A%98%E6%89%A3%E5%90%8E%E7%9A%84%E6%9C%80%E7%BB%88%E4%BB%B7%E6%A0%BC/</url>
    
    <content type="html"><![CDATA[<h4 id="商品折扣后的最终价格"><a href="https://leetcode.cn/problems/final-prices-with-a-special-discount-in-a-shop/">1475.商品折扣后的最终价格</a></h4><p>给你一个数组 <code>prices</code> ，其中 <code>prices[i]</code>是商店里第 <code>i</code> 件商品的价格。</p><p>商店里正在进行促销活动，如果你要买第 <code>i</code>件商品，那么你可以得到与 <code>prices[j]</code> 相等的折扣，其中<code>j</code> 是满足 <code>j &gt; i</code> 且<code>prices[j] &lt;= prices[i]</code> 的 <strong>最小下标</strong>，如果没有满足条件的 <code>j</code> ，你将没有任何折扣。</p><p>请你返回一个数组，数组中第 <code>i</code> 个元素是折扣后你购买商品<code>i</code> 最终需要支付的价格。</p><figure class="highlight tap"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs tap">输入：prices = [8,4,6,2,3]<br>输出：[4,2,4,2,3]<br>解释：<br>商品<span class="hljs-number"> 0 </span>的价格为 price[0]=8 ，你将得到 prices[1]=4 的折扣，所以最终价格为<span class="hljs-number"> 8 </span>-<span class="hljs-number"> 4 </span>=<span class="hljs-number"> 4 </span>。<br>商品<span class="hljs-number"> 1 </span>的价格为 price[1]=4 ，你将得到 prices[3]=2 的折扣，所以最终价格为<span class="hljs-number"> 4 </span>-<span class="hljs-number"> 2 </span>=<span class="hljs-number"> 2 </span>。<br>商品<span class="hljs-number"> 2 </span>的价格为 price[2]=6 ，你将得到 prices[3]=2 的折扣，所以最终价格为<span class="hljs-number"> 6 </span>-<span class="hljs-number"> 2 </span>=<span class="hljs-number"> 4 </span>。<br>商品<span class="hljs-number"> 3 </span>和<span class="hljs-number"> 4 </span>都没有折扣。<br></code></pre></td></tr></table></figure><h4 id="模拟">模拟</h4><p>我们看到，<code>prices[i]</code> 的范围只有 <span class="math inline">\(10^3\)</span> ，因此可以直接模拟。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">finalPrices</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; prices)</span> </span><br><span class="hljs-function">    </span>&#123;<br>        vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">ret</span>(prices.<span class="hljs-built_in">size</span>(), <span class="hljs-number">0</span>);<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; prices.<span class="hljs-built_in">size</span>(); i++)<br>        &#123;<br>            ret[i] = prices[i];<br>            <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j = i + <span class="hljs-number">1</span>; j &lt; prices.<span class="hljs-built_in">size</span>(); j++)<br>            &#123;<br>                <span class="hljs-keyword">if</span>(prices[j] &lt;= prices[i])<br>                &#123;<br>                    ret[i] = prices[i] - prices[j];<br>                    <span class="hljs-keyword">break</span>;<br>                &#125;<br>            &#125;<br>        &#125;<br>        <span class="hljs-keyword">return</span> ret;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span>，最坏的情况需要将这个数组遍历两遍</p><p>空间复杂度：<span class="math inline">\(O(1)\)</span></p><h4 id="单调栈">单调栈</h4><p>我们先说明一下何为「单调栈」。</p><p>顾名思义，单调栈中的元素具有单调性的特征，或单调递增或单调递减。</p><p>在插入元素的时候，我们仍需要让这个栈保持上述特性，如此这般，我们便得到了一个单调栈。</p><p>关于单调栈的插入，我们有以下操作：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">while</span>(!stack.<span class="hljs-built_in">empty</span>() &amp;&amp; stack.<span class="hljs-built_in">top</span>() &lt; val)<br>    stack.<span class="hljs-built_in">pop</span>();<br>stack.<span class="hljs-built_in">push</span>(val);<br></code></pre></td></tr></table></figure><p>我们来分析一下这个题为什么可以用单调栈？</p><p>具体地，我们从前往后遍历 <code>nums</code>数组，将「待打折」商品的<strong>下标</strong>存放在某个容器当中，我们考察<code>nums[i]</code> 元素</p><ul><li>若 <code>nums[i]</code> 大于该容器中任意一个元素，说明我们不能用<code>nums[i]</code> 对容器内的商品进行打折，因此需要将<code>nums[i]</code> 添加到该容器中<ul><li>更进一步，我们每次都会将一个「大于该容器中任意一个元素」的元素添加到该容器中，因此我们很容易可以得出此容器中的元素具有<strong>单调递增</strong>的特性</li></ul></li><li>若 <code>nums[i]</code>小于该容器中的最后一个元素（上一次添加的元素），那么我们便可以将尾部商品从容器中取出（因为该商品可以得到折扣）。由于该动作需要<strong>重复多次</strong>，因此我们只能使用<code>while</code> 而不能用 <code>if</code>。并且该动作需要在<strong>添加元素之前执行</strong>。</li></ul><p>根据上述讨论，该容器的元素只能从尾部进，从尾部出，因此只能是栈。到此为止，我们便构造出了一个单调栈的理论框架，下面就是代码的部分了。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">finalPrices</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; prices)</span></span><br><span class="hljs-function"></span>&#123;<br>vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">ret</span>(prices.<span class="hljs-built_in">size</span>(), <span class="hljs-number">0</span>);<br>stack&lt;<span class="hljs-type">int</span>&gt;st;<span class="hljs-comment">//存储元素索引</span><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; prices.<span class="hljs-built_in">size</span>(); i++)<br>&#123;<br><span class="hljs-keyword">while</span> (!st.<span class="hljs-built_in">empty</span>() &amp;&amp; prices[st.<span class="hljs-built_in">top</span>()] &gt;= prices[i])<br>&#123;<br><span class="hljs-type">int</span> index = st.<span class="hljs-built_in">top</span>();<br>ret[index] = prices[index] - prices[i];<br>                st.<span class="hljs-built_in">pop</span>();<span class="hljs-comment">//记得把元素弹出来</span><br>&#125;<br>st.<span class="hljs-built_in">push</span>(i);<br>ret[i] = prices[i];<br>&#125;<br>        <span class="hljs-keyword">return</span> ret;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span> ，最坏需要<span class="math inline">\(n\)</span> 个大小的栈来存储</p><p>补充：为什么是存储数组元素索引而不是直接存储数组元素？</p><p>实际上，这个问题很容易就可以得出答案，如果我们存储的是数组元素，那么该元素在出栈时就无法确定到底是<code>nums</code> 中哪个元素得到折扣，如果存储索引就没有这个问题。</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>模拟</tag>
      
      <tag>单调栈</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>异常</title>
    <link href="/2022/08/31/C++%20Primer%20Plus/%E5%BC%82%E5%B8%B8/"/>
    <url>/2022/08/31/C++%20Primer%20Plus/%E5%BC%82%E5%B8%B8/</url>
    
    <content type="html"><![CDATA[<h2 id="异常">异常</h2><p>程序几乎不可避免地会出现异常，就比如你写了一个除法程序，保不齐会有人手贱非得去试一下分母为0 会发生什么事情。因此应当异常就显得尤为重要。</p><p>异常应当的方式有三种，前两种分别是：调用 <code>abort</code>强行退出、返回错误码。这两个，emmmm，说实话，没啥技术含量，我们跳过。</p><p>我们着重看通过<strong>异常机制</strong>来处理异常</p><h3 id="异常机制">异常机制</h3><p><strong>异常提供了将控制权从程序的一个部分传递到另一个部分的途径</strong>。C++对于异常的处理由 <code>try</code> 块和 <code>catch</code> 块组成。</p><ul><li><code>try</code>块用于表示下面这段代码可能会引发异常，通常在后面会跟一个<code>throw</code> 语句（也可以没有）。<ul><li>如果 <code>try</code>块当中引发了异常，则会直接<strong>跳转</strong>到后面的<code>catch</code>块。如果没有引发异常，则会直接<strong>跳过</strong>后面<strong>所有的</strong><code>catch</code> 块</li></ul></li><li><code>throw</code>语句是<strong>跳转的意思，命令程序跳转到另一条语句</strong>。<code>throw</code>后面的类型（<strong>字符串类型或者类类型</strong>）指出的是异常的特征。<ul><li>我们这里解释一下跳转的含义：执行 <code>throw</code>语句类似于执行返回语句，但并<strong>不是将控制流返回到调用函数</strong>，而是<strong>沿着函数的调用序列后退</strong>，直到找到包含<code>try</code> 块的函数。</li></ul></li><li><code>catch</code>块是对异常处理的程序，后面位于括号内的是<strong>类型声明</strong>，这个表示<strong>需要用哪一个<code>catch</code> 块来处理这个异常</strong>。即 <code>throw</code>抛出的值会有 <code>catch</code>后面对应的<strong>类型</strong>接住。如果 <code>catch</code>后面的类型声明不知道写什么，可以写 <code>catch(...)</code>这表示任何类型都由这个部分来处理。</li></ul><h3 id="将对象用作异常类型">将对象用作异常类型</h3><p>我们可以在 <code>throw</code>后面的语句内写上一个<strong>自定义类型</strong>，这么做的好处是显然的，我们可以很方便的知道该异常所代表的含义。</p><p>需要注意的是，如果<strong>是将对象作为异常类型</strong>，在<code>catch</code>块捕获的时候一定要写上<strong>引用</strong>。具体的例子看下面的代码：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">bad_cal</span><br>&#123;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> a, b;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">bad_cal</span>(<span class="hljs-type">int</span> e1 = <span class="hljs-number">0</span>, <span class="hljs-type">int</span> e2 = <span class="hljs-number">0</span>)<br>:<span class="hljs-built_in">a</span>(e1), <span class="hljs-built_in">b</span>(e2)<br>&#123;  &#125;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">mesg</span><span class="hljs-params">()</span> </span>&#123; cout &lt;&lt; <span class="hljs-string">&quot;error&quot;</span> &lt;&lt; endl; &#125;<br>&#125;;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">cal</span><span class="hljs-params">(<span class="hljs-type">int</span> n, <span class="hljs-type">int</span> m)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (n != m)<br><span class="hljs-keyword">return</span> n + m;<br><span class="hljs-keyword">throw</span> <span class="hljs-built_in">bad_cal</span>(n, m);<span class="hljs-comment">//引发异常时编译器会创建一个临时拷贝，就算下面指定的是引用</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n, m;<br><span class="hljs-keyword">while</span> (cin &gt;&gt; n &gt;&gt; m)<br>&#123;<br><span class="hljs-keyword">try</span><br>&#123;<br>cout &lt;&lt; <span class="hljs-built_in">cal</span>(n, m) &lt;&lt; endl;<br>&#125;<br><span class="hljs-built_in">catch</span> (bad_cal&amp; bc)<span class="hljs-comment">//这会指向一个编译器创建的临时副本，并不会指向原对象</span><br>&#123;<br>bc.<span class="hljs-built_in">mesg</span>();<br>&#125;<br>&#125;<br><br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>你肯定会说，既然是一个副本，那我用引用指向它有什么用呢，引用本身就是为了不希望创建副本。</p><p>实际上这里用引用的考量并不是在这里，引用还有一个重要用途是：基类指针可以指向派生类对象。所以，如果我们有一个由继承所组成的异常类体系，只需要一个基类引用就可以指向所有的派生类对象，这不是很方便吗？</p><p>我们来看这个例子：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">bad1</span> &#123;&#125;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">bad2</span> : <span class="hljs-keyword">public</span> bad1 &#123;&#125;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">bad3</span> : <span class="hljs-keyword">public</span> bad2 &#123;&#125;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">try</span><br>    &#123;<br>        <span class="hljs-keyword">if</span>()<br>            <span class="hljs-keyword">throw</span> bad1;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>()<br>            <span class="hljs-keyword">throw</span> bad2;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">throw</span> bad3;<br>    &#125;<br>    <span class="hljs-built_in">catch</span>(bad3) &#123;  &#125;<br>    <span class="hljs-built_in">catch</span>(bad2) &#123;  &#125;<br>    <span class="hljs-built_in">catch</span>(bad1) &#123;  &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>需要注意的是，<strong>引发异常的对象将被第一个与之匹配的<code>catch</code> 块捕获</strong>，因此后面 <code>catch</code>块中的顺序需要与继承的顺序相反。即捕获位于层次结构<strong>最下面</strong>的异常类的<code>catch</code> 块将放在最上面，捕获基类异常的 <code>catch</code>块将放在在下面。</p><h3 id="异常规范和-c-11">异常规范和 C++ 11</h3><p>有时候，一项理论看似很有前途，但在实际应用当中往往不是这样，到最后就慢慢地被摒弃了。这指的就是异常规范。异常规范是在C++98 被加入到标准当中，但在 C++11却被摒弃了。这意味着这项功能目前仍可能处在标准当中但未来可能会被废除，不建议使用。</p><p>我们直接举两个例子说明这个吧：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//函数声明如下</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">cal</span><span class="hljs-params">()</span> <span class="hljs-title">throw</span><span class="hljs-params">()</span></span>;       <span class="hljs-comment">//说明该函数不会抛出异常</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">cla</span><span class="hljs-params">()</span> <span class="hljs-title">throw</span><span class="hljs-params">(bad_cal)</span></span>;<span class="hljs-comment">//说明该函数可能会抛出 bad_cal 类型的异常</span><br></code></pre></td></tr></table></figure><p>这样看的话你会觉得这东西还有点用，但下面我要说这东西的定义是什么，看完之后。。。。我对这东西的评价是：早点废除吧。</p><p>异常规范的作用之一是，告诉用户需要使用 <code>try</code>块，也就是说这个函数可能引发异常，对应于第二个例子。但这个功能吧。。。。你直接用注释不好吗？</p><p>异常规范的另一个作用是，让编译器添加程序在运行阶段检查的代码，确定该函数是否违反了异常规范。但这个功能，你想啊，你写的这个函数它没有报错，但这个函数调用的另一个函数报错了，这样就<strong>很难检查</strong>了。</p><p>当然，C++11 支持另一种异常规范，可以<strong>用 <code>noexcept</code>关键字来指出函数不会引发异常</strong>（这东西是关键字），这就跟上面的第一点一样。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">cal</span><span class="hljs-params">()</span> <span class="hljs-keyword">noexcept</span></span>;<br></code></pre></td></tr></table></figure><h3 id="栈解退">栈解退</h3><p>假设 <code>try</code>块没有直接调用引发异常的函数，而是调用了对引发异常的函数进行调用的函数，则程序控制流就会从引发异常的函数开始回退，直到找到包含<code>try</code> 块和处理模块的函数为止，这一过程我们称之为栈解退。</p><p>当涉及到函数调用时，编译器会为被调用函数在栈中为其开辟一块地址，并将这块地址的起始地址保存在调用函数中。之后，程序控制流将会跳转到被调用函数，往后会对该函数进行执行。如此这般，便是函数嵌套调用的底层实现。</p><p>当被调用函数执行完毕后，会<strong>释放所有的自动变量</strong>，控制流将自动回到调用函数，继续执行。</p><p>假设被调用函数引发了异常，那么该函数在栈中的空间将会<strong>直接被释放</strong>，不会执行引发异常后面的代码，多重嵌套调用也是同理。编译器会将该异常一直回退，直到找到包含<code>try</code> 块和 <code>catch</code> 块为止。</p><p>上面的「直接被释放」的意思是，自动变量会直接释放，类对象会调用其析构函数。</p><h3 id="exception-类">exception 类</h3><p><code>exception</code> 头文件定义了 <code>exception</code>类，代码可以引发 <code>exception</code> 异常，也可以将<code>exception</code> 类用作基类。</p><p><code>exception</code> 类的底层实现是一个 <code>string</code>类型的对象，因此可以直接用 <code>string</code>对象来对其进行初始化。除此之外，<code>exception</code>类中还包含一个虚函数成员 <code>what</code>，它会返回一个字符串，其具体的值随实现而异。</p><p>C++ 定义了很多基于 <code>exception</code>的异常类型，我们这里简单说明一下，使用起来很简单。</p><h4 id="stdexcept-异常类">stdexcept 异常类</h4><p>头文件 <code>stdexcept</code>定义了两个异常类：<code>logic_error</code> 和 <code>runtime_error</code>，它们均是 <code>exception</code> 类派生而来。</p><p><code>logic_error</code> 派生了如下类：</p><ul><li>domain_error ，定义域（domain）错误，</li><li>invalid_error ，非法参数，给函数传递了一个意料之外的参数</li><li>length_error ，没有足够的空间来执行所需的操作</li><li>out_of_bounds ，索引错误，通常用在数组下标</li></ul><p><code>runtime_error</code> 派生了如下类：</p><ul><li>range_error ，值域（range）错误</li><li>overflow_error</li><li>underflow_error</li></ul><p>一般来说浮点数会有一个可以表示的最小非零值，当计算结果比这个值还小的时候会引发下溢（underflow）错误。</p><p>同理，浮点数有一个所能表示的最大量级，当计算结果大于这个数时，会发生上溢（overflow）错误。</p><p>当计算结果不在函数所能允许的范围内，但没有发生上溢和下溢错误，则可以用<code>range_error</code></p><h4 id="bad_alloc-和-new">bad_alloc 和 new</h4><p>对于 <code>new</code> 引发的申请空间所导致的异常，C++最新的处理方法是让 <code>new</code> 抛出一个 <code>bad_alloc</code>异常，如：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">try</span><br>&#123;<br>    <span class="hljs-keyword">auto</span> pt = <span class="hljs-keyword">new</span> somthing<br>&#125;<br><span class="hljs-built_in">catch</span>(bad_alloc &amp; r)<br>&#123;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>原先的处理方式是 <code>new</code>在申请失败时返回空指针。处于兼容性的需要，编译器提供了一个标记让用户自己选择所需要的行为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">auto</span> pt = <span class="hljs-built_in">new</span> (std::nothrow) somthing<br><span class="hljs-keyword">if</span>(pt == <span class="hljs-number">0</span>)<br>&#123;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>这样就不能用异常处理程序了。</p><h3 id="异常导致的问题">异常导致的问题</h3><p>异常被引发后，在两种情况下会导致问题。如果异常是由带有异常规范的函数所引发的，则它引发的异常必须与异常规范列表中的某个异常相匹配（在继承体系中，派生类异常可以与基类异常相匹配），否则称为异常意外。默认情况下会导致程序终止。</p><p>如果异常不是在函数中引发的，必须捕获它。如果没有捕获（没有与异常匹配的<code>catch</code>块时），则该异常称为未捕获异常。默认情况下，这将导致程序终止。</p><p>实际上，异常意外与未捕获异常所导致的行为是可以修改的，但我们这里对此不过多赘述，直接去C++ Primer Plus 的 517 页看就行。</p>]]></content>
    
    
    <categories>
      
      <category>C++ Primer Plus</category>
      
    </categories>
    
    
    <tags>
      
      <tag>C++ Primer Plus</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 416.分割等和子集</title>
    <link href="/2022/08/31/Algorithm%20Solution/LC-416.%E5%88%86%E5%89%B2%E7%AD%89%E5%92%8C%E5%AD%90%E9%9B%86/"/>
    <url>/2022/08/31/Algorithm%20Solution/LC-416.%E5%88%86%E5%89%B2%E7%AD%89%E5%92%8C%E5%AD%90%E9%9B%86/</url>
    
    <content type="html"><![CDATA[<h4 id="分割等和子集"><a href="https://leetcode.cn/problems/partition-equal-subset-sum/">416.分割等和子集</a></h4><p>给你一个 <strong>只包含正整数</strong> 的 <strong>非空</strong> 数组<code>nums</code>。请你判断是否可以将这个数组分割成两个子集，使得两个子集的元素和相等。</p><figure class="highlight inform7"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs inform7">输入：nums = <span class="hljs-comment">[1,5,11,5]</span><br>输出：true<br>解释：数组可以分割成 <span class="hljs-comment">[1, 5, 5]</span> 和 <span class="hljs-comment">[11]</span> 。<br></code></pre></td></tr></table></figure><h4 id="背包">0-1 背包</h4><p>这道题其实是可以转换成背包问题的</p><p>我们需要将这个数组分割成两个<strong>元素和相等的子集</strong>。也就是说，假如这个数组的所有元素和为<code>sum</code> ，我现在要做的就是<strong>确定能否用这个数组的元素凑出<code>sum / 2</code> 。</strong></p><p>也就是说，我从这个集合当中随便选择元素，是否能够满足其元素和为<code>sum / 2</code> 。</p><p>因此问题转换成了，给一个可以装 <code>n</code>个物品的背包，每个物品的重量为 <code>nums[i]</code> ，背包最大载重为<code>sum / 2</code> ，问能否将这个背包恰好装满。</p><p>然后，我们确定 <code>dp</code> 数组的含义：<code>dp[i][j]</code>表示在前 <code>i</code> 个物品当中任意挑选，能否恰好将重量为<code>j</code> 的背包装满。</p><p>我们再来看看 base case 。</p><p>显然，对于 <code>dp[i][0]</code> 而言，背包最大载重为 0，那么这个背包可以认为一直是满的。</p><p>对于 <code>dp[0][j]</code> 而言，由于可供挑选的物品是 0，因此认为不可能将该背包装满。</p><p>最后我们分析一下放与不放这两种情况要如何处理。在 0- 1背包的模型当中我们是取二者的最大值。这是很显然的，毕竟我是要让背包的价值达到最大。在这里，不管是放还是不放，只要能将背包装满，我们就认为这种情况下是<code>true</code> 。</p><p>全部代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">check</span><span class="hljs-params">(<span class="hljs-type">int</span> target, vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-comment">//给一个可以装target和n个物品的背包，每个物品重量为nums[i]，是否存在一种装法可以恰好把背包装满</span><br><span class="hljs-type">int</span> n = nums.<span class="hljs-built_in">size</span>();<br>vector&lt;vector&lt;<span class="hljs-type">bool</span>&gt;&gt;<span class="hljs-built_in">dp</span>(n + <span class="hljs-number">1</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">bool</span>&gt;(target + <span class="hljs-number">1</span>, <span class="hljs-literal">false</span>));<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt;= n; i++)<br>dp[i][<span class="hljs-number">0</span>] = <span class="hljs-literal">true</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= n; i++)<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">1</span>; j &lt;= target; j++)<br><span class="hljs-keyword">if</span> (j - nums[i - <span class="hljs-number">1</span>] &gt;= <span class="hljs-number">0</span>)<span class="hljs-comment">//背包容量足够</span><br>dp[i][j] = dp[i - <span class="hljs-number">1</span>][j] || dp[i - <span class="hljs-number">1</span>][j - nums[i - <span class="hljs-number">1</span>]];<br><span class="hljs-keyword">else</span>                     <span class="hljs-comment">//背包容量不够</span><br>dp[i][j] = dp[i - <span class="hljs-number">1</span>][j];<br><span class="hljs-keyword">return</span> dp[n][target];<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">canPartition</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> sum = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> n : nums)<br>sum += n;<br><span class="hljs-keyword">if</span> (sum % <span class="hljs-number">2</span> == <span class="hljs-number">1</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">check</span>(sum / <span class="hljs-number">2</span>, nums);<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：$ O(n * target) $ ，<span class="math inline">\(n\)</span> 为数组的元素个数，<span class="math inline">\(target\)</span> 为所有元素和的一半</p><p>空间复杂度：$ O(n * target) $ ，同上</p><h4 id="空间压缩">空间压缩</h4><p>由于这个就是完完全全的背包问题，我们前面有具体谈过如何去压缩</p><p>这里直接给出代码</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">check</span><span class="hljs-params">(<span class="hljs-type">int</span> target, vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-comment">//给一个可以装target和n个物品的背包，每个物品重量为nums[i]，是否存在一种装法可以恰好把背包装满</span><br><span class="hljs-type">int</span> n = nums.<span class="hljs-built_in">size</span>();<br>vector&lt;<span class="hljs-type">bool</span>&gt;<span class="hljs-built_in">dp</span>(target + <span class="hljs-number">1</span>, <span class="hljs-literal">false</span>);<br>dp[<span class="hljs-number">0</span>] = <span class="hljs-literal">true</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= n; i++)<br>&#123;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = target; j &gt;= nums[i - <span class="hljs-number">1</span>]; j--)<br>&#123;<br>dp[j] = dp[j] || dp[j - nums[i - <span class="hljs-number">1</span>]];<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> dp[target];<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">canPartition</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> sum = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> n : nums)<br>sum += n;<br><span class="hljs-keyword">if</span> (sum % <span class="hljs-number">2</span> == <span class="hljs-number">1</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">check</span>(sum / <span class="hljs-number">2</span>, nums);<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 899. 有序队列</title>
    <link href="/2022/08/03/Algorithm%20Solution/LC-899.-%E6%9C%89%E5%BA%8F%E9%98%9F%E5%88%97/"/>
    <url>/2022/08/03/Algorithm%20Solution/LC-899.-%E6%9C%89%E5%BA%8F%E9%98%9F%E5%88%97/</url>
    
    <content type="html"><![CDATA[<h4 id="有序队列"><a href="https://leetcode.cn/problems/orderly-queue/">899.有序队列</a></h4><p>给定一个字符串 <code>s</code> 和一个整数 <code>k</code> 。你可以从<code>s</code> 的前 <code>k</code>个字母中选择一个，并把它加到字符串的末尾。</p><p>返回 <em>在应用上述步骤的任意数量的移动后，字典上最小的字符串</em>。</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm">输入：s = <span class="hljs-string">&quot;cba&quot;</span>, k = <span class="hljs-number">1</span><br>输出：<span class="hljs-string">&quot;acb&quot;</span><br>解释：<br>在第一步中，我们将第一个字符（“c”）移动到最后，获得字符串 “<span class="hljs-keyword">bac”。</span><br><span class="hljs-keyword"></span>在第二步中，我们将第一个字符（“<span class="hljs-keyword">b”）移动到最后，获得最终结果 </span>“acb”。<br></code></pre></td></tr></table></figure><h4 id="暴力循环">暴力循环</h4><p>这个题乍一看觉得无从下手，实际上简单的要死。</p><p>我们分开来看，先看 <code>k = 1</code> 的情况</p><p>这时，我们每次都会把第一个字符放到字符串的最后一位，这会生成<code>n</code> 个字符串，我们只需要取这 <code>n</code>个字符串中最小的那个就行。就是最简单的打擂台。这个地方的时间复杂度为<em>O( <span class="math inline">\(n^2\)</span> )</em>，因为需要遍历一遍这个字符串的同时，还需要进行插入，因此是 <span class="math inline">\(n^2\)</span> 。</p><p>然后，当 <code>k = 2</code>时，<strong>我们可以从前面两个中选择一个放到最后一位</strong>，那么只需要我们每次都选择两个中较大的那个，把它放到最后去，是不是相当于把这个字符串整体进行一次排序？</p><p>当然 <code>k &gt; 2</code> 的情况同理，这时的时间复杂度为 <em>O(<span class="math inline">\(nlogn\)</span> )</em> 。</p><p>代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function">string <span class="hljs-title">orderlyQueue</span><span class="hljs-params">(string s, <span class="hljs-type">int</span> k)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (k == <span class="hljs-number">1</span>)<br>&#123;<br><span class="hljs-function">string <span class="hljs-title">ret</span><span class="hljs-params">(s)</span></span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; s.<span class="hljs-built_in">length</span>(); i++)<br>&#123;<br>s.<span class="hljs-built_in">insert</span>(s.<span class="hljs-built_in">end</span>(), s[<span class="hljs-number">0</span>]);<br>s.<span class="hljs-built_in">erase</span>(s.<span class="hljs-built_in">begin</span>());<br><span class="hljs-keyword">if</span> (s &lt; ret)<br>ret = s;<br>&#125;<br><span class="hljs-keyword">return</span> ret;<br>&#125;<br><span class="hljs-keyword">else</span><br>&#123;<br><span class="hljs-built_in">sort</span>(s.<span class="hljs-built_in">begin</span>(), s.<span class="hljs-built_in">end</span>());<br><span class="hljs-keyword">return</span> s;<br>&#125;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<em>O( <span class="math inline">\(n^2\)</span>)</em></p><p>空间复杂度：<em>O( <span class="math inline">\(n\)</span> )</em></p><p>这个题可以用最小表示法来解，但是那个玩意我看不懂。。。。。过一段时间再来看看把。</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>字符串</tag>
      
      <tag>残留</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 622. 设计循环队列</title>
    <link href="/2022/08/02/Algorithm%20Solution/LC-622.-%E8%AE%BE%E8%AE%A1%E5%BE%AA%E7%8E%AF%E9%98%9F%E5%88%97/"/>
    <url>/2022/08/02/Algorithm%20Solution/LC-622.-%E8%AE%BE%E8%AE%A1%E5%BE%AA%E7%8E%AF%E9%98%9F%E5%88%97/</url>
    
    <content type="html"><![CDATA[<h4 id="设计循环队列"><a href="https://leetcode.cn/problems/design-circular-queue/">622.设计循环队列</a></h4><p>设计你的循环队列实现。 循环队列是一种线性数据结构，其操作表现基于FIFO（先进先出）原则并且队尾被连接在队首之后以形成一个循环。它也被称为“环形缓冲器”。</p><p>循环队列的一个好处是我们可以利用这个队列之前用过的空间。在一个普通队列里，一旦一个队列满了，我们就不能插入下一个元素，即使在队列前面仍有空间。但是使用循环队列，我们能使用这些空间去存储新的值。</p><p>你的实现应该支持如下操作：</p><ul><li><code>MyCircularQueue(k)</code>: 构造器，设置队列长度为 k 。</li><li><code>Front</code>: 从队首获取元素。如果队列为空，返回 -1 。</li><li><code>Rear</code>: 获取队尾元素。如果队列为空，返回 -1 。</li><li><code>enQueue(value)</code>:向循环队列插入一个元素。如果成功插入则返回真。</li><li><code>deQueue()</code>:从循环队列中删除一个元素。如果成功删除则返回真。</li><li><code>isEmpty()</code>: 检查循环队列是否为空。</li><li><code>isFull()</code>: 检查循环队列是否已满。</li></ul><p>说实话，这个循环队列我一时没反应过来，人直接傻了。现在复习一下。</p><p>对于一个循环队列而言，判空的条件是 <code>front == rear</code>，而判满的条件也是 <code>front == rear</code>。这样就做不出区分了，也就是说我们需要额外空出一个空间来进行标识。</p><p>这样之后，判空条件依旧是 <code>front == rear</code> ，判满则是<code>(front + 1) % capacity == rear</code> 。</p><p>这里我们简单解释一下：<code>(front + 1) % capacity</code>会回到最初的位置，而 <code>rear</code>每次指向的都是一个空位置。因此这个时候 <code>rear</code>也会回到最初的位置。</p><p>然后这个循环队列的大小为<code>(rear - front + capacity) % capacity</code> 。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">MyCircularQueue</span> &#123;<br><span class="hljs-keyword">private</span>:<br>    vector&lt;<span class="hljs-type">int</span>&gt;num;<br>    <span class="hljs-type">int</span> front, rear, capacity;<br><br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">MyCircularQueue</span>(<span class="hljs-type">int</span> k) <br>    &#123;<br>        capacity = k + <span class="hljs-number">1</span>;<br>        num = <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(capacity);<br>        front = rear = <span class="hljs-number">0</span>;<br>    &#125;<br>    <br>    <span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">enQueue</span><span class="hljs-params">(<span class="hljs-type">int</span> value)</span> </span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span>(<span class="hljs-built_in">isFull</span>())<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        num[rear] = value;<br>        rear = (rear + <span class="hljs-number">1</span>) % capacity;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>    &#125;<br>    <br>    <span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">deQueue</span><span class="hljs-params">()</span> </span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span>(<span class="hljs-built_in">isEmpty</span>())<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        front = (front + <span class="hljs-number">1</span>) % capacity;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>    &#125;<br>    <br>    <span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">Front</span><span class="hljs-params">()</span> </span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span>(<span class="hljs-built_in">isEmpty</span>())<br>            <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br>        <span class="hljs-keyword">return</span> num[front];<br>    &#125;<br>    <br>    <span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">Rear</span><span class="hljs-params">()</span> </span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span>(<span class="hljs-built_in">isEmpty</span>())<br>            <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br>        <span class="hljs-keyword">return</span> num[(rear - <span class="hljs-number">1</span> + capacity) % capacity];<br>    &#125;<br>    <br>    <span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">isEmpty</span><span class="hljs-params">()</span> </span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">return</span> front == rear;<br>    &#125;<br>    <br>    <span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">isFull</span><span class="hljs-params">()</span> </span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">return</span> (rear + <span class="hljs-number">1</span>) % capacity == front;<br>    &#125;<br><br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：每项操作时间均为 <em>O( <span class="math inline">\(1\)</span> )</em> 。</p><p>空间复杂度：<em>O( <span class="math inline">\(k\)</span> )</em>，其中 <em>k</em> 为给定的循环队列大小。</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>数据结构设计</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>动规 0-1背包问题</title>
    <link href="/2022/08/02/Algorithm%20Solution/%E5%8A%A8%E8%A7%84-0-1%E8%83%8C%E5%8C%85%E9%97%AE%E9%A2%98/"/>
    <url>/2022/08/02/Algorithm%20Solution/%E5%8A%A8%E8%A7%84-0-1%E8%83%8C%E5%8C%85%E9%97%AE%E9%A2%98/</url>
    
    <content type="html"><![CDATA[<h3 id="背包">0 - 1 背包</h3><p>问题描述：</p><p>有 <code>N</code> 件物品和一个最多能背重量为 <code>W</code>的背包。第 <code>i</code> 件物品的重量是<code>weight[i]</code>，得到的价值是 <code>value[i]</code></p><p><strong>每件物品只能用一次</strong>，求解将哪些物品装入背包里使得物品价值总和最大</p><p>​</p><h3 id="朴素解法">朴素解法</h3><p>背包问题是典型的动态规划。在此，我们需要明确两个概念：<strong>「选择」</strong>和<strong>「状态」</strong>。</p><p>什么叫「选择」？对于一个物品 <code>i</code>，我可以<strong>选择</strong>将它放入背包还是不放人背包。</p><p>而「选择」与「状态」之间的联系为：<strong>「选择」会引发「状态」的改变</strong>。基于这一点，这里的「状态」有：背包的重量、可供选择的物品。</p><p>这里你会问：背包当前的价值算不算是状态？</p><p>答案是：不能算，这只是某一个状态下所对应的值而已，仅仅是数值，不是状态。</p><p>在这里，我们可以给出 <code>dp</code>数组的定义了：<code>dp[i][j]</code> 表示<strong>在前 <code>i</code>个物品中进行挑选，在背包容量为 <code>j</code>的状态下的最大价值</strong>。</p><p>然后，我们再看初始化。</p><p>显然，在前 0 个物品中挑选的最大价值是 0 。在背包容量为 0的条件下挑选的最大价值也是 0。故：<code>dp[0][j] = 0, dp[i][0] = 0</code> 。</p><p>然后我们来看状态转移方程的部分。</p><p>我们的递归从 1 开始，因此<strong>数组下标 <code>i - 1</code> 表示第<code>i</code> 个物品</strong>（这一点很重要啊）。那么对于第<code>i</code> 个物品就有选与不选两种「选择」。</p><ul><li><p>如果我<strong>考察</strong>第 <code>i</code>个物品但<strong>不将它加入到背包中</strong></p><p>我们容易忽略的一个事实是，如果我要考察是否选择第 <code>i</code>个物品的时候，<strong>我一定是在前面 <code>i-1</code>个物品已经选择完的基础上进行讨论的</strong>，那么这一步，我们可以确定前面<code>i - 1</code> 个物品已经选完所对应的 <code>dp</code> 数组前面部分为<code>dp[i - 1][]</code></p><p>然后，我们目前是在考察第 <code>i</code>个物品，也就是说我们现在是要求的 <code>dp</code> 数组的前面部分是<code>dp[i][]</code></p><p>由于不将第 <code>i</code> 个物品加入背包，因此 <code>dp</code>数组的后面部分相同，全部写 <code>dp[][j]</code></p><p>所以这种情况对应的状态转移方程为：<code>dp[i][j] = dp[i - 1][j]</code></p></li><li><p>如果我考察第 <code>i</code>个物品并<strong>将它加入到背包中</strong></p><p>同样地，我们从<strong>对前面 <code>i-1</code>个物品已经选完</strong>的基础上开始讨论，此时所对应的 <code>dp</code>数组的前半部分为 <code>dp[i-1][]</code></p><p>然后，我们考虑<strong>对前面 <code>i</code>个物品全部选完</strong>所对应的 <code>dp</code>数组，也就是我们要求的。这里的前半部分为 <code>dp[i][]</code></p><p>我们知道，状态 <code>i-1</code> 与状态 <code>i</code>相比唯一的不同是<strong>背包所限定的重量不一样</strong>，二者之间的差值为<strong>第<code>i</code> 个物品的重量</strong>（也就是 <code>weight[i - 1]</code>，还记得吗？<strong>下标 <code>i - 1</code> 表示第 <code>i</code>个物品</strong>）。我们目前对<strong>这个部分</strong>所能确定的只有差值，其实也只需要差值。</p><p>因此，对于 <code>dp</code> 数组的第二部分，我们有两种写法</p><p><code>dp[i - 1][j - weight[i - 1]]</code> 与 <code>dp[i][j]</code>或者 <code>dp[i - 1][j]</code> 与<code>dp[i][j + weight[i - 1]]</code></p><p>显然，后者十分的不合理。为什么？</p><p>我们想想动态规划的核心，我们是需要通过 <code>dp[0 到 i - 1]</code>去求 <code>dp[i]</code> 。也就是说，<code>dp[i]</code>之后的数我们还不会遍历到，因此后者在实际的遍历当中是错误的。</p><p>然后，对于状态 <code>i - 1</code> ，我们加上第 <code>i</code>个物品的价值 <code>value[i - 1]</code> 之后就可以得出状态 <code>i</code>了</p><p>因此这种情况的状态转移方程为：<code>dp[i][j] = do[i - 1][j - weight[i - 1]] + value[i - 1]</code></p></li></ul><p>那么到此为止，我对于这玩意所有的疑问都已经解决了，下面给出相应代码：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">back</span><span class="hljs-params">(<span class="hljs-type">int</span> N, <span class="hljs-type">int</span> W, vector&lt;<span class="hljs-type">int</span>&gt;&amp; weight, vector&lt;<span class="hljs-type">int</span>&gt;&amp; value)</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;<span class="hljs-built_in">dp</span>(N + <span class="hljs-number">1</span>, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(W + <span class="hljs-number">1</span>, <span class="hljs-number">0</span>));<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= N; i++)<br>    &#123;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">1</span>; j &lt;= W; j++)<br>        &#123;<br>            <span class="hljs-keyword">if</span> (j &gt; weight[i - <span class="hljs-number">1</span>])<span class="hljs-comment">//背包装得下</span><br>                dp[i][j] = <span class="hljs-built_in">max</span>(dp[i - <span class="hljs-number">1</span>][j], <br>                               dp[i - <span class="hljs-number">1</span>][j - weight[i - <span class="hljs-number">1</span>]] + value[i - <span class="hljs-number">1</span>]);<br>        <span class="hljs-keyword">else</span>                  <span class="hljs-comment">//背包装不下，等价于不装物品的情况</span><br>                dp[i][j] = dp[i - <span class="hljs-number">1</span>][j];<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> dp[N][W];<br>&#125;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>​</p><h3 id="空间优化">空间优化</h3><p>上面的代码可以通过滚动数组将空间复杂度压缩到 <span class="math inline">\(O(n)\)</span>，但数组依旧是二维的，我们在这里介绍一种只需要一维的数组空间实现的算法，当然，空间复杂度还是<span class="math inline">\(O(n)\)</span> ，但这个算法的思想很重要</p><p>上面我们看到，状态 <code>dp[i][j]</code> 的数值仅由状态<code>dp[i - 1][j]</code> 与状态<code>dp[i - 1][j - weight[i - 1]]</code> 转移而来</p><p>联想我们之前 <a href="https://nishikichisato.github.io/2022/09/06/Algorithm%20Solution/DP-数组空间压缩/">DP数组空间压缩</a> 的技巧，我们只定义一个一维数组<code>dp[w + 1]</code></p><p>在为对该数组赋值的时候，数组里面的数值就表示<code>dp[i - 1]</code></p><p>我们只需要将 <code>dp[j]</code> 的数组与<code>dp[j - weight[i - 1]]</code> 保存下来给 <code>dp[j]</code>赋值即可</p><p>但这样其实还是有点麻烦的，我们直接反过来遍历，这样就不需要保存了</p><p>完整代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">back</span><span class="hljs-params">(<span class="hljs-type">int</span> N, <span class="hljs-type">int</span> W, vector&lt;<span class="hljs-type">int</span>&gt;&amp; weight, vector&lt;<span class="hljs-type">int</span>&gt;&amp; value)</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">dp</span>(W + <span class="hljs-number">1</span>);<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= N; i++)<br>    &#123;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = W; j &gt;= weight[i - <span class="hljs-number">1</span>]; j--)<br>        &#123;<br>            dp[j] = <span class="hljs-built_in">max</span>(dp[j], dp[j - weight[i - <span class="hljs-number">1</span>]]);<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> dp[N][W];<br>&#125;<br></code></pre></td></tr></table></figure><p>时间复杂度：<span class="math inline">\(O(n^2)\)</span></p><p>空间复杂度：<span class="math inline">\(O(n)\)</span></p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>动态规划</tag>
      
      <tag>背包</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 617. 合并二叉树</title>
    <link href="/2022/08/01/Algorithm%20Solution/LC-617.%E5%90%88%E5%B9%B6%E4%BA%8C%E5%8F%89%E6%A0%91/"/>
    <url>/2022/08/01/Algorithm%20Solution/LC-617.%E5%90%88%E5%B9%B6%E4%BA%8C%E5%8F%89%E6%A0%91/</url>
    
    <content type="html"><![CDATA[<h4 id="合并二叉树"><a href="https://leetcode.cn/problems/merge-two-binary-trees/">617.合并二叉树</a></h4><p>给你两棵二叉树： <code>root1</code> 和 <code>root2</code> 。</p><p>想象一下，当你将其中一棵覆盖到另一棵之上时，两棵树上的一些节点将会重叠（而另一些不会）。你需要将这两棵树合并成一棵新二叉树。合并的规则是：如果两个节点重叠，那么将这两个节点的值相加作为合并后节点的新值；否则，<strong>不为</strong>null 的节点将直接作为新二叉树的节点。</p><p>返回合并后的二叉树。</p><p><strong>注意:</strong> 合并过程必须从两个树的根节点开始。</p><figure class="highlight nix"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs nix">输入：<span class="hljs-attr">root1</span> = [<span class="hljs-number">1</span>,<span class="hljs-number">3</span>,<span class="hljs-number">2</span>,<span class="hljs-number">5</span>], <span class="hljs-attr">root2</span> = [<span class="hljs-number">2</span>,<span class="hljs-number">1</span>,<span class="hljs-number">3</span>,<span class="hljs-literal">null</span>,<span class="hljs-number">4</span>,<span class="hljs-literal">null</span>,<span class="hljs-number">7</span>]<br>输出：[<span class="hljs-number">3</span>,<span class="hljs-number">4</span>,<span class="hljs-number">5</span>,<span class="hljs-number">5</span>,<span class="hljs-number">4</span>,<span class="hljs-literal">null</span>,<span class="hljs-number">7</span>]<br></code></pre></td></tr></table></figure><h4 id="dfs">DFS</h4><p>用深度搜索的解法很简单，我们只需要随便选择一棵树作为基准，<strong>从新对它的节点以及左右孩子进行赋值即可</strong>。</p><p>对于节点的赋值，我们直接把另一棵树的值加上去就行。</p><p>对于左右孩子的赋值，我们通过递归实现，只需要让两棵树<strong>同时</strong>向一个方向递归就行。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function">TreeNode* <span class="hljs-title">mergeTrees</span><span class="hljs-params">(TreeNode* root1, TreeNode* root2)</span></span><br><span class="hljs-function">    </span>&#123;<br><span class="hljs-keyword">if</span> (root1 == <span class="hljs-literal">nullptr</span>)<br><span class="hljs-keyword">return</span> root2;<br><span class="hljs-keyword">if</span> (root2 == <span class="hljs-literal">nullptr</span>)<br><span class="hljs-keyword">return</span> root1;<br>root1-&gt;val += root2-&gt;val;<br>root1-&gt;left = <span class="hljs-built_in">mergeTrees</span>(root1-&gt;left, root2-&gt;left);<span class="hljs-comment">//向一个方向递归</span><br>root1-&gt;right = <span class="hljs-comment">//我们需要对root1中某个节点的作用孩子进行赋值，因此需要写上等于号</span><br>            <span class="hljs-built_in">mergeTrees</span>(root1-&gt;right, root2-&gt;right);<br>        <span class="hljs-keyword">return</span> root1;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<em>O( <span class="math inline">\(min( m, n )\)</span>)</em> ，m 、n表示两个二叉树的节点个数。这里只需要把较小的那棵二叉树执行完之后就不需要再继续执行了。</p><p>空间复杂度：<em>O( <span class="math inline">\(min( m, n )\)</span>)</em> ，理由同上。</p><p>今天太困了，先水一下吧（话说八月的第一天就水会不会不太好啊。。。。。）</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>二叉树</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>LC 1161. 最大层内元素和</title>
    <link href="/2022/07/31/Algorithm%20Solution/LC-1161.%E6%9C%80%E5%A4%A7%E5%B1%82%E5%86%85%E5%85%83%E7%B4%A0%E5%92%8C/"/>
    <url>/2022/07/31/Algorithm%20Solution/LC-1161.%E6%9C%80%E5%A4%A7%E5%B1%82%E5%86%85%E5%85%83%E7%B4%A0%E5%92%8C/</url>
    
    <content type="html"><![CDATA[<h4 id="最大层内元素和"><a href="https://leetcode.cn/problems/maximum-level-sum-of-a-binary-tree/">1161.最大层内元素和</a></h4><p>给你一个二叉树的根节点 <code>root</code>。设根节点位于二叉树的第<code>1</code> 层，而根节点的子节点位于第 <code>2</code>层，依此类推。</p><p>请返回层内元素之和 <strong>最大</strong>的那几层（可能只有一层）的层号，并返回其中 <strong>最小</strong>的那个。</p><pre><code class="hljs">输入：root = [1,7,0,7,-8,null,null]输出：2解释：第 1 层各元素之和为 1，第 2 层各元素之和为 7 + 0 = 7，第 3 层各元素之和为 7 + -8 = -1，所以我们返回第 2 层的层号，它的层内元素之和最大。</code></pre><h4 id="bfs">BFS</h4><p>既然是找每一层的最大值，那么我们直接用层序遍历即可并用一个下标来记录当前层的最大和所对应的层数，最后返回即可。还蛮简单的</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">maxLevelSum</span><span class="hljs-params">(TreeNode* root)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>queue&lt;TreeNode*&gt;que;<br>que.<span class="hljs-built_in">push</span>(root);<br><span class="hljs-type">int</span> sum = <span class="hljs-number">0</span>;<span class="hljs-comment">//记录每层的元素和</span><br><span class="hljs-type">int</span> maxsum = INT32_MIN, index = <span class="hljs-number">0</span>;<span class="hljs-comment">//记录最大那一层的元素和以及相应的下标</span><br><span class="hljs-type">int</span> deep = <span class="hljs-number">0</span>;<span class="hljs-comment">//记录层数</span><br><span class="hljs-keyword">while</span> (!que.<span class="hljs-built_in">empty</span>())<br>&#123;<br><span class="hljs-type">int</span> size = que.<span class="hljs-built_in">size</span>();<br>deep++;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; size; i++)<br>&#123;<br>TreeNode* cur = que.<span class="hljs-built_in">front</span>();<br>que.<span class="hljs-built_in">pop</span>();<br>sum += cur-&gt;val;<br><span class="hljs-keyword">if</span> (cur-&gt;left)<br>que.<span class="hljs-built_in">push</span>(cur-&gt;left);<br><span class="hljs-keyword">if</span> (cur-&gt;right)<br>que.<span class="hljs-built_in">push</span>(cur-&gt;right);<br>&#125;<br><span class="hljs-keyword">if</span> (sum &gt; maxsum)<br>&#123;<br>maxsum = sum;<br>index = deep;<br>&#125;<br>            sum = <span class="hljs-number">0</span>;<br>&#125;<br><span class="hljs-keyword">return</span> index;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<em>O( <span class="math inline">\(n\)</span> )</em> ，n为二叉树节点个数。</p><p>空间复杂的：<em>O( <span class="math inline">\(n\)</span> )</em> ，n为二叉树节点个数，最坏情况下，需要申请 n 个空间的队列。</p><h4 id="dfs">DFS</h4><p>当然，这道题我们也可以用递归来解决</p><p>这道题我们需要解决的问题只有一个：<strong>记录每一层的所有元素和及该层的层数</strong>。</p><p>那么，我们完全可以用一个数组来解决。用数组下标来表示层数，下标所对应的值表示该层所有元素的和，那么最后的结果就是这个数组中最大元素所对应的下标。</p><p>那么有一个问题是，<strong>对于当前递归的到的这个元素，我该如何确定该将它加入到哪个下标当中去呢</strong>。</p><p>显然，递归函数只有一个参数是满足不了我们的，我们还需要一个参数来记录当前层数。</p><p>当层数等于数组大小时，说明该元素在新的一层，需要插入到数组的末尾。</p><p>当层数小于数组大小时，直接让对应元素相加即可。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>vector&lt;<span class="hljs-type">int</span>&gt;ret;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">DFS</span><span class="hljs-params">(TreeNode* cur, <span class="hljs-type">int</span> deep)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (ret.<span class="hljs-built_in">size</span>() == deep)<br>ret.<span class="hljs-built_in">push_back</span>(cur-&gt;val);<span class="hljs-comment">//在后面插入</span><br><span class="hljs-keyword">else</span><br>ret[deep] += cur-&gt;val;<br><span class="hljs-keyword">if</span> (cur-&gt;left)<br><span class="hljs-built_in">DFS</span>(cur-&gt;left, deep + <span class="hljs-number">1</span>);<span class="hljs-comment">//这里不能写deep++, 因为会影响到下面cur右边的递归</span><br><span class="hljs-keyword">if</span> (cur-&gt;right)              <span class="hljs-comment">//这里不能写else, 因为我们需要保证该节点的两个子树都能递归到</span><br><span class="hljs-built_in">DFS</span>(cur-&gt;right, deep + <span class="hljs-number">1</span>);<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">maxLevelSum</span><span class="hljs-params">(TreeNode* root)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (root == <span class="hljs-literal">nullptr</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br><span class="hljs-built_in">DFS</span>(root, <span class="hljs-number">0</span>);<br><span class="hljs-type">int</span> index = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; ret.<span class="hljs-built_in">size</span>(); i++)<br><span class="hljs-keyword">if</span> (ret[index] &lt; ret[i])<br>index = i;<br><span class="hljs-keyword">return</span> index + <span class="hljs-number">1</span>;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>时间复杂度：<em>O( <span class="math inline">\(n\)</span> )</em> ，n为二叉树节点个数。有 n 个节点，每个节点的操作为 O( <span class="math inline">\(1\)</span> ) 。</p><p>空间复杂度：<em>O( <span class="math inline">\(n\)</span> )</em> ，n为二叉树节点个数。这里省略常数了，最坏情况是一条链，数组以及递归的空间复杂度都是O( <span class="math inline">\(n\)</span> ) 。</p>]]></content>
    
    
    <categories>
      
      <category>Algorithm Solution</category>
      
    </categories>
    
    
    <tags>
      
      <tag>二叉树</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>算法笔记</title>
    <link href="/2022/07/30/Algorithm%20Solution/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B0/"/>
    <url>/2022/07/30/Algorithm%20Solution/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B0/</url>
    
    <content type="html"><![CDATA[<h2 id="算法性能分析">算法性能分析</h2><h3 id="大o的定义">大O的定义</h3><ul><li><p>大O的定义指的是一个算法最坏的运行时间的上界，而我们通常说所的时间复杂度为一般情况，并不是严格的上界</p></li><li><p>由于时间复杂度会忽略常数项，<strong>因此并不是时间复杂度越低越好</strong></p><ul><li>这是因为大O衡量的是数据量级非常大的情况下所表现出来的时间复杂度，此时常数项已经不起作用</li><li>对于一个复杂的大O表达式，我们只取最高量级的数并且忽略掉它的常数</li></ul></li></ul><h4 id="一个例子">一个例子</h4><p>找出n个字符串中相同的两个字符串（假设这里只有两个相同的字符串）</p><ul><li><p>通过暴力枚举的话需要O(n^2)，但这里涉及到两个字符串的逐字节比较的问题，因此需要再乘上字符串的长度m，即O(m* n^2)</p></li><li><p>如果我们采取先排序再查找的思路，对于快排而言，由于是字符串排序，时间复杂度为O(m* n *logn)，由于是按照字典序排列的，因此相同的两个字符串是排在一起的，因此只需在在排序的基础上加上遍历n个字符串与比较两个字符串所花的时间即可，因此总时间复杂度为O(m* n * logn + m * n)，比直接暴力枚举要快</p></li></ul><hr><p>##递归算法的时间复杂度分析</p><ul><li>递归算法时间复杂度本质上是看<code>递归的次数 * 每次递归中的操作次数</code></li></ul><p>例如，我需要求x的n次方</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">fact</span><span class="hljs-params">(<span class="hljs-type">int</span> x, <span class="hljs-type">int</span> n)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> temp = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++)<br>temp *= x;<br><span class="hljs-keyword">return</span> temp;<br>&#125;<br></code></pre></td></tr></table></figure><p>我们可以写出该代码的递归代码</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">fact1</span><span class="hljs-params">(<span class="hljs-type">int</span> x, <span class="hljs-type">int</span> n)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (n == <span class="hljs-number">0</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br><span class="hljs-keyword">else</span><br><span class="hljs-keyword">return</span> <span class="hljs-built_in">fact1</span>(x, n - <span class="hljs-number">1</span>) * n;<br>&#125;<br></code></pre></td></tr></table></figure><p>该代码的执行次数为n-1，每一次只执行一次乘法，因此总的时间复杂度为O(n)</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">fact2</span><span class="hljs-params">(<span class="hljs-type">int</span> x, <span class="hljs-type">int</span> n)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (n == <span class="hljs-number">0</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br><span class="hljs-keyword">if</span> (n &amp; <span class="hljs-number">1</span>)<span class="hljs-comment">//n is odd</span><br><span class="hljs-keyword">return</span> <span class="hljs-built_in">fact2</span>(x, n / <span class="hljs-number">2</span>) * <span class="hljs-built_in">fact2</span>(x, n / <span class="hljs-number">2</span>) * x;<br><span class="hljs-keyword">else</span><br><span class="hljs-keyword">return</span> <span class="hljs-built_in">fact2</span>(x, n / <span class="hljs-number">2</span>) * <span class="hljs-built_in">fact2</span>(x, n / <span class="hljs-number">2</span>);<br>&#125;<br></code></pre></td></tr></table></figure><hr><p><strong>满二叉树相关结论</strong></p><p><code>* 二叉树的层数从1开始计算，深度为最大的层数 * 第k层最多有2的k-1次方个节点 * 总节点为n的满二叉数，其深度为logn下取整加1 * 若满二叉树的深度为h，则其总结点个数为2的h次方减1</code></p><p>该代码可以看成一个满二叉树，即：</p><figure><img src="https://img-blog.csdnimg.cn/20201209193909426.png" srcset="/img/loading.gif" lazyload alt="满二叉树"><figcaption aria-hidden="true">满二叉树</figcaption></figure><p>设x为总节点个数，h为满二叉树的深度，有<code>x = 2^h - 1, h = log 2 n + 1</code>带入得到<code>x = 2n - 1</code>因此时间复杂度为O(n)</p><p>上面的递归中<code>fact2</code>算了两次，其实我们可以用一个变量记录它的值，然后让该变量自乘一次便可</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">fact3</span><span class="hljs-params">(<span class="hljs-type">int</span> x, <span class="hljs-type">int</span> n)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (n == <span class="hljs-number">0</span>)<br><span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br><span class="hljs-type">int</span> temp = <span class="hljs-built_in">fact3</span>(x, n / <span class="hljs-number">2</span>);<br><span class="hljs-keyword">if</span> (n &amp; <span class="hljs-number">1</span>)<span class="hljs-comment">//n is odd</span><br><span class="hljs-keyword">return</span> temp * temp * x;<br><span class="hljs-keyword">else</span><br><span class="hljs-keyword">return</span> temp * temp;<br>&#125;<br></code></pre></td></tr></table></figure><p>这里的递归次数为<code>O(logn)</code>，每一次递归乘法的运算为<code>O(2)</code>，因此总的时间复杂度为<code>O(logn)</code></p><hr><h1 id="数组">数组</h1><h2 id="二分查找的写法">二分查找的写法</h2><p>二分查找有两种区间写法，左闭右闭与左闭右开。对于循环部分的判断以及mid的跳转一定要按照区间来写</p><h3 id="左闭右闭">左闭右闭</h3><p>左闭右闭说明了<strong>区间的左右端点都在数组当中有数值对应</strong>，即左端点对应第一个数，右端点对应最后一个数</p><ul><li><p>对于循环部分，应写<code>while(left &lt;= right)</code>。这是因为当<code>left = right</code>时，区间依旧有意义（它是一个点）</p></li><li><p>对于mid的跳转部分，应写<code>left = mid + 1</code>。这是因为如果<code>while(nums[mid] &lt; target)</code>时，说明<code>nums[mid]</code>一定不是target，并且不需要再去检测<code>nums[mid]</code>，即区间的左端点直接跳到<code>mid + 1</code>就行</p></li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">search</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> target)</span> </span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> left = <span class="hljs-number">0</span>, right = nums.<span class="hljs-built_in">size</span>() - <span class="hljs-number">1</span>;<span class="hljs-comment">//闭区间[left,right]写法，nums[right]为数组最后一个元素</span><br>    <span class="hljs-keyword">while</span> (left &lt;= right)<span class="hljs-comment">//对于区间[left,right]而言，区间左端点left与右端点right重合依旧有意义</span><br>    &#123;<br>        <span class="hljs-type">int</span> mid = right + (left - right) &gt;&gt; <span class="hljs-number">1</span>;<span class="hljs-comment">//直接用加号会溢出</span><br>        <span class="hljs-keyword">if</span> (nums[mid] &gt; target)<span class="hljs-comment">//nums[mid]一定不是target，表明target在[left,mid-1]当中</span><br>            right = mid - <span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nums[mid] &lt; target)<span class="hljs-comment">//nums[mid]一定不是target，target落在[mid+1,right]中</span><br>            left = mid + <span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">return</span> mid;<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<span class="hljs-comment">//循环结束，说明没有找到，直接返回-1</span><br>&#125;<br><br></code></pre></td></tr></table></figure><h3 id="左闭右开">左闭右开</h3><p>左闭右开说明<strong>区间的右端点不是数组最后一个元素，而是最后一个元素的下一个</strong>，即<code>右端点-1</code>才是数组当中最后一个元素</p><ul><li><p>对于循环部分，写<code>while(left &lt; right)</code>。因为当<code>left = right</code>时，区间无意义</p></li><li><p>对于区间左端点的跳转部分，当<code>while(nums[mid] &lt; target)</code>时，<code>nums[mid]</code>不是target，由于区间是<strong>左闭</strong>的属性，因此<code>left = mid + 1</code></p></li><li><p>对于区间右端点的跳转部分，当<code>while(nums[mid] &gt; target)</code>时，<code>nums[mid]</code>不是target，但由于区间右端点必须是开区间，因此<code>right = mid</code></p></li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">search</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> target)</span> </span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> left = <span class="hljs-number">0</span>, right = nums.<span class="hljs-built_in">size</span>();<br><span class="hljs-keyword">while</span> (left &lt; right)<br>&#123;<br><span class="hljs-type">int</span> mid = right + (left - right) &gt;&gt; <span class="hljs-number">1</span>;<br><span class="hljs-keyword">if</span> (nums[mid] &gt; target)<span class="hljs-comment">//target落在区间[left,mid)当中，由于右端点不在检测数组内</span><br>                       <span class="hljs-comment">//因此right = mid  </span><br>right = mid;<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nums[mid] &lt; target)<span class="hljs-comment">//target落在区间(mid,right)，由于左端点为闭区间</span><br>                            <span class="hljs-comment">//因此left = mid + 1</span><br>left = mid + <span class="hljs-number">1</span>;<br><span class="hljs-keyword">else</span> <span class="hljs-keyword">return</span> mid;<br>&#125;<br><span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br>&#125;<br><br></code></pre></td></tr></table></figure><hr><p><strong>二分查找本质上是对查找区间的不断缩小</strong>，最终一定会变成区间含有三个或者四个元素的情况（区间两端点均为闭区间）。</p><ul><li><p>对于前者，下一步的<code>left</code>或者<code>right</code>将会重合。假设<code>left=0, right=2</code>，这时<code>mid=1</code>，即与<code>target</code>比较的元素为<strong>区间的中点</strong>。那么下一步将会变成<code>left=0, right=0</code>，即二者重合，在进行一次判断，这时会产生<code>left&gt;right</code></p></li><li><p>对于后者，假设<code>left=0, right=3</code>，这时<code>mid=1</code>，即与<code>target</code>比较的元素为<strong>中间偏右</strong>。下一步将变成<code>left=0, right=0</code>，二者重合，之后的情况与上面一样。</p></li></ul><blockquote><p>力扣34</p><p>给定一个按照升序排列的整数数组 nums，和一个目标值target。找出给定目标值在数组中的开始位置和结束位置。</p><p>如果数组中不存在目标值 target，返回 [-1, -1]。</p></blockquote><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><br><span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">searchRange</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> target)</span> </span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> n = nums.<span class="hljs-built_in">size</span>();<br><span class="hljs-type">int</span> left = <span class="hljs-built_in">Getleft</span>(nums, target);<span class="hljs-comment">//加一才是下标位置</span><br><span class="hljs-type">int</span> right = <span class="hljs-built_in">Getright</span>(nums, target);<span class="hljs-comment">//这个是序号位置，下标位置要减一</span><br><span class="hljs-keyword">if</span> (left == <span class="hljs-number">-2</span> || right == <span class="hljs-number">-2</span>) <span class="hljs-keyword">return</span> &#123; <span class="hljs-number">-1</span>,<span class="hljs-number">-1</span> &#125;;<span class="hljs-comment">//只要有一个等于-2，说明target一定超界</span><br><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> ((right - <span class="hljs-number">1</span>) - (left + <span class="hljs-number">1</span>) &gt;= <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> &#123; left + <span class="hljs-number">1</span>,right - <span class="hljs-number">1</span> &#125;;<span class="hljs-comment">//正常情况，两个数开始与结束相差值大于0（两个数可以相等）</span><br><span class="hljs-keyword">else</span> <span class="hljs-keyword">return</span> &#123; <span class="hljs-number">-1</span>,<span class="hljs-number">-1</span> &#125;;<span class="hljs-comment">//对于这种数组中找不到的情况，想不明白，直接用else</span><br>&#125;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">Getright</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> target)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> left = <span class="hljs-number">0</span>, right = nums.<span class="hljs-built_in">size</span>() - <span class="hljs-number">1</span>;<br><span class="hljs-type">int</span> border = <span class="hljs-number">-2</span>;<br><span class="hljs-keyword">while</span> (left &lt;= right)<br>&#123;<br><span class="hljs-type">int</span> mid = left + (right - left) / <span class="hljs-number">2</span>;<br><span class="hljs-keyword">if</span> (nums[mid] &gt; target)<span class="hljs-comment">//这里必须是右边界移动，下面的左边界加一才是结果</span><br>right = mid - <span class="hljs-number">1</span>;<br><span class="hljs-keyword">else</span><span class="hljs-comment">//相等的话，我们要让左边界向右移动一位</span><br>&#123;<br>left = mid + <span class="hljs-number">1</span>;<br>border = left;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> border;<br>&#125;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">Getleft</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;nums, <span class="hljs-type">int</span> target)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> left = <span class="hljs-number">0</span>, right = nums.<span class="hljs-built_in">size</span>() - <span class="hljs-number">1</span>;<br><span class="hljs-type">int</span> border = <span class="hljs-number">-2</span>;<br><span class="hljs-keyword">while</span> (left &lt;= right)<br>&#123;<br><span class="hljs-type">int</span> mid = left + (right - left) / <span class="hljs-number">2</span>;<br><span class="hljs-keyword">if</span> (nums[mid] &lt; target)<br>left = mid + <span class="hljs-number">1</span>;<br><span class="hljs-keyword">else</span><br>&#123;<br>right = mid - <span class="hljs-number">1</span>;<br>border = right;<br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> border;<br>&#125;<br><br></code></pre></td></tr></table></figure><hr><h2 id="左右指针操作数组">左右指针操作数组</h2><p>如果需要移动（或覆盖）一个数组当中的元素，我们可以采取双指针来操作这个数组。左指针为待覆盖元素，右指针为被检测元素（即该元素是否应该去覆盖左指针所指的元素）。</p><p>左右指针可同向行进，也可相向行进。</p><blockquote><p>力扣（26）</p><p>给你一个 升序排列 的数组 nums ，请你 原地删除重复出现的元素，使每个元素 只出现一次，返回删除后数组的新长度。元素的 相对顺序 应该保持 一致 。</p><p>由于在某些语言中不能改变数组的长度，所以必须将结果放在数组nums的第一部分。更规范地说，如果在删除重复项之后有k 个元素，那么 nums 的前 k 个元素应该保存最终结果。</p><p>将最终结果插入 nums 的前 k 个位置后返回 k 。</p><p>不要使用额外的空间，你必须在 原地 修改输入数组 并在使用 O(1)额外空间的条件下完成。</p></blockquote><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">removeDuplicates</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums)</span> </span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> left = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> right = <span class="hljs-number">1</span>; right &lt; nums.<span class="hljs-built_in">size</span>(); right++)<br>&#123;<br><span class="hljs-keyword">if</span> (nums[right] != nums[left])<br>&#123;<br>nums[++left] = nums[right];<br>&#125;<br>&#125;<br>    <span class="hljs-keyword">return</span> left + <span class="hljs-number">1</span>;<br>&#125;<br><br></code></pre></td></tr></table></figure><blockquote><p>字符串的遍历（对于string类）</p></blockquote><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">for</span>(<span class="hljs-type">char</span> c : str)<span class="hljs-comment">//相当于用c对str进行遍历</span><br><br><span class="hljs-keyword">for</span>(<span class="hljs-type">char</span> &amp;c : str)<span class="hljs-comment">//同上，不过上面会将str复制一次，这个不用</span><br></code></pre></td></tr></table></figure><blockquote><p>二维数组的初始化</p></blockquote><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt; <span class="hljs-built_in">ret</span>(n, <span class="hljs-built_in">vector</span>&lt;<span class="hljs-type">int</span>&gt;(n, <span class="hljs-number">0</span>))<br></code></pre></td></tr></table></figure><hr><h2 id="滑动窗口左右指针">滑动窗口、左右指针</h2><blockquote><p>力扣209</p><p>给定一个含有 n 个正整数的数组和一个正整数 s ，找出该数组中满足其和 ≥s 的长度最小的 连续 子数组，并</p><p>返回其长度。如果不存在符合条件的子数组，返回 0。</p></blockquote><p>我们定义两个指针，通过不断移动两个指针来得到最优解的过程称之为滑动窗口</p><p>需要明确的是：</p><ul><li><p>窗口内的内容是什么</p></li><li><p>窗口的右边界确定什么，以及什么时候移动</p></li><li><p>窗口的左边界确定什么，以及什么时候移动</p></li></ul><blockquote><p>对于这道题，<strong>窗口的右边界就是遍历数组的指针，它用来一直寻找最优解，需要一直移动</strong>，窗口内容是<strong>一个和大于<code>target</code>的子序列</strong>，左边界指针<strong>用来确定当前最优解，即当窗口内容符合条件时，左指针移动</strong></p></blockquote><blockquote><p>三数之和 给你一个包含 n 个整数的数组 nums，判断 nums中是否存在三个元素 a，b，c ，使得 a + b + c = 0 ？请你找出所有和为 0且不重复的三元组</p></blockquote><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><code class="hljs cpp">vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt; <span class="hljs-built_in">threeSum</span>(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums)<br>&#123;<br>    vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;ret;<br>    <span class="hljs-built_in">sort</span>(nums.<span class="hljs-built_in">begin</span>(), nums.<span class="hljs-built_in">end</span>());<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.<span class="hljs-built_in">size</span>(); i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span> (nums[i] &gt; <span class="hljs-number">0</span>)<br>            <span class="hljs-keyword">return</span> ret;<br>        <span class="hljs-keyword">if</span> (i &gt; <span class="hljs-number">0</span> &amp;&amp; nums[i] == nums[i - <span class="hljs-number">1</span>])<br>            <span class="hljs-keyword">continue</span>;<br>        <span class="hljs-type">int</span> left = i + <span class="hljs-number">1</span>, right = nums.<span class="hljs-built_in">size</span>() - <span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">while</span> (left &lt; right)<br>        &#123;<br>            <span class="hljs-keyword">if</span> (nums[i] + nums[left] + nums[right] &gt; <span class="hljs-number">0</span>)<br>            &#123;<br>                right--;<br>                <span class="hljs-keyword">while</span> (left &lt; right &amp;&amp; nums[right] == nums[right + <span class="hljs-number">1</span>])<br>                    right--;<br>            &#125;<br>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nums[i] + nums[left] + nums[right] &lt; <span class="hljs-number">0</span>)<br>            &#123;<br>                left++;<br>                <span class="hljs-keyword">while</span> (left &lt; right &amp;&amp; nums[left] == nums[left - <span class="hljs-number">1</span>])<br>                    left++;<br>            &#125;<br>            <span class="hljs-keyword">else</span><br>            &#123;<br>                ret.<span class="hljs-built_in">push_back</span>(&#123; nums[i], nums[left], nums[right] &#125;);<br>                <span class="hljs-keyword">while</span> (left &lt; right &amp;&amp; nums[left] == nums[left + <span class="hljs-number">1</span>])<br>                    left++;<br>                <span class="hljs-keyword">while</span> (left &lt; right &amp;&amp; nums[right] == nums[right - <span class="hljs-number">1</span>])<br>                    right--;<br>                left++;<br>                right--;<br>            &#125;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> ret;<br>&#125;<br></code></pre></td></tr></table></figure><p>注：由于用的是<code>if elseif else</code>语句，在条件成立后<strong>只会走一条路</strong>，因此前面两个的去重是为了保证<strong>找到下一个不同于当前重复数子的数</strong>第三个的去重是为了保证<strong>在当前重复的数子中，现在的<code>right</code>指向的是重复区间的最后一个数字</strong></p><blockquote><p>在判断<code>nums[i] + nums[left] + nums[right] &gt; 0</code>之后的去重部分为什么是<code>nums[right] == nums[right + 1]</code></p></blockquote><p>对于循环，当<strong>条件不满足的时候才会跳出</strong>，因此跳出之后的情况就是<strong>不满足循环判断部分的条件</strong>。当<code>nums[i] + nums[left] + nums[right] &gt; 0</code>判断完之后，我需要让<code>right</code><strong>跳到下一位</strong>（<strong>不同于</strong>上一位）因此<strong>在结束循环后</strong>，我需要的状况为<code>nums[right] ！= nums[right + 1]</code></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">minSubArrayLen</span><span class="hljs-params">(<span class="hljs-type">int</span> target, vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> sum = <span class="hljs-number">0</span>;<span class="hljs-comment">//记录大小</span><br><span class="hljs-type">int</span> length = <span class="hljs-number">0</span>;<span class="hljs-comment">//记录当前长度</span><br><span class="hljs-type">int</span> ret = INT32_MAX;<span class="hljs-comment">//目前已记录的最短长度</span><br><span class="hljs-type">int</span> left = <span class="hljs-number">0</span>;<span class="hljs-comment">//左指针初始化</span><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> right = <span class="hljs-number">0</span>; right &lt; nums.<span class="hljs-built_in">size</span>(); right++)<br>&#123;<br>sum += nums[right];<span class="hljs-comment">//不需要对sum进行初始化</span><br><span class="hljs-keyword">while</span> (sum &gt;= target)<br>&#123;<br>length = right - left + <span class="hljs-number">1</span>;<br>ret = ret &lt; length ? ret : length;<br>sum -= nums[left++];<span class="hljs-comment">//sum减去left的值并且left向右移动</span><br>&#125;<br>&#125;<br><span class="hljs-keyword">return</span> ret == INT32_MAX ? <span class="hljs-number">0</span> : ret;<br>&#125;<br><br></code></pre></td></tr></table></figure><p>在使用左右指针的时候我们需要明确左指针与右指针<strong>什么时候需要移动</strong>，以及我需要<strong>通过这两个指针来操作什么</strong></p><ul><li><p>如果我是想要移动数组，那么就右指针赋值给左指针（相当于<strong>删除</strong>元素）或者左指针赋值给右指针（相当于<strong>扩充</strong>元素）</p></li><li><p>如果我想删除链表当中的某个元素（不是特定值，而是正数第几个倒数第几个），我可以先让右指针<strong>向右移动一段距离</strong>，然后再让左右指针<strong>同时</strong>移动，当右指针移动到链表结尾时左指针恰好移动到待删除元素的<strong>节点上</strong></p></li></ul><hr><h1 id="链表">链表</h1><p>由于对链表操作时通常<strong>涉及到该节点的前一个节点，但头节点并没有前一个节点这种概念</strong>，因此我们可以在链表创建时设立一个虚拟头节点<code>dummyhead</code>，这个节点不存储数据，第一个存储数据的节点为<code>dummyhead-&gt;next</code>，这么做的好处时在对链表进行操作时<strong>不需要对头节点进行单独操作</strong></p><blockquote><p>关于代码规范上：需要加入的节点写为<code>NewNode</code>，临时指针为<code>tmp</code>，遍历指针为<code>cur</code></p></blockquote><p>写链表的算法时一定要明确，我设立虚拟头节点的目的是为了对每个节点的操作是可以<strong>复制</strong>的，即<strong>我对任意一个或多个节点的操作都是相同的</strong></p><h2 id="删除倒数第n个节点">删除倒数第n个节点</h2><blockquote><p>例：给你一个链表，删除链表的倒数第 n个结点，并且返回链表的头结点。</p></blockquote><p>删除倒数第n个节点，这种题是典型的双指针的应用</p><p>通常想法是先把链表的总大小给统计出来，然后得出指针<code>cur</code>需要移动的步数</p><p>其实还有一种更简单的方法：</p><p>考虑到，如果我定义两个指针：<code>fast</code>与<code>slow</code>，我<strong>让两个指针同时运动</strong>，两个指针之间相差一定的步数，只要<code>fast</code>指向空的时候<code>slow</code>正好指向<strong>待删除节点的前一个节点</strong>，那么我们便可以实现节点的删除注：<strong>删除一个节点只需要一个指针</strong></p><blockquote><p>通过画图我们可以得知，只需要让<code>fast</code>指针移动<code>n+1</code>步就可以让两个指针同时移动了</p></blockquote><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><br><span class="hljs-function">ListNode* <span class="hljs-title">removeNthFromEnd</span><span class="hljs-params">(ListNode* head, <span class="hljs-type">int</span> n)</span> </span><br><span class="hljs-function"></span>&#123;<br>    ListNode* dummyhead = <span class="hljs-keyword">new</span> ListNode;<br>    dummyhead-&gt;next = head;<br>    ListNode* fast = dummyhead, *slow = dummyhead;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n + <span class="hljs-number">1</span>; i++)<br>        fast = fast-&gt;next;<br>    <span class="hljs-keyword">while</span>(fast)<br>    &#123;<br>        fast = fast-&gt;next;<br>        slow = slow-&gt;next;<br>    &#125;<br>    ListNode* tmp = slow-&gt;next;<br>    slow-&gt;next = tmp-&gt;next;<br>    <span class="hljs-keyword">delete</span> tmp;<br>    <span class="hljs-keyword">return</span> dummyhead-&gt;next;<br>&#125;<br><br></code></pre></td></tr></table></figure><blockquote><p>使用虚拟头节点在返回的时候可以之间返回<code>dummyhead-&gt;next</code>，这样子更方便</p></blockquote><hr><p>对链表（一个与两个链表均同理）用双指针的时候，我们两个指针可以<strong>同时同地移动</strong>，也可以<strong>同时不同地移动</strong></p><p>对于前者，我们可以让<strong>两个指针在链表的开始节点移动</strong>（数组的话可以在尾部开始移动）</p><p>对于后者，我们定义两个指针<code>fast</code>与<code>slow</code>，<strong>我们让<code>fast</code>与<code>slow</code>相差一定的节点数</strong>（可以是题目给的数也可以是两个链表的长度差），<strong>之后再让两指针同时移动</strong></p><p>当然，我们也可以定义两个指针<code>slow</code>与<code>fast</code>，我们让<strong><code>fast</code>一次走两个节点</strong>，<strong><code>slow</code>一次走一个节点</strong>，他们一定会相遇，这种通常用在循环链表上。<em>见力扣142</em></p><h2 id="链表反转">链表反转</h2><p>递归实现</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">ListNode* <span class="hljs-title">reverseList</span><span class="hljs-params">(ListNode* head)</span> </span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span>(head == <span class="hljs-literal">NULL</span>)<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;<br>    <span class="hljs-keyword">if</span>(head-&gt;next == <span class="hljs-literal">NULL</span>)<br>        <span class="hljs-keyword">return</span> head;<br>    ListNode* tmp = <span class="hljs-built_in">reverseList</span>(head-&gt;next);<br>    head-&gt;next-&gt;next = head;<br>    head-&gt;next = <span class="hljs-literal">NULL</span>;<br>    <span class="hljs-keyword">return</span> tmp;<br>    <br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>为什么要写<code>head-&gt;next-&gt;next = head</code></p></blockquote><p>tmp<strong>始终</strong>记录的都是头节点，因此只能是用head-&gt;next-&gt;next来表示<strong>当前节点</strong>（该节点可以移动），而<code>head</code>则表示<strong>当前节点的前一个节点</strong></p><blockquote><p>为什么判断<code>head-&gt;next == NULL</code>之后是返回<code>head</code></p></blockquote><p>我们<code>tmp</code>的最终值只能是最后一个节点，由于<code>tmp = reverseList(head-&gt;next)</code>，因此在<code>head-&gt;next</code>为空指针的时候我就要返回<code>head</code></p><p>双指针实现</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">ListNode* <span class="hljs-title">reverseList</span><span class="hljs-params">(ListNode* head)</span> </span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span>(head == <span class="hljs-literal">NULL</span>)<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;<br>    ListNode* pre = <span class="hljs-literal">NULL</span>, *cur = head;<span class="hljs-comment">//cur为当前处理的节点，pre为该节点的前一个节点</span><br>    <span class="hljs-keyword">while</span>(cur)<span class="hljs-comment">//我对某个节点处理，只能是对这个节点进行判断</span><br>    &#123;<br>        ListNode* tmp = cur-&gt;next;<br>        cur-&gt;next = pre;<br>        pre = cur;<br>        cur = tmp;<br>    &#125;<br>    <span class="hljs-keyword">return</span> pre;<br>&#125;<br></code></pre></td></tr></table></figure><p>双指针递归实现 <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><br><span class="hljs-function">ListNode* <span class="hljs-title">reverseList</span><span class="hljs-params">(ListNode* head)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">reverse</span>(<span class="hljs-literal">NULL</span>, head);<br>&#125;<br><br><span class="hljs-function">ListNode* <span class="hljs-title">reverse</span><span class="hljs-params">(ListNode* pre, ListNode* cur)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span>(cur == <span class="hljs-literal">NULL</span>)<br><span class="hljs-keyword">return</span> pre;<br>ListNode* tmp = cur-&gt;next;<br>cur-&gt;next = pre;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">reverse</span>(cur, tmp);<br>&#125;<br><br></code></pre></td></tr></table></figure></p><blockquote><p>为什么在判断<code>cur == NULL</code>之后返回的是<code>pre</code></p></blockquote><p>由于<code>pre</code>总表示<code>cur</code>的前一个节点，因此当<code>cur</code>为<code>NULL</code>时，说明<code>pre</code>已经到达最后一个节点，由于需要直接返回最后一个节点，因此我需要返回<code>pre</code></p><hr><h1 id="哈希表">哈希表</h1><p>哈希表是一种用于查询的工具，具体来说，我将数据录入哈希表当中，每个数据对应一个索引key值，在我需要判断某个元素是否存在这个哈希表当中时，我直接去找它的索引就行</p><p><strong>也就是说，当我需要快速判断一个元素是否在一个集合当中时，我就可以考虑用哈希表了</strong></p><p>在引入哈希表前，我们需要先介绍四个容器<strong>（这四个容器均不涉及哈希表）</strong></p><blockquote><p><code>set</code>&gt;插入容器当中的数据会<strong>自动排序（默认从小到大）</strong>，元素不能更改，并且<strong>不允许相同的元素出现</strong></p></blockquote><h2 id="相关操作">相关操作</h2><pre><code class="hljs">begin()--返回指向第一个元素的迭代器clear()--清除所有元素count()--返回某个值元素的个数empty()--如果集合为空，返回trueend()--返回指向最后一个元素的迭代器equal_range()--返回集合中与给定值相等的上下限的两个迭代器erase()--删除集合中的元素find()--返回一个指向被查找到元素的迭代器get_allocator()--返回集合的分配器insert()--在集合中插入元素lower_bound()--返回指向大于（或等于）某值的第一个元素的迭代器key_comp()--返回一个用于元素间值比较的函数max_size()--返回集合能容纳的元素的最大限值rbegin()--返回指向集合中最后一个元素的反向迭代器rend()--返回指向集合中第一个元素的反向迭代器size()--集合中元素的数目swap()--交换两个集合变量upper_bound()--返回大于某个值元素的迭代器value_comp()--返回一个用于比较元素间的值的函数</code></pre><blockquote><p><code>mutilset</code>跟<code>set</code>一样，只不过<code>mutilset</code><strong>允许相同的元素</strong></p></blockquote><hr><blockquote><p><code>map</code>容器当中的每一个元素均为一个<strong>对组</strong>，即每个元素成对出现</p><blockquote><p><code>map</code>容器当中，对组的<code>first</code>为索引<code>key</code>值，<code>second</code>为<code>value</code>值</p></blockquote><blockquote><p><code>map</code>会<strong>按照<code>key</code>值来对元素进行排序</strong>，并且<code>map</code><strong>不允许有相同的<code>key</code>值出现</strong></p></blockquote><blockquote><p><code>map</code>只检测<code>key</code>值是否相同，它允许有相同的<code>value</code>值</p></blockquote><blockquote><p><code>map</code>当中的<code>value</code>值是可以更改的，<code>set</code>当中的不行</p><blockquote><p>相关操作</p></blockquote></blockquote></blockquote><pre><code class="hljs">begin()         返回指向map头部的迭代器 clear(）        删除所有元素 count()         返回指定元素出现的次数 empty()         如果map为空则返回true end()           返回指向map末尾的迭代器 equal_range()   返回特殊条目的迭代器对 erase()         删除一个元素(按照key值删除而不是按照value值删除) find()          查找一个元素（按照key值查找，返回迭代器） get_allocator() 返回map的配置器 insert()        插入元素 key_comp()      返回比较元素key的函数 lower_bound()   返回键值&gt;=给定元素的第一个位置 max_size()      返回可以容纳的最大元素个数 rbegin()        返回一个指向map尾部的逆向迭代器 rend()          返回一个指向map头部的逆向迭代器 size()          返回map中元素的个数 swap()           交换两个map upper_bound()    返回键值&gt;给定元素的第一个位置 value_comp()     返回比较元素value的函数</code></pre><p>好了，下面我们引入两个容器，其底层实现为哈希表</p><blockquote><p><code>unordered_set</code>跟<code>set</code>一样，不过它的元素是无序的，并且不允许相同元素出现，相关操作跟<code>set</code>一样</p><p><code>unordered_set</code><strong>不允许<code>value</code>值被修改</strong></p><p><code>unordered_map</code>跟<code>map</code>一样，不过不会按照<code>key</code>值进行排序</p><p><code>unordered_map</code>的查找是根据<code>key</code>值进行查找的，而不是根据<code>value</code>值进行查找</p><p><code>unordered_map</code>支持用中括号来访问<code>value</code>值，如果该<code>key</code>值对应的<code>value</code>值不存在，那么它会自动创建一个<code>value</code>值<strong>并赋值为0</strong>，并且<code>value</code>值是可以被修改的</p></blockquote><p>在实际解题当中，由于<code>unordered_set</code>与<code>unordered_map</code>都是哈希表，但前者仅仅只是具备查询功能，后者可以实现<strong>对元素出现次数的记录</strong>，因此，<strong>如果不需要对元素出现次数进行考虑时，我们一般选择前者</strong></p><p>不管是用哈希表还是二分查找，我们都需要<strong>知道一个确定的元素才可以进行查找</strong></p><p>注：<code>vector</code>的删除是<strong>按照元素删除</strong>，<code>map</code>与<code>set</code>是<strong>按照<code>key</code>值删除</strong></p><hr><h2 id="总结">总结</h2><p>其实数组也是一种简易的哈希表，当我们在处理字符串的时候，如果<strong>都是小写字符</strong>或<strong>都是大写字符</strong>，那么我们就可以用数组来解（对于每个字符，直接减去字符a或A就可以直接得到对应下标），至于为什么不用<code>map</code>或者<code>set</code>，因为这两个底层都是红黑树，十分耗时（这种的话可以不用<code>unordered_set</code>与<code>unordered_map</code>）&gt;对应题目：力扣242、383</p><p>如果题目中<strong>没有限制数组的大小</strong>，那么这个时候就不能用数组（虽然<code>vector</code>具有自动扩容的功能，<strong>但这个数组本质上我是想把它当成哈希表一样对待</strong>，因此<strong>我需要用到数组的下标以及下标对应的元素</strong>，而一旦涉及到使用数组的下标，我就必须要对这个数组进行初始化，<strong>对于大小不确定的数组，我无法对其初始化</strong>）</p><p>除此之外还有一点，数组是连续的空间，一旦哈希值跨度很大（即数组下标跨度很大），就会造成浪费空间</p><blockquote><p>对应题目：力扣349</p></blockquote><p><code>set</code>与<code>map</code>、<code>mutilset</code>与<code>mutilmap</code>、<code>unordered_set</code>与<code>unordered_map</code>均可以做哈希表，分<code>set</code>与<code>map</code>来说（因为<code>unordered_set</code>与<code>unordered_map</code>均可以看成<code>set</code>与<code>map</code>的无序版本）</p><p>对于<code>set</code>而言，它是集合，只有<code>key</code>值的概念，并且它的<code>key</code>值是有序的。如果我<strong>仅仅需要看一个数是否在数组当中时</strong>，我便可以用<code>set</code>&gt;对应题目：力扣349、202</p><p>对于<code>map</code>而言，它有<code>key</code>值与<code>value</code>值的概念，并且它是按<code>key</code>值进行排序的。如果涉及到<strong>一个数字出现的次数等需要记录两个数据的情况</strong>，<code>set</code>便不再适用</p><p>用<code>map</code>的话一般是用<code>key</code>值表示<strong>数组元素</strong>，用<code>value</code>值表示<strong>数组下标</strong>，在这种情况下，如果不要求数组元素有序的话可以选用<code>unordered_map</code>（因为<code>map</code>与<code>mutilmap</code>均会对<code>key</code>值进行排序）</p><h1 id="字符串">字符串</h1><p>字符串跟数组是一样的，<code>vector&lt;char&gt;</code>跟<code>string</code>并没有区别，只不过<code>string</code>类增加了很多对字符的操作</p><p>正因为字符串跟数组一样的特性，因此需要删除数组当中<strong>特定元素</strong>的话可以直接用<strong>双指针</strong></p><blockquote><p>力扣27</p><p>给你一个数组 nums 和一个值 val，你需要 原地 移除所有数值等于 val的元素，并返回移除后数组的新长度。</p><p>不要使用额外的数组空间，你必须仅使用 O(1) 额外空间并 原地修改输入数组。</p><p>元素的顺序可以改变。你不需要考虑数组中超出新长度后面的元素。</p></blockquote><p>下面给出我的思路：</p><blockquote><p>一个最直接的想法是直接一层循环，当我找到特定元素之后再在里面套一层循环来更新数组（把<strong>后面的数字</strong>往前面移动，不过这<strong>一层循环需要把整个数组遍历一遍</strong>）。当内层循环结束之后，外层指针加一，不断循环这一过程。</p></blockquote><p>可以看到，这里增加时间复杂度的主要原因是<strong>只要数组当中出现了特定值，我就需要把数组的后面部分整个往前移动</strong>。也就是说我们移动的是后面的整个部分，即从<strong>出现特定值</strong>之后就要开始移动了。而两层循环实际上就是两个指针，在没有出现特定值的时候，两个指针同时移动。如果一旦出现特定值，我们让内层指针跳过这个特定值之后不断地对外层指针进行赋值，这样便实现了数组的移动。处于统一操作的需要，我们让两个指针同时移动的时候也直接赋值。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>   <span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">removeElement</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> val)</span> </span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> left = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> right = <span class="hljs-number">0</span>; right &lt; nums.<span class="hljs-built_in">size</span>(); right++)<br>&#123;<br><span class="hljs-keyword">if</span> (nums[right] != val)<br>nums[left++] = nums[right];<br>&#125;<br><span class="hljs-keyword">return</span> left;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><h2 id="kmp算法">KMP算法</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">strStr</span><span class="hljs-params">(string haystack, string needle)</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span> (needle.<span class="hljs-built_in">size</span>() == <span class="hljs-number">0</span>)<br>            <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>        <span class="hljs-type">int</span> next[<span class="hljs-number">10000</span>] = &#123; <span class="hljs-number">0</span> &#125;;<br>        <span class="hljs-built_in">GetNext</span>(next, needle);<br>        <span class="hljs-type">int</span> j = <span class="hljs-number">0</span>;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; haystack.<span class="hljs-built_in">size</span>(); i++)<br>        &#123;<br>            <span class="hljs-keyword">while</span> (j &gt; <span class="hljs-number">0</span> &amp;&amp; needle[j] != haystack[i])<br>                j = next[j - <span class="hljs-number">1</span>];<br>            <span class="hljs-keyword">if</span> (needle[j] == haystack[i])<br>                j++;<br>            <span class="hljs-keyword">if</span> (j == needle.<span class="hljs-built_in">size</span>())<br>                <span class="hljs-keyword">return</span> i - needle.<span class="hljs-built_in">size</span>() + <span class="hljs-number">1</span>;<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">GetNext</span><span class="hljs-params">(<span class="hljs-type">int</span>* next, string&amp; s)</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-type">int</span> j = <span class="hljs-number">0</span>;<br>        next[<span class="hljs-number">0</span>] = j;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt; s.<span class="hljs-built_in">size</span>(); i++)<span class="hljs-comment">//j表示前缀字符串的末尾，i表示后缀字符串的末尾</span><br>        &#123;<br>            <span class="hljs-keyword">while</span> (j &gt; <span class="hljs-number">0</span> &amp;&amp; s[j] != s[i])<br>                j = next[j - <span class="hljs-number">1</span>];<span class="hljs-comment">//j跳到j前面(不包括j)的字符串中最大相同前后缀的值所对应的字符处，相当于减小前缀的长度</span><br>                                <span class="hljs-comment">//为什么是按照next数组的值来进行跳动?</span><br>                                <span class="hljs-comment">//由于next数组里面的值均为该字符之前(包括该字符)的字符串当中最大相同前后缀的值，这样我便可以直接跳过</span><br>                                <span class="hljs-comment">//重复段(前后缀相同)</span><br>            <span class="hljs-keyword">if</span> (s[j] == s[i])<br>                j++;<br>            next[i] = j;<span class="hljs-comment">//将前缀长度赋给i, j+1表示前缀长度</span><br>        &#125;<br><br>    &#125;<br>&#125;;<br><br></code></pre></td></tr></table></figure><h1 id="栈与队列">栈与队列</h1><h2 id="概述">概述</h2><p>在进入栈与队列前，我们需要先回答四个问题</p><ol type="1"><li><p>C++中的stack是容器吗</p></li><li><p>我们使用的stack是属于哪个版本的<code>STL</code></p></li><li><p>我们使用的<code>STL</code>中的stack是如何实现的</p></li><li><p>stack提供迭代器来遍历stack空间吗</p></li></ol><p>首先我们先介绍三个最普遍的<code>STL</code>版本</p><ol type="1"><li><p><code>HP STL</code> 其他版本的C++<code>STL</code>，一般是以<code>HP STL</code>为蓝本实现出来的，<code>HP STL</code>是C++<code>STL</code>的第一个实现版本，而且开放源代码。</p></li><li><p><code>P.J.Plauger STL</code>由<code>P.J.Plauger</code>参照<code>HP STL</code>实现出来的，被VisualC++编译器所采用，不是开源的。</p></li><li><p><code>SGI STL</code> 由Silicon Graphics ComputerSystems公司参照<code>HP STL</code>实现，被Linux的C++编译器<code>GCC</code>所采用，<code>SGI STL</code>是开源软件，源码可读性甚高。</p></li></ol><p>我们先讨论栈</p><p>栈是先进后出，即它提供<code>push</code>和<code>pop</code>接口来实现对元素的操作，<strong>所有的元素都必须满足此性质</strong>，因此栈不提供随机访问的功能，也不提供迭代器</p><p>其次，<strong>栈是以底层容器完成其所有的工作，对外统一提供接口，底层容器是可以替换的（也就是说我们可以选择使用哪种容器来实现栈的功能）</strong></p><p>基于此，<code>STL</code>中的栈往往不被归类为容器，而是<code>container adapter</code>（容器适配器）</p><p>那么问题是，<code>STL</code>中的栈的底层实现是用什么容器</p><p>实际上，栈的底层实现可以是<code>vector</code>、<code>deque</code>、<code>list</code>，主要是数组与链表</p><p>我们常用的<code>SGI STL</code>，如果没有指定底层实现的话默认是用<code>deque</code>为底层结构</p><p>当然，我们也可以指定其底层实现为<code>vector</code></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">std::stack&lt;<span class="hljs-type">int</span>, vector&lt;<span class="hljs-type">int</span>&gt; &gt;s;<br></code></pre></td></tr></table></figure><p><code>STL</code>中的<code>list</code>与<code>stack</code>一样，默认是用<code>deque</code>来实现队列功能，指定其底层实现也跟<code>stack</code>一样</p><h2 id="string与vector跟stack的联系">string与vector跟stack的联系</h2><ul><li><p><code>string</code>跟<code>vector</code>一样，都支持<code>push_back</code>与<code>pop_back</code>的操作</p></li><li><p><code>push_back</code>与<code>pop_back</code>分别是尾插和尾删，即我们可以认为<code>vector</code>是一个<strong>支持随机访问的栈</strong>，<strong>栈顶元素为最后一个元素</strong></p></li></ul><h2 id="单调队列">单调队列</h2><p>我们定义这样一个队列：<strong>队列内的元素一定单调增或单调减</strong>，这样的队列称之为单调队列</p><p>单调队列的底层容器我们默认用<code>deque</code>容器来实现</p><p>值得注意的是，单调队列中不一定<code>push</code>就不能删除队列中的元素，<code>pop</code>就不能增加队列当中的元素。即<code>push</code>和<code>pop</code>函数的定义可以是</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">push</span><span class="hljs-params">(<span class="hljs-type">int</span> val)</span></span>;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">pop</span><span class="hljs-params">(<span class="hljs-type">int</span> val)</span></span>;<br></code></pre></td></tr></table></figure><p><code>push</code>表示添加值为<code>val</code>的元素，<code>pop</code>表示删除值为<code>val</code>的容器</p><p>由于单调队列的实现各不相同，因此这里我们给出一种具体实现</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">humdrumqueue</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br>    deque&lt;<span class="hljs-type">int</span>&gt;deq;<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">pop</span><span class="hljs-params">(<span class="hljs-type">int</span> value)</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span> (!deq.<span class="hljs-built_in">empty</span>() &amp;&amp; deq.<span class="hljs-built_in">front</span>() == value)<br>            deq.<span class="hljs-built_in">pop_front</span>();<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">push</span><span class="hljs-params">(<span class="hljs-type">int</span> value)</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">while</span> (!deq.<span class="hljs-built_in">empty</span>() &amp;&amp; deq.<span class="hljs-built_in">back</span>() &lt; value)<br>            deq.<span class="hljs-built_in">pop_back</span>();<br>        deq.<span class="hljs-built_in">push_back</span>(value);<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">front</span><span class="hljs-params">()</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">return</span> deq.<span class="hljs-built_in">front</span>();<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><h3 id="关于vector的补充">关于vector的补充</h3><p><code>vector</code>中的<code>insert</code>和<code>emplace</code>都可以插入元素</p><ul><li><p><code>insert</code>的插入是先将该元素单独生成，然后再复制到指定位置，即会调用该对象的<strong>构造函数</strong>和<strong>拷贝构造函数</strong></p></li><li><p><code>emplace</code>是直接在指定位置生成该元素，只调用<strong>构造函数</strong></p></li></ul><p>因此<code>emplace</code>的速度快于<code>insert</code></p><p><code>push_back</code>同理</p><h1 id="二叉树">二叉树</h1><h2 id="递归的写法">递归的写法</h2><p>二叉树经常会涉及到递归的问题，在这里我们详细总结一下递归的写法</p><ul><li><p><strong>确定递归函数的参数与返回值</strong>。我们需要知道在递归中哪些<strong>参数是需要不断改变的</strong>，那么就将这些参数放到递归函数里面。同理，我们需要明确每次<strong>递归的返回值</strong>是什么进而确定<strong>递归函数的返回值</strong>（递归的返回值与递归函数的返回值并不相同）</p></li><li><p><strong>确定终止条件</strong></p></li><li><p><strong>确定单层递归的逻辑</strong>。这里的单层递归指的是递归函数每次重复使用的部分</p></li></ul><h2 id="二叉树的翻转问题">二叉树的翻转问题</h2><blockquote><p>给你一棵二叉树的根节点 root ，翻转这棵二叉树，并返回其根节点</p></blockquote><p>这道题我们既可以用递归，也可以用迭代</p><p>递归的话只支持<strong>先序和后序</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">TreeNode* <span class="hljs-title">invertTree</span><span class="hljs-params">(TreeNode* root)</span> </span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> root;<br>    <span class="hljs-built_in">swap</span>(root-&gt;left, root-&gt;right);  <span class="hljs-comment">// 中</span><br>    <span class="hljs-built_in">invertTree</span>(root-&gt;left);         <span class="hljs-comment">// 左</span><br>    <span class="hljs-built_in">invertTree</span>(root-&gt;right);        <span class="hljs-comment">// 右</span><br>    <span class="hljs-keyword">return</span> root;<br>&#125;<br></code></pre></td></tr></table></figure><p>迭代的话我们用统一迭代法，<strong>前中后三种都可以</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">TreeNode* <span class="hljs-title">invertTree</span><span class="hljs-params">(TreeNode* root)</span> </span><br><span class="hljs-function"></span>&#123;<br>    stack&lt;TreeNode*&gt; st;<br>    <span class="hljs-keyword">if</span> (root != <span class="hljs-literal">NULL</span>) st.<span class="hljs-built_in">push</span>(root);<br>    <span class="hljs-keyword">while</span> (!st.<span class="hljs-built_in">empty</span>()) &#123;<br>        TreeNode* node = st.<span class="hljs-built_in">top</span>();<br>        <span class="hljs-keyword">if</span> (node != <span class="hljs-literal">NULL</span>) &#123;<br>            st.<span class="hljs-built_in">pop</span>();<br>            <span class="hljs-keyword">if</span> (node-&gt;right) st.<span class="hljs-built_in">push</span>(node-&gt;right);  <span class="hljs-comment">// 右</span><br>            <span class="hljs-keyword">if</span> (node-&gt;left) st.<span class="hljs-built_in">push</span>(node-&gt;left);    <span class="hljs-comment">// 左</span><br>            st.<span class="hljs-built_in">push</span>(node);                          <span class="hljs-comment">// 中</span><br>            st.<span class="hljs-built_in">push</span>(<span class="hljs-literal">NULL</span>);<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            st.<span class="hljs-built_in">pop</span>();<br>            node = st.<span class="hljs-built_in">top</span>();<br>            st.<span class="hljs-built_in">pop</span>();<br>            <span class="hljs-built_in">swap</span>(node-&gt;left, node-&gt;right);          <span class="hljs-comment">// 节点处理逻辑</span><br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> root;<br>&#125;<br></code></pre></td></tr></table></figure><p>相应地，我们也可以用层次遍历来实现，原理跟迭代相同</p><blockquote><p>为什么前中后三种顺序均可以</p></blockquote><p>迭代的是用栈来实现的，那么在不考虑将元素出栈的条件下，我将所有的节点全部压入栈中，然后挨个交换它们的左右孩子。我们发现，这个过程是不涉及<strong>节点之间的先后顺序的</strong>，因此前中后三种顺序都可以</p><blockquote><p>为什么不能用中序递归</p></blockquote><p>上面递归调用中对应的中序遍历的代码为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">TreeNode* <span class="hljs-title">invertTree</span><span class="hljs-params">(TreeNode* root)</span> </span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> root;<br><br>    <span class="hljs-built_in">invertTree</span>(root-&gt;left);         <span class="hljs-comment">// 左</span><br>    <span class="hljs-built_in">swap</span>(root-&gt;left, root-&gt;right);  <span class="hljs-comment">// 中</span><br>    <span class="hljs-built_in">invertTree</span>(root-&gt;right);        <span class="hljs-comment">// 右</span><br>    <span class="hljs-keyword">return</span> root;<br>&#125;<br></code></pre></td></tr></table></figure><p>不难发现，我会先遍历某个节点的<strong>左指针</strong>，此时<strong>左指针指向左孩子</strong>。</p><p>然后我会交换了该节点的左右孩子，此时<strong>原先的左指针指向的是右孩子，原先的右指针指向的是左孩子</strong>。</p><p>接着，我再去遍历该节点<strong>右指针</strong>，我们会发现此时的<strong>右指针指向的是左孩子</strong>，这里就会导致该节点<strong>右孩子遍历不到的问题</strong></p><p>实际上，我们的代码应该这么写：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">TreeNode* <span class="hljs-title">invertTree</span><span class="hljs-params">(TreeNode* root)</span> </span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> root;<br><br>    <span class="hljs-built_in">invertTree</span>(root-&gt;left);         <span class="hljs-comment">// 左</span><br>    <span class="hljs-built_in">swap</span>(root-&gt;left, root-&gt;right);  <span class="hljs-comment">// 中</span><br>    <span class="hljs-built_in">invertTree</span>(root-&gt;left);         <span class="hljs-comment">// 左</span><br>    <span class="hljs-keyword">return</span> root;<br>&#125;<br></code></pre></td></tr></table></figure><p>这里要说明一点，我们通常说的遍历左孩子、右孩子只是简单的表述，实际上应该是<strong>通过左指针去遍历左孩子</strong>，<strong>通过右指针去遍历右孩子</strong></p><h2 id="二叉树最大最小深度问题">二叉树最大、最小深度问题</h2><p>关于这个问题，用层次遍历可以很容易得出结果</p><p>最大深度就遍历完所有层，最小深度就是当遍历到叶子节点的时候之间返回</p><p>我们重点分析递归</p><p>先讨论最大深度的问题</p><p>按照上面的递归三部曲，我们需要确定的是单层递归的逻辑</p><p>对于一个节点而言，我们的做法是<strong>先得到其左右子树的高度</strong>，再加一，最后再返回</p><p>这里<strong>先得到其左右子树的高度</strong>，实际上就是指我们要用到<strong>后序遍历</strong></p><p>想到这里代码也就不难写了</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">maxDepth</span><span class="hljs-params">(TreeNode* root)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>    <span class="hljs-type">int</span> right = <span class="hljs-built_in">maxDepth</span>(root-&gt;right);<br>    <span class="hljs-type">int</span> left = <span class="hljs-built_in">maxDepth</span>(root-&gt;left);<br>    <span class="hljs-keyword">return</span> right &gt; left ? right + <span class="hljs-number">1</span> : left + <span class="hljs-number">1</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>我们可以简单一点，两行解决</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">maxdepth</span><span class="hljs-params">(treenode* root)</span> </span>&#123;<br>    <span class="hljs-keyword">if</span> (root == null) <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">1</span> + <span class="hljs-built_in">max</span>(<span class="hljs-built_in">maxdepth</span>(root-&gt;left), <span class="hljs-built_in">maxdepth</span>(root-&gt;right));<br>&#125;<br></code></pre></td></tr></table></figure><p>对于最小深度，实际上我们递归的逻辑是不变的，也是后序遍历，只不过在返回的时候会有改变</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">minDepth</span><span class="hljs-params">(TreeNode* root)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>    <span class="hljs-type">int</span> left = <span class="hljs-built_in">minDepth</span>(root-&gt;left);<br>    <span class="hljs-type">int</span> right = <span class="hljs-built_in">minDepth</span>(root-&gt;right);<br>    <span class="hljs-keyword">if</span> (root-&gt;left == <span class="hljs-literal">NULL</span> &amp;&amp; root-&gt;right != <span class="hljs-literal">NULL</span>)<span class="hljs-comment">//左边为空，递归向右拐</span><br>        <span class="hljs-keyword">return</span> right + <span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">if</span> (root-&gt;left != <span class="hljs-literal">NULL</span> &amp;&amp; root-&gt;right == <span class="hljs-literal">NULL</span>)<span class="hljs-comment">//右边为空，递归向左拐</span><br>        <span class="hljs-keyword">return</span> left + <span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">1</span> + <span class="hljs-built_in">min</span>(left, right);<br>&#125;<br></code></pre></td></tr></table></figure><p>对于一个节点，如果我已知它的左右子树的高度，我需要取的是<strong>二者的较小值</strong>，但这里有个问题是如果一个节点有某一个子树为空，就会导致该节点向上返回的时候必定返回0，因此我们在这种情况下要让递归转弯</p><h2 id="关于二叉树的递归与回溯问题">关于二叉树的递归与回溯问题</h2><p>在二叉树递归的时候，<strong>如果我是需要遍历整一颗树，那么我的递归函数就不能有返回值</strong>，<strong>如果仅仅是遍历某一条线路，则我的递归函数就一定有返回值</strong></p><p>二叉树的递归往往隐藏着回溯（如果代码简短的话）。事实上，有递归的地方就一定会有回溯，只是看这个回溯要如何理解</p><p>对于有返回值的函数，<strong>其回溯往往指的是该函数的返回值</strong>。（这种情况下我们通常是用<strong>后序遍历</strong>这个二叉树）这个时候我已经得到<strong>在该节点上我所需要得到的信息</strong>，之后我再对这些信息进行操作</p><p>对于没有返回值的函数，<strong>其回溯会体现在该函数的参数部分</strong>。这时的回溯我们可以理解为：我先递归往下走，<strong>最后我一定会回到我目前这个位置</strong>，即我最终还是对这个位置所能获取的信息进行处理。</p><blockquote><p>给定一个二叉树的 根节点root，请找出该二叉树的<strong>最底层最左边</strong>节点的值</p></blockquote><p>我们先用递归来解决这个问题。首先我们需要找到<strong>最底层最左边</strong>的节点，既然是最底层，那么我们可以通过比较深度来实现</p><p>只要当前深度比目前已记录的最大深度大，我们就赋值（对深度和可能满足题目条件的节点）</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> <br>&#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-type">int</span> Maxdep = INT32_MIN;<br>    <span class="hljs-type">int</span> val = <span class="hljs-number">0</span>;<br>    <br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Travel</span><span class="hljs-params">(TreeNode* root, <span class="hljs-type">int</span> depth)</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>)<br>            <span class="hljs-keyword">return</span>;<br>        <span class="hljs-keyword">if</span> (depth &gt; Maxdep)<br>        &#123;<br>            Maxdep = depth;<br>            val = root-&gt;val;<br>        &#125;<br>        <span class="hljs-keyword">if</span> (root-&gt;left)<br>            <span class="hljs-built_in">Travel</span>(root-&gt;left, depth + <span class="hljs-number">1</span>);<span class="hljs-comment">//回溯隐藏在这里</span><br>        <span class="hljs-keyword">if</span> (root-&gt;right)<br>            <span class="hljs-built_in">Travel</span>(root-&gt;right, depth + <span class="hljs-number">1</span>);<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">findBottomLeftValue</span><span class="hljs-params">(TreeNode* root)</span> </span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-built_in">Travel</span>(root, <span class="hljs-number">0</span>);<br>        <span class="hljs-keyword">return</span> val;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>上面这个代码实际上是<strong>先序遍历</strong></p><blockquote><p>这个代码能够保证一定得到的就是最左边的节点的值吗</p></blockquote><p>我最后的返回值是<code>val</code>，我是先对该节点的左子树进行遍历并且我只在高度更新的时候才对<code>val</code>赋值。如果最底层有多个叶子，由于高度没变，因此也不会赋值给<code>val</code></p><hr><ul><li>当我需要遍历二叉树的一个路径的时候，我们递归函数的返回值可以用<code>bool</code>类型。我们将该函数放在<strong>if的判断部分</strong>，对于它的终止条件，我们<strong>只有在条件成立的情况下才返回<code>true</code>，否则都返回<code>false</code></strong></li></ul><p>通常函数递归的逻辑如下</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(递归函数(root-&gt;left))<br>    <span class="hljs-keyword">return</span>;<br><span class="hljs-keyword">if</span>(递归函数(root-&gt;right))<br>    <span class="hljs-keyword">return</span>;<br></code></pre></td></tr></table></figure><p>函数返回为</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span>(条件成立)<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span><br><span class="hljs-comment">//内容</span><br>    <br><span class="hljs-comment">//最后</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br></code></pre></td></tr></table></figure><ul><li>当我们递归整颗二叉树时，我们用回溯的写法如下</li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp">返回值类型 left = 递归函数(root-&gt;left);<br>返回值类型 right = 递归函数(root-&gt;right);<br><span class="hljs-comment">//下面对left与right进行逻辑处理</span><br></code></pre></td></tr></table></figure><p>注：</p><ul><li>回溯相当于是将这棵树<strong>自下而上</strong>开始遍历，这对应<strong>后序遍历</strong></li><li>如果要<strong>自上而下</strong>开始遍历这棵树，则用到<strong>先序遍历</strong></li></ul><h2 id="递归函数返回值问题">递归函数返回值问题</h2><p>关于这个问题，我们总结以下几点：</p><ul><li>我需要<strong>遍历整棵树</strong>并且我<strong>不需要</strong>对递归的结果进行处理，那么我不用写返回值</li></ul><p>参考<a href="https://leetcode.cn/problems/find-bottom-left-tree-value/">513.找树左下角的值 - 力扣（LeetCode）</a></p><ul><li>我需要<strong>遍历整棵树</strong>并且我<strong>需要</strong>对递归的结果进行处理，那么我需要写返回值</li></ul><p>参考<a href="https://leetcode.cn/problems/count-complete-tree-nodes/">222.完全二叉树的节点个数 - 力扣（LeetCode）</a></p><p>由于代码简写，实际上是将返回值作为结果返回</p><ul><li>如果递归仅涉及一条路线，那么<strong>必须要有返回值</strong>并且条件合适要<strong>及时</strong>返回</li></ul><p>参考<a href="https://leetcode.cn/problems/path-sum/">112. 路径总和 -力扣（LeetCode）</a></p><p>这里我们以第三点为例进行说明，我们主要看回溯</p><blockquote><p>给你二叉树的根节点 root 和一个表示目标和的整数 targetSum。判断该树中是否存在 根节点到叶子节点的路径，这条路径上所有节点值相加等于目标和 targetSum 。如果存在，返回true ；否则，返回 false 。</p><p>叶子节点 是指没有子节点的节点。</p></blockquote><h3 id="递归写法">递归写法</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> <br>&#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">Traversal</span><span class="hljs-params">(TreeNode* root, <span class="hljs-type">int</span> ant)</span><span class="hljs-comment">//这里我们将ant的初始值设为targetsum，不从0开始是因为那样太麻烦</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span> (root-&gt;left == <span class="hljs-literal">NULL</span> &amp;&amp; root-&gt;right == <span class="hljs-literal">NULL</span> &amp;&amp; ant == <span class="hljs-number">0</span>)<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>        <span class="hljs-keyword">if</span> (root-&gt;left == <span class="hljs-literal">NULL</span> &amp;&amp; root-&gt;right == <span class="hljs-literal">NULL</span>)<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        <span class="hljs-keyword">if</span> (root-&gt;left)<br>            <span class="hljs-keyword">if</span> (<span class="hljs-built_in">Traversal</span>(root-&gt;left, ant - root-&gt;left-&gt;val))<span class="hljs-comment">//回溯体现在这里</span><br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>        <span class="hljs-comment">//实际上这里我们的代码是简写了，实际上应该是</span><br>        <span class="hljs-comment">/*</span><br><span class="hljs-comment">        if(root-&gt;left)</span><br><span class="hljs-comment">        &#123;</span><br><span class="hljs-comment">        ant -= root-&gt;left-&gt;val;</span><br><span class="hljs-comment">        if(Traversal(root-&gt;left, ant))</span><br><span class="hljs-comment">        return true;</span><br><span class="hljs-comment">        ant += root-&gt;left-&gt;val;</span><br><span class="hljs-comment">        &#125;</span><br><span class="hljs-comment">        先加后减，这才是真正的回溯</span><br><span class="hljs-comment">        */</span><br>        <span class="hljs-keyword">if</span> (root-&gt;right)<br>            <span class="hljs-keyword">if</span> (<span class="hljs-built_in">Traversal</span>(root-&gt;right, ant - root-&gt;right-&gt;val))<span class="hljs-comment">//上面的ant没有变化使得这里的ant也不会发生改变</span><br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">hasPathSum</span><span class="hljs-params">(TreeNode* root, <span class="hljs-type">int</span> targetSum)</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        <span class="hljs-keyword">return</span> <span class="hljs-built_in">Traversal</span>(root, targetSum - root-&gt;val);<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>我们着重分析当<strong>递归函数为<code>bool</code>类型的时候的情况</strong>。当递归函数为<code>bool</code>类型时，我们不能像<code>void</code>那样直接调用，而是应当将其放在<code>if</code>的条件判断内，至于<code>if</code>的返回值，则根据情况进行判断</p><h3 id="迭代写法">迭代写法</h3><p>首先这里不能用层次遍历，只能用<code>stack</code>来模拟递归</p><p>再者，与递归类似，我们需要一个值来记录每个节点对应的数值（从根节点到该节点的数值总和）</p><p>这里有一个问题是该数值必须要与节点对应</p><p>因此我们可以使用对组来解决这个问题</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">hasPathSum</span><span class="hljs-params">(TreeNode* root, <span class="hljs-type">int</span> targetSum)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>    stack&lt;pair&lt;TreeNode*, <span class="hljs-type">int</span>&gt; &gt;st;<span class="hljs-comment">//用对组来存储，前者为节点指针，后者为从根节点到该节点的路径总和</span><br>    st.<span class="hljs-built_in">push</span>(<span class="hljs-built_in">pair</span>&lt;TreeNode*, <span class="hljs-type">int</span>&gt;(root, root-&gt;val));<br>    <span class="hljs-keyword">while</span> (!st.<span class="hljs-built_in">empty</span>())<br>    &#123;<br>        pair&lt;TreeNode*, <span class="hljs-type">int</span>&gt; cur = st.<span class="hljs-built_in">top</span>();<br>        st.<span class="hljs-built_in">pop</span>();<br>        <span class="hljs-keyword">if</span> (cur.first-&gt;left == <span class="hljs-literal">NULL</span> &amp;&amp; cur.first-&gt;right == <span class="hljs-literal">NULL</span> &amp;&amp; cur.second == targetSum)<br>            <span class="hljs-comment">//是否为叶子节点是前置条件</span><br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>        <span class="hljs-keyword">if</span> (cur.first-&gt;left != <span class="hljs-literal">NULL</span>)<br>            st.<span class="hljs-built_in">push</span>(<span class="hljs-built_in">pair</span>&lt;TreeNode*, <span class="hljs-type">int</span>&gt;(cur.first-&gt;left, cur.second + cur.first-&gt;left-&gt;val));<br>        <span class="hljs-keyword">if</span> (cur.first-&gt;right != <span class="hljs-literal">NULL</span>)<br>            st.<span class="hljs-built_in">push</span>(<span class="hljs-built_in">pair</span>&lt;TreeNode*, <span class="hljs-type">int</span>&gt;(cur.first-&gt;right, cur.second + cur.first-&gt;right-&gt;val));<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="二叉排序树的分析">二叉排序树的分析</h2><p>关于二叉排序树，其中序遍历后得到的数组一定是<strong>有序的</strong></p><p>因此如果需要判断一个二叉树是不是二叉排序树，我们只能通过<strong>中序遍历</strong>来解决这个问题</p><h3 id="求任意两节点的最小差值">求任意两节点的最小差值</h3><p><a href="https://leetcode.cn/problems/minimum-absolute-difference-in-bst/">530.二叉搜索树的最小绝对差 - 力扣（LeetCode）</a></p><blockquote><p>给你一个二叉搜索树的根节点 <code>root</code> ，返回<strong>树中任意两不同节点值之间的最小差值</strong> 。</p><p>差值是一个正数，其数值等于两值之差的绝对</p></blockquote><h4 id="递归写法-1">递归写法</h4><p>递归的话我们不简要说明：先<strong>中序</strong>遍历得到数组后我们再判断该数组是否是有序的。<em>该数组的元素一定是递增的，并且我们只需要比较相邻的两个元素即可</em></p><p>这里需要说明的是，由于题目让我们返回<strong>任意两个不同节点的最小差值</strong>。如果是比较一个节点的两个孩子的话，其差值必定&gt;=2，如果比较的是该节点与它的其中一个孩子，那么差值必定&gt;=1，因此我们只需要比较数组中的相邻两个值即可</p><p>我们着重讨论迭代的情况</p><p>迭代的话可用的数据结构有stack和queue，stack是用于<strong>模拟递归的</strong>，queue是用于<strong>层序遍历的</strong></p><p>因此如果我们需要模拟递归，则用stack，如果需要层序遍历这颗二叉树，则用queue</p><p>这里我们应当是模拟递归，选用stack，并且只能用中序遍历</p><p>由于需要比较的节点有两个(当前节点cur和前一个结点pre)，因此我们需要两个指针</p><p>只要当前节点不为NULL，这就说明我们需要将<strong>该节点的左孩子压入stack中</strong>，该操作会一直进行直到cur为NULL(这里是<strong>左</strong>的操作)</p><p>如果当前节点为NULL，说明已经到达<strong>最左端</strong>，此时我们需要取出栈顶结点，将其赋值给cur(这里是<strong>中</strong>的操作)</p><p>然后我们再将cur与pre的差值与目前已记录的最小值进行比较，此时我们还需要一个变量来记录<strong>目前的最小值</strong></p><p>最后我们更新cur和pre(先pre后cur)</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">getMinimumDifference</span><span class="hljs-params">(TreeNode* root)</span></span><br><span class="hljs-function"></span>&#123;<br>    stack&lt;TreeNode*&gt;st;<br>    TreeNode* cur = root;<span class="hljs-comment">//前一个节点</span><br>    TreeNode* pre = <span class="hljs-literal">NULL</span>;<span class="hljs-comment">//当前处理节点</span><br>    <span class="hljs-type">int</span> ret = INT32_MAX;<br>    <span class="hljs-keyword">while</span> (!st.<span class="hljs-built_in">empty</span>() || cur != <span class="hljs-literal">NULL</span>)<br>    &#123;<br>        <span class="hljs-keyword">if</span> (cur != <span class="hljs-literal">NULL</span>)<br>        &#123;<br>            st.<span class="hljs-built_in">push</span>(cur);<br>            cur = cur-&gt;left;<span class="hljs-comment">//左</span><br>        &#125;<br>        <span class="hljs-keyword">else</span><br>        &#123;<br>            cur = st.<span class="hljs-built_in">top</span>();<span class="hljs-comment">//取栈顶元素，中</span><br>            st.<span class="hljs-built_in">pop</span>();<br>            <span class="hljs-keyword">if</span> (pre != <span class="hljs-literal">NULL</span>)<br>                ret = <span class="hljs-built_in">min</span>(ret, cur-&gt;val - pre-&gt;val);<br>            pre = cur;<br>            cur = cur-&gt;right;<span class="hljs-comment">//右</span><br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> ret;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="求二叉树中的众数">求二叉树中的众数</h3><p><a href="https://leetcode.cn/problems/find-mode-in-binary-search-tree/">501.二叉搜索树中的众数 - 力扣（LeetCode）</a></p><blockquote><p>给你一个含重复值的二叉搜索树（BST）的根节点 root ，找出并返回 BST中的所有 众数（即，出现频率最高的元素）。</p><p>如果树中有不止一个众数，可以按 任意顺序 返回。</p><p>假定 BST 满足如下定义：</p><ul><li>结点左子树中所含节点的值 小于等于 当前节点的值</li><li>结点右子树中所含节点的值 大于等于 当前节点的值</li><li>左子树和右子树都是二叉搜索树</li></ul></blockquote><h4 id="递归写法-2">递归写法</h4><p>我们首先讨论如何求一个数组当中的众数(不止一个众数)</p><p>我们可以先遍历一次数组，找出这个数组当中一个数出现的maxCount(最大频率)。然后我们再遍历一遍这个数组，将**出现频率等于maxCount的数放入结果数组中</p><p>上面这种方法遍历了两遍数组，当然我们也可以遍历一遍</p><p>我们定义两个变量：当前这个数的频率(count)，目前的最大频率(maxCount)。只要当前这个count等于maxCount，我们就将这个数加入到结果数组中</p><p>那么这样就会有一个问题，我目前的maxCount不是<strong>真正的最大频率</strong>怎么办</p><p>在这种情况下，我们需要更新maxCount并且将结果数组<strong>清空</strong>(这是因为之前的数全部不符合条件)</p><p>具体写法如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">if</span> (count == maxCount) <span class="hljs-comment">// 如果和最大值相同，放进result中</span><br>&#123;<br>    result.<span class="hljs-built_in">push_back</span>(cur-&gt;val);<br>&#125;<br><span class="hljs-keyword">if</span> (count &gt; maxCount) <span class="hljs-comment">// 如果计数大于最大值</span><br>&#123;<br>    maxCount = count;   <span class="hljs-comment">// 更新最大频率</span><br>    result.<span class="hljs-built_in">clear</span>();     <span class="hljs-comment">// 很关键的一步，不要忘记清空result，之前result里的元素都失效了</span><br>    result.<span class="hljs-built_in">push_back</span>(cur-&gt;val);<br>&#125;<br></code></pre></td></tr></table></figure><p>完整代码</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> <br>&#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-type">int</span> count = <span class="hljs-number">0</span>;<span class="hljs-comment">//记录目前这个数出现的次数</span><br>    <span class="hljs-type">int</span> Maxcount = <span class="hljs-number">0</span>;<span class="hljs-comment">//记录目前最大的频率</span><br>    TreeNode* pre = <span class="hljs-literal">NULL</span>;<span class="hljs-comment">//记录前一个节点</span><br><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Traversal</span><span class="hljs-params">(TreeNode* root, vector&lt;<span class="hljs-type">int</span>&gt;&amp; ret)</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>)<br>            <span class="hljs-keyword">return</span>;<br>        <span class="hljs-keyword">if</span> (root-&gt;left)<br>            <span class="hljs-built_in">Traversal</span>(root-&gt;left, ret);<br>        <span class="hljs-keyword">if</span> (pre == <span class="hljs-literal">NULL</span>)<span class="hljs-comment">//此时已到达最左端的节点</span><br>            count = <span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (root-&gt;val == pre-&gt;val)<span class="hljs-comment">//与前一个节点相同</span><br>            count++;<br>        <span class="hljs-keyword">else</span><span class="hljs-comment">//与前一个节点不同，将当前节点的频率设为1</span><br>            count = <span class="hljs-number">1</span>;<br><br>        pre = root;<span class="hljs-comment">//更新pre</span><br><br>        <span class="hljs-keyword">if</span> (count == Maxcount)<span class="hljs-comment">//目前的count已到达已记录的Maxcount</span><br>            ret.<span class="hljs-built_in">push_back</span>(root-&gt;val);<br>        <span class="hljs-keyword">if</span> (count &gt; Maxcount)<br>        &#123;<br>            ret.<span class="hljs-built_in">clear</span>();<span class="hljs-comment">//直接重置</span><br>            Maxcount = count;<span class="hljs-comment">//更新Maxcount</span><br>            ret.<span class="hljs-built_in">push_back</span>(root-&gt;val);<br>        &#125;<br>        <span class="hljs-built_in">Traversal</span>(root-&gt;right, ret);<br>    &#125;<br><br>    <span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">findMode</span><span class="hljs-params">(TreeNode* root)</span></span><br><span class="hljs-function">    </span>&#123;<br>        vector&lt;<span class="hljs-type">int</span>&gt;ret;<br>        <span class="hljs-built_in">Traversal</span>(root, ret);<br>        <span class="hljs-keyword">return</span> ret;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><h3 id="插入一个节点">插入一个节点</h3><p><a href="https://leetcode.cn/problems/insert-into-a-binary-search-tree/">701.二叉搜索树中的插入操作 - 力扣（LeetCode）</a></p><blockquote><p>给定二叉搜索树（BST）的根节点 root 和要插入树中的值 value，将值插入二叉搜索树。 返回插入后二叉搜索树的根节点。 输入数据 保证，新值和原始二叉搜索树中的任意节点值都不同。</p><p>注意，可能存在多种有效的插入方式，只要树在插入后仍保持为二叉搜索树即可。你可以返回 任意有效的结果 。</p></blockquote><h4 id="递归写法-3">递归写法</h4><p>对于二叉搜索树的<strong>插入与删除</strong>操作，我们都可以用<strong>先序遍历</strong>来实现</p><p>我们在遍历的时候只要<code>root-&gt;val &gt; key</code>，那么我们就<strong>向右走</strong>，只要<code>root-&gt;val &lt; key</code>，我们就<strong>向左走</strong></p><p>那么这里有一个问题是，我们该如何具体实现向右走和向左走的代码呢</p><p>由于我们用的是递归来实现，那么递归就一定会有返回值。<strong>当这一层的逻辑处理完之后将此节点返回上去</strong>，在调用递归函数的那一层我们可以用该节点的<strong>左孩子或右孩子接住其返回值</strong>，这样便实现了向左向右走</p><p>具体代码如下</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">TreeNode* <span class="hljs-title">insertIntoBST</span><span class="hljs-params">(TreeNode* root, <span class="hljs-type">int</span> val)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>)<br>    &#123;<br>        TreeNode* cur = <span class="hljs-keyword">new</span> <span class="hljs-built_in">TreeNode</span>(val);<br>        <span class="hljs-keyword">return</span> cur;<br>    &#125;<br>    <span class="hljs-keyword">if</span> (root-&gt;val &gt; val)<br>        root-&gt;left = <span class="hljs-built_in">insertIntoBST</span>(root-&gt;left, val);<br>    <span class="hljs-keyword">if</span> (root-&gt;val &lt; val)<br>        root-&gt;right = <span class="hljs-built_in">insertIntoBST</span>(root-&gt;right, val);<br>    <span class="hljs-keyword">return</span> root;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="迭代写法-1">迭代写法</h4><p>当然，我们也可以用迭代来实现，这就是对指针进行具体的赋值，很简单，直接上代码</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">TreeNode* <span class="hljs-title">insertIntoBST</span><span class="hljs-params">(TreeNode* root, <span class="hljs-type">int</span> val)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>)<br>    &#123;<br>        TreeNode* cur = <span class="hljs-keyword">new</span> <span class="hljs-built_in">TreeNode</span>(val);<br>        <span class="hljs-keyword">return</span> cur;<br>    &#125;<br>    TreeNode* pre = <span class="hljs-literal">NULL</span>;<br>    TreeNode* cur = root;<br>    <span class="hljs-keyword">while</span> (cur != <span class="hljs-literal">NULL</span>)<br>    &#123;<br>        pre = cur;<br>        <span class="hljs-keyword">if</span> (cur-&gt;val &lt; val)<br>            cur = cur-&gt;right;<br>        <span class="hljs-keyword">else</span> cur = cur-&gt;left;<br>    &#125;<br>    TreeNode* node = <span class="hljs-keyword">new</span> <span class="hljs-built_in">TreeNode</span>(val);<br>    <span class="hljs-keyword">if</span> (pre-&gt;val &lt; val)<br>        pre-&gt;right = node;<br>    <span class="hljs-keyword">else</span> pre-&gt;left = node;<br>    <span class="hljs-keyword">return</span> root;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="删除一个节点">删除一个节点</h3><p><a href="https://leetcode.cn/problems/delete-node-in-a-bst/">450.删除二叉搜索树中的节点 - 力扣（LeetCode）</a></p><blockquote><p>给定一个二叉搜索树的根节点 root 和一个值 key，删除二叉搜索树中的 key对应的节点，并保证二叉搜索树的性质不变。返回二叉搜索树（有可能被更新）的根节点的引用。</p><p>一般来说，删除节点可分为两个步骤：</p><ol type="1"><li>首先找到需要删除的节点；</li><li>如果找到了，删除它。</li></ol></blockquote><h4 id="递归写法-4">递归写法</h4><p>删除一个节点我们会有五种情况</p><ul><li><p>此节点为<code>NULL</code>的情况</p><ol type="1"><li>直接返回<code>NULL</code></li></ol></li><li><p>此节点<code>非NULL</code>的情况</p><ol start="2" type="1"><li><p>左右孩子均为<code>NULL</code>。直接删除该节点并返回<code>NULL</code></p></li><li><p>左孩子不为<code>NULL</code>，右孩子为<code>NULL</code>。删除该节点，返回左孩子</p></li><li><p>左孩子为<code>NULL</code>，右孩子不为<code>NULL</code>。删除该节点，返回右孩子</p></li><li><p>左右孩子均不为<code>NULL</code>。我们找到右子树中最靠左边的节点，将整个左子树挂在该节点的左孩子上</p></li></ol></li></ul><p>我们着重分析第五种情况</p><p>由于二叉搜索树的特性，一个节点的<strong>左孩子的所有值一定小于该节点</strong>，<strong>右节点的所有值一定大于该节点</strong></p><p><strong>对于任意一个节点，均有此性质</strong>，那么我们便可以导出如下结论：</p><ul><li><p>对于一颗子树，它的<strong>最小值一定在最左边的节点</strong>，<strong>最大值一定在最右边的节点</strong></p></li><li><p>对于一个节点的左右子树而言，<strong>左子树的所有值一定小于该节点的值</strong>，<strong>右子树的所有值一定大于该节点的值</strong></p></li></ul><p>基于这两点，如果我们想删除一个节点的话（该节点的左右孩子均存在），我们可以将<strong>该节点的左子树接到该节点右子树中最靠左边的节点的左孩子上</strong></p><p>这里有一点要注意的是：最靠左，说明不能够向右走</p><p>具体代码如下</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">TreeNode* <span class="hljs-title">deleteNode</span><span class="hljs-params">(TreeNode* root, <span class="hljs-type">int</span> key)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">NULL</span>)<span class="hljs-comment">//Preorder Traversal</span><br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;<br>    <span class="hljs-keyword">if</span> (root-&gt;val == key)<br>    &#123;<br>        <span class="hljs-keyword">if</span> (root-&gt;left == <span class="hljs-literal">NULL</span> &amp;&amp; root-&gt;right == <span class="hljs-literal">NULL</span>)<br>        &#123;<br>            <span class="hljs-keyword">delete</span> root;<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;<br>        &#125;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (root-&gt;left == <span class="hljs-literal">NULL</span> &amp;&amp; root-&gt;right != <span class="hljs-literal">NULL</span>)<br>        &#123;<br>            TreeNode* cur = root-&gt;right;<br>            <span class="hljs-keyword">delete</span> root;<br>            <span class="hljs-keyword">return</span> cur;<br>        &#125;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (root-&gt;left != <span class="hljs-literal">NULL</span> &amp;&amp; root-&gt;right == <span class="hljs-literal">NULL</span>)<br>        &#123;<br>            TreeNode* cur = root-&gt;left;<br>            <span class="hljs-keyword">delete</span> root;<br>            <span class="hljs-keyword">return</span> cur;<br>        &#125;<br>        <span class="hljs-keyword">else</span><br>        &#123;<br>            TreeNode* cur = root-&gt;right;<br>            <span class="hljs-keyword">while</span> (cur-&gt;left)<br>                cur = cur-&gt;left;<br>            cur-&gt;left = root-&gt;left;<br>            TreeNode* node = root-&gt;right;<br>            <span class="hljs-keyword">delete</span> root;<br>            <span class="hljs-keyword">return</span> node;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">if</span> (root-&gt;val &lt; key)<br>        root-&gt;right = <span class="hljs-built_in">deleteNode</span>(root-&gt;right, key);<br>    <span class="hljs-keyword">if</span> (root-&gt;val &gt; key)<br>        root-&gt;left = <span class="hljs-built_in">deleteNode</span>(root-&gt;left, key);<br>    <span class="hljs-keyword">return</span> root;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="普通二叉树的删除">普通二叉树的删除</h4><p>对于一个待删除的节点，我们首先先将该节点与<strong>其右子树中最靠左边的节点交换数组</strong>（第一次操作）</p><p>然后，我们第二次递归到该节点的时候删除该节点，如果此时该节点的右孩子不为空，则<strong>重复这两步</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">TreeNode* <span class="hljs-title">deleteNode</span><span class="hljs-params">(TreeNode* root, <span class="hljs-type">int</span> key)</span> </span>&#123;<br>    <span class="hljs-keyword">if</span> (root == <span class="hljs-literal">nullptr</span>) <span class="hljs-keyword">return</span> root;<br>    <span class="hljs-keyword">if</span> (root-&gt;val == key) &#123;<br>        <span class="hljs-keyword">if</span> (root-&gt;right == <span class="hljs-literal">nullptr</span>) &#123; <span class="hljs-comment">// 这里第二次操作目标值：最终删除的作用</span><br>            <span class="hljs-keyword">return</span> root-&gt;left;<br>        &#125;<br>        TreeNode *cur = root-&gt;right;<br>        <span class="hljs-keyword">while</span> (cur-&gt;left) &#123;<br>            cur = cur-&gt;left;<br>        &#125;<br>        <span class="hljs-built_in">swap</span>(root-&gt;val, cur-&gt;val); <span class="hljs-comment">// 这里第一次操作目标值：交换目标值其右子树最左面节点。</span><br>    &#125;<br>    root-&gt;left = <span class="hljs-built_in">deleteNode</span>(root-&gt;left, key);<br>    root-&gt;right = <span class="hljs-built_in">deleteNode</span>(root-&gt;right, key);<br>    <span class="hljs-keyword">return</span> root;<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="回溯">回溯</h1><h2 id="组合分割子集问题">组合、分割、子集问题</h2><p>回溯实际上是一种暴力搜索的方法，对于回溯的理解，我们可以将其类比成一个n叉树。树的深度表示递归的层数，树的宽度（叶子节点）表示所有可能出现的结果。</p><p>明白这一点后，简单的回溯代码也就会写了。那么我们更进一步，在回溯的基础上考虑<strong>去重</strong></p><p>首先对于这颗 n 叉树而言（ <em>各个数已经排好序</em>），<strong>一条树枝表示一个结果</strong>，<strong>同一层的各个节点表示上一次或者下一次所选择的数</strong>。那么我们的剪枝就有两种情况：对于第一种就是结果集合<strong>不能重复</strong>，对于第二种就是<strong>在这一层上不能两次都选择相同的数</strong></p><p>具体看下面这道题</p><p><a href="https://leetcode.cn/problems/combination-sum-ii/">40.组合总和 II - 力扣（LeetCode）</a></p><blockquote><p>给定一个候选人编号的集合 candidates 和一个目标数 target ，找出candidates 中所有可以使数字和为 target 的组合。</p><p>candidates 中的每个数字在每个组合中只能使用 一次 。</p><p>注意：解集不能包含重复的组合。</p></blockquote><p>这道题里面，结果集合当中可以有重复的数字，但不允许存在两个相同的解集，<strong>即在同一层上不允许选择同一个数</strong></p><p>那么我们该如何去重呢？一个很自然的想法就是只要<code>candidates[i-1] == candidates[i]</code> 我们便 continue 。</p><p>但如果我们这么做的话就会导致在<strong>解集当中也不允许出现相同的数字了</strong>，因为当我递归到下一层的时候，初始的位置相对于这一层而言就是<code>[i+1]</code>，这会导致下一层会直接跳过这个数字</p><p>那么这个地方，其实我们可以用一个 bool类型的数组来表示每个数字是否被使用过。</p><p>初始时， used 数组的值都是 false ，在我进入递归的时候将其设置为 true，那么在下一层的递归函数看来就表示<code>[i-1]</code>已经使用过了，此时<code>used[i-1]</code>为true。递归会一直向下执行，知道这个数所对应的全部结果都搜寻完毕。</p><p>之后就会走到回溯代码，也就是说此时<code>used[i-1]</code>又会变为false，<strong>这里表示在这一层而言，上一个数字已经被选过了</strong>。而对于已经排好序的数组而言，上一个数字跟这个数字相同的情况下自然就可以跳过当前这个数</p><p>具体代码如下</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    vector&lt;<span class="hljs-type">int</span>&gt;path;<br>    vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;ret;<br><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">backtracking</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; candidates, <span class="hljs-type">int</span> target, <span class="hljs-type">int</span> start, <span class="hljs-type">int</span> sum, vector&lt;<span class="hljs-type">bool</span>&gt;&amp; used)</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span> (sum == target)<br>        &#123;<br>            ret.<span class="hljs-built_in">push_back</span>(path);<br>            <span class="hljs-keyword">return</span>;<br>        &#125;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = start; i &lt; candidates.<span class="hljs-built_in">size</span>() &amp;&amp; sum + candidates[i] &lt;= target; i++)<br>        &#123;<br>            <span class="hljs-keyword">if</span> (i &gt; <span class="hljs-number">0</span> &amp;&amp; candidates[i] == candidates[i - <span class="hljs-number">1</span>] &amp;&amp; !used[i - <span class="hljs-number">1</span>])<br>            &#123;<br>                <span class="hljs-keyword">continue</span>;<br>            &#125;<br>            path.<span class="hljs-built_in">push_back</span>(candidates[i]);<br>            sum += candidates[i];<br>            used[i] = <span class="hljs-literal">true</span>;<br>            <span class="hljs-built_in">backtracking</span>(candidates, target, i + <span class="hljs-number">1</span>, sum, used);<br>            used[i] = <span class="hljs-literal">false</span>;<br>            sum -= candidates[i];<br>            path.<span class="hljs-built_in">pop_back</span>();<br>        &#125;<br>        <span class="hljs-keyword">return</span>;<br>    &#125;<br><br>    vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt; <span class="hljs-built_in">combinationSum2</span>(vector&lt;<span class="hljs-type">int</span>&gt;&amp; candidates, <span class="hljs-type">int</span> target)<br>    &#123;<br>        <span class="hljs-built_in">sort</span>(candidates.<span class="hljs-built_in">begin</span>(), candidates.<span class="hljs-built_in">end</span>());<br>        vector&lt;<span class="hljs-type">bool</span>&gt;<span class="hljs-built_in">used</span>(candidates.<span class="hljs-built_in">size</span>(), <span class="hljs-literal">false</span>);<br>        <span class="hljs-built_in">backtracking</span>(candidates, target, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>, used);<br>        <span class="hljs-keyword">return</span> ret;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>需要特殊说明的是</p><ul><li><code>used[i-1] == true</code>表示在一个解集内去重，<strong>这能保证一个解集内不会出现两个一样的数字</strong></li><li><code>used[i-1] == false</code>表示在同一层去重，<strong>这能保证不会出现两个一样的解集</strong></li></ul><p><a href="https://leetcode.cn/problems/subsets/">78. 子集 -力扣（LeetCode）</a></p><blockquote><p>给你一个整数数组 <code>nums</code> ，数组中的元素<strong>互不相同</strong> 。返回该数组所有可能的子集（幂集）。</p><p>解集 <strong>不能</strong> 包含重复的子集。你可以按<strong>任意顺序</strong> 返回解集。</p></blockquote><p>写回溯算法的收集部分时，要看清楚是<strong>收集叶子</strong>还是收集<strong>各个节点</strong></p><p>如果是收集叶子，那么我们需要加上判断条件，此条件即为我们的<strong>收集条件</strong></p><p>如果是收集树的各个节点，那什么都不需要写，直接往里面压就行</p><p>具体代码如下</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    vector&lt;<span class="hljs-type">int</span>&gt;path;<br>    vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;ret;<br><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">backtracking</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> start)</span></span><br><span class="hljs-function">    </span>&#123;<br>        ret.<span class="hljs-built_in">push_back</span>(path);<span class="hljs-comment">//什么都不用管，直接往里面放，这就是收集树的各个节点</span><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = start; i &lt; nums.<span class="hljs-built_in">size</span>(); i++)<br>        &#123;<br>            path.<span class="hljs-built_in">push_back</span>(nums[i]);<br>            <span class="hljs-built_in">backtracking</span>(nums, i + <span class="hljs-number">1</span>);<br>            path.<span class="hljs-built_in">pop_back</span>();<br>        &#125;<br>        <span class="hljs-keyword">return</span>;<br>    &#125;<br><br>    vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt; <span class="hljs-built_in">subsets</span>(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums)<br>    &#123;<br>        <span class="hljs-built_in">backtracking</span>(nums, <span class="hljs-number">0</span>);<br>        <span class="hljs-keyword">return</span> ret;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><h2 id="树层去重和树枝去重">树层去重和树枝去重</h2><p>在讨论这个问题之前，我们需要明确一点：在收集阶段，无论我是收集节点还是收集树叶，<strong>这两个动作都与去重无关</strong></p><p>实际上，<strong>收集动作发生在 for循环之前</strong>，收集节点还是收集树叶的区别仅仅在于是否有 if判断。而<strong>去重的动作则发生在 for 循环之内</strong></p><p>（当然，我们假定数组已经排序完毕）实际上，树枝去重是保证<strong>结果集合当中的数字不能有重复</strong>，而树层去重则是保证<strong>不会出现两个一模一样的结果集合</strong>。</p><h2 id="排列问题">排列问题</h2><p>我们以这道题为例</p><p><a href="https://leetcode.cn/problems/permutations/">46. 全排列 -力扣（LeetCode）</a></p><blockquote><p>给定一个不含重复数字的数组 <code>nums</code> ，返回其<em>所有可能的全排列</em> 。你可以 <strong>按任意顺序</strong>返回答案。</p></blockquote><p>排列问题相对于前面不同的是：<code>nums[i]</code>后面的数字<strong>以及前面</strong>的数字都有可能从新被选到，而如果写<code>strat</code> 就选不到 <code>nums[i]</code>前面的数字。因此对于排列问题，<strong>不能写 <code>tart</code></strong>。</p><p>既然没有了 <code>start</code> ，那么对应的去重逻辑就要发生变化。原先i 会加一，因此我们看的是 <code>i - 1</code>的情况。而现在没有了<code>start</code> 的限制，i 会从 0 开始递增，<strong>只要<code>used[i]</code>是被选过的，那么我们就直接跳过</strong>。具体代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    vector&lt;<span class="hljs-type">int</span>&gt;path;<br>    vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;ret;<br><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">backtracking</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, vector&lt;<span class="hljs-type">bool</span>&gt;&amp; used)</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span> (path.<span class="hljs-built_in">size</span>() == nums.<span class="hljs-built_in">size</span>())<br>        &#123;<br>            ret.<span class="hljs-built_in">push_back</span>(path);<br>            <span class="hljs-keyword">return</span>;<br>        &#125;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.<span class="hljs-built_in">size</span>(); i++)<br>        &#123;<br>            <span class="hljs-keyword">if</span> (used[i] == <span class="hljs-literal">true</span>)<br>                <span class="hljs-keyword">continue</span>;<br>            path.<span class="hljs-built_in">push_back</span>(nums[i]);<br>            used[i] = <span class="hljs-literal">true</span>;<br>            <span class="hljs-built_in">backtracking</span>(nums, used);<br>            used[i] = <span class="hljs-literal">false</span>;<br>            path.<span class="hljs-built_in">pop_back</span>();<br>        &#125;<br>    &#125;<br>    vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt; <span class="hljs-built_in">permute</span>(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums)<br>    &#123;<br>        vector&lt;<span class="hljs-type">bool</span>&gt;<span class="hljs-built_in">used</span>(nums.<span class="hljs-built_in">size</span>(), <span class="hljs-literal">false</span>);<br>        <span class="hljs-built_in">backtracking</span>(nums, used);<br>        <span class="hljs-keyword">return</span> ret;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>上面代码中 <code>used[i] == true</code>的意思是：<strong>下一层不要选到与上一层相同的数</strong></p><p>上面这道题比较简单，毕竟元素没有重复。我们来看一道元素重复的</p><p><a href="https://leetcode.cn/problems/permutations-ii/">47. 全排列 II- 力扣（LeetCode）</a></p><blockquote><p>给定一个可包含重复数字的序列 <code>nums</code>，<strong><em>按任意顺序</em></strong> 返回所有不重复的全排列。</p></blockquote><p>首先一点：<strong>去重，一定要排序</strong>。</p><p>其次，我们知道：树层去重可以使得结果数组中不存在两个相同的。</p><p>这里是求全排列， i 会从 0开始遍历整个数组，那么其结果数组必然不能有两个相同的，因此树层去重是必要的，即<code>used[i-1] == false</code></p><p>其次，参考没有重复的情况，我们必须保证已经选过的数字不能再选一次，即<code>used[i] == true</code>。</p><p>去重逻辑明白后，这道题就很简单了</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><code class="hljs cpp">vector&lt;<span class="hljs-type">int</span>&gt;path;<br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt;ret;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">backtracking</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, vector&lt;<span class="hljs-type">bool</span>&gt;&amp; used)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span> (path.<span class="hljs-built_in">size</span>() == nums.<span class="hljs-built_in">size</span>())<br>    &#123;<br>        ret.<span class="hljs-built_in">push_back</span>(path);<br>        <span class="hljs-keyword">return</span>;<br>    &#125;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; nums.<span class="hljs-built_in">size</span>(); i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span> ((i &gt; <span class="hljs-number">0</span> &amp;&amp; nums[i - <span class="hljs-number">1</span>] == nums[i] &amp;&amp; used[i - <span class="hljs-number">1</span>] == <span class="hljs-literal">false</span>) || used[i] == <span class="hljs-literal">true</span>)<br>            <span class="hljs-keyword">continue</span>;<br>        used[i] = <span class="hljs-literal">true</span>;<br>        path.<span class="hljs-built_in">push_back</span>(nums[i]);<br>        <span class="hljs-built_in">backtracking</span>(nums, used);<br>        path.<span class="hljs-built_in">pop_back</span>();<br>        used[i] = <span class="hljs-literal">false</span>;<br>    &#125;<br>&#125;<br><br>vector&lt;vector&lt;<span class="hljs-type">int</span>&gt;&gt; <span class="hljs-built_in">permuteUnique</span>(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums)<br>&#123;<br>    vector&lt;<span class="hljs-type">bool</span>&gt;<span class="hljs-built_in">used</span>(nums.<span class="hljs-built_in">size</span>(), <span class="hljs-literal">false</span>);<br>    <span class="hljs-built_in">backtracking</span>(nums, used);<br>    <span class="hljs-keyword">return</span> ret;<br>&#125;<br></code></pre></td></tr></table></figure><p>这里我们再补充两点：</p><ul><li>在全排列中，树层去重和树枝去重的结果相同，但前者更快</li></ul><p>对于树枝去重，我们可以想象这是一个满 n叉树，只有叶子节点当中存在相同的数，那么它就不会进入到结果数组中</p><ul><li>对于我上面写的代码，那个 <code>used[i] == true</code> 是必要的</li></ul><p>因为这个条件是为了保证下一层的 i循环到的数字与上一层所选数字相同时可以跳过</p><h2 id="n-皇后">N 皇后</h2><p><a href="https://leetcode.cn/problems/n-queens/">51. N 皇后 -力扣（LeetCode）</a></p><blockquote><p>按照国际象棋的规则，皇后可以攻击与之处在同一行或同一列或同一斜线上的棋子。</p><p>n 皇后问题 研究的是如何将 n 个皇后放置在 n×n的棋盘上，并且使皇后彼此之间不能相互攻击。</p><p>给你一个整数 n ，返回所有不同的 n 皇后问题 的解决方案。</p><p>每一种解法包含一个不同的 n 皇后问题 的棋子放置方案，该方案中 'Q' 和'.' 分别代表了皇后和空位。</p></blockquote><p>我们来看一道 N 皇后问题。</p><p>这里我想说的是<strong>回溯函数的返回值问题</strong>。</p><p>我们通常会把回溯这一过程等效为一棵 N叉树，而<strong>回溯的收集部分要么是节点，要么是叶子</strong>。</p><p>这两种情况下所对应的返回值是 <code>void</code>，自然也不需要注意。我想说的是如果我<strong>只需要这棵 N叉树的一个树枝或者其中一个叶子节点</strong>，那么我们的返回值就应该写<code>bool</code> 。</p><p>即如果我们需要找出所有的情况，则应该是 <code>void</code>，如果只找一种情况，则为 <code>bool</code> 。</p><p>显然，这里应为 <code>void</code> 。</p><p>将 N 个皇后放在 N * N的棋盘上，<strong>必须是每行放置一个</strong>，否则就会出现同一行出现两个皇后这种情况。</p><p>那么，我们定义一个变量 <code>row</code>，用来表示<strong>当前所递归的行数</strong>，而终止条件为<code>row = n</code> 。此时我们应该将结果放入结果数组当中。</p><p>遍历的时候，我们从每一行的最左边开始遍历，<strong>在保证合法的情况下进入下一层的递归</strong>。</p><p>在检验合法性的时候，我们需要对这个位置的同一行、同一列、两个斜对角进行判重</p><p>同一行这个条件可以省略，因为本身我就是一行一行遍历的。前面如果有皇后，那么这一行就结束了，而某一位置的后面不可能会有皇后（因为还没有循环到那里）。</p><p>我们着重对两个斜对角进行考虑。同样地道理，这一行以下的部分并没有放置皇后，因此我们只需要对这一行<strong>往上</strong>的两个对角进行判重即可，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    vector&lt;vector&lt;string&gt;&gt;ret;<br><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">backtarcking</span><span class="hljs-params">(vector&lt;string&gt;&amp; board, <span class="hljs-type">int</span> row, <span class="hljs-type">int</span> n)</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">if</span> (row == n)<br>        &#123;<br>            ret.<span class="hljs-built_in">push_back</span>(board);<br>            <span class="hljs-keyword">return</span>;<br>        &#125;<br><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> col = <span class="hljs-number">0</span>; col &lt; board.<span class="hljs-built_in">size</span>(); col++)<br>        &#123;<br>            <span class="hljs-keyword">if</span> (<span class="hljs-built_in">IsValid</span>(board, row, col))<br>            &#123;<br>                board[row][col] = <span class="hljs-string">&#x27;Q&#x27;</span>;<br>                <span class="hljs-built_in">backtarcking</span>(board, row + <span class="hljs-number">1</span>, n);<br>                board[row][col] = <span class="hljs-string">&#x27;.&#x27;</span>;<br>            &#125;<br>        &#125;<br>        <span class="hljs-keyword">return</span>;<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">IsValid</span><span class="hljs-params">(vector&lt;string&gt;&amp; board, <span class="hljs-type">int</span> row, <span class="hljs-type">int</span> col)</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt;= row; i++)<br>            <span class="hljs-keyword">if</span> (board[i][col] == <span class="hljs-string">&#x27;Q&#x27;</span>)<br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = row - <span class="hljs-number">1</span>, j = col - <span class="hljs-number">1</span>; i &gt;= <span class="hljs-number">0</span> &amp;&amp; j &gt;= <span class="hljs-number">0</span>; i--, j--)<br>            <span class="hljs-keyword">if</span> (board[i][j] == <span class="hljs-string">&#x27;Q&#x27;</span>)<br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = row - <span class="hljs-number">1</span>, j = col + <span class="hljs-number">1</span>; i &gt;= <span class="hljs-number">0</span> &amp;&amp; j &lt;= board.<span class="hljs-built_in">size</span>(); i--, j++)<br>            <span class="hljs-keyword">if</span> (board[i][j] == <span class="hljs-string">&#x27;Q&#x27;</span>)<br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>    &#125;<br><br>    vector&lt;vector&lt;string&gt;&gt; <span class="hljs-built_in">solveNQueens</span>(<span class="hljs-type">int</span> n)<br>    &#123;<br>        vector&lt;string&gt;<span class="hljs-built_in">board</span>(n, <span class="hljs-built_in">string</span>(n, <span class="hljs-string">&#x27;.&#x27;</span>));<br>        <span class="hljs-built_in">backtarcking</span>(board, <span class="hljs-number">0</span>, n);<br>        <span class="hljs-keyword">return</span> ret;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><h1 id="动态规划">动态规划</h1><p>我们首先来讨论一下一个问题是否能用动态规划来求解</p><p>能有动态规划的一个非常重要的条件是：存在<strong>「重叠子问题」</strong>，什么意思？</p><p>意思是：当我把一个问题拆分为多个子问题后，<strong>对于子问题的处理依旧可以采取与原问题相同的拆分思路</strong>。即对于原问题的处理与对于子问题的处理方法<strong>完全一致</strong>。这便是判断一道题能否用动态规划的关键。</p><p>我们通常使用动态规划的目的是为了求<strong>最值</strong>，既然是最值，那么我们需要将所有的可能<strong>全部列出来</strong>。也就是说，我们需要遍历所有的<strong>「状态」</strong>。</p><p>然后，我们需要着重理解一下动态规划当中的<strong>「状态」</strong>，这里的<strong>状态</strong>体现在<code>dp</code> 数组当中，即当下标取 <code>i</code>时，<code>dp[i]</code> 的含义是什么？</p><p>接着，我们需要找到 <code>dp[i]</code> 与其子问题之间的联系，即用<code>dp[i]</code> 的子问题 <code>dp[i - 1]</code>或其他来<strong>表示</strong> <code>dp[i]</code>。这便是<strong>「状态转移方程」</strong>。当然，在列这玩意之前需要<strong>初始化</strong>。</p><p>嘛，上面我说的这些都是属于一个很直接的想法，在实际做题的时候我们要按照下面的步骤来走</p><ol type="1"><li>明确「状态」<ul><li>这里的状态指的是 <code>dp</code>数组中下标取某一数值时的<strong>含义</strong>。</li></ul></li><li>明确「选择」<ul><li>选择，即是<strong>导致状态改变的动作</strong>。</li></ul></li><li>明确 base case<ul><li>也就是初始化</li></ul></li><li>列状态转移方程。我们给出如下目标</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//自顶向下式的。这种采用的是函数</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">dp</span><span class="hljs-params">(状态<span class="hljs-number">1</span>, 状态<span class="hljs-number">2</span>, 状态<span class="hljs-number">3</span>, ...)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">for</span> 做出选择 to 所能做出选择的所有范围<br>        <span class="hljs-comment">//此时状态会发生改变</span><br>        result = 求最值(result, <span class="hljs-built_in">dp</span>(状态<span class="hljs-number">1</span>, 状态<span class="hljs-number">2</span>, 状态<span class="hljs-number">3</span>, ...));<br>    <span class="hljs-keyword">return</span> result;<br>&#125;<br><br><span class="hljs-comment">//自底向上式的。这种采取迭代</span><br>初始化 base <span class="hljs-keyword">case</span><br><span class="hljs-keyword">for</span> 状态<span class="hljs-number">1</span> to 状态<span class="hljs-number">1</span>的所有情况<br>    <span class="hljs-keyword">for</span> 状态<span class="hljs-number">2</span> to 状态<span class="hljs-number">2</span>的所有情况<br>        <span class="hljs-keyword">for</span> 状态<span class="hljs-number">3</span> to 状态<span class="hljs-number">3</span>的所有情况<br>            ...<br>            dp[状态<span class="hljs-number">1</span>][状态<span class="hljs-number">2</span>][状态<span class="hljs-number">3</span>][...] = 求最值(选择<span class="hljs-number">1</span>, 选择<span class="hljs-number">2</span>, 选择<span class="hljs-number">3</span>, ...)<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>算法笔记</category>
      
    </categories>
    
    
    <tags>
      
      <tag>算法笔记</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>小林图解网络笔记</title>
    <link href="/2022/07/30/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/%E5%9B%BE%E8%A7%A3%E7%BD%91%E7%BB%9C/"/>
    <url>/2022/07/30/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/%E5%9B%BE%E8%A7%A3%E7%BD%91%E7%BB%9C/</url>
    
    <content type="html"><![CDATA[<h1 id="tcpip-网络模型有哪几层">TCP/IP 网络模型有哪几层</h1><p>由于设备的类型是多种多样的，因此在不同的设备间进行通信需要协商出一套<strong>通用的网络协议</strong>。</p><p>该网络协议是分层的，对于 TCP/IP协议而言，从上到下分为：应用层、传输层、网络层、网络接口层。</p><p>这些分层都是理论模型，并不涉及具体的硬件实现</p><h2 id="应用层">应用层</h2><p>应用层为最上层，我们所使用的软件都是在应用层实现的。</p><p>当两台设备需要传输数据时，应用层只需要把数据传输给下一层，也就是传输层。并且应用层不会去关心数据时如何传输的。</p><p>应用层工作在操作系统的用户态，传输层及以下的则工作在核心态</p><h2 id="传输层">传输层</h2><p>应用层的数据包会传输给传输层，传输层为应用提供网络支持。</p><p>传输层有两个传输协议：<strong>TCP 和 UDP</strong>。</p><p>大多数应用使用的传输协议是 TCP。TCP 比 UDP多了很多特性，这些特性能够保证数据能够可靠地传输给对方。</p><p>UDP则没有那么多屁事。它只负责发送数据，不保证数据包能否抵达对方，但它的实时性也相对更好，传输效率也高。</p><p>TCP 会在原本数据的基础上<strong>加上 TCP 头部</strong>组成 TCP报文，然后再将 TCP 报文传输给<strong>网络层</strong></p><p>应用需要传输的数据可能非常大，如果直接传输会出现很多问题。因此当数据包大小超过MSS（TCP最大报文长度）时，就要对其分块。这样如果有某一个分块损坏只需要重传这个分块即可。在TCP协议中，每个分块称为一个 <strong>TCP 段</strong>。</p><p>上面都是应用作为发送方的情况。当设备作为接受方时，传输层需要将数据传输给应用。但一台设备上可能会有很多应用在接受数据，这时就需要一个编号来将应用区分开，这个编号就是<strong>端口。</strong></p><p>比如 80 端口通常是给服务器使用的，22端口是远程登录服务器使用的。由于浏览器（客户端）当中的每一个标签页都是一个独立的进程，操作系统会为这些进程分配临时的端口号。</p><p>传输层当中的报文都会携带端口号，因此接收方可以辨别出这个报文是发给谁的。</p><h2 id="网络层">网络层</h2><p>传输层的任务是服务于应用，负责应用与应用之间的传输，而实际的传输则要靠网络层。</p><p>网络层所使用的是 <strong>IP 协议</strong>。IP协议会将<strong>传输层</strong>的报文作为<strong>数据部分</strong>，在前面加上IP 头部组成 IP 报文。</p><p>如果 IP 报文的大小超过 MTU就会再次进行分片，最终得到一个准备发送到网络当中的 IP 报文</p><figure><img src="12.jpg" srcset="/img/loading.gif" lazyload alt="img"><figcaption aria-hidden="true">img</figcaption></figure><p>网络层负责将数据从一个设备传输到另一个设备。那么这里就必须要有一个编号来对设备进行区分，这个编号就是我们通常说的<strong>IP 地址</strong>。</p><p><strong>端口号是对接受数据的应用进行区分，IP地址是对设备进行区分</strong>，显然后者的范围更大</p><p>我们会用 IP 地址给设备编号。对于 IPv4 协议，IP 地址有 32位，它们被分成四段（如：192.168.100.1），每段 8 位，每段的最大数是255。</p><p>但是如果只有一个 IP地址来对设备进行区分，那么寻址起来会非常麻烦。因为全世界会有非常多的设备，我们不能一个一个去匹配。</p><p>因此，我们将 IP 地址划分为了两种意义</p><ul><li>一个是<strong>网络号</strong>，用来表示该 IP 地址属于哪个「子网」</li><li>一个是<strong>主机号</strong>，用来表示同一「子网」下的不同主机</li></ul><p>这里面具体的划分就需要引入<strong>子网掩码</strong>的概念了。</p><p>举个例子，假设 IP 地址为 10.100.122.0/24 ，这后面的 /24 表示255.255.255.0（相当于 32 位当中有 24 个 1，为了简化就写成了 /24 ）。</p><p>那么我们要具体如何计算呢：很简单，只需要将 IP地址与子网掩码进行<strong>按位与</strong>的操作就可以得到<strong>网络号</strong></p><figure><img src="16.jpg" srcset="/img/loading.gif" lazyload alt="img"><figcaption aria-hidden="true">img</figcaption></figure><p>按位与是「两个都是 1 结果才是1，只要有一个是 0 那么结果就是 0 」</p><p>最终的网络号是 32 位的。</p><p>将 <strong>取反后的IP地址</strong>与子网掩码进行<strong>按位与</strong>运算，就可以得到<strong>主机号。</strong></p><p>在寻址过程中，会先匹配网络号（先找子网），才会去找主机号。</p><p>IP协议除了用于寻址外，还有一个重要能力是<strong>路由</strong>。在实际的场景中，两台设备之间并不是直接通过一根网线连接的，这中间会有很多网关、路由器、交换机等设备。每当网络包到达一个节点（设备）时，都需要通过路由算法来决定接下来往哪里走。</p><p>路由器的工作是将这个网络包转发到对应的子网内，它只需要知道网络号就可以了。</p><figure><img src="17.jpg" srcset="/img/loading.gif" lazyload alt="图3"><figcaption aria-hidden="true">图3</figcaption></figure><h2 id="网络接口层">网络接口层</h2><p>在生成了 IP 头部之后，接下来要交给<strong>网络接口层</strong>，它会在IP 头部的前面加上 MAC头部，在结尾加上校验码，将这个整体封装成<strong>数据帧</strong>发送到以太网上。</p><p>以太网在判断网络包的目的地的方式不同于 IP，因此需要相应的方式才能在以太网中将数据包传输到正确的地方，而 MAC头部就是干这个的。因此，<strong>在以太网中寻址主要靠 MAC地址</strong>。</p><p>MAC 头部当中包含了<strong>发送方</strong>和<strong>接收方</strong>的MAC 地址，我们可以通过 ARP 协议来获取对方的 MAC 地址。</p><p>网络接口层主要是为网络层提供「物理上」的传输服务，负责在以太网、WIFI等底层网络上发送数据包，通过 MAC 地址来标识设备。</p><h2 id="总结">总结</h2><p>具体的封装如下图：</p><figure><img src="封装.png" srcset="/img/loading.gif" lazyload alt="图4"><figcaption aria-hidden="true">图4</figcaption></figure><p>HTTP 的传输单位是消息或报文（ message ）、TCP 层的传输单位是段（segment ）、IP 层的传输单位是包（ packet ），网络接口层的传输单位是帧（frame ）</p><h1 id="键入网址到网页显示期间发生了什么">键入网址到网页显示，期间发生了什么</h1><p>总体流程如下：</p><figure><img src="2.jpg" srcset="/img/loading.gif" lazyload alt="图5"><figcaption aria-hidden="true">图5</figcaption></figure><h2 id="http">HTTP</h2><ul><li>浏览器做的第一步是解析 URL（网址）</li></ul><p>浏览器会先对 URL 进行解析，从而生成发送给 web 服务器的请求信息。</p><p><img src="3.jpg" srcset="/img/loading.gif" lazyload></p><p>URL的前面一部分是服务器名称，后面一部分是服务器某一目录下的文件资源，即浏览器请求的文件资源。</p><p>如果没有路径名，那么会默认访问事先设置的<strong>默认文件</strong>，也就是<code>/index.hrml</code> 或者 <code>/default.html</code>，这样就不会发生混乱。</p><ul><li>生成 HTTP 请求消息</li></ul><p>对 URL 进行解析之后，浏览器就知道了 <strong>Web服务器和文件名</strong> ，接下来就是根据这俩生成 HTTP 请求消息了。</p><p>HTTP阶段的数据包称为消息或报文。既然有请求报文自然也有响应报文，如下：</p><p><img src="4.jpg" srcset="/img/loading.gif" lazyload></p><h2 id="dns-寻址">DNS 寻址</h2><p>浏览器解析完 URL 并生成 HTTP 报文后，需要委托操作系统将报文发送给 Web服务器。然后你就会发现：我只知道这个服务器的名称，我不知道它的 IP地址啊。</p><p>浏览器不可能将所有服务器的 IP地址全部保存，最多会有个几分钟的缓存。也就是说我们要去查询服务器的 IP地址。</p><p>为解决这个问题，专门有一种服务器保存了 Web 服务器域名与 IP之间的对应关系，这就是 DNS 服务器。</p><ul><li>域名之间的层级关系</li></ul><p>服务器的域名都是用句点来进行分割的，比如 <code>www.serve.com</code>，句点表示不同的层级，<strong>越靠右层级越高</strong>。</p><p>实际上域名的最后还有一个点，即完整域名为 <code>www.serve.com.</code>，最后那个点代表根域名。</p><p>也就是说，<code>.</code> 表示根域，下一层是顶级域 <code>.com</code>，再下面是 <code>serve.com</code> 。</p><p>它们之间的关系为：</p><ul><li><p>根 DNS 服务器 <code>.</code></p></li><li><p>顶级域 DNS 服务器 <code>.com</code></p></li><li><p>权威 DNS 服务器 <code>serve.com</code></p></li></ul><p><img src="5.jpg" srcset="/img/loading.gif" lazyload></p><p><strong>根域的 DNS 服务器信息会保存在互联网中所有的 DNS服务器中</strong>。</p><p>因此，只需要找到一台服务器，就可以找到并访问根域服务器。然后再从根域服务器逐步向下寻找，最终就可以找到目标服务器的IP 地址。</p><ul><li>DNS 请求流程</li></ul><ol type="1"><li>客户端首先会发出一个 DNS 请求给本地的 DNS 服务器（就是客户端的TCP/IP 设置中填写的 DNS 服务器地址），问 <code>www.serve.com</code> 的IP 地址是啥。</li><li>本地 DNS 服务器收到客户端的请求后，如果缓存当中能找到该域名对应的 IP，那么将直接返回。如果没有，那么它会向根域名服务器发出请求</li><li>根 DNS 服务器收到本地 DNS 服务器的请求后，发现该域名后缀是<code>.com</code> ，于是返回 <code>.com</code> 的 IP 地址，让本地 DNS服务器去 <code>.com</code> 服务器那里查找</li><li>收到地址后，本地 DNS 服务器向 <code>.com</code>服务器发出请求，<code>.com</code> 则向本地服务器返回<code>www.serve.com</code> 的服务器 IP 地址</li><li>接着，<code>www.serve.com</code> 域名下的 DNS 服务器会返回本地 DNS服务器所请求的域名所对应的 IP 地址</li><li>最后，本地服务器将该 IP 地址返回给客户端</li></ol><p>具体的如下图</p><p><img src="6.jpg" srcset="/img/loading.gif" lazyload></p><p>当然，并不是说每一次输入网址都会进行这么多的步骤，是的话还要缓存干什么。</p><p>浏览器会先看看自己有没有这个域名对应的 IP地址的缓存，有的话就直接返回。没有的话会去问操作系统，操作系统会看一看自己的缓存。如果还没有，再去hosts 文件看，这还没有，才会去问本地 DNS 服务器。</p><h2 id="协议栈">协议栈</h2><p>应用程序通过 DNS 服务器获取到域名的 IP 地址后，会将 HTTP的报文<strong>传输</strong>工作交由操作系统当中的<strong>协议栈</strong>执行。</p><p>协议栈由上下两部分组成，上面会向下面的部分委托工作，下面的部分则会实际进行执行。即上面接受任务，下面执行任务。</p><p>当然，应用程序（浏览器）并不能直接委托协议栈进行工作，这有悖于操作系统的规则。因此应用程序需要通过一个工具来委托协议栈来对HTTP 报文进行传输，这个工具就是 <strong>Socket 库</strong>。</p><p>协议栈的上半部分接收数据的协议有 TCP 和 UDP两种。协议栈的下面部分是通过 IP协议来控制网络包的<strong>实际收发的</strong>。</p><p>也就是说， HTTP 报文会先经过 TCP 部分，加上了 TCP 头部之后会委托给 IP部分，再加上 IP 头部后会执行实际的收发操作。</p><p>此外，IP 中还包括 ICMP 协议和 ARP 协议。</p><ul><li>ICMP 协议用于告知网络包传输过程中产生的错误以及各种控制信息</li><li>ARP 协议用于根据 IP 来查询响应的 MAC 地址</li></ul><h2 id="tcp">TCP</h2><h3 id="tcp-头部组成">TCP 头部组成</h3><p>当网络包进过 TCP 模块时，会在其前面加上 TCP 头部，其格式如下：</p><p><img src="8.jpg" srcset="/img/loading.gif" lazyload></p><p>这里的<strong>源端口号</strong>和<strong>目标端口号</strong>指的是报文发出的应用以及接收的应用。<strong>包的序号</strong>是为了解决包乱序的问题。</p><p>当然，TCP会检查对方是否收到，因此会有<strong>确认号</strong>。如果对方没有收到，则需要重新发送。</p><p>TCP 还有一些状态位来检验双方目前所处在的状态。</p><ul><li><code>SYN</code> 是发起一个连接</li><li><code>ACK</code> 是回复</li><li><code>RST</code> 是重新连接</li><li><code>FIN</code> 是结束连接</li></ul><p>TCP是面向连接的，包当中的这些状态位的标志说明了<strong>连接双方目前所处在的状态</strong>。</p><p>还有一个是<strong>窗口大小</strong>。这是因为 TCP要做<strong>流量控制</strong>，通信的双方各有一个窗口（缓存），该窗口的大小即为处理数据的能力大小。</p><p>除了流量控制以外，TCP还会做<strong>拥塞控制</strong>。这里实际上指的是包的发送速度。你发的太快，对面撑死，发的太慢，对面饿死。</p><h3 id="三次握手">三次握手</h3><ul><li>TCP 在传输数据之前，会先进行三次握手来建立连接</li></ul><p>连接，实际上是一个状态。<strong>每当 TCP头部当中的状态位发生变化时，双方的状态就会发生变化</strong>，具体的情况如下：</p><p><img src="TCP三次握手.drawio.png" srcset="/img/loading.gif" lazyload></p><ul><li>一开始，客户端与服务端都处于 <code>CLOSE</code>状态，但是服务端会<strong>主动</strong>监听某个端口，即服务端会处于<code>LISTEN</code> 状态。</li><li>往后，客户端会发起连接 <code>SYN</code> ，即<strong>发送一个<code>SYN</code>序列号（这个序列号是由客户端自己算出来的）</strong>。此后客户端处于<code>SYN-SENT</code> 状态。<ul><li>对应蓝色箭头</li></ul></li><li>服务端收到后会<strong>返回一个 <code>SYN</code> 号和<code>ACK</code>号</strong>。<strong>前者为服务端自己生成的</strong>，<strong>后者为在客户端发送过来的<code>SYN</code> 号的基础上加上 1 之后的值</strong>。此后，服务端处于<code>SYN-RECD</code> 状态。<ul><li>对应橙色箭头</li></ul></li><li>客户端收到服务端发来的 <code>SYN</code> 号和 <code>ACK</code>号之后，<strong>会返回一个 <code>ACK</code>号（数值上为在服务器发送过来的序列号的基础上加上 1之后的值）</strong>，此后客户端处于 <code>ESTABLISHED</code>。到此为止，客户端部分的握手结束。<ul><li>对应绿色箭头</li></ul></li><li>服务端在收到客户端发送过来的 <code>ACK</code> 号之后会进入<code>ESTABLISHED</code> 。到此为止，服务端部分的握手结束。</li></ul><p>整个握手过程，<strong>客户端是一发一收</strong>，<strong>服务端也是一发一收</strong>。均为一发一收，因此<strong>握手的目的是保证双方均有发送数据和接受数据的能力</strong>。</p><ul><li>注：<strong>三次握手阶段双方只会发送 TCP头部信息</strong>，这只是建立连接前的工作，不会发送数据</li></ul><h3 id="查看-tcp-状态">查看 TCP 状态</h3><p>在 Linux 下，可以通过 <code>netstat -napt</code> 命令查看。</p><p><img src="10.jpg" srcset="/img/loading.gif" lazyload></p><h3 id="tcp-分割数据">TCP 分割数据</h3><p>如果 HTTP 请求消息比较长，超过了 <code>Mss</code> 的长度，这个时候TCP 就会将 HTTP 的数据拆成一块一块来发送，而不是全部发送。</p><p><img src="11.jpg" srcset="/img/loading.gif" lazyload></p><ul><li>MSS 除去 IP 头部和 TCP 头部后的数据长度</li><li>MTU 一个网络包的最大长度，以太网中为 1500 字节</li></ul><p>HTTP 报文数据会被以 <strong>MSS</strong>的数据长度进行拆分，拆分出来的每一块数据都会被放进单独的网络包中。也就是在MSS 大小的数据前面加上 TCP 头部，之后交由 IP 模块来发送数据。</p><p><img src="12-165917866868731.jpg" srcset="/img/loading.gif" lazyload></p><h3 id="tcp-段也就是-tcp-报文的生成">TCP 段（也就是 TCP报文）的生成</h3><p>TCP协议里面会有两个端口，一个是浏览器监听的端口（浏览器接收数据的端口，通常随机生成），另一个是Web 服务器监听的端口（ HTTP 协议下的端口号是 80， HTTPS 协议下的端口号是443 ）。</p><p>双方建立连接后，TCP 报文当中的数据部分为 「HTTP报文加上应用数据部分」，再次基础上加上 TCP 头部就可以组成 TCP 段（也就是TCP 报文）。</p><p>至此，网络包的报文如下：</p><p><img src="13.jpg" srcset="/img/loading.gif" lazyload></p><h2 id="ip">IP</h2><p>TCP模块在<strong>执行连接、收发、断开等</strong>各阶段操作时，都需要委托 IP模块将数据封装成<strong>网络包</strong>来发送给通信对象。</p><h3 id="ip包ip-报文的-ip-头部">IP包（IP 报文）的 IP 头部</h3><p>IP 头部的格式如下：</p><p><img src="14.jpg" srcset="/img/loading.gif" lazyload></p><p>IP 协议里面需要有<strong>源地址 IP</strong> 和<strong>目标地址IP</strong></p><ul><li>源地址 IP 为客户端输出的 IP 地址</li><li>目标地址 IP 为通过 DNS 域名解析得到 Web 服务器 IP 地址</li></ul><p>因为 HTTP 报文是经过 TCP 协议传输的，所有在 IP包头的<strong>协议号</strong>当中要填 06（十六进制），表示协议为TCP。</p><h3 id="ip-源地址的判断">IP 源地址的判断</h3><p>假设客户端存在多个网卡，就会有多个 IP 地址，那么源地址 IP应该选择哪个？</p><p>这个时候我们可以通过<strong>路由表</strong>来判断应该使用哪一个网卡来作为源地址IP</p><p>在 Linux 下，可以用 <code>route -n</code> 来查看系统当前的路由表</p><p><img src="15.jpg" srcset="/img/loading.gif" lazyload></p><p>假设 Web 服务器的目标地址为 <code>192.168.10.200</code>。我们<strong>拿这个目标地址进行运算</strong>。</p><ol type="1"><li>先将 <code>192.168.10.200</code>与第一条目的<strong>子网掩码（Genmask）</strong>进行<strong>与运算</strong>，其结果为<code>192.168.10.0</code> ，而第一条目的 <code>Destination</code> 是<code>192.168.3.0</code> ，二者并不匹配。</li><li>再将 <code>192.168.10.200</code>与第二条目的子网掩码进行与运算，结果为 <code>192.168.10.0</code>刚好与第二条目的 <code>Destination</code>向吻合。因此<strong>将第二条目的网卡 <code>eth1</code> 的 IP地址作为源地址 IP</strong>。</li></ol><p>如果所有条目都无法匹配，则会默认匹配第三个，这个是<strong>默认网关</strong>，它的目标地址和子网掩码都是<code>0.0.0.0</code> 。</p><p>在这里，我们将 <strong>Web 服务器的 IP 地址填入目标 IP地址，将「对应网卡的 IP 地址」填入源 IP 地址 </strong>。</p><p><strong><code>Gateway</code> 就是路由器的 IP地址</strong>。上面匹配成功是将<strong>网卡的 IP 地址作为源地址 IP，不是将路由器的 IP 地址作为源地址 IP</strong> 。</p><p>其具体匹配过程如下图：</p><p><img src="16-165917868996440.jpg" srcset="/img/loading.gif" lazyload></p><h3 id="ip-报文的生成">IP 报文的生成</h3><p>至此，网络包的报文如下：</p><p><img src="17-165917869953443.jpg" srcset="/img/loading.gif" lazyload></p><h2 id="mac">MAC</h2><p>生成 IP 头部之后，还需要在前面加上 <strong>MAC 头部</strong>。</p><h3 id="mac-头部格式">MAC 头部格式</h3><p>MAC 头部为以太网所使用的头部，它包含接收方和发送发的 MAC地址等信息。</p><p><img src="18.jpg" srcset="/img/loading.gif" lazyload></p><p>在 MAC 包头里需要<strong>发送方 MAC 地址</strong>和<strong>接收方 MAC地址</strong>，这主要是用于<strong>两点之间的传输</strong>。</p><p>一般在 TCP/IP 通信里， MAC包头的<strong>协议类型</strong>只使用：</p><ul><li><code>0800</code> 对应 IP 协议</li><li><code>0806</code> 对应 ARP 协议</li></ul><h3 id="mac-发送方和接收方地址如何确认">MAC发送方和接收方地址如何确认</h3><p><strong>发送方</strong>的 MAC 地址的获取比较简单，MAC地址是在网卡生产时写入到ROM（内存）里的，只需要将这个数值读取出来就行。</p><p><strong>接收方</strong>的 MAC地址的获取就相对复杂。我们目前知道<strong>目标的 IP 地址，但不知道目标的MAC 地址</strong>，这个时候就需要 <strong>ARP协议</strong>来帮我们获取目标路由器的 IP 地址。</p><p>ARP协议会在以太网中以<strong>广播</strong>的形式向所有以太网当中的设备发出请求，收到请求的<strong>设备的IP</strong> 地址如果跟 ARP 所广播的 IP地址相同，那么该设备将会返回自己的 MAC 地址给数据发送方的设备。</p><p>如果对方和自己处于同一个子网当中吗，那么通过上述操作就可以获得对方的MAC 地址。之后，我们将该 MAC 地址写入<strong>接收方的 MAC地址</strong>即可 。</p><p>当然，我们并不需要每次都去广播获权，操作系统会将本次的查询结果放在<strong>ARP 缓存</strong>当中供以后使用，不过缓存也就几分钟而已。</p><p>那么整个发包过程可以概述为：</p><ul><li>先查询 ARP 缓存，如果有目标的 MAC 地址的话就不需要进行 ARP广播，直接将该地址写入 MAC 头部即可</li><li>如果 ARP 缓存当中不存在对方的 MAC 地址时，则通过 ARP 广播查询。</li></ul><p>在 Liunx 下，我们可以使用 <code>arp -a</code> 来查询 ARP缓存的内容</p><p><img src="20.jpg" srcset="/img/loading.gif" lazyload></p><h3 id="mac-报文的生成">MAC 报文的生成</h3><p>至此，网络包的报文如下：</p><p><img src="21.jpg" srcset="/img/loading.gif" lazyload></p><h2 id="网卡">网卡</h2><p>网络包本质上只是一串二进制数字，这玩意是数字信号，并不能直接传输过去。因此我们需要将<strong>数字信号转换成电信号</strong>，而转换的工具便是<strong>网卡</strong>，控制网卡的是<strong>网卡驱动程序</strong>。</p><p>网卡驱动获取到网络包之后，会将其<strong>复制</strong>到网卡内的缓存区当中，接着会在其<strong>开头加上「报头和起始帧分界符」，在末尾加上用于检测错误的「帧校验序列」</strong>。</p><p><img src="数据包.drawio.png" srcset="/img/loading.gif" lazyload></p><ul><li>起始帧分界符用于表示<strong>这个包的起始位置</strong>。</li><li>末尾的FCS（帧校验序列）则用来<strong>检查包在传输过程中是否损坏</strong>。</li></ul><p>网卡会将包转化成电信号，通过网线发出去。</p><h2 id="交换机">交换机</h2><p>交换机的设计是将网络包<strong>原样</strong>转发到目的地。交换机工作在MAC 层，<strong>并不涉及到 IP</strong>，也称为<strong>二层网络设备</strong>。</p><h3 id="交换机的包接收工作">交换机的包接收工作</h3><p>当电信号到达网线接口时，交换机内部的模块会进行接收，<strong>将电信号转化成数字信号</strong>。</p><p>然后通过包末尾的 FCS来校验错误，如果没问题则直接放到缓冲区。这一部分的操作与计算机的网卡基本相同，但二者不同的是：</p><ul><li>计算机的网卡<strong>本身具有 MAC地址</strong>，它可以通过检查收到的包的 MAC地址来判断<strong>这个包是不是发给自己的</strong>，如果不是则直接丢弃，是的话便接收。</li><li>交换机的端口<strong>并没有 MAC地址</strong>，<strong>它不会核对包的接收方的 MAC地址</strong>，而是<strong>将所有包全部放到缓冲区</strong>。</li></ul><p>将包放入缓冲区后，只需要查询 <strong>MAC地址表</strong>当中是否有当前 MAC 地址的记录。</p><p>交换机的 MAC 地址表主要包含两个信息：</p><ul><li>设备的 MAC 地址</li><li>该设备连接在交换机的哪个端口上（该 MAC 地址对应哪个端口）</li></ul><p><img src="23.jpg" srcset="/img/loading.gif" lazyload></p><p>我们举个例子，如果接收方的 MAC 地址为 <code>00-02-B3-1C-9C-F9</code>，则该 MAC 地址对应端口 3 ，因此交换机直接<strong>将数字信号往端口 3去发送</strong>。</p><p>因此，<strong>交换机会根据 MAC 地址表查找 MAC地址，然后将信号发送到相应端口</strong>。</p><p>那么这里就有一个问题是：如果在 MAC 地址表当中找不到指定的 MAC地址怎么办？</p><p>这种情况会对应两种可能：</p><ul><li>具有该 MAC 地址的设备还没有向交换机发送过包</li><li>该设备一段时间内没有工作导致其地址被从交换机的 MAC地址表中删除了</li></ul><p>这种情况下，交换机将无法判断出应该将包转发到哪个端口，<strong>只能将包转发到除了源端口以外的所有端口上</strong>，这样无论该设备在哪个端口上就都可以收到这个包。</p><p>这样做并不会有任何的问题，因为以太网设计的原则是：<strong>将包发送到整个网络，只有相应的接收者才接收这个包，其他的则直接忽略掉这个包</strong>。</p><p>此外，如果接收方的 MAC地址是一个<strong>广播地址</strong>，那么交换机也会将这个包<strong>发送到除源端口以外的所有端口</strong>。</p><p>以下两个为广播地址：</p><ul><li>MAC 地址：<code>FF:FF:FF:FF:FF:FF</code></li><li>IP 地址：<code>255.255.255.255</code></li></ul><h2 id="路由器">路由器</h2><h3 id="路由器与交换机的区别">路由器与交换机的区别</h3><p>网络包经过交换机之后就会到达<strong>路由器</strong>，并在此被转发到下一个<strong>路由器或者目标设备</strong>。</p><p>这里转发的工作原理和交换机类似，也是通过查表来判断转发目标。</p><p>但在具体操作上，二者依旧有区别：</p><ul><li>路由器是<strong>基于 IP设计的</strong>，即<strong>三层</strong>网络设备，<strong>路由器的各个端口都具有MAC 地址和 IP 地址</strong> 。</li><li>交换机是<strong>基于以太网设计的</strong>，即<strong>二层</strong>网络设备，<strong>交换机的端口并不具有MAC 地址</strong> 。</li></ul><h3 id="路由器的基本原理">路由器的基本原理</h3><p>路由器的端口具有 MAC地址，因此它能够<strong>成为以太网的发送方和接收方</strong>，它还具有 IP地址，在这一点上它跟网卡差不多。</p><p>当转发包时，路由器会先从端口接收<strong>发给自己的网络包</strong>，然后通过<strong>路由表</strong>来查询转发目标，再通过相应端口将以太网包发送出去。</p><h3 id="路由器的包接收操作">路由器的包接收操作</h3><p>当电信号到达网络接口部分时，路由器当中的模块会将电信号转换成数字信号，并通过末尾的FCS 进行错误校验。</p><p>如果没有发生错误，则检查 MAC 头部当中的<strong>接收方 MAC地址</strong>，看看是不是发给自己的。如果是则放到缓冲区当中，如果不是则直接丢弃这个包。</p><h3 id="查询路由表确定输出端口">查询路由表确定输出端口</h3><p>完成包的接收操作之后，路由器会<strong>去掉</strong>这个包的<strong>MAC 头部</strong>。</p><p><strong>MAC头部的作用就是将包送达路由器</strong>，<strong>这里面的接收方的 MAC地址就是路由器端口的 MAC 地址</strong>。因此当包到达路由器之后，MAC头部的任务也就完成了，于是 <strong>MAC 头部就会被丢弃</strong>。</p><p>往后，路由器会<strong>根据后面的 IP 头部对包进行转发</strong>。</p><p>首先会查询<strong>路由表</strong>来判断转发目标。</p><p><img src="24.jpg" srcset="/img/loading.gif" lazyload></p><p>假设地址为 <code>10.10.1.101</code> 的设备要想地址为<code>192.0.2.100</code>的服务器发送一个包，这个包会先到达图中的路由器。</p><p>然后，我们将服务器的 IP 地址 <code>192.0.2.100</code>与路由表当中的子网掩码做<strong>与运算</strong>，最终得到的结果如果跟<strong>路由表前面的Destination 当中的 IP 相吻合</strong>，则会从该端口（eth3端口）转发出去。</p><p>如果实在匹配不到路由，则会选择<strong>默认路由</strong>。其对应的子网掩码为<code>0.0.0.0</code> 任何地址与其做与运算的结果都是 <code>0.0.0.0</code>，这样的话会走 eth1 端口。</p><h3 id="路由器的发送操作">路由器的发送操作</h3><p>接下来会进入包的<strong>发送操作</strong>。</p><p>首先，我们需要根据<strong>路由表的网关列</strong>来判断对方的地址。</p><ul><li>如果网关是一个 IP 地址，则该 IP地址就是我们要转发的目标。<strong>说明此时还未抵达终点</strong>，需要路由器继续转发</li><li>如果网关为空，则 IP 头部当中接收方的 IP地址就是我们的目标地址。<strong>说明下一站即是终点</strong>。</li></ul><p>往后的操作跟交换机差不多。指定目的地的 IP 地址后，需要通过 ARP协议来确定对方的 MAC 地址。</p><p>当然，路由器也有 ARP缓存。因此路由器会先在缓存当中查找，找不到再去发送 ARP 查询请求。</p><p>然后，填上发送方端口的 MAC 地址。还有一个以太类型的字段，填写<code>0800</code> （十六进制）用以表示 IP 协议。</p><p>至此，MAC头部已添加完成。往后将有网关将数字信号转换成电信号发送出去。</p><p>发送出去的网络包会通过<strong>交换机</strong>到达下一个路由器或者直接到达服务器。</p><p>对于前者而言，由于接收方的 MAC地址就是下一个路由器，因此交换机会根据这一地址将包转发到路由器。</p><p>上述过程进过层层转发后，最终会到达目的地。</p><p>在包这一整个转发过程中，<strong>源 IP 和目标 IP是始终不会发生变化的，一直在变的是 MAC 地址</strong>。MAC地址的每一次改变都是路由器通过 ARP 协议来查找目标 IP 所对应的 MAC地址之后再将该地址写入 MAC 头部的结果。<strong>而 MAC地址就是用于在以太网内进行两个设备之间包的传输</strong>。</p><h2 id="服务器与客户端">服务器与客户端</h2><p>这一部分就是二者相互扒皮的存在了。</p><p>当网络包到达服务器之后，服务器就会开始扒网络包的皮。</p><p>首先会先查看 MAC 头部，看是否和服务器自己的 MAC地址相吻合，吻合的话就把包收起来。</p><p>然后再看 IP 头部，看 IP 地址是否与自己的相吻合。根据 IP头当中的协议项，知道自己上层是 TCP 协议。</p><p>然后去看 TCP头部。这里面有序列号，需要看一下这个序列的包是不是我想要的，如果是的话就将其放入缓存并返回一个ACK ，如果不是则丢弃。TCP 头部里面还有端口号，而服务器的 HTTP正在监听该端口号。</p><p>接着，服务器便知道是 HTTP 进程需要这个包，便将包发送给 HTTP进程。</p><p>之后 HTTP 会返回一个响应报文，这一过程跟刚刚的相同，需要有 TCP 、IP、 MAC 头部。只不过源地址 IP 是服务器，目标地址 IP 是客户端。</p><p>往后的过程跟来的时候一样。当这个包到达客户端后，客户端也会开始扒皮。</p><p>最后客户端要离开了，<strong>会向服务器发起 TCP四次挥手</strong>，至此双方的连接就断开了。</p><h1 id="linux-系统是如何收发网络包的">Linux 系统是如何收发网络包的</h1><p>我们首先来介绍 OSI七层参考模型，分别是应用层、表示层、会话层、传输层、网络层、数据链路层和物理层。</p><p>每一层的职能如下：</p><ul><li>应用层，负责给程序提供统一的接口</li><li>表示层，负责把数据转换成兼容另一个系统能识别的格式</li><li>会话层，负责建立、管理和终止表示层实体之间的通信会话</li><li>传输层，负责端到端的数据传输</li><li>网络层，负责数据的路由、转发、分片</li><li>数据链路层，负责数据的装帧和差错检测以及 MAC 寻址</li><li>物理层，负责在物理网络中传输数据帧</li></ul><p>由于这玩意只是个理论，我们实际上采用的是四层模型，即 TCP/IP网络模型， Linux 系统也正是按照这套网络模型来实现网络协议栈的。</p><p>TCP/IP网络模型共有四层，分别是应用层、传输层、网络层和网络接口层，每一层职能如下：</p><ul><li>应用层，负责向用户提供一组应用程序，如 HTTP 、 DNS 、 FTP 等</li><li>传输层，负责端到端的通信，比如 TCP 、 UDP 等</li><li>网络层，负责网络包的封装、分片、路由、转发，比如 IP 、 ICMP 等</li><li>网络接口层，负责网络包在物理网络中的传输，比如网络包的装帧、 MAC寻址、差错检测，以及通过网卡传输网络帧等。</li></ul><p>TCP/IP 与 OSI 之间的关系如下：</p><p><img src="OSI与TCP.png" srcset="/img/loading.gif" lazyload></p><h2 id="linux-网络协议栈">Linux 网络协议栈</h2><p>TCP/IP的每一层协议，我们可以理解为「每经过一层，数据体<strong>所增加的内容在意义上相同</strong>」。我们具体看下面的图：</p><p><img src="封装-165917873063660.png" srcset="/img/loading.gif" lazyload></p><p>其中：</p><ul><li>传输层，给应用程序增加 TCP 头部</li><li>网络层，在 TCP 段的基础上增加了 IP 头部</li><li>网络接口层，在 IP 包的基础上分别增加了帧头（这里是包括 MAC头部在内的）和帧尾</li></ul><p>这些新增加的头部和尾部，<strong>都是按照特定的协议进行填充的</strong>。每一层都增加了协议头，那么网络包的大小自然就增大了。但以太网中并不能传输任意大小的数据包，因此这便有了最大传输单元MTU ，其大小为 1500 字节。</p><p>当网络包超过 MTU 的大小，就会进行分片。当 MTU越大，所需分片越小，网络吞吐量越大；当 MTU越小，所需分片越多，网络吞吐量越小。</p><p>明白 TCP/IP 网络模型后，我们说 Linux 网络协议栈就<strong>类似于TCP/IP 网络模型</strong>。</p><p><img src="协议栈.png" srcset="/img/loading.gif" lazyload></p><p>从中我们可以得出：</p><ul><li>应用程序需要通过系统调用来跟 Socket 层进行数据交互</li><li>Socket 层往下是传输层、网络层、网络接口层</li><li>最下面一层是网卡驱动程序和网卡设备</li></ul><h2 id="linux-接收网络包的流程">Linux 接收网络包的流程</h2><p>网卡是计算机内部一个专门用于接收、发生网络包的硬件。当网卡收到一个网络包后，会通过DMA 技术将该网络包放入到 Ring Buffer ，这玩意是一个环形缓冲区。</p><p>当网卡接收到网络包之后，需要告诉操作系统这个网络包到达了，网卡告诉操作系统的手段就是<strong>触发中断</strong>。</p><p>但是，这会出现一个问题是：在高性能网络场景下，网络包的数量会非常多，这就会导致网卡多次触发中断。而一旦触发中断，CPU就会停下手上正做着的事情转而去处理中断，处理完毕之后才回去处理原来的事情。那么如果频繁触发中断，就会导致CPU一直在处理这些中断，进而使得其他任务无法正常推进，从而影响了整体的效率。</p><p>为了解决频繁中断而带来的系统开销， Linux 内核在 2.6 版本引入了 NAPI机制。它是用混合了「中断和轮询」的方式来接收网络包。它的核心是<strong>不采用中断来读取数据</strong>，而是<strong>采用中断来唤醒数据接收的服务程序</strong>，然后用<code>poll</code> 来轮询数据。</p><p>例如，当有网络包到达网卡时，网卡引发系统中断，于是操作系统会执行网卡硬件中断处理函数，<strong>中断处理函数处理完会「暂时屏蔽中断」，然后唤醒「软中断」来轮询处理数据</strong>，当没有新数据到达时才恢复中断。这样下来，一次中断实际上是<strong>用来唤起数据接收的程序的</strong>，并且一次中断可以处理多个网络包。</p><p>那么软中断是如何处理网络包的呢？</p><p>首先它会从 Ring Buffer 中拷贝数据到内核的 struct sk_buff缓冲区，从而可以作为一个网络包交给网络协议栈进行逐层处理。</p><p>之后，进入到网络接口层。在这里会检查这个包的合法性，不合法则丢弃，合法则接收。去掉其头部和尾部（起始帧分界符、MAC头部、FSC 校验符），再找出网络包上层协议的类型是 IPv4 还是IPv6，之后再交给网络层。</p><p>到达网络层后，取出 IP包，这时要<strong>判断网络包的下一步走向</strong>，是<strong>交给上层还是转发出去</strong>（这也就是为什么需要IP 来区别每一个设备了。实际上 IP 地址和 MAC 地址都是用来区别设备的，区别在于服务器、电脑二者都有，而路由器则只有 MAC 地址）</p><p>当确认是发给本机的后，就往 IP 头部里面看上层协议是 TCP 还是 UDP，接着去掉 IP 头部，转交给传输层。</p><p>传输层取出 TCP 头部或 UDP 头部，根据四元组「源 IP 、源端口、目的 IP、目的端口」来找出对应的 Socket ，并将数据拷贝到 Socket的接收缓冲区。</p><p>最后，应用程序调用 Socket 接口，从内核的 Socket接收缓冲区中读取数据。</p><p>至此，接收工作全部完成，而发送则与之相反，具体如下图：</p><p><img src="收发流程.png" srcset="/img/loading.gif" lazyload></p><h2 id="linux-发送网络包的流程">Linux 发送网络包的流程</h2><p>这里对应上图的右半部分，发包流程与接收流程正好相反。</p><p>首先，应用程序会调用 Socket接口来发送数据包，由于这是一个系统调用，因此操作系统会从用户态陷入到内核态的Socket 层。此时 Socket 层会将应用层的数据拷贝到 Socket 发送缓冲区。</p><p>接下来，会轮到协议栈来工作。协议栈从 Socket发送缓冲区取出数据包后，按照 TCP/IP 协议从上到下逐层处理。</p><p>如果使用的是 TCP 协议，那么会在传输层的时候在包头前面加上 TCP头部，之后交给网络层。</p><p>网络层会给数据加上 IP 头部，并按照 MTU进行分片。通过查询路由表得知下一站的 IP 地址。</p><p>往后交给网络接口层，这里会用目前已知的 IP 地址来通过 ARP协议广播得到下一站的 MAC 地址。之后增加帧头和帧尾，加入到发包队列（RingBuffer）当中。</p><p>这一切都搞定之后，<strong>会触发中断</strong>告诉网卡驱动程序有新的网络包需要发送。最后通过DMA 技术将包放入到硬件网卡的队列当中，随后将其物理上地发送出去。</p><h1 id="http-篇">HTTP 篇</h1><h2 id="http-基本概念">HTTP 基本概念</h2><h3 id="http-是什么">HTTP 是什么</h3><p>HTTP是超文本传输协议。这里涉及三个概念：超文本、传输、协议，我们一个一个看。</p><ul><li>超文本<ul><li>HTTP传输的内容是超文本。超文本是文字、图片、视频的混合体，最关键的是存在超链接，能够从一个文本跳到另一个文本。</li><li>HTML是最常见的超文本。它本身是纯文本文件，但内部用很多标签定义了图片、视频等链接。经过浏览器的解释最终得到的就是一个网页</li></ul></li><li>传输<ul><li>HTTP 是一个双向协议。应用 HTTP协议时，必定一端是客户端，另一端是服务端。即对于单次 HTTP传输而言，这两者是一定可用确定的</li><li>HTTP是在<strong>两点</strong>之间进行数据的传输的，并<strong>不一定是从互联网服务器传输超文本到本地浏览器，也可以是两个服务器之间相互传输超文本数据</strong>。<strong>但对于单次传输而言，一定有客户端和服务端</strong>。</li></ul></li><li>协议<ul><li>协议，就是一种规范。它在计算机之间确定了一种交流通信的规范以及相关错误的处理方式</li></ul></li></ul><p>综合起来看就是：HTTP是一个在<strong>两点</strong>之间传输<strong>超文本</strong>数据的<strong>约定和规范</strong>。</p><h3 id="http-常见响应状态码">HTTP 常见响应状态码</h3><figure><img src="6-五大类HTTP状态码.png" srcset="/img/loading.gif" lazyload alt="五大类 HTTP 状态码"><figcaption aria-hidden="true">五大类 HTTP 状态码</figcaption></figure><p><code>1xx</code>表示服务端接收到的请求正在处理，实际用到的比较少。</p><p><code>2xx</code>表示服务端<strong>成功</strong>处理了客户端的请求。</p><ul><li><code>200 OK</code> 是最常见的成功状态码，表示一切正常<ul><li>在响应报文内，随状态码返回的信息会因方法的不同而发生改变。使用 GET方法时，对应请求资源的实体会作为响应消息返回。使用 HEAD方法时，对应请求资源的<strong>实体主体</strong>不随响应消息返回，仅返回响应首部。</li></ul></li><li><code>204 No Content</code> 也是常见的成功状态码，与<code>200 OK</code> 基本相同，但响应头没有 Body 数据</li><li><code>206 Partial Content</code> 表示返回的 Body数据不是资源的全部，而是其中的一部分，同样也是处理成功的状态。</li></ul><p><code>3xx</code>类状态码表示客户端请求的资源位置发生了变动，需要客户端用新的 URL重新发送请求，也就是<strong>重定向</strong>。</p><ul><li><code>301 Moved Permanently</code>表示永久重定向，说明请求的资源已经不存在了，需要用新的 URL再次访问。</li><li><code>302 Found</code>表示临时重定向，说明请求的资源还在，但暂时需要用另一个 URL 进行访问</li></ul><p>服务端返回 301 和 302 都会在响应头里使用（服务端使用）字段<code>Location</code> ，指明后续要跳转的 URL ，浏览器会自动重定向到新的URL 。</p><ul><li><code>304 Not Modified</code>不具有跳转的含义，表示资源未修改，重定向已存在的缓存文件，也称缓存重定向。<strong>告诉客户端可用继续使用缓存资源</strong>，用于缓存控制。</li></ul><p><code>4xx</code>类状态码表示<strong>客户端发送的报文有误</strong>，服务器无法处理，也就是客户端错误码的含义。</p><ul><li><code>400 Bad Request</code>表示<strong>客户端的请求报文有误</strong>，但只是个<strong>笼统</strong>的错误。</li><li><code>403 Forbidden</code>表示<strong>服务器禁止访问资源</strong>，并不是客户端的请求出错。</li><li><code>404 Not Found</code>表示<strong>请求的资源在服务端上不存在或找不到</strong>，因此无法提供给客户端。</li></ul><p><code>5xx</code>类状态码表示<strong>客户端请求报文正确</strong>，但服务端处理时内部发生错误，属于<strong>服务端错误码</strong>。</p><ul><li><code>500 Internal Server Error</code> 与 400类似，只是一个笼统的错误码，无法得知服务器发生了什么错误。</li><li><code>501 Not Implemented</code> 表示客户端请求的功能还不支持。</li><li><code>502 Bad Gateway</code>通常是服务器作为网关或代理时返回的错误码。表示服务器自身工作正常，访问后端服务器发生了错误。</li><li><code>503 Service Unavailible</code>表示服务器当前很忙，暂时无法响应客户端。</li></ul><h3 id="http-常见字段有哪些">HTTP 常见字段有哪些</h3><ul><li><p><em>Host</em> 字段</p><ul><li>客户端发送请求时，用来指定<strong>服务器的域名</strong>。</li><li><code>Host : www.bilibili.com</code></li><li>有了 <code>Host</code>字段，就可以请求<strong>同一台</strong>服务器上的不同网站的资源</li></ul></li><li><p><em>Content-Length</em> 字段</p><ul><li>服务器在<strong>返回数据</strong>时，会有<code>Content-Length</code>字段，表明本次回应的数据长度，以字节为单位。</li><li><code>Content-Length : 1000</code></li><li>本次返回数据的长度为 1000 字节</li></ul></li><li><p><em>Connection</em> 字段</p><ul><li><p><code>Connection</code> 字段用于客户端要求服务器使用 TCP持久连接，以便客户端请求复用。</p></li><li><figure><img src="9-connection字段.png" srcset="/img/loading.gif" lazyload alt="img"><figcaption aria-hidden="true">img</figcaption></figure></li><li><p>HTTP/1.1版本的默认连接为<strong>持久连接</strong>，但为了兼容老版本的 HTTP，因此需要指定 <code>Connection</code> 首部字段的值为<code>Keep-Alive</code></p></li><li><p><code>Connection : Keep-Alive</code></p></li><li><p>这样，一个可用复用的 TCP连接就建立了，知道客户端或服务端主动关闭连接。</p></li></ul></li><li><p><em>Content-Type</em> 字段</p><ul><li><p><code>Content-Type</code>字段用于服务端回应客户端本次数据是什么<strong>格式</strong>。</p></li><li><p><img src="10-content-type字段.png" srcset="/img/loading.gif" lazyload></p></li><li><p><code>Content-Type : text/html; charset=utf-8</code></p></li><li><p>这表明，返回 HTML 格式的数据，数据编码为 UTF-8。</p></li><li><p>当然，客户端也可以用 <code>Accept</code>字段来表示自己<strong>接收哪些数据格式</strong>。</p></li></ul></li><li><p><em>Content-Encoding</em> 字段</p><ul><li><p><code>Content-Encoding</code>字段说明了数据的<strong>压缩方式</strong>。表明服务端返回的数据使用什么格式进行压缩的。</p></li><li><p><img src="11-content-encoding字段.png" srcset="/img/loading.gif" lazyload></p></li><li><p>客户端在请求是可用用 <code>Accept-Encoding</code>字段来说明自己接收哪些压缩方式</p></li></ul></li></ul><h2 id="get-与-post">GET 与 POST</h2><h3 id="get-和-post-有什么区别">GET 和 POST 有什么区别</h3><p><strong>GET 是从服务端获取指定的资源</strong>。GET请求的参数部分一般是写资源在服务器当中的目录（也就是除开服务器域名之后剩余的URL ）。</p><p>由于 URL 规定只支持 ASCII ，因此 GET 请求的参数只允许出现 ASCII字符，并且浏览器回对 URL 的长度进行限制（HTTP 本身对 URL并没有任何规定）。</p><p><img src="12-Get请求.png" srcset="/img/loading.gif" lazyload></p><p>也就是说，<strong>Host 字段指明了服务器的域名，GET字段指明了访问资源在服务器上的位置</strong>。</p><p>POST 是请求服务端对指定资源做出修改。POST请求携带数据的位置一般是写在报文 Body 中，Body中的数据可用是任意格式的数据。</p><p>这种请求一般是用于提交表单当中的。这种情况下客户端会向服务端发送一个POST 请求，将客户填写好的表单放在报文 Body 中，拼接好 POST 头部，通过TCP 协议发送给服务器。</p><p><img src="13-Post请求.png" srcset="/img/loading.gif" lazyload></p><h3 id="get-和-post-方法都是安全和幂等的吗">GET 和 POST方法都是安全和幂等的吗</h3><p>我们先来说明一下 HTTP 里面的「安全」和「幂等」的概念：</p><ul><li>「安全」是指客户端请求的方法<strong>不会</strong>破坏服务端上面的资源</li><li>「幂等」是指重复执行多次操作，结果是<strong>相同</strong>的。</li></ul><p>按照 RFC 规范来看：</p><ul><li>GET就是<strong>安全且幂等</strong>的。因为这是<strong>「只读」</strong>操作，仅仅是客户端请求服务端的数据，服务端的数据并不会被修改，无论执行多少次结果都一样。<ul><li>基于结果相同这一结论，我们可以对 GET请求的数据做缓存，这个缓存可以做到浏览器上，也可以做到代理上，除此之外，浏览器可以将GET 请求保存为书签</li></ul></li><li>POST请求会「新增或提交数据」，它会修改服务端上面的资源，因此是<strong>不安全</strong>的，而且多次操作结果也不相同，因此也<strong>不是幂等</strong>的。所有，浏览器一般不会缓存POST 请求，也不能把 POST 请求保存为书签。</li></ul><p>当然，这些都是 RFC规范来的，实际的开发中并不一定会按照这种规范，即：</p><ul><li>可以用 GET 方法实现增加或删除数据的操作</li><li>可以用 POST 方法实现查询数据的请求</li></ul><p>POST 请求用 Body 传输数据，而 GET 用 URL传输，这样的数据在浏览器的地址栏就可以轻易看到，但我们并不能说 GET 不如POST 安全。</p><p>由于 HTTP的传输都是明文，因此只需要对数据抓个包就都可以看到了。对应于信息需要加密的需求，这便诞生了HTTPS ，不过这个我们下面再说。</p><h2 id="http-缓存技术">HTTP 缓存技术</h2><h3 id="http-缓存实现方式">HTTP 缓存实现方式</h3><p>对于一些重复性的 HTTP请求，我们就可以不用每次都去请求服务器这么麻烦，而是直接把这对「请求-相应」数据存放在本地，下次有需要直接从本地读取就行。</p><p>因此，避免重复性地发生 HTTP请求的方法就是<strong>缓存技术</strong>，在 HTTP协议的头部有不少是针对缓存设计的字段。 HTTP实现的缓存分为两种：<strong>强制缓存</strong>和<strong>协商缓存</strong>。</p><h3 id="强制缓存">强制缓存</h3><p>强制缓存是<strong>只要浏览器判断缓存没有过期</strong>，那么浏览器就会<strong>直接使用本地缓存</strong>，决定是否使用缓存的主动性在浏览器这边。</p><p>下面图中，在 200 状态码后面加上了 <code>from disk cache</code>，就表明使用的是强制缓存（服务器要求浏览器使用强制缓存）</p><figure><img src="1cb6bc37597e4af8adfef412bfc57a42.png" srcset="/img/loading.gif" lazyload alt="img"><figcaption aria-hidden="true">img</figcaption></figure><p>强制缓存利用的是下面这两个<strong>响应头部</strong>字段来实现的，它们都表示资源在客户端的有效期：</p><ul><li><code>Cache-Control</code> 是一个相对时间</li><li><code>Expries</code> 是一个绝对时间</li></ul><p>如果在 HTTP响应头部中二者都存在的话，那么<strong>前者的优先级高于后者</strong>。</p><p><code>Cache-Control</code>的选项更多，设置更精细，因此最好用它来实现强制缓存，具体流程如下：</p><ul><li>当浏览器第一次访问该数据时，服务器会在响应报文的头部加上<code>Cache-Control</code> 字段，这里设置了该资源有效期的大小。</li><li>当浏览器再次访问该资源时，会<strong>将请求资源的时间于<code>Cache-Control</code>当中设置的时间相计算，进而判断该资源是否过期</strong>。如果没有过去则使用缓存，否则重新向服务器发送请求。</li><li>当服务器再次收到请求后，会在响应报文中更新<code>Cache-Control</code> 的大小，进而再次重复刚才的步骤。</li></ul><h3 id="协商缓存">协商缓存</h3><p>上面的强制缓存的意思是「只要资源没有过期，就使用本地资源」，协商缓存的意思是「在资源已经过去的前提下，与服务器协商，<strong>是否使用原来的缓存</strong>」。</p><p>也就是说，出现协商缓存就说明该缓存已经是过期的了，那么如何判断是否过期呢？</p><p>很简单，<strong>只要没有使用强制缓存，那么就说明该资源一定过期了</strong>。</p><p>协商缓存基于两种不同的 HTTP 头部来实现：</p><p>第一种：通过<strong>请求头部</strong>当中的<code>If-Modified-Since</code> 字段与<strong>响应头部</strong>当中的<code>Last-Modified</code> 字段来实现，它们的意思是：</p><ul><li><code>Last-Modified</code> ，用来表示这个资源最后的修改时间</li><li><code>If-Modified-Since</code> ，这东西比较复杂，是一个过程。<ul><li>当浏览器请求资源的时候发现该资源已经过期（浏览器会先检查缓存），浏览器会向服务器发送HTTP 请求，然后服务器会在响应报文当中加上 <code>Last-Modified</code>声明。</li><li>客户端收到响应报文后，会<strong>再次</strong>发起请求并在请求报文当中带上<code>If-Modified-Since</code> 字段，该字段的值是服务器响应报文中<code>Last-Modified</code> 的值。</li><li>服务器收到客户端<strong>第二次</strong>发送过来的请求后，发现请求报文中带有<code>If-Modified-Since</code> 字段，它会与被请求资源最后的修改时间<code>Last-Modified</code> 进行对比。</li><li>如果最后修改时间较新，则返回最新资源；如果最后修改时间较旧，则返回304 响应报文告诉客户端继续使用缓存。</li></ul></li></ul><p>第二种：通过请求头部中的 <code>If-None-Match</code>字段与响应头部中的 <code>ETag</code> 字段，它们的意思分别是：</p><ul><li><code>ETag</code> ，用来标识响应资源</li><li><code>If-None-Match</code> ，这东西也比较复杂，是一个过程<ul><li>当浏览器在第一次请求该资源时，服务器会在响应头部加上<code>ETag</code> 唯一标识，这个标识是根据当前请求的资源生成的。</li><li>当浏览器再次访问该资源时，（在本地缓存已过期的条件下）会在请求头部加上<code>If-None-Match</code> 字段，该字段的值就是 <code>ETag</code>的值。</li><li>服务器第二次收到请求后，会根据 <code>If-None-Match</code>的值与当前请求所生成的 <code>Etag</code> 值进行比较。</li><li>如果值相等，则返回 304 ，告诉浏览器继续使用缓存</li><li>如果不相等，则直接返回新的资源并在响应头部加上新的 <code>ETag</code>标识，再重复上述过程。</li></ul></li></ul><p>整体的过程如下图：</p><p><img src="d92026ce085b401c95cf02b7ce9b7fae.png" srcset="/img/loading.gif" lazyload></p>]]></content>
    
    
    <categories>
      
      <category>计算机网络</category>
      
    </categories>
    
    
    <tags>
      
      <tag>计算机网络</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>C++ 类模板小结</title>
    <link href="/2022/07/30/C++%20Primer%20Plus/%E7%B1%BB%E6%A8%A1%E6%9D%BF/"/>
    <url>/2022/07/30/C++%20Primer%20Plus/%E7%B1%BB%E6%A8%A1%E6%9D%BF/</url>
    
    <content type="html"><![CDATA[<h2 id="模板具体化">模板具体化</h2><p>类模板与函数模板很相似，都有隐式实例化，显示实例化、显示具体化，这些统称为具体化(implicit instantiation)。类模板的声明用<strong>泛型的方式来描述类</strong>，而具体化则是以<strong>具体的类型来生成对应的类声明</strong>。</p><h3 id="隐式实例化">隐式实例化</h3><p>在用类声明对象时，我们需要指定其类型，之后编译器会通过类模板来为该类型生成一个类定义（也就是类的具体化，类的模板参数变为已知），即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">stack&lt;<span class="hljs-type">int</span>&gt;st;<span class="hljs-comment">//编译器会生成一个int类型的类定义，然后用该类定义来实例化对象</span><br></code></pre></td></tr></table></figure><p>这里需要说明的是：类的具体化是指「用指定的类型通过类模板声明来生成对应类型的类定义」。</p><p>我们通常会写出一个带有模板参数的类声明（这个类当中的所有成员方法都写出了模板定义，但我们依旧称整个类为模板类声明），当我们给模板参数指定一个类型的时候，编译器会利用类模板声明自动生成该<strong>类型</strong>所对应的类模板定义（这个时候类模板中的所有模板参数替换为指定的类型）</p><p>这个地方，我们是需要<strong>创建一个对象</strong>之后才会发生隐式实例化，如果没有对象需要创建则不会发生隐式实例化，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">stack&lt;<span class="hljs-type">int</span>&gt;pt*;<span class="hljs-comment">//不会创建对象，自然也不会有隐式实例化，编译器不会为int生成一个类定义</span><br></code></pre></td></tr></table></figure><p>那么为什么是「隐式」呢？</p><p>这里的<strong>隐式</strong>说的应该是类的具体化是隐式的。具体来说，由于<strong>需要创建对象</strong>，那么在创建对象之前就需要<strong>有相应的类定义</strong>，这个时候<strong>类的「具体化」就会被隐式地执行</strong>。</p><p>下面地显式实例化也是同理。虽然没有创建对象，但由于<strong>显式</strong>制定了类型，也就相当于<strong>类的「具体化」会被显式执行</strong>。</p><p>那么为什么要叫「实例化」呢，直接叫「隐式具体化」不就行了？</p><p>我们所说的实例化指的是<strong>用类定义类创建一个对象</strong>，「具体化」仅仅是生成对应的类定义，并不涉及到对象的创建。</p><p>然后，我们需要在这里说明一下什么叫「类声明」和「类定义」。我的理解是：类声明就是只写出类中方法的声明而没有给出定义，类定义是<strong>在前者的基础上给出了类方法的定义</strong>。</p><p>那么「类模板声明」和「类模板定义」就是：前者为<strong>类定义</strong>，不过类当中的类型为模板参数，后者为<strong>类定义</strong>并且类当中的类型为指定类型</p><h3 id="显式实例化">显式实例化</h3><p>显示实例化必须<strong>放在类模板定义所处的名称空间中</strong>。这种情况下，虽然没有创建对象但编译器<strong>利用类模板声明</strong>也会<strong>生成对应类型的类定义</strong>。</p><p>我们只需要用 template 来指定所需类型来声明类即可</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">stack</span>&lt;<span class="hljs-type">int</span>&gt;;<br></code></pre></td></tr></table></figure><p>这么做之后，<strong>编译器会直接创建一个类型为 <code>int</code>的类定义</strong>，就算没有指定对象。</p><h3 id="显式具体化">显式具体化</h3><p>前面说过，<strong>具体化就是用类模板声明来创建对应类型的定义</strong>。有的时候，这种类模板定义并不能满足对于一些特定类型的需求。因此我们需要<strong>为特定的类型单独生成其类模板定义</strong>，这便是显式具体化。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">className</span>&lt;specialized-type-name&gt;<br>&#123;<br>    ...<br>&#125;;<br></code></pre></td></tr></table></figure><p>当需要为 <code>speciailized-type-name</code>创建对应的类模板定义时，编译器<strong>不会通过类模板声明来创建</strong>而是直接使用显式具体化得到的类定义（看到了吗，具体化就是生成对应类型的类定义）。</p><h3 id="部分具体化">部分具体化</h3><p>如果有多个模板参数，那么我们可以给部分模板参数指定类型，这便是「部分具体化」</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T1</span>, <span class="hljs-keyword">class</span> <span class="hljs-title class_">T2</span>&gt;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">P</span> &#123;...&#125;;<br><br><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T1</span>&gt;<span class="hljs-comment">//这个部分写的是没有被具体化的模板参数</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">P</span>&lt;T1, <span class="hljs-type">int</span>&gt; <span class="hljs-comment">//这个部分不用写 class</span><br>&#123;...&#125;;<br></code></pre></td></tr></table></figure><p>当然，有多个模板可供选择的话编译器会选择<strong>具体化程度高的</strong>。</p><p>我们也可以这样：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">P</span>&#123;...&#125;;<br><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>*&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">P</span>&#123;...&#125;;<br><br>P&lt;<span class="hljs-type">char</span>&gt; t1;<span class="hljs-comment">//匹配第一个</span><br>P&lt;<span class="hljs-type">char</span>*&gt; t2;<span class="hljs-comment">//匹配第二个</span><br></code></pre></td></tr></table></figure><p>我们还可以更骚：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T1</span>, <span class="hljs-keyword">class</span> <span class="hljs-title class_">T2</span>, <span class="hljs-keyword">class</span> <span class="hljs-title class_">T3</span>&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">P</span>&#123;...&#125;;<span class="hljs-comment">//下面两个都是这个的特化</span><br><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T1</span>. <span class="hljs-keyword">class</span> <span class="hljs-title class_">T2</span>&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">P</span>&lt;T1, T2, T2&gt;&#123;...&#125;;<br><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T1</span>&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">P</span>&lt;T1, T1*, T1*&gt;&#123;...&#125;;<br></code></pre></td></tr></table></figure><h2 id="成员模板">成员模板</h2><p>一个类模板当中是可以嵌套另外一个类模板的，这一部分我们主要说明一下如何在类外定义这些东西</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt;<span class="hljs-comment">//类声明</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">TP</span><br>&#123;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">V</span>&gt;<span class="hljs-comment">//这个地方不能写 T , 下面同理</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">HD</span>;        <br>HD&lt;T&gt;a;<br>HD&lt;T&gt;b;<br><br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">TP</span>(T e1, T e2, T e3, T e4)<br>: <span class="hljs-built_in">a</span>(e1, e3), <span class="hljs-built_in">b</span>(e2, e4)<br>&#123;  &#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">show</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span>;<br><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> Y&gt;</span><br><span class="hljs-function">Y <span class="hljs-title">cal</span><span class="hljs-params">()</span></span>;<br>&#125;;<br></code></pre></td></tr></table></figure><p>这个例子基本上囊括全了，在类模板当中既有<strong>类模板声明</strong>也有<strong>函数模板声明</strong>。不管你怎么写，他们的在类外的定义总归是一样的</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt;<br><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">V</span>&gt;<span class="hljs-comment">//同样，不能写 T</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">TP</span>&lt;T&gt;::HD <span class="hljs-comment">//类的话前面就是 class ，函数的话前面就是返回值</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">HD</span>(V e1, V e2 = e1)<br>:<span class="hljs-built_in">a</span>(e1), <span class="hljs-built_in">b</span>(e2)<br>&#123;  &#125;<br><br><span class="hljs-function">V <span class="hljs-title">re_a</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123; <span class="hljs-keyword">return</span> a; &#125;<br><span class="hljs-function">V <span class="hljs-title">re_b</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123; <span class="hljs-keyword">return</span> b; &#125;<br><br><span class="hljs-keyword">private</span>:<br>V a, b;<br>&#125;;<br><br><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt;<br><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> Y&gt;<br>Y TP&lt;T&gt;::<span class="hljs-built_in">cal</span>()<br>&#123;<br><span class="hljs-keyword">return</span> a.<span class="hljs-built_in">re_a</span>() + a.<span class="hljs-built_in">re_b</span>() + b.<span class="hljs-built_in">re_a</span>() + b.<span class="hljs-built_in">re_b</span>();<br>&#125;<br><br><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt;<br><span class="hljs-type">void</span> TP&lt;T&gt;::<span class="hljs-built_in">show</span>()<span class="hljs-type">const</span><br>&#123;<br>cout &lt;&lt; a.<span class="hljs-built_in">re_a</span>() &lt;&lt; <span class="hljs-string">&quot; &quot;</span> &lt;&lt; a.<span class="hljs-built_in">re_b</span>() &lt;&lt; endl;<br>cout &lt;&lt; b.<span class="hljs-built_in">re_a</span>() &lt;&lt; <span class="hljs-string">&quot; &quot;</span> &lt;&lt; b.<span class="hljs-built_in">re_b</span>() &lt;&lt; endl;<br>&#125;<br></code></pre></td></tr></table></figure><p>关于写了两层 <code>template</code>，我需要说明一点的是：由于类内部的类 <code>HD</code> 只能由类名<code>TP</code> 解析而得（因为模板是<strong>嵌套</strong>的，绝对不能用<code>template T, template V</code>这种，这就<strong>不是嵌套了</strong>），而关于类名 <code>TP</code>必须要给出其模板参数类型才行，而类 <code>HD</code> 的定义本身也需要一层<code>template</code> ，这便才有了两层。</p><p>我需要额外说明一点：这里的 HD 的模板参数跟 TP的模板参数不同，当然也可以写成相同，这样的话只需要在类外写一层<code>template</code> 就行。</p><h2 id="将模板作为参数">将模板作为参数</h2><p>我们上面提到的类模板的声明都是 <code>template&lt;class T&gt;</code>。这里面的 <code>T</code>可以表示任何类型，注意，是<strong>「类型」</strong>。那么什么叫「类型」？</p><p>个人理解是「能够用于初始化对象的」叫做类型。</p><p>就比如可以用 <code>int</code> 初始化对象，因此 <code>int</code>就是类型；可以用 <code>stack&lt;int&gt;</code> 初始化变量，那么<code>stack&lt;int&gt;</code> 就是类型。而我们在模板中所用的参数<code>T</code> ，实际上是用<strong>这个类型</strong>来声明模板类。</p><p>也就是说，类模板中的所有成员变量的<strong>类型</strong>都是<code>T</code> 。</p><p>那么这里就有问题了。如果我目前所需要写的一个类模板当中的大部分功能可以直接通过<code>STL</code> 容器实现，即<strong>我可以通过对 <code>STL</code>容器采取复合的操作来实现我的需求</strong>。而这个「复合」，指的就是我自己写的那个类当中的成员变量需要有<strong>类模板</strong>。</p><p>而类模板并不是类型，无法于模板参数 <code>T</code>相匹配。为了解决这个问题，我们引入了<strong>将模板作为参数</strong>。</p><p>我们直接看例子吧：</p><p>我们知道，<code>stack</code> 的模板声明为<code>template&lt;class _Ty, class _Container = deque&lt;_Ty&gt;&gt;</code>。如果我们在类模板中希望使用 <code>stack</code>类，那么我们的类模板可以这么定义：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">_Ty</span>, <span class="hljs-keyword">class</span> <span class="hljs-title class_">_Container</span> = deque&lt;_Ty&gt;&gt;<span class="hljs-keyword">class</span> Type&gt;<br><span class="hljs-keyword">class</span> TE<br>&#123;<br><span class="hljs-keyword">private</span>:<br>Type&lt;<span class="hljs-type">int</span>&gt;s1;<span class="hljs-comment">//这里可以是其他的类型</span><br>Type&lt;<span class="hljs-type">int</span>&gt;s2;<br>&#125;;<br></code></pre></td></tr></table></figure><p>我们着重分析一下第一行。一般的都是<code>template&lt;class T&gt;</code> （我们这里统一用 <code>class</code>）。</p><p>这里的 <code>T</code>是<strong>非模板类型</strong>模板参数的意思，这里的 <code>Type</code>是<strong>模板类型</strong>模板参数的意思。</p><p>后者需要在前者的前面加上 <code>Type</code>是哪种类型的模板（这个模板的模板参数是什么）。</p><p>（这个地方我感觉我的语言说不清楚，我。。。觉得我理解了，但我说不出来。。。。）</p><p>我们后面在使用的时候是这么写的：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">TE&lt;stack&gt;T;<br></code></pre></td></tr></table></figure><p>编译器会用 <code>stack</code> 替代 <code>Type</code> ，因此<code>Type</code> 必须为模板类型并且该模板类型必须与 <code>stack</code>的模板类型一致。</p><h2 id="模板类和友元">模板类和友元</h2><p>模板类的声明中可以加入友元。模板友元分三类。</p><ul><li>非模板友元</li><li>约束模板友元<ul><li>友元类型取决于类被实例化时的类型</li></ul></li><li>非约束模板友元<ul><li>友元的所有具体化都是类的每一个具体化的友元</li></ul></li></ul><h3 id="非模板友元">非模板友元</h3><p>友元，实际上是给类外对象访问类内成员的一种方法（虽然这种方法会破坏封装）</p><p>我们在友元函数参数的部分通常会写这个类的类型。同理，对于类模板而言，我们需要为该类模板<strong>显式具体化</strong>。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-keyword">friend</span> <span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(Test&lt;T&gt; rhs)</span></span>;<span class="hljs-comment">//使用类模板中的成员</span><br>    <span class="hljs-function"><span class="hljs-keyword">friend</span> <span class="hljs-type">void</span> <span class="hljs-title">show</span><span class="hljs-params">()</span></span>;<span class="hljs-comment">//不使用类模板中的成员</span><br>&#125;;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(Test&lt;<span class="hljs-type">int</span>&gt; rhs)</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; <span class="hljs-string">&quot;Chisato&quot;</span> &lt;&lt; endl;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">show</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    cout &lt;&lt; <span class="hljs-string">&quot;Chisato&quot;</span> &lt;&lt; endl;<br>&#125;<br></code></pre></td></tr></table></figure><p>这个其实没有什么好说的，跟普通的类写友元函数一样。我们重点看后面两个</p><h3 id="约束模板友元">约束模板友元</h3><p>你会发现，上面那种写法的话会很麻烦。为什么？</p><p>你想啊， <code>Test</code> 是一个类模板，可以适用于任何类型，而<code>fun</code> 函数只能用于 <code>Test</code> 对于 <code>int</code>类型的显式具体化，而对于其他类型的显式具体化则没有办法。</p><p>说人话就是，你每用 <code>Test</code>来生成某个类型的具体化时，都需要对 <code>fun</code> 进行一次重载。</p><p>好家伙，这不累死人。作为一个优雅（<del>懒惰</del>）的程序员我觉得不允许这种事情的发生。这里便引入了约束模板友元。</p><p>我们想到，既然函数模板可以适用于任何类型，类模板也可以适用于任何类型。如果我将函数模板作为类的友元不就完美了？</p><p>事实上，约束模板友元就是这么做的。</p><p>我们直接看代码吧，这东西。。。。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt; <span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(T&amp; rhs)</span></span>;<span class="hljs-comment">//一定要在类模板的前面先声明函数模板</span><br><br><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt; <span class="hljs-type">void</span> <span class="hljs-title">show</span><span class="hljs-params">()</span></span>;<br><br><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-keyword">friend</span> <span class="hljs-type">void</span> fun&lt;Test&lt;T&gt;&gt;(Test&lt;T&gt;&amp; rhs);<br>    <span class="hljs-comment">//也可以这么写：friend void fun&lt;&gt;(Test&lt;T&gt;&amp; rhs);</span><br><span class="hljs-keyword">friend</span> <span class="hljs-type">void</span> <span class="hljs-built_in">show</span>&lt;T&gt;();<br><br><span class="hljs-built_in">Test</span>(T rhs = <span class="hljs-number">0</span>)<br>: <span class="hljs-built_in">n</span>(rhs)<br>&#123;  &#125;<br><span class="hljs-keyword">private</span>:<br>T n;<br>&#125;;<br><br><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(T&amp; rhs)</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; rhs.n &lt;&lt; endl;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">show</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; <span class="hljs-string">&quot;Chisato&quot;</span> &lt;&lt; endl;<br>&#125;<br></code></pre></td></tr></table></figure><p>首先，最上面那个模板声明只能写在同一行，分两行写会报警告（我也不知道为什么，哦卡西）</p><p>至于类模板当中为什么要写成<code>friend void fun&lt;Test&lt;T&gt;&gt;(Test&lt;T&gt;&amp; rhs)</code>，我们带入一个具体类型可以理解了。</p><p>把 <code>T</code> 替换成 <code>int</code> ，由于 <code>fun</code>本身是模板函数，因此需要显式指出其模板参数，那么对于<code>Test&lt;int&gt;</code> 类型的对象， <code>fun</code>的声明就应该写成<code>fun&lt;Test&lt;int&gt;&gt;(Test&lt;int&gt;&amp; rhs)</code> 。</p><p>实际上在模板里面也可以写成<code>fun&lt;&gt;(Test&lt;T&gt;&amp; rhs)</code>，因为可以通过函数的参数推导得出<strong>前面</strong>那么尖括号里面的内容。</p><p>而如果调用的是 <code>show</code>，这玩意没有参数，就需要显式指出其类型了。具体的调用如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-function">Test&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">t</span><span class="hljs-params">(<span class="hljs-number">5</span>)</span></span>;<br><span class="hljs-built_in">fun</span>(t);<br>Test&lt;<span class="hljs-type">double</span>&gt;<span class="hljs-built_in">r</span>(<span class="hljs-number">15</span>);<br><span class="hljs-built_in">fun</span>(r);<br><span class="hljs-built_in">show</span>&lt;<span class="hljs-type">int</span>&gt;();<br><span class="hljs-built_in">show</span>&lt;<span class="hljs-type">double</span>&gt;();<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="非约束友元函数">非约束友元函数</h3><p>前面一种的情况是：<code>int</code> 类型的类的具体化会生成<code>int</code> 类型的函数的具体化，反过来， <code>int</code>类型的函数的<strong>只能访问</strong> <code>int</code> 类型的类的具体化。这被称为是<strong>约束的</strong>。</p><p>当然还有非约束的，这种的情况是：对于一个友元函数，它可以访问<strong>任何</strong>类型的类当中的成员，即<strong>每个函数具体化都是每个类具体化的友元</strong>。</p><p>我们只需要在类内声明友元函数就可以实现这一点。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> C, <span class="hljs-keyword">typename</span> U&gt; <span class="hljs-keyword">friend</span> <span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(C&amp;, U&amp;)</span></span>;<br><br><span class="hljs-built_in">Test</span>(T rhs = <span class="hljs-number">0</span>)<br>: <span class="hljs-built_in">n</span>(rhs)<br>&#123;  &#125;<br><span class="hljs-keyword">private</span>:<br>T n;<br>&#125;;<br><br><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> C, <span class="hljs-keyword">typename</span> U&gt; <span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(C&amp; rhs, U&amp; chs)</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; rhs.n &lt;&lt; <span class="hljs-string">&quot; &quot;</span> &lt;&lt; chs.n &lt;&lt; endl;<br>&#125;<br></code></pre></td></tr></table></figure><p>这种没什么好说的，知道是什么意思就行，还满简单的</p><p>其实就是一个函数模板作为友元存在，整个非约束友元，花里胡哨的（（（</p><p>下面是具体的调用代码：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>Test&lt;<span class="hljs-type">int</span>&gt;<span class="hljs-built_in">t</span>(<span class="hljs-number">10</span>);<br>Test&lt;<span class="hljs-type">double</span>&gt;<span class="hljs-built_in">r</span>(<span class="hljs-number">1.5</span>);<br><span class="hljs-built_in">fun</span>(t, r);<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>类模板结束了， 我可以去看 Effective C++当中泛型的部分了，走起！！！！</p>]]></content>
    
    
    <categories>
      
      <category>C++ Primer Plus</category>
      
    </categories>
    
    
    <tags>
      
      <tag>C++ Primer Plus</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Effective C++ 笔记</title>
    <link href="/2022/07/30/Effective%20C++/Effective%20C++/"/>
    <url>/2022/07/30/Effective%20C++/Effective%20C++/</url>
    
    <content type="html"><![CDATA[<h1 id="尽量以-const-enum-inline-替换-define">1.尽量以 const, enum,inline 替换 #define</h1><p>当我写出</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">define</span> SIZE = 1.638;</span><br></code></pre></td></tr></table></figure><p>记号SIZE会在预编译阶段就被预处理器给替换掉，<strong>这使得SIZE这个名称不会被编译器看到，进而导致SIZE不会进入记号表(symboltable)</strong>。当我用此常量而获得一个错误的编译信息时，这个错误信息会提到1.638 而不会提到 SIZE 。如果我将 SIZE写在一个头文件内，这便会导致追踪该变量会浪费时间</p><p>因此，我们可以使用以下代码来进行替换</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">const</span> <span class="hljs-type">double</span> Size = <span class="hljs-number">1.638</span>;<br></code></pre></td></tr></table></figure><p>作为一个常量，它一定会被编译器看见进而进入记号表内</p><p>在用常量替换 #define 时，我们需要注意两种情况。</p><ul><li>第一个是定义常量指针。</li></ul><p>当我们定义常量指针并将其放入头文件内时，我们需要将该指针声明为 const。一般来说，这种常量指针会指向一个字符常量，即</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">const</span> <span class="hljs-type">char</span>* <span class="hljs-type">const</span> authorName = <span class="hljs-string">&quot;Mellina&quot;</span>;<br></code></pre></td></tr></table></figure><p>这里我们需要使用两个 const ，当然我们也可以用 string 来代替，即</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">const</span> string authorName = <span class="hljs-string">&quot;Mellina&quot;</span>;<br></code></pre></td></tr></table></figure><ul><li>第二个是 class 内部的常量</li></ul><p>当我需要将常量的作用域限定于 class 内时，我需要对成员变量使用 const，与此同时，既然是常量，那么它就<strong>只能有一个实体</strong>，因此我还需要用static 来进行修饰。</p><p>对于非 const 修饰的 static 成员变量而言，<strong>必须在 class内部声明， 在 class 外部赋值</strong>，即声明式需要写在 class内，定义式需要写在 class 外。</p><p>但是，当该成员变量被 const 修饰时，我们可以<strong>直接在 class内部对其赋值</strong>，即</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-type">const</span> <span class="hljs-type">static</span> <span class="hljs-type">int</span> size = <span class="hljs-number">10</span>;<br><span class="hljs-type">int</span> arr[size];<br>&#125;;<br></code></pre></td></tr></table></figure><p>对于 class 专属常量又被 static 修饰且为整数类型（int, char,bool），只要不取出它们的地址，那么我便可以声明便使用它而无需提供定义式。但如果我取出它的地址或者编译器要求看到其定义式，那么我们需要另外提供其定义式：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">const</span> <span class="hljs-type">int</span> Test::size;<br></code></pre></td></tr></table></figure><p>将该式子放入一个<strong>实现文件</strong>当中即可（这里不需要赋值，因为在声明的时候已经有初值了）</p><p>当然，如果编译器不支持上述语法（这种语法也只对整型常量进行），我们按照static 修饰成员变量的方式赋值即可</p><p>对于 #define 言，它并不支持任何封装，即它不能用于定义 class专属常量</p><p>如果编译器不允许上述做法，我们还可以通过 enum 来进行实现，即</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-keyword">enum</span><br>    &#123;<br>        size = <span class="hljs-number">5</span>    <br>    &#125;<br><span class="hljs-type">int</span> arr[size];<br>&#125;;<br></code></pre></td></tr></table></figure><p>使用 enum ，将 size 变成一个表示 5的记号。值得注意的是，我们<strong>取一个 enum类型的变量的地址是不合法的，同样地，取一个 #define地地址也是不合法的</strong></p><p>#define的另一种用法是定义宏，其优点是可以避免进行函数调用进而实现更快速的运算。当然，宏会出现一些意料之外的问题（哪怕我加了括号也一样），这个时候我们可以<strong>使用inline 来对宏进行替换</strong>，它们的本质都是对代码块的替换</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">define</span> FUN(X, Y) f((X) &gt; (Y) ? (X) : (Y));<span class="hljs-comment">//f为一个函数</span></span><br></code></pre></td></tr></table></figure><p>该代码有如下问题</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> a = <span class="hljs-number">5</span>, b = <span class="hljs-number">0</span>;<br><span class="hljs-built_in">FUN</span>(++a, b);     <span class="hljs-comment">//a 会自增两次</span><br><span class="hljs-built_in">FUN</span>(++a, b + <span class="hljs-number">10</span>);<span class="hljs-comment">//a 只会自增一次</span><br></code></pre></td></tr></table></figure><p>自然，我们可以用 inline 加上 template 来进行替换</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp">templete &lt;<span class="hljs-keyword">typename</span> T&gt;<br><span class="hljs-function"><span class="hljs-keyword">inline</span> <span class="hljs-type">void</span> <span class="hljs-title">FUN</span><span class="hljs-params">(<span class="hljs-type">const</span> T&amp; x, <span class="hljs-type">const</span> T&amp; y)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">f</span>(x &gt; y ? x : y);<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="尽可以使用-const">2.尽可以使用 const</h1><p>如果 const 出现在星号的左边，表示该指针所指向的物是常量，即<strong>不能通过该指针对这个值进行修改，但指针的指向可以被修改</strong>；如果出现在星号右边，说明该指针是常量，即<strong>该指针的指向不允许被修改，但该指针指向的值可以被指针修改</strong></p><p>在STL中，iterator本质上是个 T* 的指针，当我用 const对其进行修饰时，说明该指针是一个常量，不能随便修改，即对应于 T *const。如果我希望STL给我模拟一个指向常量的指针，即对应于 T const*，那么我应该使用 const_iterator</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp">vector&lt;<span class="hljs-type">int</span>&gt;vec;<br><span class="hljs-type">const</span> vector&lt;<span class="hljs-type">int</span>&gt;::iterator it = vec.<span class="hljs-built_in">begin</span>();<br>*it = <span class="hljs-number">10</span>;<span class="hljs-comment">//Allow</span><br><span class="hljs-comment">//it++;//Error</span><br><br>vector&lt;<span class="hljs-type">int</span>&gt;::const_iterator cIt = vec.<span class="hljs-built_in">begin</span>();<br><span class="hljs-comment">//cIt = 10;//Error</span><br>cIt++;<span class="hljs-comment">//Allow</span><br></code></pre></td></tr></table></figure><p>令函数返回一个常量值（返回值被 const修饰），这样做可以避免相关错误而造成的意外。</p><p>我们考虑一个自定义的数据类型 Real ，当我们对 *进行重载运算符时，我们可以写成</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">const</span> Real <span class="hljs-keyword">operator</span>* (<span class="hljs-type">const</span> Real&amp; e1, <span class="hljs-type">const</span> Real&amp; e2);<br></code></pre></td></tr></table></figure><p>参数部分的 const我们不做讨论，我们将重点放在返回值上。在返回值那里加上 const是为了避免因手误写成</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">(a * b) = c;<br></code></pre></td></tr></table></figure><p>而导致程序出错（说不定真的会有天才这么写吧）。对于函数的形参，<strong>除非是该函数会修改这个值，否则都应该用const 修饰</strong></p><h2 id="const-成员函数">const 成员函数</h2><p>对于 class的成员函数，我们可以通过其常量性对其进行重载，即同一个重载函数对应于const 对象版本和 non-const 对象版本</p><p>当然，我们需要知道：</p><ul><li>常对象（被 const 修饰的对象）只能调用常函数（class 中被 const修饰的函数）</li><li>常函数不能修改 class 当中任何 non-const 的成员，当然 const成员可以被其修改</li></ul><p>基于这两点，我们看下面的代码</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-type">char</span>&amp; <span class="hljs-keyword">operator</span>[](std::<span class="hljs-type">size_t</span> pos)<br>&#123;<br><span class="hljs-keyword">return</span> str[pos];<br>&#125;<br><span class="hljs-type">const</span> <span class="hljs-type">char</span>&amp; <span class="hljs-keyword">operator</span>[](std::<span class="hljs-type">size_t</span> pos) <span class="hljs-type">const</span><br>&#123;<br><span class="hljs-keyword">return</span> str[pos];<span class="hljs-comment">//我们不允许常对象修改 class 内部的成员变量，因此在返回值那里要加上 const</span><br>&#125;<br><span class="hljs-built_in">Test</span>(<span class="hljs-type">const</span> std::string&amp; str)<br>&#123;<br><span class="hljs-keyword">this</span>-&gt;str = str;<br>&#125;<br><span class="hljs-keyword">private</span>:<br>std::string str;<br>&#125;;<br></code></pre></td></tr></table></figure><p>对于 non-const 对象而言，我们调用 [] 时用的是 non-const成员函数，这也是通常情况；对于 const 对象而言，我们调用 [] 时用的是const 成员函数，这里便可以于上面结合起来了。</p><p>在我们写函数的时候，如果形参不会对输入值进行改变，那么我们要加上const 。而形参可以是 Test 类型的数据，如果在形参部分加上了 const，那么就说明该形参是一个常对象，它不能调用 non-const 成员函数。</p><p>因此，<strong>当成员函数不会改变 class 中的任何一个成员（包括 const和 non-const）时，我们写重载函数时需要写两个</strong></p><p>我们刚刚所讨论的全部都是基于 const 对象与 const 成员函数不能修改class 内部的成员变量这一点的，如果我 class 的内部当中的部分数据被 const对象与 const 成员函数所修改是允许的，即我希望 const 成员函数能够修改部分class 当中的成员变量。</p><p>为了应对这一需求，<strong>我们可以在变量前面加上 mutable来允许该变量可以被 const 所修饰的成员函数来修改</strong></p><p>我们看如下代码</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">CTest</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-type">char</span>&amp; <span class="hljs-keyword">operator</span>[](std::<span class="hljs-type">size_t</span> pos)<br>&#123;<br><span class="hljs-keyword">return</span> str[pos];<br>&#125;<br><span class="hljs-type">const</span> <span class="hljs-type">char</span>&amp; <span class="hljs-keyword">operator</span>[](std::<span class="hljs-type">size_t</span> pos) <span class="hljs-type">const</span><br>&#123;<br><span class="hljs-keyword">return</span> str[pos];<br>&#125;<br><span class="hljs-function">std::<span class="hljs-type">size_t</span> <span class="hljs-title">length</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">if</span> (!lengthIsValid)<br>&#123;<br>Length = std::<span class="hljs-built_in">strlen</span>(str);<br>lengthIsValid = <span class="hljs-literal">true</span>;<br>&#125;<br><span class="hljs-keyword">return</span> Length;<br>&#125;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">char</span>* str;<br><span class="hljs-keyword">mutable</span> std::<span class="hljs-type">size_t</span> Length;<span class="hljs-comment">//最近一次计算的长度</span><br><span class="hljs-keyword">mutable</span> <span class="hljs-type">bool</span> lengthIsValid;<span class="hljs-comment">//目前长度是否有效</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>对于 length 函数，它并不会改变 str的值，仅仅只是想获取其长度。但于此同时我们需要对该长度是否有效进行检验，这就必定会导致需要修改其成员变量，而mutable 则很好地解决了这个问题。</p><p>事实上，这个地方我们还有一个很严重的问题，就是我们的重载函数写了两个版本，而这两个版本的函数体是完全一样的！！！作为一个优雅的程序员，你怎么可能允许这种事情发生，当然关于这个问题，我们也有其解决办法。嘛，今天就先到这里，明天我们再看。</p><p>首先，我们回顾一下C风格地转型动作</p><ol start="20" type="A"><li>expression //将 expression 转型为 T</li></ol><p>函数类型是这样的：</p><p>T(expression) //将 expression 转型为T</p><p>C++提供四种新式类型转换</p><ul><li>const_cast<T>( expression )</T></li><li>dynamic_cast<T>( expression )</T></li><li>reinterpret_cast<T>( expression )</T></li><li>static_cast<T>( expression )</T></li></ul><p>它们的作用为</p><ol type="1"><li>const_cast 通常用于将对象的常量性移除，即将 const类型的对象转换为对应 non-const 类型的对象</li><li>dynamic_cast 主要用于执行“安全向下转型“(safe downcasting)，也就是用来决定某对象是否归属继承体系中的某个类型</li><li>reinterpret_cast意图执行低级转型，实际动作（及结果）可能取决于编译器，因此它是不可移植的。例如将一个pointer to int 转型为 int。</li><li>static_cast 用来强迫隐式转换，例如将 nont-const 对象转换为 const对象，或将 int 转为 double 等待</li></ol><p>知道了这些之后，我们再来看那个代码复用的问题。我们有两个一样的函数，如果我能在一个函数中调用另一个函数，那么我就可以完美解决代码复用这个问题，那么现在问题的关键就变成我该在哪个函数中调用另一个函数。</p><p>如果我们考虑在调用 const成员函数时，让传入的参数发生类型转换进而得以调用另一个函数，那么这就会导致违背const 成员函数自身的承诺——不会改变对象的逻辑状态。</p><p>说人话就是 const 成员函数不允许传入的对象有任何的更改，但我想通过const 函数调用 non-const函数就必定需要发生转换，这里就产生了矛盾，因此我们采用这种——当我调用non-const 函数时，对其发生类型转换进而调用 const 函数。实际代码为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">char</span>&amp; <span class="hljs-keyword">operator</span>[](std::<span class="hljs-type">size_t</span> pos)<br>&#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-built_in">const_cast</span>&lt;<span class="hljs-type">char</span>&amp;&gt;(<span class="hljs-built_in">static_cast</span>&lt;<span class="hljs-type">const</span> CTest&amp;&gt;(*<span class="hljs-keyword">this</span>)[pos]);<br>&#125;<br><span class="hljs-type">const</span> <span class="hljs-type">char</span>&amp; <span class="hljs-keyword">operator</span>[](std::<span class="hljs-type">size_t</span> pos) <span class="hljs-type">const</span><br>&#123;<br>    <span class="hljs-keyword">return</span> str[pos];<br>&#125;<br></code></pre></td></tr></table></figure><p>这里类型转换发生了两次。<strong>第一次是将该对象从 non-const 转换为const</strong> ，这一步实施完成后才能调用 const 成员函数。然后 const成员函数返回的是 const char&amp; ，我们需要将该类型转换成 char&amp;，<strong>这里便是第二次转换</strong></p><p>有一点需要值得注意的是第一次转换的类型是 constCTest&amp;。这里我的理解是：this 指针本身指向调用该成员函数的对象，*this 则是该对象的值，这个值的类型是 CTest&amp; （它的类型应该是CTest，但这里在C语言里面，C++引入了左值引用和右值引用的概念后，这里应当为CTest&amp;），我们需要在它的基础是加上 const ，于是便有了 constCTest&amp;。如果不加这个引用那么它将不会更改 this 所指向的对象的值</p><h1 id="确认对象被使用前已先被初始化">3.确认对象被使用前已先被初始化</h1><p>对于无任何成员的内置数据类型，<strong>我们需要在其使用前就手动将其初始化</strong>。对于自定义数据类型，其初始化则需要靠构造函数，即<strong>确保每一个构造函数都能将对象的每一个成员初始化</strong></p><p>考虑下面这个例子：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Test</span>(<span class="hljs-type">const</span> string&amp; e1, <span class="hljs-type">const</span> vector&lt;<span class="hljs-type">int</span>&gt;&amp; e2, <span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e3)<br>&#123;<br>str = e1;<br>vec = e2;<br>num = e3;<br>&#125;<br><span class="hljs-keyword">private</span>:<br>string str;<br>vector&lt;<span class="hljs-type">int</span>&gt; vec;<br><span class="hljs-type">int</span> num;<br>&#125;;<br></code></pre></td></tr></table></figure><p>C++规定，<strong>对象的成员变量的初始化动作发生在进入构造函数之前</strong>。也就是说在Test 构造函数内，str 、 vec 、 num都不是被初始化而是被赋值，它们的初始化<strong>发生在调用构造函数之前</strong>，即这些成员变量的<em>default</em>构造函数发生时机<strong>早于</strong>该对象的构造函数发生时机。但这一点对于内置数据类型（这里的int）并不有效，即<strong>对于内置数据类型就是在对象的构造函数内初始化的</strong></p><p>我们考察上面这一过程，发现调用该对象的构造函数时发生了：调用该成员变量的<em>default</em> 构造函数和对该成员变量进行赋值。那么 <em>default</em>构造函数所做的工作就被浪费了，想要避免这种情况发生，我们可以使用初始化列表</p><p>将上述构造函数改为</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-built_in">Test</span>(<span class="hljs-type">const</span> string&amp; e1, <span class="hljs-type">const</span> vector&lt;<span class="hljs-type">int</span>&gt;&amp; e2, <span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e3) :<br><span class="hljs-built_in">str</span>(e1),<br><span class="hljs-built_in">vec</span>(e2),<br><span class="hljs-built_in">num</span>(e3)<br>&#123; &#125;<br></code></pre></td></tr></table></figure><p>采用初始化列表之后，成员变量便会调用 <em>copy</em>构造函数来进行初始化。</p><p>当然，在该对象的 <em>default</em> 构造函数内，我们需要<em>default</em> 构造一个成员变量，我们可以这么写：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-built_in">Test</span>() :<br><span class="hljs-built_in">str</span>(),<br><span class="hljs-built_in">vec</span>(),<br><span class="hljs-built_in">num</span>(<span class="hljs-number">0</span>)<br>&#123; &#125;<br></code></pre></td></tr></table></figure><p>总结一下就是<strong>最后使用初始化列表，并且要列出所有的成员变量</strong></p><p>当然，如果变量很多，或者存在多重继承的情况下，可以把那些”初始化和赋值看起来是一样“的变量封装起来（通常是private权限），供构造函数调用。这种做法适用于该成员变量需要<strong>从文件或者数据库中读入的情况</strong></p><h2 id="初始化次序">初始化次序</h2><p>对于一个 class当中的成员变量而言，它们的初始化是有顺序的，它们必定是按照<strong>从上到下</strong>的顺序来进行初始化，尽管在初始化列表那里可能写的不一样（但这并没有关系）</p><p>这样的好处在于，如果存在一个数组，其大小需要用到该 class中其他的成员变量，我们只需要将该成员变量<strong>先于</strong>该数组初始化即可，并且这里的<strong>先于</strong>是可控的（毕竟只需要放在数组定义的前面就行）</p><p>在此，我们来考虑一个更深层次的问题——如果我写了多个源文件，这些源文件当中的变量需要相互初始化，我该如何确定它们的初始化次序？说的专业一点就是：不同编译单元内定义的non-local static 对象的初始化次序</p><p>我们先看 static 对象。所谓 static对象，其生命周期是从被构造那一刻起一直到程序结束为止。很显然，这包括global 对象、定义在 namespace 作用域下的对象、在 class内、在函数内、以及在 file 作用域内被 static 修饰的对象。程序结束时static 对象会被自动销毁，在 main 函数的结尾会自动调用它们的析构代码</p><p>这里有一个分类是：<strong>如果是在函数内定义的 static 对象，称为local static 对象</strong>（相对于函数而言是 local的），<strong>其余的都是 non-local static 对象</strong></p><p>所谓编译单元是指产出单一目标文件（ obj 文件，一个源文件会对应一个 obj文件）的那些源码。</p><p>明白了这些概念之后上面那个问题可以更加清晰地表述为：如果某个编译单元内的non-local static 对象使用了另外一个编译单元内的 non-local static对象，但它所使用的这个对象可能并没有在它前面完成初始化。这是因为C++对”定义于不同编译单元内的non-local static对象“的初始化次序并无规定。（C++居然对这个没有规定，我直接 虎躯一震.jpg加 黑人问号.jpg ）</p><p>我们用具体实现来分析这个问题</p><p>目前我们有一个 class ，并且用 extern声明了一个变量供其他源文件使用</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Fun</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">size_t</span> <span class="hljs-title">Cal</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span>;<span class="hljs-comment">//其中一个成员函数</span><br>&#125;;<br><br><span class="hljs-keyword">extern</span> Fun tfs;<br></code></pre></td></tr></table></figure><p>其他源文件使用了外部变量 tfs 。你也可以用这个 class 来创建对象</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Dir</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Dir</span>()<br>&#123;<br><span class="hljs-type">size_t</span> tem = tfs.<span class="hljs-built_in">Cal</span>();<span class="hljs-comment">//使用 tfs 来创建对象</span><br>&#125;<br>&#125;;<br><br>Dir temp;<br></code></pre></td></tr></table></figure><p>这个时候就会出现上面所说的问题。那么问题是我们要怎么解决呢？</p><p>我们将每个 non-local static对象都搬到自己的专属函数里面去，在此函数内将该变量声明为 static，并且该函数返回该变量的 reference。也就是说，我们用一个函数来对non-local static 对象进行封装，将其变为 local static 对象</p><p>具体实现为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Fun</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">size_t</span> <span class="hljs-title">Cal</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span>;<span class="hljs-comment">//其中一个成员函数</span><br>&#125;;<br><span class="hljs-comment">//核心在这里</span><br><span class="hljs-function"><span class="hljs-keyword">inline</span> Fun&amp; <span class="hljs-title">tfs</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">static</span> Fun tmp;<br><span class="hljs-keyword">return</span> tmp;<br>&#125;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Dir</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Dir</span>()<br>&#123;<br><span class="hljs-type">size_t</span> tem = <span class="hljs-built_in">tfs</span>().<span class="hljs-built_in">Cal</span>();<span class="hljs-comment">//这里发生了变化</span><br>&#125;<br>&#125;;<br><br><span class="hljs-function"><span class="hljs-keyword">inline</span> Dir&amp; <span class="hljs-title">temp</span><span class="hljs-params">()</span><span class="hljs-comment">//直接使用 temp 函数来创建对象，当然这是在其他源文件需要使用 temp 对象的情况下才需要这么做</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">static</span> Dir tmp;<br><span class="hljs-keyword">return</span> tmp;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>这种封装在频繁调用的情况下可以写成 inline 的形式</strong></p><h1 id="了解c默默编写并调用了哪些函数">4.了解C++默默编写并调用了哪些函数</h1><p>当我写下一个 class 时，编译器会自动帮我创建 <em>default</em>构造函数、<em>copy</em> 构造函数、 <em>copy assignment</em>操作符和一个析构函数，这些函数都是 public 和 inline的。当然，这些函数<strong>只有在被调用时才会创建</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Empty</span><br>&#123;<br>    <span class="hljs-built_in">Empty</span>() &#123;&#125;<br>    <span class="hljs-built_in">Empty</span>(<span class="hljs-type">const</span> Empty&amp; e1) &#123;&#125;<br>    ~<span class="hljs-built_in">Empty</span>() &#123;&#125;<br>    Empty&amp; <span class="hljs-keyword">operator</span>=(<span class="hljs-type">const</span> Empty&amp; e1) &#123;&#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>在这里我们需要注意的是赋值操作符，这个函数有的时候编译器并不会帮你创建</p><p>编译器自动创建的 <em>copy assignment</em>操作符是逐字节赋值（这当然会引发深拷贝的问题，但我们暂且不提这一点），也就是说它会将新对象的成员变量全部<strong>赋值</strong>为原对象的成员变量（这里是赋值，不是初始化）。</p><p>那么这里的问题就是如果成员变量当中存在 referencd 或者 const类型，该对象已经完成初始化了（也就是前面的二者已经有值了），这个时候如果调用编译器创建的<em>copy assignment</em> 操作符，就会导致新的对象的 reference 或者 const成员的值发生改变，这当然是不允许的，因此编译器会禁止这种行为。这就需要我们手动写<em>copy assignment</em> 操作符了。</p><h1 id="若不想使用编译器自动生成的函数就该明确拒绝">5.若不想使用编译器自动生成的函数，就该明确拒绝</h1><p>假设有一个类，我们不希望它被拷贝，即我们希望 <code>p2 = p1</code> 与<code>p2(p1)</code>这两种操作会被编译器禁止。一个最容易想到的方法就是不写 <em>copy</em>构造函数与 <em>copy assignment</em> 操作符。</p><p>但问题是就算我们不写，编译器也会在需要的时候自动帮我们创建。当然，要解决这个问题，我们也有办法：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-comment">//...</span><br><span class="hljs-keyword">private</span>:<br><span class="hljs-built_in">Test</span>(<span class="hljs-type">const</span> Test&amp;);<br>Test&amp; <span class="hljs-keyword">operator</span>=(<span class="hljs-type">const</span> Test&amp;);<br>&#125;;<br></code></pre></td></tr></table></figure><p>我们可以将它的 <em>copy</em> 构造函数和 <em>copy assignment</em>操作符<strong>声明</strong>为 <em>private</em>权限，我们仅仅<strong>只是声明，不予实现</strong>。这样便完成了不让这个类进行拷贝的操作。但这里也有一个问题是：<em>member</em> 函数与 <em>firend</em> 函数依旧可以访问这两个<em>copy</em>函数，为了解决这个问题，我们可以采取继承的方式（关于继承方式的区别，其实我自己目前也不明白，往后看一看说不定就懂了）</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Uncopyable</span><br>&#123;<br><span class="hljs-keyword">protected</span>:<br><span class="hljs-built_in">Uncopyable</span>();<br>~<span class="hljs-built_in">Uncopyable</span>();<br><span class="hljs-keyword">private</span>:<br><span class="hljs-built_in">Uncopyable</span>(<span class="hljs-type">const</span> Uncopyable&amp;);<span class="hljs-comment">//将这两个函数声明为 private</span><br>Uncopyable&amp; <span class="hljs-keyword">operator</span>= (<span class="hljs-type">const</span> Uncopyable&amp;);<br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span>: <span class="hljs-keyword">private</span> Uncopyable<span class="hljs-comment">//用 private 继承，这样可以使得派生类只能调用构造和析构函数</span><br>&#123;      <span class="hljs-comment">//不知道用 public 继承行不行</span><br><span class="hljs-keyword">public</span>:<br><span class="hljs-comment">//...</span><br><br>&#125;;<br></code></pre></td></tr></table></figure><p>当 <em>derived class</em> 试图调用 <em>copy</em> 构造函数时，由于<em>derived class</em> 并没有该函数，因此编译器会自动创建。由于<em>derived class</em> 采取 private 继承自 <em>base class</em>，因此编译器自己创建的 <em>copy</em>构造函数中会<strong>试图</strong>调用 <em>base class</em> 的<em>copy</em> 构造函数。</p><p>由于 <em>base class</em> 的 <em>copy</em>构造函数<strong>只有声明，没有定义</strong>，因此编译器不会<strong>自动创建<em>base class</em> 的 <em>copy</em> 构造函数</strong>。由于该<em>copy</em> 构造函数并无实现，因此编译器会报错。</p><h1 id="为多态基类声明-virtual-析构函数">6.为多态基类声明 virtual析构函数</h1><p>我们来看一个例子：有许多方法可以记录时间，我们来设计一个 TimeKeeperbase class 和一些 derived class 来表示不同的计时方法</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TimeKeeper</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">TimeKeeper</span>();<br>~<span class="hljs-built_in">TimeKeeper</span>();<br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">AtomicClock</span> : <span class="hljs-keyword">public</span> TimeKeeper &#123;  &#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">WtaerClock</span> :<span class="hljs-keyword">public</span> TimeKeeper &#123;  &#125;;<br></code></pre></td></tr></table></figure><p>然后，我们定义一个<strong>基类指针</strong>（当然要 new一个），使得其可以指向所有的 <em>derived class</em> ，这么做合情合理</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp">TimeKeeper* pt = <span class="hljs-keyword">new</span> <span class="hljs-built_in">AtomicClock</span>();<span class="hljs-comment">//这里只要是 derived class 就可以</span><br><span class="hljs-comment">// use pt for ...</span><br><span class="hljs-keyword">delete</span> pt;<br></code></pre></td></tr></table></figure><p>这个地方，pt 指向 <em>derived class</em> 对象并且该指针通过被<em>base class</em> 指针删除，并且 <em>base class</em> 还有个non-virtual 析构函数，这会导致非常刺激的事情发生。（嘛，说人话就是 base指针指向的 derived 对象在 delete 的时候，如果基类存在 non-virtual析构函数就会出事）</p><p>那么具体是什么事情呢？ delete 掉 <em>base class</em>指针的时候只会释放掉 <em>base class</em> 当中的成员变量而不会释放<em>derived class</em> 的成员变量，即<strong>它只会执行 <em>baseclass</em> 的析构函数而不会执行 <em>derived class</em>的析构函数</strong>（这就是局部销毁，做事情只做一半，给你玩明白了属于是）</p><p>要解决这个问题也十分简单，只需要在 <em>base class</em>的析构函数前面加上 <em>virtual</em> 就行，即</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TimeKeeper</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">TimeKeeper</span>();<br><span class="hljs-keyword">virtual</span> ~<span class="hljs-built_in">TimeKeeper</span>();<br>&#125;;<br></code></pre></td></tr></table></figure><p>这样做之后，在 delete 掉指针的时候就会析构掉 <em>base class</em> 和<em>derived class</em> ，当然析构顺序是先 <em>derived</em> 后<em>base</em></p><p>实际上<strong>对于 <em>base class</em> 而言， virtual不仅仅可以用在析构函数处，也可以用在其 <em>member</em>函数处</strong>。此时 <em>base class</em>当中的这个函数可以是空实现，而其不同的 <em>derived class</em>当中写不同的该函数的实现。</p><p>也就是说，<strong>任何 <em>base class</em> 只要带有 virtual函数就必须要有一个 virtual 析构函数</strong>，这是因为<strong>如果一个class 不含 virtual ，那么也就表面该函数不会被用做一个 <em>baseclass</em> </strong></p><p>STL容器和 string 类当中的析构函数是 non-virtual的，也就是说，不可以将<strong>STL容器和 string 类作为 <em>baseclass</em> 来使用</strong></p><p>当然，我们也可以让 <em>base class</em> 带有一个 pure virtual析构函数，这将使其变成 abstract class ——不能用这个 class来实例化对象。具体操作如下</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TimeKeeper</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">TimeKeeper</span>();<br><span class="hljs-keyword">virtual</span> ~<span class="hljs-built_in">TimeKeeper</span>() = <span class="hljs-number">0</span>;<br>&#125;;<br></code></pre></td></tr></table></figure><p>当然，对于 pure virtual析构函数，我们还需要额外为其<strong>在类外</strong>提供一份定义</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp">TimeKeeper::~<span class="hljs-built_in">RimeKeeper</span>()<br>&#123;<br>    ;<span class="hljs-comment">//空实现就行</span><br>&#125;<br></code></pre></td></tr></table></figure><h1 id="别让异常逃离析构函数">7.别让异常逃离析构函数</h1><p>假设有一个场景——我有一个含有10个对象的 vector数组，在我函数结束的时候程序会自动<strong>依次</strong>执行每个对象的析构函数。如果第一个对象在执行析构的时候抛出了一个异常，紧接着后面一个对象同样也抛出了一个异常。而在两个异常同时存在的情况下，会导致程序提早结束或者导致不明确的行为。也就是说，C++不喜欢析构函数吐出异常。</p><p>我们来考虑一个例子，假设我使用一个 class 来负责数据库的连接</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">DBConnecton</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">static</span> DBConnection <span class="hljs-title">creat</span><span class="hljs-params">()</span></span>;<span class="hljs-comment">//创建一个对象</span><br>    <br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">close</span><span class="hljs-params">()</span></span>;<span class="hljs-comment">//关闭连接</span><br>&#125;<br></code></pre></td></tr></table></figure><p>为确保在以后每次的调用当中不忘记调用 close ，我们可以使用一个 class来管理 DBConnection 资源，并在其析构函数中执行 close</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">DBCon</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br>    ~<span class="hljs-built_in">DBCon</span>()<br>    &#123;<br>        db.<span class="hljs-built_in">close</span>();<br>    &#125;<br><span class="hljs-keyword">private</span>:<br>    DBConnection db;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果在析构函数中成功调用 close，那么一切都很完美。但如果该调用导致了异常，而 DBCon的析构函数将此异常抛出，也就是<strong>允许此异常离开析构函数</strong>，那就会造成问题。为避免这个问题，我们可以：</p><ul><li>如果抛出异常那么就结束程序，可以用 abort 来实现</li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs cpp">~<span class="hljs-built_in">DBCon</span>()<br>&#123;<br>    <span class="hljs-keyword">try</span>&#123;<br>        db.close;<br>    &#125;<br>    <span class="hljs-built_in">catch</span>(...)<br>    &#123;<br>        <span class="hljs-comment">//记录下对 close 的调用失败</span><br>        std::<span class="hljs-built_in">abort</span>();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这种方法是——只要一出现异常，则立刻结束程序</p><ul><li>吞下 close 引发的异常</li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp">~<span class="hljs-built_in">DBCon</span>()<br>&#123;<br>    <span class="hljs-keyword">try</span>&#123;<br>        db.<span class="hljs-built_in">close</span>();<br>    &#125;<br>    <span class="hljs-built_in">catch</span>(...)<br>    &#123;<br>        <span class="hljs-comment">//对异常进行记录</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这相当于 catch 当中只记录该异常而不做处理，美其名曰：吞下</p><p>上面的这些操作，都是对发生异常之后的补救措施，并且都是在析构函数的地方进行补救（也就是说只有才<strong>程序结束的时候</strong>我才知道发生了异常，并不能确定异常出现在哪里）。实际上，我们是需要一种能够对"导致close 发生异常"的情况进行处理的办法才行</p><p>那么，我们可以从新设计这个接口，我们可以提供一个手动关闭的接口，这样子只要用户调用了这个函数就能够知道问题出在哪里，就算用户忘记调用，也有析构函数兜底（只不过会发生上面的情况而言，不过总体而言要好一点），代码如下</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">DBCon</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">close</span><span class="hljs-params">()</span></span><br><span class="hljs-function">    </span>&#123;<br>        db.<span class="hljs-built_in">close</span>();<br>        closed = <span class="hljs-literal">true</span>;<br>    &#125;<br>    ~<span class="hljs-built_in">DBCon</span>()<br>    &#123;<br>        <span class="hljs-keyword">if</span>(closed == <span class="hljs-literal">false</span>)<br>        &#123;<br>        <span class="hljs-keyword">try</span>&#123;<br>                db.<span class="hljs-built_in">close</span>();<br>            &#125;<br>            <span class="hljs-built_in">catch</span>(...)<br>            &#123;<br>                <span class="hljs-comment">//强制结束或者吞下异常</span><br>            &#125;<br>        &#125;<br>    &#125;<br><span class="hljs-keyword">private</span>:<br>    DBConnection db;<br>    closed;<br>&#125;<br></code></pre></td></tr></table></figure><p>也就是说，如果用户需要对某个操作函数在运行期间抛出的异常做出反应，那么class 应该提供一个普通函数来执行该操作，而非在析构函数中</p><h1 id="确认你的-public-继承塑膜出-is-a-关系">8.确认你的 public继承塑膜出 is-a 关系</h1><p>在 C++ 当中， public inheritance （公开继承）意味着 <strong>is-a（是一种或是一个）</strong>的关系。</p><p>如果令 class D (Derived) 以 public 方式继承 class B (Base)，那么就意味着：<strong>每一个类型为 class D 的对象都是一个类型为 classB 的对象，而反过来则不成立</strong>。也就是说， <strong>B 的概念比 D的概念更一般化， D 的概念比 B 更特殊化</strong>，B 能够做到的事情， D一样可以做到。<strong>每一个 D 对象都是一个 B对象，反之则不成立</strong>。</p><p>由于是 public 继承，即 <em>derived class</em> 会拥有 <em>baseclass</em> 的所有属性与方法，而 <em>derived class</em>则可以拥有额外的属性与方法。因此我们说： <em>base class</em>能做到的事情， <em>derived class</em> 一定也能做到，<strong><em>derivedclass</em> 的范围大于 <em>base class</em> 的范围</strong>。</p><p>C++中的 public 继承一定遵循上面的原则，举个例子：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Person</span> &#123;&#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span>: <span class="hljs-keyword">public</span> Person &#123;&#125;;<br></code></pre></td></tr></table></figure><p>根据常识，人能做到的事情，学生一定能做到，而学生能做到的事情，人则不一定能做到。因此，<strong>任何函数期望获得一个类型为Person 的实参（形参的类型为 Person ），也都愿意接受一个类型为 Student的对象</strong>。这也就说明了为什么 <strong><em>base class</em>的指针可以指向 <em>derived class</em> 对象而 <em>derived class</em>的指针则不能指向一个 <em>base class</em> 的对象</strong></p><p>再次重申，C++坚持—— B 能做到的事情， D一定可以做到，反之则错误。但有的时候直觉会带来意想不到的错误，我们考虑下面这个例子：企鹅（penguin）是一种鸟，这是事实。鸟可以飞，这也是事实。当我们用C++描述这层关系时，哦吼，好玩的事情来了。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Bird</span><br>&#123;<br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">fly</span><span class="hljs-params">()</span> </span>= <span class="hljs-number">0</span>;<span class="hljs-comment">//鸟可以飞</span><br>&#125;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Penguin</span>: <span class="hljs-keyword">public</span> Bird<span class="hljs-comment">//企鹅是鸟</span><br>&#123;<br>    <br>&#125;<br></code></pre></td></tr></table></figure><p>按照上面的说法，我们就会发现，企鹅居然能飞？？？好吧，这实际上是语言不严谨导致的问题。当我们说鸟能飞的时候，并不是说所有的鸟都会飞，而是部分鸟会飞。因此，实际的继承关系应该如下</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Bird</span><br>&#123;<br>    <span class="hljs-comment">//不声明 fly 函数</span><br>&#125;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">FlyBird</span>: <span class="hljs-keyword">public</span> Bird<br>&#123;<br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">fly</span><span class="hljs-params">()</span> </span>= <span class="hljs-number">0</span>;<span class="hljs-comment">//会飞的鸟</span><br>&#125;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Penguin</span>: <span class="hljs-keyword">public</span> Bird<br>&#123;<br>    <span class="hljs-comment">//不声明 fly 函数</span><br>&#125;<br></code></pre></td></tr></table></figure><p>当然，还有一种做法是：承认企鹅会飞，但在运行阶段报错，具体如下</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">error</span><span class="hljs-params">(<span class="hljs-type">const</span> string&amp; s)</span></span>;<span class="hljs-comment">//定义与别处</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Penguin</span>: <span class="hljs-keyword">public</span> Bird<br>&#123;<br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">fly</span><span class="hljs-params">()</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-built_in">error</span>(<span class="hljs-string">&quot;Penguin can&#x27;t fly!!!&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>二者的差异在哪里？我们从错误被侦测出的时间点来看，前者的错误在编译阶段就会被发现，而后者则需要到运行阶段才行。显然，我们<strong>应当采用前者</strong>。</p><p>然后我们再看一个例子，按照我们基础几何学的知识，所有的正方形一定是矩形，而反过来则不是。也就是说，正方形（Square ）应当以 public 方式来继承矩形（ Rectangle）。然后，我们考虑下面这段代码</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Rectangle</span><br>&#123;<br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">setwight</span><span class="hljs-params">(<span class="hljs-type">int</span> wight)</span></span>;<br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">setheight</span><span class="hljs-params">(<span class="hljs-type">int</span> height)</span></span>;<br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">int</span> <span class="hljs-title">wight</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span>;<span class="hljs-comment">//返回当前值</span><br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">int</span> <span class="hljs-title">height</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span>;<span class="hljs-comment">//用 virtual 使得 derived class 进行重写</span><br>&#125;;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Bigger</span><span class="hljs-params">(Rectangle&amp; r)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> oldwight = r.<span class="hljs-built_in">wight</span>();<br>    r.<span class="hljs-built_in">setwight</span>(oldwight + <span class="hljs-number">10</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>Bigger 只会改变 Rectangle的宽度，显然这对于矩形而言是正确的。然后我们让 Square 以 public方式继承</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Square</span>: <span class="hljs-keyword">public</span> Rectangle &#123;&#125;;<br></code></pre></td></tr></table></figure><p>然后，非常好玩的事情发生了。Bigger 函数可以接受一个 Square对象，也就是说 Square对象可以只被改变宽度而不被改变高度，但这对于正方形而言显然是不对的。</p><p>关于这个问题的解决，可以有很多的办法。在这里我只是想说：有的时候直觉跟public 继承真不太一样</p><h1 id="绝不在构造和析构过程中调用-virtual-函数">9.绝不在构造和析构过程中调用virtual 函数</h1><p>假设我们有一个继承体系用来，塑膜股市交易。而每当我们创建一个交易对象时，都需要进行一次记录</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Transaction</span> <span class="hljs-comment">//base class</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Transaction</span>();<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">logTransaction</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>= <span class="hljs-number">0</span>;<span class="hljs-comment">//因不同的类而进行不同的记录，因此要 virtual</span><br>&#125;;<br><br>Transaction::<span class="hljs-built_in">Transaction</span>()<br>&#123;<br>    <span class="hljs-comment">//...</span><br><span class="hljs-built_in">logTransaction</span>();<br>&#125;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">BuyTransaction</span>: <span class="hljs-keyword">public</span> Transaction <span class="hljs-comment">//derived class</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">logTransaction</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span>;<br>&#125;;<br></code></pre></td></tr></table></figure><p>最直接的想法是：每构造一个 <em>derived class</em> 对象都要执行一遍<em>base class</em> 的构造函数，因此我将 logTransaction 放在 <em>baseclass</em> 的构造函数内就行。</p><p>现在，当我们用 BuyTransaction来创建一个对象时，我们来考察发生了什么事情。</p><p>首先， BuyTransaction 的构造函数会被调用，而由于它是 <em>derivedclass</em> ，<strong>因此其 <em>base class</em> 的构造函数的执行会先于<em>derived class</em> 的构造函数的执行（ <em>derived class</em> 当中的<em>base class</em> 成分会在 <em>derived class</em>自身成分被构造前先构造妥当）</strong>。所以会先执行 Transaction的构造函数。其构造函数的最后一行是一个纯虚函数 logTransaction。当程序执行到这一行时，好玩的来了。<strong>此时调用的 logTransaction 是<em>base class</em> 当中的版本而不是 <em>derived class</em>当中的版本</strong>。也就是说，<strong><em>base class</em> 构造期间的virtual 函数是不会下降到 <em>derived class</em> 的阶层的</strong>。</p><p>在这里你肯定会问：为什么 virtual 函数不会下降到 <em>derivedclass</em> 的阶层呢？这里的原因是：由于 <em>base class</em>的构造函数的执行先于 <em>derived class</em> 的构造函数的执行，因此此时<em>derived class</em> 的成员变量仍处在为初始化阶段，如果这个时候下降到<em>derived class</em>的阶层，就会导致“要求使用对象内未初始化的成分”。要知道，这在C++当中可是禁止的。</p><p>对于上述解释，其实还有一个更根本的理由：<strong>在 <em>derivedclass</em> 对象的 <em>base class</em> 构造期间，该对象的类型是 <em>baseclass</em> 而不是 <em>derived class</em></strong>。要知道，对于C++而言，如果对象为初始化，最安全的做法就是视它们不存在。即<strong>对象在<em>derived class</em> 的构造函数开始执行前不会成为一个 <em>derivedclass</em> 对象</strong>。</p><p>在构造函数期间， <strong><em>base class</em> 的构造函数的执行会先于<em>derived class</em> 的构造函数的执行</strong>，而析构则刚好相反，即<strong><em>derived class</em> 的析构函数的执行会先于 <em>baseclass</em>的析构函数的执行</strong>。而上述的法则对于析构函数同样有效。在<em>derived class</em> 的析构函数执行完毕后， <em>derived class</em>的成员变量则会处于未定义的状态，此时C++视它们不存在。<strong>因此在进入<em>base class</em> 析构函数后，该对象会成为一个 <em>base class</em>对象</strong>。</p><p>在上面这个例子中，由于在构造与析构函数中直接调用了 pure virtual函数，因此一些编译器会发出警报。我们来考虑下面这一种情景：当 Transaction有多个构造函数，并且它们有很多相同的部分，那么想当然的我会把它们都放在一个init 函数里面（这里也包括 logTransaction ），即我们会写出如下代码：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Transaction</span> <span class="hljs-comment">//base class</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Transaction</span>()<br>    &#123;<br>        <span class="hljs-built_in">init</span>();<br>    &#125;<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">logTransaction</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>= <span class="hljs-number">0</span>;<span class="hljs-comment">//因不同的类而进行不同的记录，因此要 virtual</span><br><span class="hljs-keyword">private</span>:<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">init</span><span class="hljs-params">()</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-comment">//...</span><br>        <span class="hljs-built_in">logTransaction</span>();<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>对于这段代码，如果 logTransaction 依旧是 pure virtual函数，那么编译器仍会报警。但如果这是一个 non-pure virtual函数，并且它还带有一份具体的实现代码，那么编译器就不会报警。这么做的代价是：你压根不知道为什么在创建<em>derived class</em> 对象时会调用错误版本的 logTransaction 。</p><p>该问题的唯一解决办法是：<strong>确保构造函数和析构函数都没有调用virtual 函数，而它们所调用的所以函数也同样要适用这条约束</strong>。</p><p>但目前我们的问题是：只要有 Transaction继承体系的对象被创建，就要有相应版本的 logTransaction 函数被调用。</p><p>通过上述分析，我们可以得出： <em>derived class</em>对象的构造一定会执行 <em>base class</em> 的构造函数。再此基础上，如果<em>base class</em> 的构造函数内调用 pure virtual函数，那么编译器会报错，如果调用 non-pure virtual函数，那么会导致该对象在构造时调用了错误版本的 logTransaction 函数。</p><p>那么这里的做法可以是：在 <em>base class</em> 中将 logTransaction函数改为 non-virtual 函数，在 <em>base class</em> 的构造函数中调用该non-virtual 函数并将需要记录的信息传递过来（这里的代码看51页）。</p><h1 id="避免遮掩继承而来的名称">10.避免遮掩继承而来的名称</h1><p>我们考虑如下这段代码：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> x;           <span class="hljs-comment">//global 变量</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">double</span> x = <span class="hljs-number">0</span>;<span class="hljs-comment">//local 变量</span><br>    cin &gt;&gt; x;<br>&#125;<br></code></pre></td></tr></table></figure><p>这里 cin 的对象是 local 变量而不是 global变量，这是因为<strong>内层作用域的名称会遮掩外层作用域的名称</strong>。</p><p>当编译器处于 fun 的作用域下并遇到 x 时，它会先在 local 作用域下寻找 x，如果找不到再去 global 作用域下去找 x。找到了直接使用就行。这时你会说：这两个 x 的类型不一样啊。</p><p>嘛，C++的名称遮掩规则不跟你搞那些花里胡哨的，它要做的只有一件事：遮掩名称。它不会关注这个名称的类型或者这是一个变量名称还是一个函数名称，这些统统不管。</p><p>也就是说，在这里 double x 遮掩了 int x。</p><p>现在，我们在此基础上导入继承，来看看会发生什么</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Base</span><br>&#123;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> x;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">mf1</span><span class="hljs-params">()</span> </span>= <span class="hljs-number">0</span>;<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">mf2</span><span class="hljs-params">()</span></span>;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">mf3</span><span class="hljs-params">()</span></span>;<br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base<br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">mf1</span><span class="hljs-params">()</span></span>;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">mf4</span><span class="hljs-params">()</span></span>;<br>&#125;;<br></code></pre></td></tr></table></figure><p>这里面的函数有 pure virtual 、 virtual 、 non-virtual函数，但这些都不重要，C++只看名称。</p><p>我们假定 mf4 当中的实现为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Derived::mf4</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">mf2</span>();<br>&#125;<br></code></pre></td></tr></table></figure><p>当编译器看到 mf2 时，<strong>它会先在 local作用域下进行查找其声明</strong>，这里显然没有 mf2的声明。然后编译器<strong>会去其外围作用域下查找</strong>，这里就到了Derived的作用域。依旧没有找到，编译器会<strong>继续向外围拓展</strong>，到 Base作用域下查找，这时找到 mf2 的声明式，于是便在此停止。如果 Base里面还是没有，那么编译器<strong>只能去 global作用域下查找</strong>，如果还是找不到就只能报错。</p><p>也就是说，<strong>编译器对于名称声明式的查找是由内向外的</strong>，现在local ，然后再逐渐向外围拓展。</p><p>到此，我们就说清楚了C++对于名称的查找原则了，我们在原来的基础上加上重载</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Base</span><br>&#123;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> x;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">mf1</span><span class="hljs-params">()</span> </span>= <span class="hljs-number">0</span>;<br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">mf1</span><span class="hljs-params">(<span class="hljs-type">int</span>)</span></span>; <span class="hljs-comment">//这里</span><br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">mf2</span><span class="hljs-params">()</span></span>;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">mf3</span><span class="hljs-params">()</span></span>;<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">mf3</span><span class="hljs-params">(<span class="hljs-type">double</span>)</span></span>; <span class="hljs-comment">//这里</span><br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base<br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">mf1</span><span class="hljs-params">()</span></span>;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">mf4</span><span class="hljs-params">()</span></span>;<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">mf3</span><span class="hljs-params">()</span></span>; <span class="hljs-comment">//这里</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>同样地，对于重载，遮掩规则依旧适用。对于一个子类的对象，它只能调用<strong>Derived 作用域内的 mf1 和 mf3，它不能调用 Base 作用域下的 mf1 和mf3</strong> 。</p><p>这种简单粗暴的遮掩法则会违背了 pubic 继承中的 is-a关系（基类能做到的，派生类也一定要能够做到，而遮掩法则会忽视掉基类当中同名的函数或者变量）。想要解决这个问题，我们只需要在<em>derived class</em> 当中加上 using 声明，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base<br>&#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-keyword">using</span> Base::mf1;<br>    <span class="hljs-keyword">using</span> Base::mf3;<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">mf1</span><span class="hljs-params">()</span></span>;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">mf4</span><span class="hljs-params">()</span></span>;<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">mf3</span><span class="hljs-params">()</span></span>;<br>&#125;;<br></code></pre></td></tr></table></figure><p>那么这样子，我便可以在 Berived 作用域下找到 Base 当中的 mf1 和 mf2，其重载函数也能正常调用。</p><p><strong>这里的 using 是放在 public作用域下的</strong>，至于原因？还是派生类必须全部包含基类的所有成员</p><p>using 在这里会有一个问题是：<strong>它会使得 <em>base class</em>当中同名的变量与函数在 <em>derived class</em>当中均可见</strong>。如果我们只希望继承部分的同名函数，那么 using便不能再使用了。</p><p>关于这个问题，我们可以用一个转接函数并<strong>配合 private继承</strong>来解决。由于在 <em>derived class</em> 内部是可以通过<em>base class</em>的类名来访问其成员函数的，因此我只需要写一些函数（这些函数与我希望继承<em>base class</em> 当中的函数重名），在这些函数内部调用 <em>baseclass</em> 当中对应的函数即可。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Base</span><br>&#123;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> x = <span class="hljs-number">10</span>;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">mf1</span><span class="hljs-params">()</span> </span>= <span class="hljs-number">0</span>;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">mf1</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; <span class="hljs-string">&quot;fun1&quot;</span> &lt;&lt; endl;<br>&#125;<br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">private</span> Base <span class="hljs-comment">//用 private 继承</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">mf1</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>Base::<span class="hljs-built_in">mf1</span>(<span class="hljs-number">10</span>); <span class="hljs-comment">//用基类类名来调用</span><br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>为什么不用 public 继承？</p><p>还是那个问题，这会违背 is-a的原则，即派生类必须全部包含基类的所有成员。但我们这里是部分继承，显然不满足这一点。</p><h1 id="区分接口继承和实现继承">11.区分接口继承和实现继承</h1><p>public 继承，实际上分为函数接口继承和函数实现继承。</p><p>有的时候，我们希望 <em>derived class</em>只<strong>继承成员函数的接口（只继承该函数的声明，对于其定义则需要<em>derived class</em> 自己实现）</strong>；有时我们又希望 <em>derivedclass</em> 同时<strong>继承函数接口和一份默认实现</strong>；有时我们希望<em>derived class</em>同时<strong>继承函数接口和一份强制实现</strong></p><p>我们来看一个例子：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Shape</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">draw</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>= <span class="hljs-number">0</span>;<br><span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">error</span><span class="hljs-params">(string&amp; str)</span></span>;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">ID</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span>;<br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Rectangle</span> : <span class="hljs-keyword">public</span> Shape<br>&#123;<br><br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Ellipse</span> : <span class="hljs-keyword">public</span> Shape<br>&#123;<br><br>&#125;;<br></code></pre></td></tr></table></figure><ul><li>成员函数的接口总会被继承</li></ul><p>函数的接口指的是该函数的声明。成员函数的接口被继承的意思是<strong><em>derived class</em> 当中有一份函数声明，该声明来自 <em>baseclass</em> </strong>。</p><ul><li>声明一个 pure virtual 函数的目的是让 <em>derived class</em>只继承函数接口</li></ul><p>当 <em>base class</em> 存在 pure virtual 函数时， <em>derivedclass</em> 需要重写该 pure virtual 函数，即 <strong><em>derivedclass</em> 需要自己提供一份该函数的定义</strong>。</p><p>上面的 draw 对应这种情况。</p><p>pure virtual 函数的声明式对其 <em>derived class</em>的设计者说：<strong>你必须提供一份该函数（draw）的实现，但我并不干涉你如何实现</strong>。</p><p>一般来说， pure virtual函数是不会有具体实现的，但<strong>你如果写了也没关系</strong>，但调用它们的唯一途径是要指明作用域</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Rectangle r;<br>r.Shape::<span class="hljs-built_in">draw</span>(); <span class="hljs-comment">//像这样</span><br></code></pre></td></tr></table></figure><ul><li>声明一个 impure virtual 函数的目的是让 <em>derived class</em>继承该函数的接口和默认实现</li></ul><p><strong>对于 impure virtual 函数，我们需要在 <em>base class</em>中提供一份实现</strong></p><p>上面的 error 对应这种情况。</p><p>impure virtual 函数的声明式对其 <em>derived class</em>的设计者说：<strong>你必须提供一份该函数（error）的实现，如果你不想写，也可以使用<em>base class</em> 的默认版本</strong>。</p><ul><li>声明一个 non-virtual 函数的目的是为了令 <em>derived class</em>继承函数的接口和一份强制性实现</li></ul><p>由于 <em>derived class</em> 并不能复写 non-virtual 函数，因此<em>derived class</em> 会继承一份强制性实现</p><h1 id="通过复合塑膜出-has-a-或根据某物实现出">12.通过复合塑膜出 has-a或“根据某物实现出”</h1><p>复合是类型当中的一种关系，当某种类型的对象含有其它类型的对象时就属于这种关系</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Address</span> &#123;  &#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">PhoneNumber</span> &#123;  &#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Person</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-comment">//...</span><br><span class="hljs-keyword">private</span>:<br>string Name;<br>Address add;<br>PhoneNumber phone;<br>&#125;;<br></code></pre></td></tr></table></figure><p>这里 Person 含有 string 、Address 、PhoneNumber 这些类型，因此 Person跟前面这些类型属于复合关系</p><p>我们之前说过，public 继承带有is-a（是一种）的意义。而复合则带有两种意义。复合意味着has-a（有一个）或者 “根据某物实现出” 的意义。</p><p>程序当中的对象（用类实例化的，如自己定义的一些类）属于应用域，其余对象（组成前者的成分，如缓冲区、查找树）属于实现域。</p><p><strong>当复合发生在应用域的对象之间时，表现为has-a（有一个）的关系，当复合发生在实现域的对象之间时，表现为“根据某物实现出” 的关系</strong></p><p>我们看上面的例子。我们会说 Personhas-a（有一个）Name，has-a（有一个）add，has-a（有一个）phone。我们不会说 Person is-a（是一种）Name。</p><p>也就是说，has-a关系可以理解为组成这个类的成分，当然这个成分是其他类。</p><p>这一层是应用域，实现域会更偏向底层。我们通常会说：stack的底层实现默认是 deque，你也可以把它改为 vector 或者 list。这里我们说的是 “实现” ，也就是说这一层就属于实现域。</p><p>同样，这里是两个类的复合，因此属于 “根据某物实现出”</p><h1 id="明智而审慎地使用-private-继承">13.明智而审慎地使用 private继承</h1><p>我们设计一个类时，它的底层实现我们会把它设置成 private属性。同样的道理，我们用 private 继承的时候，<em>base class</em> 当中的public 与 protected 属性的成员会在 <em>derived class</em> 当中变成private 。这正好符合作为另一种类的底层实现，即 private 继承是另一种的"根据某物实现出"。</p><p>private 继承纯粹是一种实现技术，D private 继承 B ，意思是 D 对象根据B 对象的实现而得，再没有其他含义了。</p><p>这里我们只讨论 private 继承的意义，其具体的应用场景参考原书 190页</p><h1 id="在-operator-中处理自我赋值">14. 在 operator= 中处理自我赋值</h1><p>自我赋值很少会被刻意写出来，但它被写出来的时候通常很隐蔽，如</p><p><code>a[i] = a[j]</code></p><p>这种就是隐藏的自我赋值。我们来考虑一个继承体系当中的 operator=的例子</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Base</span> &#123; &#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span> : <span class="hljs-keyword">public</span> Base<br>&#123;<br><span class="hljs-keyword">public</span>:<br>Test&amp; <span class="hljs-keyword">operator</span>=(<span class="hljs-type">const</span> Test&amp; rhs)<br>&#123;<br><span class="hljs-keyword">delete</span> pb;<span class="hljs-comment">//删除原先的指针，避免内存泄漏</span><br>pb = <span class="hljs-keyword">new</span> <span class="hljs-built_in">Base</span>(*rhs.pb);<br><span class="hljs-keyword">return</span> *<span class="hljs-keyword">this</span>;<br>&#125;<br><span class="hljs-keyword">private</span>:<br>Base* pb;<br>&#125;;<br></code></pre></td></tr></table></figure><p>如果出现了自我赋值，即 <code>w = w</code> 这种情况。在 operator=函数中，会先将 pb 给 delete 掉，那么这样就相当于是把 w 自己的 pb 给delete 掉了。也就是说，执行完这个函数之后 w 当中的 pb是未定义的（能不能执行完都不好说）</p><p>因此这么设计是有问题的，问题处在 rhs 与 *this相同的时候依旧会执行该函数。</p><p>那么我们可以改为如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Test&amp; <span class="hljs-keyword">operator</span>=(<span class="hljs-type">const</span> Test&amp; rhs)<br>&#123;<br>    <span class="hljs-keyword">if</span>(&amp;rhs == <span class="hljs-keyword">this</span>)<br>        <span class="hljs-keyword">return</span> *<span class="hljs-keyword">this</span>;<br><span class="hljs-keyword">delete</span> pb;<span class="hljs-comment">//删除原先的指针，避免内存泄漏</span><br>pb = <span class="hljs-keyword">new</span> <span class="hljs-built_in">Base</span>(*rhs.pb);<br><span class="hljs-keyword">return</span> *<span class="hljs-keyword">this</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>在判断条件那块，<strong>我们用指针来判断，不要用指针所指向的值来判断</strong>。因为前者只需要判断指针的指向是否相同，而后者需要重载== 运算符才行</p><p>当然，我们还有一种办法：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Test&amp; <span class="hljs-keyword">operator</span>=(<span class="hljs-type">const</span> Test&amp; rhs)<br>&#123;<br>    Base* tmp = pb;<span class="hljs-comment">//记住原先 pb 所指向的位置</span><br>    pb = <span class="hljs-keyword">new</span> <span class="hljs-built_in">Base</span>(*rhs.pb);<br>    <span class="hljs-keyword">delete</span> tmp;<br>    <span class="hljs-keyword">return</span> *<span class="hljs-keyword">this</span>;<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="复制对象时勿忘其每一个成分">15.复制对象时勿忘其每一个成分</h1><p>如果我们希望当对象每执行一次 <em>copy</em> 构造或者 <em>copyassignment</em>操作符时就进行一次记录，那么我们肯定不能用编译器给我们提供的函数，我们应当自己实现，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">logCall</span><span class="hljs-params">(<span class="hljs-type">const</span> string&amp; FunName)</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; FunName &lt;&lt; endl;<br>&#125;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">base</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">base</span>(<span class="hljs-type">const</span> base&amp; rhs);<br>base&amp; <span class="hljs-keyword">operator</span>=(<span class="hljs-type">const</span> base&amp; rhs);<br><span class="hljs-keyword">private</span>:<br>string name;<br>&#125;;<br><br>base::<span class="hljs-built_in">base</span>(<span class="hljs-type">const</span> base&amp; rhs)<br>&#123;<br><span class="hljs-built_in">logCall</span>(<span class="hljs-string">&quot;base copy Constructor&quot;</span>);<span class="hljs-comment">// 每调用一次就调用 logCall</span><br>name = rhs.name;<br>&#125;<br><br>base&amp; base::<span class="hljs-keyword">operator</span>=(<span class="hljs-type">const</span> base&amp; rhs)<br>&#123;<br><span class="hljs-built_in">logCall</span>(<span class="hljs-string">&quot;base copy assignment operator&quot;</span>);<br>name = rhs.name;<br>&#125;<br></code></pre></td></tr></table></figure><p>用自己声明的函数来替代编译器生成的函数可能会导致的一个问题是：当我的代码必然出错时编译器并不会提醒我</p><p>上面这一切看似非常美好，但如果我们新添加一个变量，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Date</span><br>&#123;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> data;<br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">base</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">base</span>(<span class="hljs-type">const</span> base&amp; rhs);<br>base&amp; <span class="hljs-keyword">operator</span>=(<span class="hljs-type">const</span> base&amp; rhs);<br><span class="hljs-keyword">private</span>:<br>string name;<br>Date date;<span class="hljs-comment">// 这里</span><br>&#125;;<br><br>base::<span class="hljs-built_in">base</span>(<span class="hljs-type">const</span> base&amp; rhs)<br>&#123;<br><span class="hljs-built_in">logCall</span>(<span class="hljs-string">&quot;base copy Constructor&quot;</span>);<br>name = rhs.name;<br>&#125;<br><br>base&amp; base::<span class="hljs-keyword">operator</span>=(<span class="hljs-type">const</span> base&amp; rhs)<br>&#123;<br><span class="hljs-built_in">logCall</span>(<span class="hljs-string">&quot;base copy assignment operator&quot;</span>);<br>name = rhs.name;<br>&#125;<br></code></pre></td></tr></table></figure><p>这个时候，我们并没有改变 <em>copy</em> 构造函数和 <em>copyassignment</em>操作符函数的定义。此时就会发生<strong>局部赋值</strong>，即<code>name</code> 会被初始化而 <code>date</code>并不会被初始化。当然，<strong>编译器并不会报错</strong>。</p><p>也就是说，<strong>当我增加一个成员变量时，我需要修改所有的构造函数！！！</strong></p><p>如果这一切发生在继承，那么将会更要命。我们考虑下面的代码（<strong>这里我们以初代的base 为例</strong>）：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> base<br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Derived</span>();<br><span class="hljs-built_in">Derived</span>(<span class="hljs-type">const</span> Derived&amp; rhs);<br>Derived&amp; <span class="hljs-keyword">operator</span>=(<span class="hljs-type">const</span> Derived&amp; rhs);<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> ths;<br>&#125;;<br><br>Derived::<span class="hljs-built_in">Derived</span>() <span class="hljs-comment">//default</span><br>:<span class="hljs-built_in">ths</span>(<span class="hljs-number">0</span>)<br>&#123;<br><br>&#125;<br><br>Derived::<span class="hljs-built_in">Derived</span>(<span class="hljs-type">const</span> Derived&amp; rhs)<br>: <span class="hljs-built_in">ths</span>(rhs.ths)<br>&#123;<br><br>&#125;<br><br>Derived&amp; Derived::<span class="hljs-keyword">operator</span>=(<span class="hljs-type">const</span> Derived&amp; rhs)<br>&#123;<br><span class="hljs-keyword">this</span>-&gt;ths = rhs.ths;<br><span class="hljs-keyword">return</span> *<span class="hljs-keyword">this</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>看上去， <em>derived class</em> 的 <em>copy</em> 构造函数和 <em>copyassignment</em> 操作符对 <em>derived class</em>进行了拷贝。但是，<strong>这仅仅只对属于 <em>derived class</em>的成员变量进行了拷贝，对属于 <em>base class</em>的成员变量并不会进行拷贝</strong>。</p><p>由于 <em>derived class</em> 的 <em>copy</em> 构造函数和 <em>copyassignment</em> 操作符函数在初始化时并没有提到 <em>base class</em>所对应的函数，因此在调用 <em>derived class</em>的函数时，<strong>会默认调用 <em>base class</em> 的 <em>default</em>构造函数</strong>。这也正是问题的所在。</p><p>因此，为避免这个问题。当我们调用 <em>derived class</em>的构造函数时，需要将 <em>base class</em> 的部分也带上，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Derived::<span class="hljs-built_in">Derived</span>(<span class="hljs-type">const</span> Derived&amp; rhs)<br>: <span class="hljs-built_in">ths</span>(rhs.ths), <span class="hljs-built_in">base</span>(rhs) <span class="hljs-comment">//直接把 rhs 传进去就行</span><br>&#123;<br><br>&#125;<br><br>Derived&amp; Derived::<span class="hljs-keyword">operator</span>=(<span class="hljs-type">const</span> Derived&amp; rhs)<br>&#123;<br><span class="hljs-keyword">this</span>-&gt;ths = rhs.ths;<br>base::<span class="hljs-built_in">base</span>(rhs);<br><span class="hljs-keyword">return</span> *<span class="hljs-keyword">this</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>这里直接把 <em>derived class</em> 对象作为参数传给 <em>baseclas</em>s 的 <em>copy</em> 构造函数或者 <em>copy assignment</em>操作符函数即可</strong>。</p><h1 id="以对象管理资源">16.以对象管理资源</h1><p>我们来考虑一个类：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">JK</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">JK</span>()<br>&#123;<br>cout &lt;&lt; <span class="hljs-string">&quot;Default&quot;</span> &lt;&lt; endl;<br>&#125;<br>~<span class="hljs-built_in">JK</span>()<br>&#123;<br>cout &lt;&lt; <span class="hljs-string">&quot;~Constructor&quot;</span> &lt;&lt; endl;<br>&#125;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Pirnt</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; <span class="hljs-string">&quot;233&quot;</span> &lt;&lt; endl;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>通常，我们会用一个 <em><code>Factory Function</code></em>来获取一个对象，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">JK* <span class="hljs-title">createJK</span><span class="hljs-params">()</span> <span class="hljs-comment">// Factury Function </span></span><br><span class="hljs-function"></span>&#123;<br>JK* ptr = <span class="hljs-keyword">new</span> <span class="hljs-built_in">JK</span>();<br><span class="hljs-keyword">return</span> ptr;<br>&#125;<br></code></pre></td></tr></table></figure><p>假设我们在某个函数当中需要用到该对象，我们可以这么写：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>JK* ptr1 = <span class="hljs-built_in">createJK</span>();<br><span class="hljs-comment">//...</span><br><span class="hljs-keyword">delete</span> ptr1;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>我们总会在 return前就将该指针析构掉</strong>，但问题是，当代码进入日常维护时， "..."部分的代码可能会有所更改，比如新添加一个 <code>return</code>什么的。不管加了什么，如果导致<strong>「当控制流离开这个函数或者区块时资源没有被释放」</strong>，就会出现危险。</p><p>也就是说，在这里我们需要一种<strong>「能够在程序执行完函数或代码块就自动析构掉资源」</strong>的工具。十分幸运的是，C++提供了智能指针这个工具能够实现这一点。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>auto_ptr&lt;JK&gt;<span class="hljs-built_in">ptr1</span>(<span class="hljs-built_in">createJK</span>()); <span class="hljs-comment">//这玩意是一个类模板，需要通过初始化来调用其内部函数</span><br><span class="hljs-comment">//...</span><br>&#125;<br></code></pre></td></tr></table></figure><p>这里体现了「以对象管理资源」的两个特点：</p><ul><li>获得资源（<code>new</code>结束后）后立即放入管理对象（<code>auto_ptr</code>）</li><li>该管理对象运用析构函数使得该对象得以被释放</li></ul><p>这里有一个问题是：<code>auto_ptr</code>会在程序结束时自动调用析构函数来销毁掉对象，但如果多个<code>auto_ptr</code>指向同一个对象的话，你的对象就会被删除多次。「未定义行为」的列车将一去不复返。</p><p>为了解决这个问题， <code>auto_ptr</code>不允许两个指针指向同一块内存地址，即<strong>试图通过 <em>copy</em>构造函数或 <em>copy assignment</em> 操作符复制它们，它们将会变为 NULL，而复制所得的指针将用于该资源的唯一所有权</strong> 。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">auto_ptr&lt;JK&gt; <span class="hljs-title">ptr1</span><span class="hljs-params">(createJK())</span></span>;<br><span class="hljs-function">auto_ptr&lt;JK&gt; <span class="hljs-title">ptr2</span><span class="hljs-params">(ptr1)</span></span>;<span class="hljs-comment">// ptr2 指向对象, ptr1 为 NULL</span><br>ptr1 = ptr2;<span class="hljs-comment">// ptr1 指向对象, ptr2 为 NULL</span><br></code></pre></td></tr></table></figure><p>因为这种特性，这会导致<strong>受 auto_ptr 所指的资源不会有一个以上的auto_ptr 同时指向它</strong>。因为此特性，STL 当中的容器容不得<code>auto_ptr</code> 。</p><p><code>auto_ptr</code>的替代方案是「引用计数型智能指针（RCSP）」。RCSP指针可以持续跟踪共有多少个对象同时指向该资源，并在无人指向该资源时自动删除。但RCSP 无法打破环状引用，即两个已经没有被使用的对象互指。</p><p>TR1 当中的 <code>tr1::shared_ptr</code> 就是一个 RCSP ，即上面的 fun也可以这么写：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>tr1::shared_ptr&lt;JK&gt;<span class="hljs-built_in">ptr1</span>(<span class="hljs-built_in">createJK</span>());<br>&#125;<br></code></pre></td></tr></table></figure><p>该指针允许相互赋值，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp">tr1::shared_ptr&lt;JK&gt;<span class="hljs-built_in">ptr1</span>(<span class="hljs-built_in">createJK</span>());<br><br><span class="hljs-function">tr1::shared_ptr&lt;JK&gt; <span class="hljs-title">ptr2</span><span class="hljs-params">(ptr1)</span></span>;<span class="hljs-comment">//ptr1 与 ptr2 均指向同一对象</span><br></code></pre></td></tr></table></figure><p>显然，<code>tr1::shared_ptr</code> 的这种特性可以被用于 STL或者任何「auto_ptr 的非传统赋值不适用」的情况下。</p><p><code>auto_ptr</code> 和 <code>tr1::shared_ptr</code><strong>两者在其析构函数内执行的是 <code>delete</code> 动作而不是<code>delete []</code> 的动作</strong>，<strong>这意味着在二者身上分配<code>array</code> 是一种不合适的行为</strong>。</p><p>看到这里你会想，为什么没有针对 C++ 动态数组而设计的智能指针呢？</p><p>啊，实际上 vector 和 string 可以完美替代掉动态数组这么个东西，因此C++ 没有！！！（这个理由是我见过最牛逼的）</p><h1 id="在资源管理类中小心-copy-行为">17.在资源管理类中小心<em>copy</em> 行为</h1><p>我们说，获取资源后立刻放入管理对象，这句话的意思实际是：当我用<code>new</code>取得了一块空间后，需要用一个指针来指向这块空间的首地址。在这里面，前者是<strong>获取资源</strong>，后者是<strong>将资源放入管理对象</strong>，管理对象就是这个指针。</p><p>在 C++当中引入了对象的概念后，我们将「以对象管理资源」称为「资源取得时便是初始化时机（<em>Resource Acquisition Is Initialization</em> ; RAII）」。我们在获取资源时就给 RAII 对象赋值，在控制流离开函数段或代码段时RAII 对象自动将该资源销毁掉。这一切看似非常美好，直到 <em>copy</em>行为的出现。</p><p>试想，如果我对 RAII 对象采取 <em>copy</em>行为，即会有两个一样的对象来管理同一个资源。那么在析构的时候就会将该资源销毁掉两次，这里就出事了！！！</p><p>因此，关于这个问题的解决，我们提出以下几个办法：</p><ul><li>禁止复制</li></ul><p>就像我们之前所讨论的一样，用 private 继承一个 Uncopyable的类来禁止调用 <em>copy</em> 构造函数或 <em>copy assignment</em>操作符</p><ul><li>对底层资源采取「引用计数法」</li></ul><p>这里其实我也不太懂，参考 P68。一个例子是 <code>tr1::shared_ptr</code>。</p><ul><li>复制底部资源</li></ul><p><strong>采取深拷贝的方式</strong>，使得两个 RAII对象所指向的资源<strong>是相同的但不是同一个</strong>。</p><ul><li>转移资源所有权</li></ul><p>这里参考 <code>auto_ptr</code> 的做法。当 <em>copy</em>发生时，原指针会指向 NULL ，后面的指针会指向该资源。</p><h1 id="在资源管理类中提供对原始资源的访问">18.在资源管理类中提供对原始资源的访问</h1><p>假设我们有一个 <em>factory</em> 函数用以提供 <code>Test</code>类型的资源，在获取资源后我们立刻将其放入到智能指针当中，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">tr1::shared_ptr&lt;Test&gt;<span class="hljs-built_in">ptr</span>(<span class="hljs-built_in">createTest</span>());<br></code></pre></td></tr></table></figure><p>现有一个函数，其声明为 <code>void fun(Test*)</code> 。它接受一个<code>Test*</code> 的指针，但我们并不能直接将 <code>ptr</code>传给它，因为 <code>ptr</code> 的类型为<code>tr1::shared_ptr&lt;Test&gt;</code> 。</p><p>实际上，在智能指针当中提供了对原始资源的访问方法。在<code>tr1::shared_ptr</code> 和 <code>auto_ptr</code> 当中，都提供了<code>get</code>方法来用于显式转换，即该函数会返回<strong>智能指针内部的原始指针（的复件）</strong>。</p><p>对于我们自己写的资源管理的类，同样可以用这种方法。</p><p>上面的这是显示转换，还有隐式转换的方法</p><p>直接写<strong>转换函数</strong>即可，通过转换函数可以隐式转换</p><p>（我觉得）这里最好加上 <code>explicit</code>，将隐式转换变为显式强制类型转换</p><h1 id="成对使用-new-和-delete-时要采取相同形式">19.成对使用 new 和delete 时要采取相同形式</h1><p>当使用 <code>new</code> 时（通过 <code>new</code>动态生成一个或多个对象），会有两件事情发生。第一，内存会被分配出来（<strong>通过名为<code>operator new</code>的函数</strong>）。第二针对此内存<strong>会有一个或多个构造函数被调用</strong>。</p><p>当使用 <code>delete</code>时，也会有两件事情发生。第一，针对此内存<strong>会有一个或多个析构函数被调用</strong>。第二，内存会被回收（<strong>通过名为<code>operator delete</code> 的函数</strong>）。</p><p>当我用 <code>new []</code>来生成一个数组时，其底层实现可能会是：数组前面留有一个空间用来存储对象的数量，之后才是对象数组。因此在使用<code>new</code> 时，必须用 <code>delete</code> ；在使用<code>new []</code> 时，必须使用 <code>delete[]</code> 。</p><p>实际上，由于有 <code>string</code> 和 <code>vector</code> 的存在，用<code>new</code> 来生成数组的需求可以几乎降为零。</p><h1 id="以独立语句将-new-对象置入智能指针">20.以独立语句将 new对象置入智能指针</h1><p>我们来考虑一个问题：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">Priority</span><span class="hljs-params">()</span></span>;<span class="hljs-comment">//这是函数声明，我们暂且不管定义</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(str::tr1::shared_ptr&lt;Test&gt; ptr, <span class="hljs-type">int</span> priority)</span><span class="hljs-comment">//同样是函数声明，第一个参数为智能指针，第二个为一个整型</span></span><br></code></pre></td></tr></table></figure><p>还记得我们前面所讨论的吗？以对象来管理资源，当对象一生成就把它交给智能指针。因此我们很容易<strong>在函数调用时</strong>会写出以下代码：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-built_in">fun</span>(<span class="hljs-keyword">new</span> Test, <span class="hljs-built_in">Priority</span>());<span class="hljs-comment">//前面用 new 来生成一个对象，后面是调用另一个函数</span><br></code></pre></td></tr></table></figure><p>但庆新的是，上面的代码并不会通过编译。原因在于<code>tr1::shared_ptr</code> 的构造函数是一个 <code>explict</code>的，它无法将一个 <code>Test*</code> 类型的对象转换成<code>tr1::shared_ptr&lt;Test&gt;</code>类型的对象。因此编译器会报错（你应该感到高兴）。</p><p>经过思考，你不难会<strong>在函数调用阶段</strong>写出：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-built_in">fun</span>(tr1::<span class="hljs-built_in">shared_ptr</span>&lt;Test&gt;(<span class="hljs-keyword">new</span> Test), <span class="hljs-built_in">Priority</span>());<br></code></pre></td></tr></table></figure><p>（相信我，聪明的你一定会这么想）但这么做是有问题的。我们来仔细分析一下：</p><p>编译器在调用函数前，会核算即将被传递的各个实参。这里面第二个没什么好说的，就是一个函数的调用，得到返回值就行。</p><p>重点是第一个。<code>tr1::shared_ptr&lt;Test&gt;(new Test)</code>由两部分组成：</p><ul><li>调用 <code>new Test</code> 来生成 <code>Test</code> 对象</li><li>调用 <code>tr1::shared_ptr</code> 的<strong>构造函数</strong></li></ul><p>于是，在调用函数 <code>fun</code> 之前，编译器会先做三件事：</p><ol type="1"><li>调用 <code>Priority</code> 函数</li><li>调用 <code>new</code> 来生成对象</li><li>调用 <code>tr1::Shared_ptr&lt;Test&gt;</code> 的构造函数</li></ol><p>我们能确定的是，调用 <code>new</code> 一定先于调用<code>tr1::shared_ptr</code>的构造函数。因为前者的结果要作为后者的参数。</p><p><strong>但是，对于调用 <code>Priority</code>的次序则没有规定</strong>。这个函数的调用，<strong>可以在第一个，可以是第二个，可以是第三个</strong>，这一切取决于编译器。</p><p>如果，<code>Priority</code> 是在第二个调用的，也就是说<strong>这时<code>Test</code> 对象已经生成好了</strong>。如果在<code>Priority</code>内部抛出异常导致程序终止，<strong>那么已经生成的对象就会导致内存泄漏</strong>。这是因为<strong>资源被创建</strong>与<strong>资源被放进管理对象</strong>这两件事并不是在同一时间内完成的。</p><p>要解决这个问题也很简单，只需要在<strong>单独语句内对智能指针进行赋值即可</strong>，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp">tr1::shared_ptr&lt;Test&gt;<span class="hljs-built_in">ptr</span>(<span class="hljs-keyword">new</span> Test);<br><span class="hljs-built_in">fun</span>(ptr, <span class="hljs-built_in">Priority</span>());<br></code></pre></td></tr></table></figure><h1 id="宁以-pass-by-reference-to-const-替换掉-pass-by-value">21.宁以Pass-by-reference-to-const 替换掉 Pass-by-value</h1><p>对于自定义类型而言，<em>pass by value</em> 的成本往往高于 <em>pass byreference</em> ，具体我们看下面这个例子： <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Person</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Person</span>();<br><span class="hljs-built_in">Person</span>(<span class="hljs-type">const</span> string&amp; rhs, <span class="hljs-type">const</span> string&amp; chs);<br>~<span class="hljs-built_in">Person</span>();<br><span class="hljs-keyword">private</span>:<br>string name;<br>string adress;<br>&#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span> : <span class="hljs-keyword">public</span> Person<br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Student</span>();<br><span class="hljs-built_in">Student</span>(<span class="hljs-type">const</span> string&amp; rhs, <span class="hljs-type">const</span> string&amp; chs);<br>~<span class="hljs-built_in">Student</span>();<br><span class="hljs-keyword">private</span>:<br>string schoolname;<br>string schooladress;<br>&#125;;<br></code></pre></td></tr></table></figure></p><p>假设我们有一个函数，其声明为 <code>void fun(Student s)</code>，当我写出：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Student stu;<br><span class="hljs-built_in">fun</span>(stu);<br></code></pre></td></tr></table></figure><p>该函数的形参是 <code>s</code>，当该函数被调用时，<strong><code>Student</code> 的 <em>copy</em>构造函数会被调用，它会以 <code>stu</code> 为蓝本复制一份给形参<code>s</code> </strong>，相应地，该函数结束时 <code>s</code>会被销毁。因此总的调用成本是：一次 <code>Student</code> 的 <em>copy</em>构造函数调用和一次 <code>Student</code> 的析构函数调用。</p><p>嘛从宏观上看是这样，但我们要知道，<code>Student</code> 是继承自<code>Person</code> ，并且 <em>derived class</em> 当中有两个<code>string</code> 对象，<em>base class</em> 当中也有两个<code>string</code> 对象。</p><p>所以，总的调用应该是：一对 <code>Student</code> 的 <em>copy</em>构造函数调用和析构函数调用，一对 <code>Person</code> 的 <em>copy</em>构造函数调用和析构函数调用以及四个 <code>string</code> 对象的<em>copy</em> 构造函数调用和析构函数调用。总共调用了 6个构造函数和析构函数。</p><p>当然，我们也有办法能够回避掉这些函数的调用，那就是用<code>pass by reference</code> 。当然，你会选择<code>pass by value</code>的一个很重要的原因可能是：你不想该函数改变我原本对象的值。当然，这一点也可以得到解决，就是<code>pass by reference to const</code> ，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">const</span> Student&amp; s)</span></span>;<br></code></pre></td></tr></table></figure><p>这样便不会有那么多次的构造函数调用和析构函数调用。</p><p>除此之外，<code>pass by reference to const</code>也可以很好地避免<strong>对象切割</strong>的问题。还是上面那个例子，但我们加两行代码：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Person</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Person</span>();<br><span class="hljs-built_in">Person</span>(<span class="hljs-type">const</span> string&amp; rhs, <span class="hljs-type">const</span> string&amp; chs);<br><span class="hljs-keyword">virtual</span> ~<span class="hljs-built_in">Person</span>();<span class="hljs-comment">//bass class 一定要写 virtual</span><br>    <br>    <span class="hljs-function">vertual <span class="hljs-title">dosomething</span><span class="hljs-params">()</span></span>;<span class="hljs-comment">//虚函数，用于提供接口</span><br><span class="hljs-keyword">private</span>:<br>string name;<br>string adress;<br>&#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span> : <span class="hljs-keyword">public</span> Person<br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Student</span>();<br><span class="hljs-built_in">Student</span>(<span class="hljs-type">const</span> string&amp; rhs, <span class="hljs-type">const</span> string&amp; chs);<br>~<span class="hljs-built_in">Student</span>();<br>    <br>    <span class="hljs-built_in">dosomething</span>();<br><span class="hljs-keyword">private</span>:<br>string schoolname;<br>string schooladress;<br>&#125;;<br></code></pre></td></tr></table></figure><p>假定我们有一个函数，它<strong>接受一个 <em>bass class</em>对象为参数（并且是 <code>pass by value</code>）</strong>，但我传入一个<em>derived class</em> 对象，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(Person p)</span></span>;<br>Student stu;<br><span class="hljs-built_in">fun</span>(stu);<br></code></pre></td></tr></table></figure><p>当一个 <strong><em>derived class</em>对象</strong>以<strong>值传递</strong>的方式传递给一个<strong>接受<em>bass class</em> 对象</strong>的函数时，<strong><em>base class</em>的 <em>copy</em> 构造函数会被调用</strong>（还记得吗，<em>baseclass</em> 形参也愿意接受一个 <em>derived class</em>的实参），这将会使得<strong>「 <em>derived class</em> 当中那些区别于<em>base class</em> 的那些特质」</strong>全部被切割掉。</p><p>我们具体说明一下：</p><p><code>fun</code> 函数的形参是一个 <code>Person</code> 类型，当传入<em>derived class</em> 对象时，会调用 <em>base class</em> 的<em>copy</em> 构造函数，最终得到的是一个 <strong><em>base class</em>对象</strong>而不是 <em>derived class</em>对象。也就是说，当我试图在此函数中调用 <em>derived class</em>当中虚函数的接口时，<strong>实际调用的是 <em>base class</em>当中该函数的定义而不是 <em>derived class</em>当中该函数的定义</strong>。这才是关键！！！</p><p>也就是说，这里的 <em>base class</em> 是不能提供 pure virtual函数的，因为这个函数在 <em>base class</em>当中并没有定义，就算你给一份缺省定义也没有，因为 pure virtual函数不能实例化对象。</p><p>所以，必须把 <code>fun(Person p)</code> 改为<code>fun(const Person&amp; p)</code>才行，<strong>这样传进来的是什么类型，那么 <code>p</code>就表示什么类型</strong>。</p><h1 id="将成员变量声明为-private">22.将成员变量声明为 private</h1><p>从封装的角度看类，它只有两种属性：封装与不封装</p><p>封装对应的是 privited 权限下的成员，不封装对应的是 public权限下的成员</p><p>这里想要理解很简单，对于 privited权限下的成员，类外无法直接访问。对于 public权限下的成员，类外可以直接访问。</p><p>那么对于 protected 权限呢？</p><p>这玩意虽然类外不能访问，这里看似和 privited 一样，实际上并不是的</p><p>protected 权限下的成员<strong>对于以 public 继承的形式， <em>baseclass</em> 当中 protected 成员在 <em>derived class</em> 当前全部变为public 成员</strong>。因此我们也认为：<strong>protected权限没有封装性</strong></p><h1 id="若所有参数皆需类型转换请为此采用-non-member-函数">23.若所有参数皆需类型转换，请为此采用non-member 函数</h1><p>我们知道，友元函数会破坏封装性（毕竟非成员函数可以直接对类当中的privited 权限的成员进行访问）</p><p>一般来说，友元函数用的时候多半是需要用全局函数重载运算符的时候</p><p>这一条款的建议是，<strong>最好是通过公共的接口对类当中的 privited成员进行访问而不要通过友元函数进行访问</strong>。</p><h1 id="尽可能延后变量定义式的出现时间">24.尽可能延后变量定义式的出现时间</h1><p>当我定义了一个变量而其类型带有构造和析构函数。那么程序的控制流在到达它与离开代码块时都会<strong>产生构造与析构的成本</strong>。就算这个变量不会被使用，这些成本也会产生。</p><p>你可能会说：我定义了一个变量就一定会使用啊，怎么可能会不用呢？</p><p>我们来考虑一个场景，我在函数的开头定义了一个 <code>string</code>变量，然后<strong>在使用该函数前抛出了异常</strong>，这导致控制流离开该函数，这便造成了「没有使用该变量但产生了构造与析构成本」。</p><p>因此我们需要「尽可能延后该定义出现的时间」。这里的意思<strong>不是</strong>直到使用该变量的前一刻才给与定义，而是<strong>尝试延后这份定义直到避免出现无意义的<em>default</em> 构造函数行为</strong>。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp">string str;<br>str = <span class="hljs-string">&quot;233&quot;</span>;<span class="hljs-comment">//这种并不合适，应该是下面</span><br><br><span class="hljs-function">string <span class="hljs-title">str</span><span class="hljs-params">(<span class="hljs-string">&quot;233&quot;</span>)</span></span>;<span class="hljs-comment">//这一可以避免出现默认构造函数的成本</span><br></code></pre></td></tr></table></figure><p>这个问题到这里我们就已经是完美解决了，还有一个问题是：如果出现循环怎么办？</p><p>考虑下面两个例子：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp">string s1;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++)<br>&#123;<br>    s1 = <span class="hljs-built_in">function</span>(i);<span class="hljs-comment">//取决于 i 的值</span><br>&#125;<br><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++)<br>&#123;<br>    <span class="hljs-function">string <span class="hljs-title">s2</span><span class="hljs-params">(function(i))</span></span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>这里的话我们稍微分析一下就行：</p><p>对于第一个，它有 1 个构造函数、 1 个析构函数、 n 次赋值运算</p><p>对于第二个，它有 n 个构造函数、 n 个析构函数</p><p>在这里，我们选择第一种做法的仅有这两个理由：</p><ul><li>赋值成本低于构造加析构的成本</li><li>在循环外定义的变量不会对后面的代码造成冲突</li></ul><h1 id="尽量少做转型动作">25.尽量少做转型动作</h1><p>关于 C++ 新式转型，前面其实已经提过了，在这里我们复习一下：</p><ul><li><code>const_cast&lt;T&gt; ( expression )</code><ul><li>通常被用作将对象的常量性移除，即将 const 变量改为 non-const变量</li></ul></li><li><code>dynamic_cast&lt;T&gt; ( expression )</code><ul><li>用来决定某对象是否<strong>归属</strong>继承体系中的某个<strong>类型</strong>。这个东西我也不太懂，只知道最好不要用，运行太慢了</li></ul></li><li><code>reinterpret_cast&lt;T&gt; ( expression )</code><ul><li>实际结果取决于编译器，不可移植</li></ul></li><li><code>static_cast&lt;T&gt; ( expression )</code><ul><li>强迫隐式转换，可以将 non-const 转为 const ，或者将 int 转 double。它也可以用来执行上述转换的反向转换。当然 const 到 non-const 只能<code>const_cast</code> 才能做到</li></ul></li></ul><p>关于转型，我们来看一个非常有意思的例子</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TP</span><br>&#123;<br>  <span class="hljs-function"><span class="hljs-keyword">explicit</span> <span class="hljs-title">TP</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span>;  <br>&#125;;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">const</span> TP&amp; t)</span></span>;<br><br><span class="hljs-built_in">fun</span>(<span class="hljs-built_in">TP</span>(<span class="hljs-number">10</span>));<span class="hljs-comment">//这没有任何问题，属于函数风格的转型</span><br><span class="hljs-built_in">fun</span>(<span class="hljs-built_in">static_cast</span>&lt;YP&gt;(<span class="hljs-number">15</span>));<span class="hljs-comment">//这也没有问题，属于C++风格的转型</span><br></code></pre></td></tr></table></figure><p>两种做法都没错，我觉得很有意思，就把它们写了下来。还有一个有意思的：</p><p>很多人会相信，转型其实什么也没做，只不过是让编译器以另一个视角看待同一块内存空间当中的内容。啊啊啊，实际上不是这样的。</p><p>任何一个类型转换，不管是显示的还是隐式的，都会真的令编译器产生相关的代码。</p><p>我们来简单的考虑一个例子：我们说，基类指针可以指向派生类对象，派生类指针也可以指向派生类对象，那么这两个指针的地址一样吗？</p><p>显然不可能啊，派生类的内容是多余基类的。在构造函数阶段是基类的部分先被构造，因此基类的部分应当处于<strong>栈的下面</strong>，因此派生类指针的地址应该是高于基类指针的。也就是说二者是存在一个<strong>偏移量</strong>的。</p><p>因此在运行期间，会有这个偏移量会施加在派生类的指针身上用以取得正确的基类指针的值。</p><p>从这一个角度也可以说明，<strong>对于单一对象而言，可能会有一个以上的地址</strong>（刚刚不就是嘛，基类和派生类的指针都可以指向该对象，而这两个指针的地址并不相同）。</p><p>这个有意思的故事说完了，我们回归正题，来讨论一个转型所带来的问题：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Base</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">Resize</span><span class="hljs-params">()</span><span class="hljs-comment">//虚函数，下面有缺省实现</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-comment">//...</span><br>    &#125;<br><br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base<br>&#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Resize</span><span class="hljs-params">()</span><span class="hljs-comment">//派生类需要重写基类的虚函数</span></span><br><span class="hljs-function">    </span>&#123;<br>        <span class="hljs-comment">//处于某种需要，我们要在第一行就调用 基类 中该函数的实现</span><br>        <span class="hljs-comment">//然后，我们便想通过转型实现</span><br><br>        (<span class="hljs-built_in">static_cast</span>&lt;Base&gt;(*<span class="hljs-keyword">this</span>)).<span class="hljs-built_in">Resize</span>();<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>在这里我们试图将 *this 转型为 Base ，然后以此调用基类当中的 Resize函数。</p><p>然而实际上并不是这样的。它<strong>调用的并不是当前对象身上的函数</strong>，而是转型动作所建立的一个「*this 对象的 base class 成分」的暂时副本身上的 Resize 函数。</p><p>意思就是：转型所得到的 Base 类型的对象是原对象当中 Base部分的<strong>副本</strong>而已。这很容易出事，因为如果 Resize这个函数修改了某些成员变量，<strong>并不会导致</strong>当前对象身上的成员被改变，改变的<strong>仅仅是副本</strong>。</p><p>然后，这部分之后的 dynamic_cast转型我看不懂。嘛，反正不要用就是了。话说这一条款的标题就是少用转型，这不是废话嘛.........</p><h1 id="避免返回引用指针来指向对象的内部成分">26.避免返回引用、指针来指向对象的内部成分</h1><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">tp</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><br><span class="hljs-built_in">tp</span>()<br>:<span class="hljs-built_in">rh</span>(<span class="hljs-number">0</span>), <span class="hljs-built_in">ch</span>(<span class="hljs-number">0</span>), <span class="hljs-built_in">close</span>(<span class="hljs-literal">false</span>)<br>&#123;  &#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span>&amp; <span class="hljs-title">re_rh</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123; <span class="hljs-keyword">return</span> rh; &#125;<span class="hljs-comment">//这两个部分都加了引用</span><br><span class="hljs-function"><span class="hljs-type">int</span>&amp; <span class="hljs-title">re_ch</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123; <span class="hljs-keyword">return</span> ch; &#125;<br><br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> rh, ch;<br>&#125;; <br></code></pre></td></tr></table></figure><p>我们上面有说过，传引用的速度快过传值的速度，但并不能什么情况都传引用，特别是在返回值的地方</p><p>当我们调用这两个函数的时候，由于其返回值是一个<strong>指向对象内部数据的引用</strong>，因此我可以直接<strong>通过这个引用来修改对象内部的数据</strong>，这会导致<strong>原本private 权限下的变量变为 public 权限下的变量</strong>，破坏了封装性</p><p>好，你也许看到之后会说，不就是会修改嘛，我加个 const 就行</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; <span class="hljs-title">re_rh</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123; <span class="hljs-keyword">return</span> rh; &#125;<br></code></pre></td></tr></table></figure><p>但这样子其实也会造成问题</p><p>假定我们有一个匿名对象，并用一个指针来进行接收</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span>* a = &amp;<span class="hljs-built_in">tp</span>().<span class="hljs-built_in">re_ch</span>();<br></code></pre></td></tr></table></figure><p>然后你会发现，这个匿名对象在执行完这一行之后就会被自动销毁，那么这就会导致这个指针<strong>指向的是一块未定义的内存</strong></p><p>因此为了避免出现这两种问题，最好的办法就是<strong>不要在返回值的地方写上引用和指针</strong></p><h1 id="绝不从新定义继承而来的-non-virtual-函数">27.绝不从新定义继承而来的non-virtual 函数</h1><p>前面我们说过，基类定义不同的函数就意味着不同的意思：</p><ul><li>当基类定义一个 pure virtual函数时，意味着派生类<strong>只继承该函数的接口（声明）</strong>（为purevirtual 函数添加缺省实现并没有什么意义）</li><li>当基类定义一个 virtual函数时，意味着派生类<strong>继承该函数接口和一份默认实现</strong></li><li>当基类定义一个 non-virtual函数时，意味着派生类<strong>继承该函数接口和一份强制实现</strong></li></ul><p>基于这三点，我们需要在此说明的是：不用在派生类当中重写基类的non-virtual函数，这除了会发生名称遮掩这个问题外，还会有更严重的事情发生。我们考虑下面这个例子：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Base</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; <span class="hljs-string">&quot;Base&quot;</span> &lt;&lt; endl;<br>&#125;<br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base<br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">()</span><span class="hljs-comment">//派生类从新定义了基类的函数</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; <span class="hljs-string">&quot;Derived&quot;</span> &lt;&lt; endl;<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>然后，我们直到一个派生类对象可以由两个指针指向它（这两个指针的地址不同，但C++ 允许这样），即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Derived x;<br>Base* pt = &amp;x;<br>Derived* pr = &amp;x;<br><br><span class="hljs-comment">//当我们调用函数时，这么做会发生意外</span><br>pt-&gt;<span class="hljs-built_in">fun</span>();<span class="hljs-comment">//Base::fun() 的调用</span><br>pr-&gt;<span class="hljs-built_in">fun</span>();<span class="hljs-comment">//Derived::fun() 的调用</span><br></code></pre></td></tr></table></figure><p>也就是说，当函数 <code>fun</code> 被调用时，任何一个对象（这里指的是x）可能即表现出基类的特性，也表现出派生类的特性。说人话就是，一个对象可以同时表现出基类和派生类的行为（我通过一个指针来调用对象当中的某个成员方法，但实际的结果会因为我指针的类型不同而不同）。这里的原因不在于对象自身，而在于<strong>指向这个对象的指针的类型</strong>。</p><p>这里的原因是： <strong>non-virtual函数都是静态绑定的，也就是说如果一个指针被声明为 pointer-to-Base，那么通过该指针所调用的 non-virtual 函数就永远是在 Base中定义的版本，就算该指针指向一个派生类对象也没用</strong>。</p><h1 id="了解隐式接口和编译期多态">28.了解隐式接口和编译期多态</h1><p>对于一个对象，如果我们可以通过查看头文件就知道该对象的接口（如果该对象为变量，则接口为类型，而函数则为声明），那么这种接口称为<strong>显示接口</strong></p><p>而<strong>隐式接口则是那种无法在编译阶段就能确定的接口</strong>（典型的就是动态分配的对象和virtual 函数）</p><p>多态分为：<strong>编译期多态</strong>和<strong>运行期多态</strong>。我们说函数重载就是一种多态，再细一点，<strong>函数重载是编译期多态</strong>。因为在编译阶段就能够知道程序要调用哪个函数。</p><p>而运行期多态则在编译阶段无法确定需要调用哪个函数，具体调用得到运行阶段才能确定。典型例子是virtual 函数和函数模板。</p><p>接下来，我们详细来讨论一下函数模板的隐式接口问题。我们有一个函数模板：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(T w)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span>(w.<span class="hljs-built_in">size</span>() &gt; <span class="hljs-number">10</span> &amp;&amp; w != otherValue)<br>        ...<br>&#125;<br></code></pre></td></tr></table></figure><p>对于 w 的隐式接口 T ，它看起来需要有以下的约束：</p><ul><li>它必须提供一个 size 函数接口并且该函数返回一个整数</li><li>它必须提供一个 <code>operator!=</code> 用来比较两个 T 对象（我们假设otherValue 的类型为 T ）</li></ul><p>实际上，这两个条件都不需要满足（函数模板真牛逼）。确实，T 必须支持size 函数，但该函数可以从基类<strong>继承而来</strong>。再进一步，这个size 函数可以<strong>不需要返回一个 <code>int</code>类型</strong>，它可以返回一个类型 x ，只要 x 重载了<code>operator&gt;</code> 使得类型 x 可以与 <code>int</code>相比较即可。</p><p>想到这里你格局还是小了，这里牛逼的地方在于：</p><p>size 函数<strong>只需要返回一个类型为 x 的对象</strong>，这个 x可以是任何类型，只要类型 x 加上 <code>int</code> 能够调用<code>operator&gt;</code> 即可（即 x 不需要重载<code>operator&gt;</code> ）。</p><p>更进一步， <code>operator&gt;</code> 所接受的参数可以不是 x，可以是另外一个类型 y ，<strong>只要存在一个构造函数能够将类型 x隐式转换成类型 y 就行</strong>。</p><p>不得不感叹，这东西真牛啊。想出这玩意的是个天才！！！！！</p><p>同样， T 并不需要提供 <code>operator!=</code>的接口，<code>operator!=</code> 这个函数只需要接受一个类型为 x的对象和一个类型为 y 的对象，只要 T 可以被隐式转换成 x 而 otherValue可以被隐式转换成 y 就行。</p><p>因此，这种隐式接口的表达式仅仅<strong>由一组有效表达式构成</strong>。</p><h1 id="了解-typename-的双重意义">29.了解 typename 的双重意义</h1><p>提一个问题：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">tp</span>&#123;&#125;;<br><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">tp</span>&#123;&#125;;<br></code></pre></td></tr></table></figure><p>这当中的 <code>typename</code> 和 <code>class</code> 有何不同？</p><p>答案是，没有任何不同。是的，没有任何不同。在这种情况下二者完全一样。</p><p>然而，C++ 并不总将 <code>typename</code> 与 <code>class</code>视为等价，具体看下面这个例子：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> C&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">const</span> C&amp; container)</span></span><br><span class="hljs-function"></span>&#123;<br>    C::const_iterator iter = container.<span class="hljs-built_in">begin</span>();<br>    <span class="hljs-type">int</span> value = *iter;<br>&#125;<br></code></pre></td></tr></table></figure><p>我们的想法是：定义一个迭代器 <code>iter</code>并将它赋值为传进来的容器的起始位置，将此迭代器的值赋值给一个<code>int</code> 类型的变量</p><p>这个函数当中，存在两个 <code>local</code> 变量：<code>iter</code> 和<code>value</code> 。</p><p><code>iter</code> 的类型为 <code>C::iterator</code>，实际的类型必须由模板参数 C来进行确定。因此我们说：<strong>模板内如果出现的名称相依于某个模板参数的话，该名称称之为从属名称</strong>。<strong>如果从属名称在class 内程嵌套状，则称为嵌套从属名称</strong>（这里的类型<code>C::iterator</code>属于一个类当中的嵌套类型，而用此类型定义的名称称为从属名称，合起来就是嵌套从属名称）。</p><p>另外一个 <code>local</code> 变量 value 的类型是 <code>int</code>，<strong>不依赖于模板参数类型</strong>，因此它是<strong>非从属名称</strong>。</p><p>嵌套从属名称会导致编译器解析困难，我们假定在函数中有如下代码：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> C&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">const</span> C&amp; container)</span></span><br><span class="hljs-function"></span>&#123;<br>    C::const_iterator* iter;<br>&#125;<br></code></pre></td></tr></table></figure><p>编译器在这里会有两种解释：</p><ol type="1"><li>认为 <code>C::const_iterator</code>是一个嵌套从属类型，那么这里就是在声明一个指针。</li><li>认为 <code>C::const_iterator</code> 是一个<strong>类当中定义的<code>static</code> 类型的变量</strong>（这么想是合法的，因为<code>static</code> 类型的变量可以直接通过类名来访问）而<code>iter</code>可以是一个<strong>全局变量</strong>，那么这里编译器会认为这是在将两个变量<strong>做乘法</strong>。</li></ol><p>这里便出现了歧义，原因在于：在不能确定模板参数是什么的情况下，没有任何办法知道<code>C::const_iterator</code> 是否是一个类型。</p><p>当然，C++有一个规则来解析这个问题：如果解析器在模板中遭遇一个嵌套从属名称，那么它将假设该名称不是类型，除非你告诉它。即<strong>在缺省状态下，嵌套从属名称不是类型</strong>。</p><p>因此在默认状态下，<code>C::iterator iter</code><strong>不是合法的代码</strong>。<strong>如果我们想告诉编译器嵌套从属名称是一个类型，只需要在前面加上<code>typename</code> 即可</strong>。</p><p>我们来看下面这个例子：模板函数接受一个容器和一个指向容器的迭代器</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> C&gt;<span class="hljs-comment">//模板声明</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">const</span> C&amp; container,              <span class="hljs-comment">//不允许加 typename</span></span></span><br><span class="hljs-params"><span class="hljs-function">        <span class="hljs-keyword">typename</span> C::const_iterator&amp; iter)</span></span>;<span class="hljs-comment">//必须要加 typename</span><br></code></pre></td></tr></table></figure><p>这个例子说明了：只要在需要确定一个嵌套从属名称为类型的时候才允许使用<code>typename</code> 。即 <strong><code>typename</code>必须作为嵌套从属名称的前缀词</strong>。</p><p>当然，「<code>typename</code>必须作为嵌套从属名称的前缀词」是有例外的，这一例外规则是<strong>「<code>typename</code>不可以出现在 base class list内的嵌套从属名称前面，也不允许出现在初始化列表的嵌套从属名称前面」</strong>，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base&lt;T&gt;::Nested <span class="hljs-comment">//这里属于 base class list ，这里的嵌套从属名称前面不允许加 typename</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-keyword">explicit</span> <span class="hljs-title">Derived</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function">        : Base&lt;T&gt;::Nested(x) //构造函数的初始化列表当中的嵌套类型前也不允许加上 typename</span><br><span class="hljs-function">        &#123;</span><br>            <span class="hljs-keyword">typename</span> Base&lt;T&gt;::Nested x;<span class="hljs-comment">//嵌套从属名称前面必须要加上 typename</span><br>        &#125;<br>    <br>&#125;<br></code></pre></td></tr></table></figure><p>其实这里也很容易理解，毕竟在 base class list（基类列表）和初始化列表当中的嵌套类型不可能会是一个类当中的静态成员变量。</p><p>我们最后再看一个 <code>typename</code>例子，这将会发生在真实程序当中。</p><p>假设我们需要写一个模板函数，该函数接受一个迭代器，而我们打算为该迭代器指涉的对象做一份<code>local</code> 复件 temp（也就是将这个指针所指向的对象做一份拷贝）。我们可以这么写：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> iterT&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(iterT iter)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">typename</span> std::iterator_traits&lt;iterT&gt;::<span class="hljs-function">value_type <span class="hljs-title">temp</span><span class="hljs-params">(*iter)</span></span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>别被 <code>std::iterator_traits&lt;iterT&gt;::value_type</code>给吓到了，它只不过是标准 traits class 的运用而已，相当于在说：「类型为<code>iterT</code> 的对象所指之物的」类型。</p><p>也就是说，这里会将 temp 的类型声明为<strong>「<code>iterT</code>类型的迭代器所指向对象的类型」</strong>（是指针指向对象的类型），之后再对temp 进行赋值，就可以得到此迭代器指涉对象的一份拷贝（如果<code>iterT</code> 的类型为 <code>vector&lt;int&gt;::iterator</code>，那么 temp 的类型就是 <code>int</code> ；如果 <code>iterT</code>的类型为 <code>stack&lt;string&gt;::iterator</code> ，那么 temp 的类型为<code>string</code>）。</p><p>当然，这玩意是个嵌套从属类型，需要在前面加上 <code>typename</code>。</p><p>当然，每次打那么长也会觉得很不舒服，因此我们可以使用<code>typedef</code> 来简化：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> iterT&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(iterT iter)</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">typedef</span> <span class="hljs-keyword">typename</span> std::iterator_traits&lt;iterT&gt;::value_type value_type;<br><span class="hljs-function">value_type <span class="hljs-title">temp</span><span class="hljs-params">(*iter)</span></span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>这么做也是合法的，<strong>不过要记得加上<code>typename</code></strong> 。</p><h1 id="绝不从新定义继承而来的缺省参数值">30.绝不从新定义继承而来的缺省参数值</h1><p>这里我们只把理解说一下，具体的例子去看书</p><p>我们先来讨论静态类型和动态类型</p><p>对象的静态类型就是它在程序中<strong>被声明时所采用的类型</strong>，而动态类型则是「目前对象所指的类型」（这里默认对象是指针了）</p><p>我们在基类当中写一个 virtual函数，然后这个函数存在一个默认值。如果派生类重写这个函数的时候修改了这个默认值，就会出现一些问题</p><p>我们通常会用基类指针来指涉派生类对象，由于 virtual函数的默认参数是静态绑定的，而 virtual函数则是动态绑定的，因此我在用基类指针调用派生类对象的 virtual函数时，派生类的 virtual函数的默认参数将会变为基类的默认参数。关于这一点我们是不希望看到的。</p><p>因此，我们最好<strong>不要在派生类当中从新定义 virtual函数的默认参数</strong>。如果确实需要，我们可以将 virtual 函数放在private 权限下，然后对外提供一个 public 的 non-virtual函数接口。通过这个 non-virtual 函数调用 virtual 函数，并在 non-virtual函数中写上默认参数，这样所有的派生类就都不会从新定义 virtual函数的默认参数了。</p><h1 id="学习处理模板化基类的名称">31.学习处理模板化基类的名称</h1><p>本例主要讨论当基类为类模板时候访问基类函数可能会出现的问题。具体看下面的例子：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Base</span><br>&#123;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">func</span><span class="hljs-params">()</span><span class="hljs-type">const</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; <span class="hljs-string">&quot;Chisato&quot;</span> &lt;&lt; endl;<br>&#125;<br>&#125;;<br><br><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">Ty</span>&gt;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base&lt;Ty&gt;<br>&#123;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">()</span><span class="hljs-comment">//与基类函数名称不一样，避免发生遮掩</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-built_in">func</span>();<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>这里我们有一个基类模板，并通过派生类来基础它，我们希望在派生类当中调用基类函数<code>func</code> ，但这么做编译器会报错。</p><p>这里的报错原因是编译器不认识 <code>func</code>这个标识符？？？你会很纳闷，什么玩意儿，这不是继承吗为什么会有这种情况？？？</p><p>模板这玩意在搞的时候总是会出奇奇怪怪的问题，习以为常了（</p><p>我们先看基类，对于模板参数 <code>T</code>，我们可能会因为这个类无法满足某些需求而对一些特定的类型进行<strong>全特化</strong>的处理，在这个全特化的过程中，我们可能会删掉类当中的一些函数，也可能会添加。</p><p>明确这一点之后，我们回到派生类，由于无法确定模板参数 <code>Ty</code>，因此我们也<strong>无法确定基类是否会对一些特定的类进行全特化的处理</strong>。C++知道这一点，并且它也知道如果进行了全特化，那个全特化的版本也有可能会不提供基类函数<code>fun</code> ，因此 C++ 会直接在这里报错。</p><p>要解决这个问题有三种方法</p><p>第一个是在函数调用前加上 <code>this</code> 指针，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base&lt;T&gt;<br>&#123;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">()</span><span class="hljs-comment">//与基类函数名称不一样，避免发生遮掩</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">this</span>-&gt;<span class="hljs-built_in">func</span>();<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>第二种是通过 <code>using</code>声明来让该名称在派生类当中可见，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base&lt;T&gt;<br>&#123;<br><span class="hljs-keyword">using</span> Base&lt;T&gt;::func;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">()</span><span class="hljs-comment">//与基类函数名称不一样，避免发生遮掩</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-built_in">func</span>();<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure><p>第三种是直接用基类的名称来访问，不过如果该函数是 virtual函数的话就会关闭其 virtual 绑定行为</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base&lt;T&gt;<br>&#123;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">()</span><span class="hljs-comment">//与基类函数名称不一样，避免发生遮掩</span></span><br><span class="hljs-function"></span>&#123;<br>Base&lt;T&gt;::<span class="hljs-built_in">func</span>();<br>&#125;<br>&#125;;<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>Effective C++</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Effective C++</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>C++ Primer Plus 笔记</title>
    <link href="/2022/07/30/C++%20Primer%20Plus/C++%20primer%20plus%E7%AC%94%E8%AE%B0/"/>
    <url>/2022/07/30/C++%20Primer%20Plus/C++%20primer%20plus%E7%AC%94%E8%AE%B0/</url>
    
    <content type="html"><![CDATA[<h1 id="函数">函数</h1><h2 id="基础">基础</h2><h3 id="函数是如何返回返回值的">函数是如何返回返回值的</h3><p>通常，函数将返回值复制到指定的CPU寄存器或内存单元中。随后，调用该函数的程序查看该内存单元。函数原型（声明）告诉调用程序返回值的类型，函数定义命令被调用函数用什么类型的数据返回。即返回函数和调用函数必须就该内存单元的数据类型必须要达成一致</p><h3 id="关于函数原型声明">关于函数原型（声明）</h3><ul><li>为什么要用原型</li></ul><p>函数原型将该函数的参数类型、数量和返回值类型告诉编译器，使得编译器可以正确解释它们</p><ul><li>原型的语法</li></ul><p>函数原型只需要提供函数返回值类型、函数名、参数列表（参数类型、数量）（不需要参数名称，实际上参数名称相当于<strong>占位符</strong>的存在）</p><p>当然，<strong>函数的原型部分也可以不指定参数列表</strong>，函数原型的参数部分为空表示参数默认为<code>void</code>，并不是不指定参数列表的意思。在C++中，<strong>不指定参数列表应当用省略号：</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">say_hi</span><span class="hljs-params">(...)</span></span>;<br></code></pre></td></tr></table></figure><p>通常，仅当与接受可变参数的C函数（如printf)交互时才需要这么做</p><h3 id="指针和const">指针和const</h3><p>如果数据类型本身不是指针，则可以将const数据或非const数据的<strong>地址</strong>赋值给被const修饰的指针，只能将非const数据的地址赋值给非const指针（不能将const数据的地址赋值给非const修饰的指针，这会使得const的状态很荒谬）</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">const</span> <span class="hljs-type">int</span> a = <span class="hljs-number">10</span>;<br><span class="hljs-type">int</span>* pa = &amp;a;<span class="hljs-comment">//INVAILD</span><br></code></pre></td></tr></table></figure><p>如果数据类型本身是指针，则可以将非const数据的地址赋值给const或非const修饰的指针，只能将const数据的地址赋值给const指针（不允许将const数据的地址赋值给非const修饰的指针，这一点与上面一致）</p><h2 id="内联函数">内联函数</h2><p>编译过程的最终产物是可执行程序——一组机器指令的集合。当执行该程序时，操作系统会将这些指令都载入到内存当中，因此每一条指令都会对应一个内存编号。随后，计算机将逐步执行这些指令，（当遇到循环时）有时会向前、向后跳跃到特定地址执行对应的指令。函数的调用过程也是如此，当调用该函数时，操作系统会跳到该函数的起始地址开始执行，在函数结束时返回。</p><p>程序在跳跃执行函数的过程中，会产生一定的开销。当跳跃所需的开销大于实际执行的开销，并且在重复调用该函数的时候这会十分浪费资源。为了解决这一问题，C++内联函数提供了另一种选择。</p><p>编译内联函数时，<strong>编译器会使用相应的函数代码来替换掉函数调用</strong>，这样程序就不需要跳跃到其他内存地址来执行对应的代码了。因此内联函数的执行速度快于其他函数，但代价是会<strong>带来更大的内存</strong>。如果我在一段程序中调用了该函数十次，在编译后这里就会出现该函数代码的十个拷贝。</p><p>需要注意的是：</p><ul><li>内联函数不应该过大，占用了多行就没有必要了</li><li>内联函数不能递归</li></ul><p>我们在函数的前面加上 <code>inline</code>，这只是建议编译器将该函数改为 <code>inline</code>的，实际上是不是还得取决于编译器。</p><p>对于在类内<strong>定义的函数</strong>，都是默认在前面加上<code>inline</code> 的，对于类外定义的函数，默认是不加<code>inline</code> 的。</p><h2 id="引用变量">引用变量</h2><p>当我们声明一个变量时，编译器会为该变量分配一个内存地址，也就是说我可以通过该变量名来操作该内存地址当中的内容。引用变量的作用是<strong>再给这块内存地址另外一个名字</strong>，即我可以通过这两个不同的名字来操纵同一个内存地址，它们是完全等价的。</p><p>注：引用必须在其声明时就赋值，不能先初始化后赋值</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> rats;<br><span class="hljs-type">int</span> &amp; rodent = rats;<br><span class="hljs-type">int</span>* <span class="hljs-type">const</span> ptr = &amp;rats;<span class="hljs-comment">//引用等价于这个</span><br></code></pre></td></tr></table></figure><p>因此，引用在对其初始化之后的值就不能被改变（这里指的是该引用的指向）</p><p>当函数的形参是一个引用类型的时候，对比不加引用的情况，其实参的要求更加严格。由于引用是实际上是一个变量的别名，因此函数的实参必须是变量，一个普通的数字是不允许的。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">int</span>&amp; e1)</span></span>;<br><span class="hljs-built_in">fun</span>(<span class="hljs-number">10</span>);<span class="hljs-comment">//Illeague</span><br><span class="hljs-built_in">fun</span>(a);<span class="hljs-comment">//League</span><br><span class="hljs-built_in">fun</span>(a + <span class="hljs-number">10</span>);<span class="hljs-comment">//Illeague</span><br></code></pre></td></tr></table></figure><p>至于如何判断函数的实参是否会出问题，我们可以用一个很简单的方法——将其作为左值。如果是变量，那么它作为左值是被允许的，而非变量的数（如表达式）则不被允许</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp">a = <span class="hljs-number">5</span>;<span class="hljs-comment">//League</span><br>a + <span class="hljs-number">10</span> = <span class="hljs-number">5</span>;<span class="hljs-comment">//Illeague</span><br></code></pre></td></tr></table></figure><p>那么这里就引出了一个问题：什么是左值？常规变量和 const变量都可以视为左值，因为它们都可以通过地址来访问。常规变量属于可修改的左值，const 变量属于不可修改的左值。</p><ul><li>左值是允许通过地址来访问的，而右值不允许、</li></ul><p>也就是说右值实际上是在一块临时的内存空间当中，上面的 fun(10) 与 fun(a+ 10) ，这两个实参都是右值，它们存在于一个临时的内存空间当中</p><h3 id="临时变量">临时变量</h3><p>如果实参与引用类型不匹配时，C++将生成临时变量，当然这么做只在引用被const 修饰的时候才允许。如果引用参数为 const，在以下两种情况下编译器会生成临时变量</p><ul><li>实参类型正确，但不是左值</li><li>实参类型不正确，但可以转换成正确的类型</li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e1)</span></span>;<br><span class="hljs-built_in">fun</span>(<span class="hljs-number">10</span>);<span class="hljs-comment">//类型正确，但不是左值</span><br><span class="hljs-built_in">fun</span>(x + <span class="hljs-number">10</span>);<span class="hljs-comment">//与上一种情况一样，会生成临时变量</span><br><span class="hljs-built_in">fun</span>((<span class="hljs-type">double</span>)<span class="hljs-number">10.5</span>);<span class="hljs-comment">//类型不正确，但可以转换成正确的类型</span><br></code></pre></td></tr></table></figure><p>总结一下就是，对于形参为 const引用的函数，如果实参类型不匹配，编译器会生成临时变量使得形参的引用指向该临时变量，这里与按值传递就没有区别了</p><h3 id="引用与继承">引用与继承</h3><p>继承实际上就是派生类具有一些基类的性质。在引用这里，当一个函数是以基类引用作为形参时，<strong>在传入的实参部分可以是基类，也可以是派生类</strong></p><p>举一个简单的例子是：ofstream 是 ostream的派生类，当函数的形参部分写的是 ostream&amp; 时，我既可以传入 ostream类型的左值，也可以传入 ofstream 类型的左值</p><h2 id="函数重载">函数重载</h2><p>函数发生重载的关键是函数的参数列表——也称为函数的特征标（functionsignature），只要两个函数的参<strong>类型</strong>与<strong>数目</strong>不同，那么它们就可以发生重载。</p><ul><li>C++不会依据变量名来发生函数重载</li><li>C++不会依据返回值类型来发生函数重载</li></ul><p>我们来看下面的几个函数</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">int</span> a, <span class="hljs-type">int</span> b)</span></span>;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">long</span> a, <span class="hljs-type">int</span> b)</span></span>;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">double</span> a, <span class="hljs-type">int</span> b)</span></span>;<br></code></pre></td></tr></table></figure><p>当我调用 fun( (unsigned int)10, 5 )时，它不与任何一个函数原型相匹配。当出现没有原型与之匹配时，C++会尝试使用<strong>标准类型转换</strong>强制进行匹配。</p><p>放在这个例子当中，10可以被转换成三种不同的类型，这将分别对应三种不同的接口。因此在这种情况下，C++会拒绝这种函数调用，将其视为错误</p><p>有一点需要注意的是，编译器在检查特征标时，会将<em><strong>类型引用</strong>和<strong>类型</strong>视为同一个特征标</em>，即不能通过引用来发生重载。考虑下面的例子</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">int</span> a)</span></span>;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">int</span>&amp; a)</span></span>;<br></code></pre></td></tr></table></figure><p>当我调用 fun(10) 时，确实会走入不同的接口，但是当我调用 fun(a)时，两个接口都可以使用，这样就会导致错误，因此我们应当禁止这种行为的发生。</p><p>函数的形参部分是否含有 const 是可以发生重载的，原因在于<strong>将const 对象的值赋给 non-const 对象是合法的，但将 non-const 对象的值赋给const 对象则是非法的</strong></p><p>实际上，我们可以通过默认参数的形式来替换掉函数重载，这样只需要写一份代码，编译时也只需要分配一份内存。但如果参数的类型不一致，那就乖乖地用函数重载吧</p><h2 id="函数模板">函数模板</h2><p>函数模板是允许重载的，并且<strong>模板参数不一定非得是模板参数类型</strong>，例如：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">swap</span><span class="hljs-params">(T&amp; a, T&amp; b)</span></span>;<br><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">swap</span><span class="hljs-params">(T&amp;a, T&amp;b, <span class="hljs-type">int</span> c)</span></span>;<span class="hljs-comment">//最后一个参数类型可以不是模板参数类型</span><br></code></pre></td></tr></table></figure><p>当然，模板是有其局限性的。由于模板参数类型可以代表所有的类型，但有些类型可能并不支持一些运算（例如class就不支持+）。当然，C++允许运算符重载，这是一种解决办法，但这里要介绍另一种——为特定类型提供具体化模板</p><h3 id="显示具体化">显示具体化</h3><p>首先有以下三点原则：</p><ul><li>对于给定的函数名，可以有其非模板函数，模板函数，显示具体化模板函数及其重载版本</li><li>显示具体化要以 template&lt;&gt; 开头，并在参数部分指出其类型</li><li>非模板函数优先于常规模板函数和具体化模板函数，具体化模板函数优先于常规模板函数</li></ul><p>假设我们有一个 class ：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">KA</span> &#123;...&#125;;<br></code></pre></td></tr></table></figure><p>对于上面的 swap 函数，其显示具体化模板为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">swap</span><span class="hljs-params">(T&amp; a, T&amp; b)</span></span>;<span class="hljs-comment">//常规模板</span><br><span class="hljs-keyword">template</span>&lt;&gt; <span class="hljs-type">void</span> <span class="hljs-built_in">swap</span>&lt;KA&gt;(KA&amp; a, KA&amp; b);<span class="hljs-comment">//具体化模板</span><br><span class="hljs-keyword">template</span>&lt;&gt; <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">swap</span><span class="hljs-params">(KA&amp; a, KA&amp; b)</span></span>;<span class="hljs-comment">//也可以写出这样</span><br></code></pre></td></tr></table></figure><h3 id="实例化与具体化">实例化与具体化</h3><p>在代码中包含函数模板本身<strong>并不会生成函数定义</strong>，他只是一个用于<strong>生成函数定义的方案</strong>。当编译器用模板为特定的类型生成函数定义时，得到的是模板实例（这里编译器为自定义类型KA 生成的函数定义称为<strong>模板实例</strong>，该实例使用 KA类型）。<strong>模板并非函数定义，但使用 int的模板实例是函数定义</strong>。</p><p><strong>编译器之所以知道需要为该类型生成对应的函数定义，是因为程序在调用模板函数时提供了具体的类型，因此也称这种实例化方式为隐式实例化</strong></p><p>简单来说，隐式实例化是<strong>用该函数模板来生成特定类型的函数定义，该类型来自于函数调用</strong>。既然有隐式实例化，那么自然也有显示实例化。这意味着可以直接命令编译器创建特定的类型，其语法是——在原函数模板的基础上指定其类型，并在最前面加上template</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Swap</span><span class="hljs-params">(T&amp; a, T&amp; b)</span></span>;<span class="hljs-comment">//函数模板的声明</span><br><br><span class="hljs-keyword">template</span> <span class="hljs-type">void</span> <span class="hljs-built_in">Swap</span>&lt;<span class="hljs-type">int</span>&gt;(<span class="hljs-type">int</span>&amp; a, <span class="hljs-type">int</span>&amp; b);<span class="hljs-comment">//显示实例化的声明</span><br></code></pre></td></tr></table></figure><p>看到上述声明后，编译器将会<strong>用函数模板来生成一个使用 int类型的实例</strong>，再说一次，是用函数模板来生成一个使用 int类型的实例。</p><p>我们一般会<strong>将显示实例化的声明放在头文件内</strong></p><p>于显示实例化不同的是，显示具体化使用的是下面两个<strong>等价</strong>声明</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span>&lt;&gt; <span class="hljs-type">void</span> <span class="hljs-built_in">Swap</span>&lt;<span class="hljs-type">int</span>&gt;(<span class="hljs-type">int</span>&amp; a, <span class="hljs-type">int</span>&amp; b);<br><span class="hljs-keyword">template</span>&lt;&gt; <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Swap</span><span class="hljs-params">(<span class="hljs-type">int</span>&amp; a, <span class="hljs-type">int</span>&amp; b)</span></span>;<br></code></pre></td></tr></table></figure><p>区别在于，这两个声明的意思是：<strong>不要使用模板来生成对应的函数定义，而是使用专门为int类型显示定义的函数定义</strong>。看到区别了吗？显示具体化直接绕过了通过模板生成函数定义这一步骤。显示具体化的template 后面会带有 &lt;&gt; ，而显示实例化则没有。</p><ul><li>注：不允许在一个文件内同时出现同一类型的显示具体化和显示实例化</li></ul><p>隐式实例化、显示具体化、显示实例化总称为具体化</p><table style="width:100%;"><thead><tr class="header"><th style="text-align: center;">template</th><th style="text-align: center;">隐式实例化</th><th style="text-align: center;">显示实例化</th><th style="text-align: center;">显示具体化</th></tr></thead><tbody><tr class="odd"><td style="text-align: center;">template<typename t> void Swap(T&amp; a,T&amp;b)</typename></td><td style="text-align: center;">Swap(a, b) ( a, b is int )</td><td style="text-align: center;">template void Swap<char>(char&amp; a,char&amp; b)</char></td><td style="text-align: center;">template&lt;&gt; Swap<short>(short&amp;a, short&amp; b) or template&lt;&gt; Swap(double&amp; a, double&amp;b)</short></td></tr></tbody></table><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Swap</span><span class="hljs-params">(T&amp; a, T&amp; b)</span></span>;<span class="hljs-comment">//其他类型走这条，隐式实例化</span><br><span class="hljs-keyword">template</span> <span class="hljs-type">void</span> <span class="hljs-built_in">Swap</span>&lt;<span class="hljs-type">int</span>&gt;(<span class="hljs-type">int</span>&amp; a, <span class="hljs-type">int</span>&amp; b);<span class="hljs-comment">//这玩意只是声明，强制编译器用模板来生成 int 实例</span><br><span class="hljs-keyword">template</span>&lt;&gt; <span class="hljs-type">void</span> <span class="hljs-built_in">Swap</span>&lt;<span class="hljs-type">short</span>&gt;(<span class="hljs-type">short</span>&amp; a, <span class="hljs-type">short</span>&amp; b);<span class="hljs-comment">//short 类型走这条，显示具体化</span><br></code></pre></td></tr></table></figure><p>我们来进一步区分这三者</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T&gt;</span><br><span class="hljs-function">T <span class="hljs-title">Lesser</span><span class="hljs-params">(<span class="hljs-type">const</span> T&amp; a, <span class="hljs-type">const</span> T&amp; b)</span><span class="hljs-comment">//函数模板</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; <span class="hljs-string">&quot;Template&quot;</span> &lt;&lt; endl;<br><span class="hljs-keyword">return</span> a &lt; b ? a : b;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">Lesser</span><span class="hljs-params">(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; a, <span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; b)</span><span class="hljs-comment">//函数重载</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; <span class="hljs-string">&quot;Overload&quot;</span> &lt;&lt; endl;<br><span class="hljs-keyword">return</span> a &lt; b ? a : b;<br>&#125;<br><br><span class="hljs-keyword">template</span> <span class="hljs-type">short</span> <span class="hljs-built_in">Lesser</span>&lt;<span class="hljs-type">short</span>&gt;(<span class="hljs-type">const</span> <span class="hljs-type">short</span>&amp; a, <span class="hljs-type">const</span> <span class="hljs-type">short</span>&amp; b);<span class="hljs-comment">//显示实例化声明，放在头文件</span><br><br><span class="hljs-keyword">template</span>&lt;&gt; <span class="hljs-function"><span class="hljs-type">double</span> <span class="hljs-title">Lesser</span><span class="hljs-params">(<span class="hljs-type">const</span> <span class="hljs-type">double</span>&amp; a, <span class="hljs-type">const</span> <span class="hljs-type">double</span>&amp; b)</span><span class="hljs-comment">//显示具体化</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; <span class="hljs-string">&quot;Specialization&quot;</span> &lt;&lt; endl;<br><span class="hljs-keyword">return</span> a &lt; b ? a : b;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> a = <span class="hljs-number">5</span>, b = <span class="hljs-number">10</span>;<br><span class="hljs-type">short</span> n = <span class="hljs-number">1</span>, m = <span class="hljs-number">2</span>;<br><span class="hljs-type">double</span> x = <span class="hljs-number">3</span>, y = <span class="hljs-number">6</span>;<br>cout &lt;&lt; <span class="hljs-built_in">Lesser</span>(a, b) &lt;&lt; endl;<br>cout &lt;&lt; <span class="hljs-built_in">Lesser</span>(n, m) &lt;&lt; endl;<br>cout &lt;&lt; <span class="hljs-built_in">Lesser</span>(x, y) &lt;&lt; endl;<br><br>cout &lt;&lt; Lesser&lt;&gt;(a, b) &lt;&lt; endl;<span class="hljs-comment">//前两个函数模板</span><br>cout &lt;&lt; Lesser&lt;&gt;(n, m) &lt;&lt; endl;<br>cout &lt;&lt; Lesser&lt;&gt;(x, y) &lt;&lt; endl;<span class="hljs-comment">//显示具体化</span><br><br>cout &lt;&lt; <span class="hljs-built_in">Lesser</span>&lt;<span class="hljs-type">int</span>&gt;(a, b) &lt;&lt; endl;<span class="hljs-comment">//全部都是函数模板</span><br>cout &lt;&lt; <span class="hljs-built_in">Lesser</span>&lt;<span class="hljs-type">int</span>&gt;(n, m) &lt;&lt; endl;<br>cout &lt;&lt; <span class="hljs-built_in">Lesser</span>&lt;<span class="hljs-type">int</span>&gt;(x, y) &lt;&lt; endl;<span class="hljs-comment">//三者都是显示具体化，这是因为指定了模板参数类型</span><br><br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果我在调用的时候<strong>指定了参数类型，那么就是显示实例化，如果没有，则为隐式实例化</strong>。</p><h3 id="decltype-和-后置返回类型均为c11">decltype 和后置返回类型（均为C++11）</h3><p>考虑下面这个问题</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">template</span>&lt;<span class="hljs-keyword">typename</span> T1, <span class="hljs-keyword">typename</span> T2&gt;</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">(T1 a, T2 b)</span></span><br><span class="hljs-function"></span>&#123;<br>    x = a + b;<br>&#125;<br></code></pre></td></tr></table></figure><p>那么 x 应该为什么类型？</p><p>显然，在模板参数不确定的时候，我们根本没办法来判断。</p><p>这个时候， decltype 则解决了这个问题，我们可以这么使用：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> x;<br><span class="hljs-keyword">decltype</span>(x) y<br></code></pre></td></tr></table></figure><p>这里表示，<strong>使 y 的类型跟 x的相同</strong>。对于上面那个问题，我们可以这么写：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">decltype</span>(a + b) x = a + b;<span class="hljs-comment">//这表示 x 的类型与 a + b 相同</span><br></code></pre></td></tr></table></figure><p>现有声明：<em>decltype ( expression ) var</em> ，则 var的类型按如下原则确定</p><ul><li>如果 expression 是一个没有括号括起的标识符，则 var的类型与标识符的类型相同，包括 const 等限定符</li><li>如果 expression 是一个函数调用，则 var 的类型与函数的返回值相同<ul><li><strong>这里只是检查函数的返回类型，不会实际调用函数</strong></li></ul></li><li>如果 espression 是一个左值，则 var 为指向其类型的引用</li><li>如果前面的条件都不满足，则 var 的类型与 expression 相同</li></ul><p>对于上面那个函数，我们目前解决的是 x的类型的问题，但该函数的返回值仍然不确定，但可以明确的是，返回值的类型一定与x 相同，而 x 的类型又依靠 a + b 。</p><p>C++11提供一种叫后置返回类型的操作，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">auto</span> <span class="hljs-title">fun</span><span class="hljs-params">(<span class="hljs-type">int</span> a, <span class="hljs-type">int</span> b)</span> -&gt; <span class="hljs-type">double</span></span><br></code></pre></td></tr></table></figure><p>这样我们可以将返回值的类型写在后面，而 <strong>auto是占位符</strong>。同理，对于上面那个问题，我们可以：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">auto</span> <span class="hljs-title">fun</span><span class="hljs-params">(T1 a, T2 b)</span> -&gt; <span class="hljs-title">decltype</span><span class="hljs-params">(a + b)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">return</span> a + b;<br>&#125;<br></code></pre></td></tr></table></figure><h1 id="内存模型与名称空间">内存模型与名称空间</h1><h2 id="单独编译">单独编译</h2><p>在头文件中，我们可以包含：</p><ul><li>函数原型</li><li>使用 #define 或 const 定义的符号常量</li><li>结构声明</li><li>类声明</li><li>模板声明</li><li>内联函数</li></ul><p>在包含我们自己写的头文件是，要用 "" ，而不是 &lt;&gt;。如果是用尖括号，则编译器会去存储标准头文件的目录下查找，如果是用双引号，则编译器会先在源代码目前去查找头文件，如果找不到才会去标准头文件目录下去查找</p><p>在C++中，我们需要尽可能避免多次包含头文件，具体方法如下</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">ifndef</span> HHDD__ <span class="hljs-comment">//如果没有定义 HHDD__ 则会执行下面的代码（到 endif 为止）</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> HHDD__</span><br><span class="hljs-comment">//...</span><br><span class="hljs-meta">#<span class="hljs-keyword">endif</span></span><br></code></pre></td></tr></table></figure><h2 id="存储持续性作用域和链接性">存储持续性、作用域和链接性</h2><p>C++使用三种（C++11是4四种）不同的方案来存储数据，它们的区别在于<strong>数据保留在内存当中的时间</strong></p><ul><li>自动存储持续性：在函数定义中声明的变量（包括函数参数）的存储连续性为自动的。当执行完函数或代码块时，它们的内存会自动释放</li><li>静态存储持续性：在函数定义外定义的变量或者用 static定义的变量。它们在程序运行的整个期间都存在</li><li>动态存储持续性：用 new 所分配的内存将会一直存在，直到将这块内存delete 掉</li><li>线程存储持续性（C++11）：如果变量是使用 thread_local声明的，则其声明周期与所属线程一样长</li></ul><h3 id="作用域与链接">作用域与链接</h3><p><strong>作用域描述了名称在文件的多大范围内可见</strong>。如，我在一个源文件的中间定义了一个变量，那么从中间到结尾该变量均可用，这里也是该变量的作用域，而从文件开头到中间则不是它的作用域</p><p><strong>链接性描述了名称如何在不同文件间共享</strong>。<strong>链接性为外部的名称可以在文件间共享，链接性为内部的名称只能由该文件当中的函数共享</strong>。对于上面那个例子，该变量在当前源文件中使用的范围，这关乎它的作用域，而其他文件是否可以使用该变量则关乎它的链接性</p><ul><li>全局变量的作用域为其定义位置到文件结尾</li><li>在类中声明的成员的作用域为整个类</li><li>在名称空间中声明的变量的作用域为整个名称空间（全局作用域可以看出名称空间作用域的特例）</li><li>函数的作用域可以是整个类或者整个名称空间，但不能是局部的</li></ul><h3 id="自动存储持续性">自动存储持续性</h3><p>在函数中声明的变量和函数参数的<strong>存储持续性为自动</strong>，<strong>作用域为局部</strong>、<strong>不具备链接性</strong>。我们直接看下面这个例子：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-type">int</span> x = <span class="hljs-number">5</span>;<br>cout &lt;&lt; x &lt;&lt; endl;<br>cout &lt;&lt; (<span class="hljs-type">int</span>*)&amp;x &lt;&lt; endl;<br>&#123;<br><span class="hljs-type">int</span> x = <span class="hljs-number">10</span>;<br>cout &lt;&lt; x &lt;&lt; endl;<br>cout &lt;&lt; (<span class="hljs-type">int</span>*)&amp;x &lt;&lt; endl;<br>&#125;<br>cout &lt;&lt; x &lt;&lt; endl;<br>cout &lt;&lt; (<span class="hljs-type">int</span>*)&amp;x &lt;&lt; endl;<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>注：花括号可以单独使用，其括起来的部分为一个代码块</li></ul><p>在进入 main 函数的时候，定义了一个 x。在进入代码块的时候，又定义了一个 x 。后定义的 x由于处在代码块中，它会遮掩（替换）掉原来的 x（控制流目前在代码块中，由于遮掩的存在导致程序看不到原来的那个 x，只能看到在代码块内定义的 x，这便是<strong>遮掩</strong>）。也就是说，这个 x 并不是对原本 x的赋值而是重新开辟了一块内存。</p><p><strong>两个 x的作用域不同，因此它们存储的位置也不一样（尽管名称相同，但同一时间只会使用一个，因为存在遮掩），而如果它们的作用域相同，则后面的x 为赋值</strong></p><p>在C++11中， auto用于自动类型推断，在以前版本的C++与C中，它用于显示指出变量为自动存储</p><p>在C++11中，register用于显示指出变量是自动的。使用它的原因是：程序员想使用一个自动变量，这个变量的名称可能与外部变量相同。register原先的用法为：建议编译器用寄存器来存储自动变量</p><h3 id="静态持续变量">静态持续变量</h3><p>原先我们对于变量类型的分类主要是：全局变量、局部变量、静态变量（这里我们不讨论动态内存分配的变量）</p><p>实际上，这么讲是不对的，真正的分类应该只有两种：<strong>自动变量</strong>和<strong>静态变量</strong></p><p>关于自动变量，前面已经有过讨论了。<strong>作用域为局部</strong>，<strong>无链接性</strong>。</p><p>我们来讨论静态变量。</p><p>C++为静态变量提供了三种链接性，而静态变量的作用域则取决于其链接性。具体如下，括号内为作用域</p><ul><li>外部链接性（可在文件中访问）</li><li>内部链接性（仅允许在当前文件内访问）</li><li>无链接性（只能在当前函数或代码块中访问）</li></ul><p>由于静态类型的变量数目在整个程序执行期间都是不变的，因此编译器会分配固定的内存来存储它们，<strong>它们在整个程序执行期间一直存在</strong>。另外，<strong>如果没有显示初始化静态变量，它们会被编译器设置为0 </strong>（也就是零初始化）。</p><p>我们举例说明如何创建这三种类型的静态变量</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> a = <span class="hljs-number">10</span>;<span class="hljs-comment">//静态变量，外部链接性。等同于全局变量，其他文件也可以访问</span><br><span class="hljs-type">static</span> <span class="hljs-type">int</span> b = <span class="hljs-number">10</span>;<span class="hljs-comment">//静态变量，内部链接性。只能在当前文件访问</span><br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">fun</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">static</span> <span class="hljs-type">int</span> c = <span class="hljs-number">10</span>;<span class="hljs-comment">//静态变量，无链接性。只能在代码块内部或函数内部进行访问</span><br>&#125;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><p>不难看出：想要创建链接性为外部的静态变量，<strong>需要在代码块外面声明它</strong>；想要创建链接性为内部的静态变量，<strong>需要在代码块外部声明它并加上<code>static</code>进行修饰</strong>；想要创建无链接性的静态变量，<strong>需要在代码块内部声明它并加上<code>static</code> </strong>。</p><table><thead><tr class="header"><th style="text-align: center;">四种变量的存储方式</th></tr></thead><tbody></tbody></table><table><thead><tr class="header"><th style="text-align: center;">存储描述</th><th style="text-align: center;">持续性</th><th style="text-align: center;">作用域</th><th style="text-align: center;">链接性</th><th style="text-align: center;">如何声明</th></tr></thead><tbody><tr class="odd"><td style="text-align: center;">自动</td><td style="text-align: center;">自动</td><td style="text-align: center;">局部</td><td style="text-align: center;">无</td><td style="text-align: center;">代码块内部或函数内部</td></tr><tr class="even"><td style="text-align: center;">静态、无链接性</td><td style="text-align: center;">静态</td><td style="text-align: center;">代码块或函数内</td><td style="text-align: center;">无</td><td style="text-align: center;">在代码块内部或函数内部加上<code>static</code></td></tr><tr class="odd"><td style="text-align: center;">静态、内部链接性</td><td style="text-align: center;">静态</td><td style="text-align: center;">当前文件内</td><td style="text-align: center;">内部</td><td style="text-align: center;">外代码块外声明并加上<code>static</code></td></tr><tr class="even"><td style="text-align: center;">静态、外部链接性</td><td style="text-align: center;">静态</td><td style="text-align: center;">所有文件</td><td style="text-align: center;">外部</td><td style="text-align: center;">在代码块外部声明即可</td></tr></tbody></table><p>关于静态变量初始化的部分，我们在这里小提一嘴。</p><p>静态变量的零初始化<strong>会将零强制类型转换成合适的类型</strong>。并且，零初始化和常量表达式初始化统称为<strong>静态初始化</strong>，这意味着在它们是在编译阶段就已经被初始化了的，而<strong>动态初始化</strong>则意味着需要在运行阶段进行初始化。</p><p>除此之外，所有的静态变量会先零初始化，<strong>而不管该变量是否被显式地初始化过</strong>。简单来说就是会先进行零初始化，在进行静态或动态初始化。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> a;<span class="hljs-comment">//直接就是零初始化</span><br><span class="hljs-type">int</span> b = <span class="hljs-number">10</span>;<span class="hljs-comment">//先零初始化，然后再静态初始化</span><br></code></pre></td></tr></table></figure><h3 id="静态持续性外部链接性外部变量也称为全局变量">静态持续性、外部链接性（外部变量，也称为全局变量）</h3><p>链接性为外部的变量称为外部变量，存储持续性为静态，作用域为整个文件。外部变量是在函数外面定义的，因此它可以在该定义往后或者其他文件当中使用。也因此外部变量也称为全局变量</p><p>首先，C++有「单定义规则」，即变量只能有一次定义；另一方面，每个使用外部变量的文件都需要对该变量进行声明</p><p>为了满足这种需求，C++ 提供了两种变量声明</p><ul><li>定义声明，简称定义，它<strong>会给变量分配存储空间</strong></li><li>引用声明，简称声明，它<strong>不会给变量分配存储空间</strong>，因为它只会引用已有的变量</li></ul><p>引用声明使用关键字 extern，并且<strong>不能进行初始化</strong>；否则此声明为定义，会给变量分配存储空间</p><p>如果需要在多个文件当中使用外部变量，只需要在一个文件当中进行定义，在其他文件当中使用extern 进行声明即可</p><p>如果需要定义一个全局变量，最好是在源文件当中定义，在头文件当中声明（加extern），这是因为源文件和头文件实际上是一个编译单元</p><p>值得注意的是，如果我对一个全局变量加上了 const，<strong>那么它的链接性将会变为内部</strong>，即</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">const</span> <span class="hljs-type">int</span> a = <span class="hljs-number">10</span>;<span class="hljs-comment">//等价于 const static int a = 10;</span><br></code></pre></td></tr></table></figure><p>这么做是有原因的，我们通常不会将全局变量的定义放到头文件，但我们会将常量放到头文件。如果这个常量是外部链接性，那么就相当于是把一个全局变量放到了头文件，这是会出错的</p><p>如果处于某种原因，需要将一个常量的链接性改为外部，则我们可以通过extern 来覆盖掉原先的内部链接性，即</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">extern</span> <span class="hljs-type">const</span> <span class="hljs-type">int</span> a = <span class="hljs-number">10</span>;<br></code></pre></td></tr></table></figure><p>那么，该变量就会跟全局变量一样，只不过不允许更改。在需要使用的文件中要加上extern 。当然，我们也可以在源文件当中定义，在头文件当中声明（加 extern）</p><h3 id="静态持续性内部链接性static-修饰的全局变量">静态持续性，内部链接性（static修饰的全局变量）</h3><p>将 static关键字用于作用域为整个文件的外部变量时，该变量的链接性会变为内部。<strong>链接性为内部的变量只能在其所属文件内使用</strong></p><p>链接性为外部的静态变量可以使得<strong>在不同文件当中共享数据</strong>，链接性为内部的静态变量可以使得<strong>在同一文件的不同函数之间共享数据</strong></p><p>如果链接性为外部的变量被 static修饰，这样就不用担心其名称与其他文件中作用域为整个文件的变量名冲突</p><h3 id="静态持续性无链接性static-修饰的局部变量">静态持续性，无链接性（static修饰的局部变量）</h3><p>在函数中用 static修饰局部变量会使得该变量只在代码块中可以，当控制流离开代码块时<strong>该变量依旧存在</strong></p><p>另外，如果初始化了静态局部变量，那么程序只会在启动时进行一次初始化，以后再调用这个函数将不会初始化它</p><h3 id="函数和链接性">函数和链接性</h3><p>C++不允许在一个函数中定义另一个函数，因此所有的函数存储持续性自动为静态，在整个程序运行期间都存在。</p><p>默认情况下，函数的链接性为外部，即函数可以在文件当中共享（这一点与全局变量一致，加extern 就行）</p><p>当然，在函数前面加上 static后其链接性变为内部（在函数的声明和定义部分都要加），该函数只能在当前文件当中使用</p><p>与变量一样，<strong>链接性为内部的函数会遮掩链接性为外部的同名函数</strong>。</p><p>变量的单定义规则适用于<strong>非内联函数</strong>，即该函数只能有一个定义，可以有很多个声明（定义在源文件，声明在头文件，做法跟全局变量很像啊）</p><p>对于内联函数，<strong>单定义规则并不适用</strong>。因此我们可以<strong>将内联函数的定义放到头文件当中</strong>。这样，包含了所有头文件的源文件当中都会有一份内联函数的定义。需要注意的是，<strong>同一个内联函数的所有定义必须相同</strong>（一个内联函数可以写在不同的头文件当中，但这些定义<strong>必须</strong>相同）</p><h2 id="名称空间">名称空间</h2><p>在讨论名称空间之前，我们需要知道两个概念</p><ul><li>声明区域<ul><li>声明区域是可以声明该对象（可以是变量，也可以是函数）的区域</li><li>例如，全局变量的声明区域为其所在文件，局部变量的声明区域为其所在的代码块</li></ul></li><li>潜在作用域<ul><li>潜在作用域从其声明点开始，一直到声明区域的结尾。显然，潜在作用域的范围小于声明区域的范围</li></ul></li></ul><p>我们在上面提到过作用域的概念。实际上，<strong>变量对于程序而言是可见的范围称为作用域</strong>。因此，作用域的范围<strong>小于等于</strong>潜在作用域的范围。</p><p>C++新增加了这样一种功能，即通过定义一个新的声明区域来创建命名的名称空间，这样做的目的之一是提供了一个声明名称的区域。</p><p>一个名称空间的名称不会与另一个名称空间中的相同名称起冲突，同时允许程序的其他部分使用名称空间中声明的东西。</p><p><strong>名称空间可以是全局的，也可以声明在另一个名称空间内</strong>，但不能声明于代码块中。<strong>因此默认情况下，名称空间的链接性为外部</strong>（除非引用了常量）。</p><p>除了用户定义的名称空间外，还存在一个名称空间——全局名称空间。它对应文件级声明区域，全局变量会默认放在此名称空间内。</p><p>名称空间是开放的，<strong>既可以将名称加入到已有的名称空间内，也可以在文件后面或者另一个文件中再次使用该名称空间来提供相关的定义</strong>。</p><p>名称空间当中的名称，其<strong>存储持续性为静态</strong>，<strong>链接性为内部</strong>（只在名称空间内部可用）。也就是说，名称空间内部的<strong>变量</strong>一定会有<strong>零初始化</strong>。</p><h3 id="名称空间的使用">名称空间的使用</h3><p>未被装饰的名称（如 cout）称为未限定名称，被装饰的名称（如std::cout）称为限定名称。</p><p>我们不希望每次使用名称都对其进行限定，因此 C++提供了两种机制来简化名称空间的使用：</p><ul><li>using 声明</li></ul><p>using声明使特定的标识符可用，它可<strong>将特定的名称添加到它所属的声明区域中</strong>。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">namespace</span> Test<br>&#123;<br><span class="hljs-type">int</span> n;<br>&#125;<br><br><span class="hljs-type">int</span> n;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">using</span> Test::n;<span class="hljs-comment">//可用将 Test 添加到使用这条语句对应的声明区域中</span><br>n = <span class="hljs-number">10</span>;  <span class="hljs-comment">//定义 Test::n，这里的 n 会直接遮掩掉全局变量 n </span><br>::n = <span class="hljs-number">5</span>; <span class="hljs-comment">//定义全局变量 n</span><br>     <span class="hljs-comment">//直接以 :: 开头说明是在全局名称空间查找</span><br>cout &lt;&lt; n &lt;&lt; <span class="hljs-string">&quot; &quot;</span> &lt;&lt; ::n &lt;&lt; endl;<br><br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>相应地，在函数外面使用 <code>using Test::n</code>，可用将名称添加到全局名称空间中。</p><ul><li>using 编译指令</li></ul><p>using 编译指令使名称空间中所有地名称都可用。</p><p>在函数中使用 using 编译指令，使得该名称在函数当中可用</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">namespace</span> Test<br>&#123;<br><span class="hljs-type">int</span> n;<br>&#125;<br><br><span class="hljs-type">int</span> n;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> Test;<br><br>n = <span class="hljs-number">10</span>;<span class="hljs-comment">//这样子会起冲突</span><br><br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>但如果用 using 声明的话就没问题</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">using</span> Test::n;<br><br>n = <span class="hljs-number">10</span>;<span class="hljs-comment">//using Test::n insted of global n</span><br><br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>当然，这两种添加指令有可能会造成名称其冲突，这样的话还是老老实实地用限定名称吧</p><h3 id="两种导入方式的比较问题暂留">两种导入方式的比较（问题暂留）</h3><p>这个地方说实话我还不太懂，先留着</p><p>假设名称空间和声明区域定义了相同的名称。如果试图用 using声明来导入名称的话，会<strong>导致两个名称起冲突</strong>，从而出错。如果试图用using编译指令来导入名称的话，<strong>局部的名称会覆盖掉名称空间的名称</strong>。</p><hr><p>需要补充的是，未命名的名称空间中声明的名称就跟持续性为静态，链接性为内部的名称一样，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">static</span> <span class="hljs-type">int</span> count;<br><br><span class="hljs-comment">//等价于</span><br><span class="hljs-keyword">namespace</span><br>&#123;<br>    <span class="hljs-type">int</span> count;<span class="hljs-comment">//完全等价啊，这东西也是静态持续性，一定会有零初始化</span><br>&#125;<br></code></pre></td></tr></table></figure><h1 id="类继承">类继承</h1><p>派生类具有以下特征</p><ul><li>派生类对象存储了基类数据成员（<strong>派生类继承了基类的实现</strong>）</li><li>派生类对象可以使用基类的方法（<strong>派生类继承了基类的接口</strong>）</li></ul><p>派生类不能访问基类的 private成员，<strong>只能通过基类方法进行访问</strong>，这也是为什么派生类也会存在基类当中的private 成员</p><ul><li>基类当中的变量通常是 private权限，它们的赋值主要依靠对外提供的方法（构造函数等）。派生类继承过了之后也同样只能靠这些方法来进行访问这些private 权限的变量。</li><li><strong>派生类的构造函数必须使用基类的构造函数</strong>，通常是在派生类构造函数的初始化列表调用基类的构造函数。<ul><li>如果在派生类的构造函数（默认构造、有参构造、拷贝构造都一样）处没有调用基类的构造函数，那么编译器会<strong>自动调用基类的默认构造函数</strong></li></ul></li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Base</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Base</span>(<span class="hljs-type">int</span> rhs)<br>: <span class="hljs-built_in">x</span>(rhs)<br>&#123;<br><br>&#125;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> x;<br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base<br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Derived</span>(<span class="hljs-type">int</span> rh, <span class="hljs-type">int</span> ch)<br>: <span class="hljs-built_in">Base</span>(ch), <span class="hljs-built_in">a</span>(rh)<br>&#123;<br><br>&#125;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> a;<br>&#125;;<br></code></pre></td></tr></table></figure><p>对于一个基类及其派生类，我们通常会将<strong>它们的声明全部放在同一个头文件</strong>，<strong>它们的实现全部放在同一个源文件</strong></p><p>基类的指针或引用可以指向派生类对象，但是<strong>该指针或引用只能调用基类的方法</strong>，<strong>不能调用子类的方法</strong>（不涉及虚函数并且基类方法为public 权限）</p><ul><li>基于这一点，我们可以用派生类对象来对基类进行赋值</li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">Derived <span class="hljs-title">d</span><span class="hljs-params">(<span class="hljs-number">10</span>, <span class="hljs-number">5</span>)</span></span>;<br><span class="hljs-function">Base <span class="hljs-title">b</span><span class="hljs-params">(d)</span></span>;<br></code></pre></td></tr></table></figure><p>这是因为基类的拷贝构造函数的声明为：<code>Base(const Base&amp;)</code>，<strong>形参部分为基类的引用，它可以指向一个派生类的对象</strong>。在这里指向的派生类对象是d。<strong>因此可以通过派生类对象来对基类进行赋值</strong>（<em>copy</em>构造和 <em>copy assignment</em> 操作符都一样）</p><h2 id="静态联编和动态联编静态绑定和动态绑定">静态联编和动态联编（静态绑定和动态绑定）</h2><p>将源代码中的函数调用解释为执行特定的函数代码块被称为函数名联编</p><p>编译器对于非虚方法均是使用静态联编。使用静态联编时，通过该指针调用的函数<strong>就是它所定义的类型</strong>对应的函数（该指针是基类类型，那么它只能调用基类<strong>拥有</strong>的函数；该指针是子类类型，就只能调用子类<strong>拥有</strong>的函数），这在编译阶段就已经确定了</p><p>编译器对于虚方法则使用动态联编。使用动态联编时，通过该指针调用的函数<strong>跟指针所指的对象的类型有关</strong>，需要在程序运行时来确定该对象的类型</p><h2 id="虚函数工作原理">虚函数工作原理</h2><p>通常，编译器对于虚函数的处理方法是：会先给每个对象添加一个隐藏成员。隐藏成员是一个指向函数地址数组的指针，这个数组称为虚函数表（vbtl ）。vbtl 中存储了类当中声明的虚函数的地址。</p><p>例如，<strong>每个</strong>基类对象中包含一个指针，这个指针指向的数组包含了基类中所有虚函数的地址。<strong>每个</strong>派生类对象也会包含一个指向虚函数表的指针，不过这两个指针的值并不相同。</p><p>如果派生类从新定义了这个虚函数，那么会将新定义的虚函数的地址<strong>替换掉</strong>原来虚函数的地址；如果没有从新定义，则用原来的地址。</p><p>需要注意的是，无论有多少个虚函数，对象当中仅仅是添加了一个指针的大小，<strong>改变大小的是虚函数表</strong>。除此之外，这个虚函数表当中地址的排列是按照虚函数声明的顺序进行排列的。</p><p>对于虚函数，我们需要补充几点：</p><ul><li>构造函数不能是虚函数<ul><li>派生类是不会继承基类的构造函数的，所以在构造函数前面加 virtual没有意义</li></ul></li><li>析构函数在做基类的时候一定要加上 virtual</li><li>友元函数不能是虚函数，因为友元不是成员函数，只有成员函数才能是虚函数</li><li><strong>如果基类的虚函数的返回值是基类类型，那么对应的派生类的此函数的返回类型可以是派生类</strong>，这一例外只适用于返回值，不适用于参数</li><li>如果基类声明被重载了，那么派生类需要重新定义所有的基类函数</li></ul><h1 id="使用类">使用类</h1><h2 id="关于运算符重载的一点说明">关于运算符重载的一点说明</h2><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Test a, b;<br>Test c = a + b;<br><span class="hljs-comment">//等价于</span><br>Test c = a.<span class="hljs-built_in">operator</span>(b);<span class="hljs-comment">//其中 a 是调用对象， b 是作为参数传递的对象</span><br><span class="hljs-comment">//重载定义</span><br>Test <span class="hljs-keyword">operator</span>+(<span class="hljs-type">const</span> Test&amp; e1) <span class="hljs-type">const</span><br>&#123;<br>    Test tmp;<br>    <span class="hljs-comment">//相关定义</span><br>    <span class="hljs-keyword">return</span> tmp;<br>&#125;<br></code></pre></td></tr></table></figure><p>对于有两个操作数的运算符重载，运算符左边的是<strong>调用对象</strong>，右边的是作为<strong>参数传递的对象</strong>。</p><p>大多数运算符可以通过成员函数和全局函数进行重载，如：</p><p>成员函数：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Test</span>(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e1);<br><br>Test <span class="hljs-keyword">operator</span>+(<span class="hljs-type">const</span> Test&amp; e1);<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">print</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span>;<br><br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> num;<br>&#125;;<br><br>Test::<span class="hljs-built_in">Test</span>(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e1)<br>:<span class="hljs-built_in">num</span>(e1)<br>&#123;<br><br>&#125;<br><br>Test Test::<span class="hljs-keyword">operator</span>+(<span class="hljs-type">const</span> Test&amp; e1)<br>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">Test</span>(<span class="hljs-keyword">this</span>-&gt;num + e1.num);<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">Test::print</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; <span class="hljs-keyword">this</span>-&gt;num &lt;&lt; endl;<br>&#125;<br></code></pre></td></tr></table></figure><p>全局函数重载有一个问题是：一般来说我们类当中的成员属性是私有的，而用全局函数重载就属于类外访问。只能是类提供公共的接口供外部调用这些资源，或者为全局函数声明友元，<strong>不可以通过<code>operator</code>来隐式转换获取这些资源</strong>。下面就是个错误的例子：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Test</span>(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e1);<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">print</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span>;<br><br><span class="hljs-function"><span class="hljs-keyword">operator</span> <span class="hljs-title">int</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span>;<br><br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> num;<br>&#125;;<br><br>Test::<span class="hljs-built_in">Test</span>(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e1)<br>:<span class="hljs-built_in">num</span>(e1)<br>&#123;<br><br>&#125;<br><br><span class="hljs-function">Test::<span class="hljs-keyword">operator</span> <span class="hljs-title">int</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>-&gt;num;<br>&#125;<br><br>Test <span class="hljs-keyword">operator</span>+(<span class="hljs-type">const</span> Test&amp; e1, <span class="hljs-type">const</span> Test&amp; e2)<br>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">Test</span>(e1 + e2);<span class="hljs-comment">//通过隐式转换获取底层资源</span><br>&#125;<br></code></pre></td></tr></table></figure><p>这里原本的想法是 <code>e1</code> 和 <code>e2</code> 可以被隐式转换成<code>int</code> 类型，然后两个 <code>int</code> 类型进行相加最后再执行<code>Test</code> 的构造函数。</p><p>额。。。然后现实是 <code>e1</code> 与 <code>e2</code>并不会被隐式类型转换，而是会在这里一直调用 <code>operator+</code>进而导致死循环。</p><p>也就是说，通过全局函数重载运算符的话，<strong>一定要在类的类部提供外界可以访问其底层资源的方法</strong>，<strong>或者为该全局函数声明友元</strong>。</p><h3 id="运算符重载的限制">运算符重载的限制</h3><ol type="1"><li><p>重载后的运算符必须<strong>至少有一个操作数是用户定义的类型</strong>。</p><ul><li>比如对于一个特殊的自定义类型 <code>Test</code>，<code>operator+</code> 的实际意义是让两个 <code>Test</code>类型的对象相减。如果违背这一条，就相当于承认可以让两个 <code>int</code>类型的数据在相加时实际执行的操作是相减！！那不是乱套了吗</li></ul></li><li><p>使用重载运算符时<strong>不能违反该运算符原来的语法规则</strong>，相应地，<strong>运算符的优先级也不会改变</strong></p><ul><li>比如取模运算符 <code>%</code>的操作数必须是两个，你不能将该操作符重载成使用一个操作数<code>% 5</code> 。这是什么东西？？？</li></ul></li><li><p>不能创建新的运算符</p><ul><li>比如不能定义 <code>**</code> 为求幂的操作</li></ul></li><li><p>不能重载下面的运算符</p><ul><li><code>sizeof</code> 运算符</li><li><code>.</code> 类成员运算符</li><li><code>.*</code> 类成员指针运算符（我也不知道这是啥）</li><li><code>::</code> 作用域解析运算符</li><li><code>?:</code> 条件运算符</li><li><code>typeid</code> 一个 RTTI 运算符（求变量的类型，至于那个 RTTI，我也不知道是啥）</li><li><code>const_cast</code> 、 <code>dynamic_cast</code> 、<code>reinterpret_cast</code> 、 <code>static_cast</code>等强制类型转换运算符</li></ul></li><li><p>以下运算符只能在成员函数中重载</p><ul><li><p><code>=</code> 赋值运算符</p></li><li><p><code>()</code> 函数调用运算符</p></li><li><p><code>[]</code> 下标运算符</p></li><li><p><code>-&gt;</code> 指针访问类成员运算符</p></li></ul></li></ol><h2 id="友元">友元</h2><p>一般来说，对于 C++ 的类当中的 <code>private</code> 部分的访问只能通过<code>public</code> 方法这一种访问途径，这实在是有点太严格。因此 C++允许通过友元技术来对类当中 <code>private</code> 成员进行访问。</p><p>友元有三种，即：</p><ul><li>友元函数</li><li>友元类</li><li>友元成员函数</li></ul><h3 id="友元函数">友元函数</h3><p>通过让函数成为类的友元，<strong>可以赋予该函数拥有与类的成员函数一样的访问权限</strong>。</p><p>我们来看一个问题：如果我重载了 <code>operator*</code>使得一个自定义类型 <code>Test</code> 可以与一个 <code>int</code>类型相计算，并且返回一个 <code>Test</code> 类型，那么：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Test a;<br>Test b = a * <span class="hljs-number">10</span>;<span class="hljs-comment">//这没问题，等价于 </span><br><span class="hljs-comment">//Test b = a.operator(10);</span><br>Test c = <span class="hljs-number">10</span> * a;<span class="hljs-comment">//这样不行，因为运算符左边必须是调用对象，右边是作为参数传递的对象</span><br></code></pre></td></tr></table></figure><p>但直觉上第三种是没问题的，要解决这个问题，我们可以通过<strong>重载全局函数来解决</strong>。当然在这之前，就需要涉及到友元函数了。</p><p>创建友元函数的第一步是将<strong>该函数的原型（也就是声明）放在类的声明中，并在前面加上<code>friend</code> </strong>。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span><br>&#123;<br><span class="hljs-keyword">friend</span> Test <span class="hljs-keyword">operator</span>+(<span class="hljs-type">const</span> Test&amp; e1, <span class="hljs-type">const</span> Test&amp; e2);<span class="hljs-comment">//这玩意应该不需要访问权限</span><br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">Test</span>(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e1);<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">print</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span>;<br><br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> num;<br>&#125;;<br></code></pre></td></tr></table></figure><p>这意味着以下两点：</p><ul><li>该函数<strong>不是这个类的成员函数</strong>，因此<strong>不可以将它当中成员函数进行调用</strong></li><li>该函数<strong>虽然不是这个类的成员函数</strong>，但<strong>它拥有与这个类当中成员函数一样的访问权限</strong>。</li></ul><p>之后是编写定义。由于它<strong>不是成员函数</strong>，因此<strong>不能加类名限定符</strong>。另外，也不要使用<code>friend</code> 关键字。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Test <span class="hljs-keyword">operator</span>+(<span class="hljs-type">const</span> Test&amp; e1, <span class="hljs-type">const</span> Test&amp; e2)<br>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">Test</span>(e1.num + e2.num);<br>&#125;<br></code></pre></td></tr></table></figure><p>这样之后，刚刚那个问题也一并解决了。</p><p>除了用全局函数以外，我们依旧可以用友元函数来解决</p><p>我们刚刚在类内重载了 <code>operator*(int)</code>函数，这个函数可以应对<code>自定义类型 * int</code> 的情况。现在，我只需要在类外重载一个<code>operator*(int, const Test&amp;)</code> ，这样，便可以应当<code>int * 自定义类型</code> 这种情况了。</p><hr><p>这里我们来详细说一下两种方法的优缺点：</p><ul><li>全局函数重载<ul><li>这种依赖于隐式转换（如果函数的两个参数都是自定义类型的话），也就是说构造函数前面不能加<code>explicit</code></li><li>优点是工作较少，出错机会小</li><li>缺点是会增加内存开销</li></ul></li><li>类内函数加类外函数重载<ul><li>优点是运行速度更快</li><li>缺点是工作更多</li></ul></li></ul><p>如果程序使用这种运算符十分频繁，则应采取第二种。如果只是偶尔使用，则采用第一种。但为了保险，也最好使用第一种（毕竟不能关掉隐式类型转换）。</p><h3 id="重载-运算符">重载 &lt;&lt; 运算符</h3><p>对于一个自定义类型，我们可以通过重载 &lt;&lt;运算符来实现对该类型的输出，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TP</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">TP</span>(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e1, <span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e2)<br>: <span class="hljs-built_in">rh</span>(e1), <span class="hljs-built_in">ch</span>(e2), <span class="hljs-built_in">Close</span>(<span class="hljs-literal">false</span>)<br>&#123;  &#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">re_rh</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123; <span class="hljs-keyword">return</span> rh; &#125;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">re_ch</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123; <span class="hljs-keyword">return</span> ch; &#125;<br><br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> rh, ch;<br><span class="hljs-keyword">mutable</span> <span class="hljs-type">bool</span> Close;<br><br><span class="hljs-keyword">friend</span> ostream&amp; <span class="hljs-keyword">operator</span>&lt;&lt;(ostream&amp; os, <span class="hljs-type">const</span> TP&amp; rhs);<br>&#125;;<br><br>ostream&amp; <span class="hljs-keyword">operator</span>&lt;&lt;(ostream&amp; os, <span class="hljs-type">const</span> TP&amp; rhs)<br>&#123;<br>os &lt;&lt; rhs.rh &lt;&lt; <span class="hljs-string">&quot; &quot;</span> &lt;&lt; rhs.ch &lt;&lt; endl;<br><span class="hljs-keyword">return</span> os;<br>&#125;<br></code></pre></td></tr></table></figure><p>这个重载函数只能是全局的。如果该函数是局部的，那么它只能通过对象（假设trip 是一个 TP对象）来调用左移操作符，这就会出现<code>trip &lt;&lt; cout</code>这样显然是有问题的，因此我们必须用全局函数来重载。这样的话就势必会涉及友元。</p><p>你可能会有一个问题是：为什么友元的声明是<strong>在自定义的类当中的</strong>，<code>ostream</code> 类当中不需要吗？</p><p>emmm， 如果每一个类都需要去 <code>ostream</code>当中声明一个友元函数的话，这将会是一件风险极大的事情（因为你改变了标准库）</p><p>实际上，我们<strong>声明友元的目的是为了访问这个类当中<code>private</code> 作用域下的成员</strong>，而对于<code>ostream</code>类而言，我们是将它<strong>作为一个整体来使用的，因此不需要声明友元</strong>。</p><p>还有一个问题是：当我调用 <code>cout &lt;&lt; trip</code>时，我调用的是 <code>cout</code>对象本身的引用，<strong>而不是它的拷贝</strong>，因此<strong>在形参部分要加上引用</strong>。</p><p>由于 <code>ofstream</code> 类是 <code>ostream</code>类的派生类，因此也可以将 trip 输出得到文件上，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp">ofstream ofs;<br>ofs.<span class="hljs-built_in">open</span>(...);<br>ofs &lt;&lt; trip;<br></code></pre></td></tr></table></figure><p>这么写不会有任何的问题，因为<strong>基类引用的形参也愿意接受一个派生类的对象</strong>。</p><h3 id="类的自动转换和强制类型转换">类的自动转换和强制类型转换</h3><p>我们先来复习一下 C++ 是如何处理内置类型转换的。</p><p>当一个标准类型的变量赋值给另一个标准类型的变量时，如果二者的<strong>类型兼容</strong>，则C++ 将<strong>自动</strong>将这个值的类型转换成接收变量的类型。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">long</span> a = <span class="hljs-number">8</span>;<span class="hljs-comment">//int to long</span><br><span class="hljs-type">double</span> b = <span class="hljs-number">10</span>;<span class="hljs-comment">//int to double</span><br><span class="hljs-type">int</span> c = <span class="hljs-number">3.14</span>;<span class="hljs-comment">//double to int</span><br></code></pre></td></tr></table></figure><p>我们不管是否会降低精度，<strong>这些都属于自动类型转换</strong>。</p><p>C++ 不会自动转换类型不兼容的变量，例如</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span>* a = <span class="hljs-number">10</span>;<br></code></pre></td></tr></table></figure><p>这个语句是非法的，因为左边是指针类型，右边是整型，二者类型并不兼容。然而，<strong>在无法自动转换时</strong>，我们可以<strong>使用强制类型转换</strong>。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span>* a = (<span class="hljs-type">int</span>*)<span class="hljs-number">10</span>;<br></code></pre></td></tr></table></figure><p>这个语句是否有意义暂且不谈，但它是合法的。这便是 C++关于内置数据类型的转换（当然还有一些关键字，不过那个我们暂且不谈）。</p><p>下面，我们来看自定义数据类型的转换。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TP</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><br><span class="hljs-built_in">TP</span>()<br>:<span class="hljs-built_in">rh</span>(<span class="hljs-number">0</span>), <span class="hljs-built_in">ch</span>(<span class="hljs-number">0</span>), <span class="hljs-built_in">Close</span>(<span class="hljs-literal">false</span>)<br>&#123;  &#125;<br><span class="hljs-built_in">TP</span>(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e1, <span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e2 = <span class="hljs-number">0</span>)<br>: <span class="hljs-built_in">rh</span>(e1), <span class="hljs-built_in">ch</span>(e2), <span class="hljs-built_in">Close</span>(<span class="hljs-literal">false</span>)<br>&#123;  &#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">re_rh</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123; <span class="hljs-keyword">return</span> rh; &#125;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">re_ch</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123; <span class="hljs-keyword">return</span> ch; &#125;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">print</span><span class="hljs-params">()</span> <span class="hljs-type">const</span></span>;<br><br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> rh, ch;<br><span class="hljs-keyword">mutable</span> <span class="hljs-type">bool</span> Close;<br><br><span class="hljs-keyword">friend</span> ostream&amp; <span class="hljs-keyword">operator</span>&lt;&lt;(ostream&amp; os, <span class="hljs-type">const</span> TP&amp; rhs);<br>&#125;;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>TP t1;<br>t1 = <span class="hljs-number">2</span>;<span class="hljs-comment">//看这里</span><br><br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>构造函数当中，有一个参数有默认值，因此可以认为该构造函数只有一个参数。而我们可以通过该构造函数将<code>int</code> 类型的数据转换为自定义类型的数据</p><p>在 C++中，接收一个参数的构造函数可以将「<strong>类型与构造函数参数相同</strong>的值」转化为类的类型。</p><p>也就是说，当我写出：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp">TP t1;<br>t1 = <span class="hljs-number">2</span>;<br></code></pre></td></tr></table></figure><p>程序将采用<strong>构造函数<code>TP(const int&amp; e1, const int&amp; e2 = 0)</code>来创建一个临时的对象</strong>（其内部属性为<code>rh = 2, ch = 0</code>）。然后再<strong>通过 <em>copy</em>构造函数来将该对象赋值给 <code>t1</code></strong>。这一过程被称为<strong>隐式类型转换</strong>。</p><p><strong>只有接收一个参数的构造函数</strong>才可以用于隐式类型转换，接收两个参数的不行。</p><ul><li>补充：<ul><li>如果函数声明为<code>TP(const int&amp; e1, const int&amp; e2)</code>，那么我们可以通过<code>t1 = &#123; 2, 0 &#125;</code>来达到与上面一样的效果，其过程也是一样（先创建临时变量，再通过<em>copy</em>构造函数进行赋值）。但这<strong>不能称为隐式类型转换</strong>。</li><li>在这里我们是为第二个参数添加了默认值，因此可以认为该构造函数<strong>只有一个参数</strong>。</li></ul></li></ul><p>我们依旧用 <code>TP(const int&amp; e1, const int&amp; e2 = 0)</code>来进行讨论。这种隐式类型转换有时会带来不好的结果，因此 C++提供了一个关键字 <code>explicit</code>来关闭掉这种隐式类型转换。即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">explicit</span> <span class="hljs-title">TP</span><span class="hljs-params">(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e1, <span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e2 = <span class="hljs-number">0</span>)</span></span><br><span class="hljs-function">: rh(e1), ch(e2), Close(false)</span><br><span class="hljs-function">&#123;</span>  &#125;<br></code></pre></td></tr></table></figure><p>这样过后，就不能通过隐式类型转换来转换类型，但依旧<strong>允许显示类型转换</strong>，即<strong>允许显示强制类型转换</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp">TP t1;<br>t1 = <span class="hljs-number">2</span>;<span class="hljs-comment">//Not Allow</span><br>t1 = <span class="hljs-built_in">TP</span>(<span class="hljs-number">2</span>);<span class="hljs-comment">//OK</span><br>t1 = (TP)<span class="hljs-number">2</span>;<span class="hljs-comment">//OK</span><br></code></pre></td></tr></table></figure><p><strong>后面两个都属于强制类型转换！！！</strong></p><h4 id="关于隐式类型转换的分析重点">关于隐式类型转换的分析（重点）</h4><p>这里我重申一点：<strong>当构造函数只接受一个参数时，它是可以通过隐式类型转换来对自定义类型进行赋值的</strong>。</p><p><strong>在 <code>TP(const int&amp; e1, const int&amp; e2 = 0)</code>下，有：</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">TP <span class="hljs-title">t1</span><span class="hljs-params">(<span class="hljs-number">2</span>)</span></span>;<br></code></pre></td></tr></table></figure><ul><li>上面这是隐式类型转换，通过将 2 这个整型<strong>隐式</strong>转换成TP 类型。</li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">TP t1 = <span class="hljs-number">2</span>;<br></code></pre></td></tr></table></figure><ul><li>这个跟上面一样，都是将整型<strong>隐式</strong>类型转换成<strong>自定义类型</strong>。</li></ul><p>这两个的过程是：先创建一个临时对象，然后再通过 <em>copy</em>构造函数来对 <code>t1</code> 进行赋值</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">TP t1 = <span class="hljs-built_in">TP</span>(<span class="hljs-number">2</span>);<br></code></pre></td></tr></table></figure><ul><li>这是<strong>显示强制类型转换</strong></li></ul><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">TP t1 = (TP)<span class="hljs-number">2</span>;<br></code></pre></td></tr></table></figure><ul><li>这个也是显示强制类型转换</li></ul><p>这两个的过程跟上面一样。</p><p>我通常写的 <code>TP t1(2)</code>，这个玩意叫<strong>直接初始化</strong>，这会调用它的构造函数，<strong>属于显示类型转换</strong>，<strong>不是隐式类型转换</strong>。</p><p>如果是在 <code>TP(const int&amp; e1, const int&amp; e2)</code>下，那么将<strong>不存在隐式类型转换</strong>，而<strong>显示强制类型转换跟原来的相同</strong>。</p><hr><p>题外话说完了，我们接着刚才的。也就是说，如果在构造函数<code>TP(const int&amp; e1, const int&amp; e2 = 0)</code> 前面声明了<code>explicit</code>，那么该构造函数将<strong>只能用于显示强制类型转换</strong>，否则依旧可以使用下面的隐式类型转换：</p><ul><li>将 TP 对象初始化为 <code>int</code> 值</li><li>将 <code>int</code> 值赋给 TP 对象<ul><li><code>TP t1 = 10;</code></li></ul></li><li>将 <code>int</code> 值传递给接受 TP 类型参数的函数<ul><li>假定我们有一个函数，其声明为<code>void fun(const TP&amp; e1)</code></li><li>那么我在调用 <code>fun(10)</code>时，这个时候就会发生隐式类型转换</li></ul></li><li>返回值被声明为 TP 的函数试图返回 <code>int</code> 类型<ul><li>依旧假定我们有一个函数，其声明为 <code>TP fun()</code></li><li>在函数体内，我们写出<code>return rhs; //rhs 是 int 类型的变量</code>时，会发生隐式类型转换</li><li>这是因为需要调用构造函数将 <code>int</code> 转化为 TP类型的对象</li></ul></li><li>在上述任何一种情况下，<strong>使用可以通过自动类型转换变为<code>int</code> 的类型</strong><ul><li><code>double</code> 可以通过自动类型转换成 <code>int</code>，那么上述的所有情况对 <code>double</code> 也同样适用</li></ul></li></ul><p>关于最后一点，我们需要进行一点说明。</p><p>上面的函数原型是<code>TP(const int&amp; e1, const int&amp; e2 = 0)</code>，如果我还写了一个另一个构造函数，只是把 <code>int</code> 改为了<code>long</code> ，其余不变，即<code>TP(const long&amp; e1, const long&amp; e2 = 0)</code> 。</p><p>那么当我写 <code>TP t1 = 2.5</code> 时，编译器会报错。这是因为<code>double</code> 类型既可以转换成 <code>int</code> 也可以转化成<code>long</code> ，这就出现了二义性。因此下面的代码会报错</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TP</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><br><span class="hljs-built_in">TP</span>()<br>:<span class="hljs-built_in">rh</span>(<span class="hljs-number">0</span>), <span class="hljs-built_in">ch</span>(<span class="hljs-number">0</span>), <span class="hljs-built_in">Close</span>(<span class="hljs-literal">false</span>)<br>&#123;  &#125;<br><span class="hljs-built_in">TP</span>(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e1, <span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; e2 = <span class="hljs-number">0</span>)<br>: <span class="hljs-built_in">rh</span>(e1), <span class="hljs-built_in">ch</span>(e2), <span class="hljs-built_in">Close</span>(<span class="hljs-literal">false</span>)<br>&#123;  &#125;<br><br><span class="hljs-built_in">TP</span>(<span class="hljs-type">const</span> <span class="hljs-type">long</span>&amp; e1, <span class="hljs-type">const</span> <span class="hljs-type">long</span>&amp; e2 = <span class="hljs-number">0</span>)<br>:<span class="hljs-built_in">rh</span>(e1), <span class="hljs-built_in">ch</span>(e2), <span class="hljs-built_in">Close</span>(<span class="hljs-literal">false</span>)<br>&#123;  &#125;<br><br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> rh, ch;<br><span class="hljs-keyword">mutable</span> <span class="hljs-type">bool</span> Close;<br><br>&#125;;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>TP t1 = <span class="hljs-number">2.5</span>;<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="转换函数">转换函数</h3><p>在 C++当中，一个参数的构造函数<strong>定义了从某种类型到「类类型」的转换</strong>，如果需要进行相反类型的转换则需要通过<strong>转换函数</strong>。</p><p>转换函数的语法为：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-keyword">operator</span> <span class="hljs-title">typeName</span><span class="hljs-params">()</span></span>;<br></code></pre></td></tr></table></figure><p>有几点需要注意的是：</p><ul><li>转换函数必须是类方法</li><li>转换函数不能有返回值</li><li>转换函数不能有参数</li></ul><p><code>typeName</code> 指定了要转换成的类型，可以是<code>int</code>、<code>double</code> 等内置数据类型，当然也可以是自定义数据类型。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TP</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">TP</span>(<span class="hljs-type">const</span> <span class="hljs-type">double</span>&amp; rh)<br>:<span class="hljs-built_in">n</span>(rh)<br>&#123;  &#125;<br><br><span class="hljs-function"><span class="hljs-keyword">operator</span> <span class="hljs-title">int</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> n;<br>&#125;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">double</span> n;<br>&#125;;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>cout &lt;&lt; <span class="hljs-built_in">int</span>(<span class="hljs-number">12.6</span>) &lt;&lt; endl;<br><span class="hljs-function">TP <span class="hljs-title">t</span><span class="hljs-params">(<span class="hljs-number">12.6</span>)</span></span>;<br>cout &lt;&lt; <span class="hljs-built_in">int</span>(t) &lt;&lt; endl;<br><span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>转换函数实际上就是用户定义的强制类型转换</strong>，即：<strong>它是强制类型转换！！！</strong></p><p>因此<strong>强制类型转换的语法可以用在它的身上</strong>，它可以将类类型转换为用户定义的类型。</p><p>如果在类当中加一个转换函数，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TP</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">TP</span>(<span class="hljs-type">const</span> <span class="hljs-type">double</span>&amp; rh)<br>:<span class="hljs-built_in">n</span>(rh)<br>&#123;  &#125;<br><br><span class="hljs-function"><span class="hljs-keyword">operator</span> <span class="hljs-title">int</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> n;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">operator</span> <span class="hljs-title">double</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> n;<br>&#125;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">double</span> n;<br>&#125;;<br></code></pre></td></tr></table></figure><p>当我写出</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">cout &lt;&lt; t &lt;&lt; endl;<br></code></pre></td></tr></table></figure><p>由于没有重载右移操作符，并且定义了两个转换函数，使得可以将<code>TP</code> 类型转换为 <code>int</code> 或 <code>double</code>，那么编译器将会报错，因为这里出现了二义性的转换。</p><p>在这种情况下，当我写出</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">long</span> a = t;<br></code></pre></td></tr></table></figure><p>由于 <code>TP</code> 可以被转成 <code>int</code> 或<code>double</code> ，而这两个内置数据类型都可以转成 <code>long</code>，因此在这里编译器也会报错。</p><p>当然，我前面说过。这种转换函数的语法等同于强制类型转换的语法，因此你可以通过这种方式来<strong>指定</strong>使用哪个转换函数，即</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">long</span> a = <span class="hljs-built_in">int</span>(t);<br><span class="hljs-type">long</span> b = <span class="hljs-built_in">double</span>(t);<br></code></pre></td></tr></table></figure><p>这两个都可以通过编译。</p><p>这种自动类型转换有时会发生不好的事情，</p><p>例如，你可能会将上面的 <code>TP</code>类型的对象作为数组下标使用（我知道你脑子清醒的时候不会，但说不定你那天头昏呢），因为你声明了这种隐式类型转换，它将自动转换为<code>int</code>，但这很明显是错误的。要关掉它也很简单，在该函数前面加上<code>explicit</code> （这一点在 C++98 是不允许的，但 C++11则没有这种限制），即</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TP</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">TP</span>(<span class="hljs-type">const</span> <span class="hljs-type">double</span>&amp; rh)<br>:<span class="hljs-built_in">n</span>(rh)<br>&#123;  &#125;<br><br><span class="hljs-function"><span class="hljs-keyword">explicit</span> <span class="hljs-keyword">operator</span> <span class="hljs-title">int</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> n;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">explicit</span> <span class="hljs-keyword">operator</span> <span class="hljs-title">double</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> n;<br>&#125;<br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">double</span> n;<br>&#125;;<br></code></pre></td></tr></table></figure><p>用了之后，<strong>便不会再出现隐式调用，需要通过显示强制调用才行</strong>。</p><p>总之，C++ 为类提供了两种类型转换：</p><ul><li>构造函数用于将「类型与该参数相同的值」转换为类类型</li><li>转换函数用于将类类型转换为其他类型</li></ul><h3 id="转换函数和友元函数">转换函数和友元函数</h3><p>我们来看一个《简单》的例子（嗯，带简单两个字的往往学起来不简单）</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TP</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">TP</span>(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; rh)<br>:<span class="hljs-built_in">n</span>(rh)<br>&#123;  &#125;<br><br><br>TP <span class="hljs-keyword">operator</span>+(<span class="hljs-type">const</span> TP&amp; t)<br>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">TP</span>(<span class="hljs-keyword">this</span>-&gt;n + t.n);<br>&#125;<br><br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> n;<br>&#125;;<br></code></pre></td></tr></table></figure><p>注意：这里面没有写 <code>operator int</code> 转换函数</p><p>好，然后当你写下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">TP <span class="hljs-title">t1</span><span class="hljs-params">(<span class="hljs-number">10</span>)</span></span>;<br><span class="hljs-type">int</span> a = <span class="hljs-number">20</span>;<br>TP t3 = t1 + a;<br></code></pre></td></tr></table></figure><p>然后，如果你提供了友元全局函数重载的话，就允许这么做：（成员函数重载和友元函数重载只能存在一个）</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp">TP t3 = a + t1;<br></code></pre></td></tr></table></figure><p>当然，这里的故事我们前面已经说过了（操作符左边的为调用函数的对象，右边为作为参数传递的对象），并且解决办法在前面也有讨论（在友元函数 那里）。</p><p>下面是重头戏！！！</p><p>我们要再此基础上加上转换函数，即：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">TP</span><br>&#123;<br><span class="hljs-keyword">public</span>:<br><span class="hljs-built_in">TP</span>(<span class="hljs-type">const</span> <span class="hljs-type">int</span>&amp; rh)<br>:<span class="hljs-built_in">n</span>(rh)<br>&#123;  &#125;<br><br><span class="hljs-function"><span class="hljs-keyword">operator</span> <span class="hljs-title">int</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br><span class="hljs-keyword">return</span> n;<br>&#125;<br><br>TP <span class="hljs-keyword">operator</span>+(<span class="hljs-type">const</span> TP&amp; t)<br>&#123;<br><span class="hljs-keyword">return</span> <span class="hljs-built_in">TP</span>(<span class="hljs-keyword">this</span>-&gt;n + t.n);<br>&#125;<br><br><span class="hljs-keyword">private</span>:<br><span class="hljs-type">int</span> n;<br>&#125;;<br></code></pre></td></tr></table></figure><p>当我写出：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">TP <span class="hljs-title">t1</span><span class="hljs-params">(<span class="hljs-number">10</span>)</span></span>;<br><span class="hljs-type">int</span> a = <span class="hljs-number">20</span>;<br>TP t3 = t1 + a;<br></code></pre></td></tr></table></figure><p>由于可以<strong>将 <code>TP</code> 对象转换为 <code>int</code>，也可以将 <code>int</code> 类型转换为 <code>TP</code></strong>。因此这里再次出现了二义性</p><p>解决办法也很简单，<strong>只</strong>需要为<strong>转换函数</strong>声明一个<code>explicit</code> 就行。这样就只能是 <code>int</code> 转换为<code>TP</code> 。</p><p>注意，这里不能在构造函数前面加上 <code>explicit</code>，因为这样只是不允许将 <code>int</code> 类型转换为 <code>TP</code>。因此 <code>a</code> 将没办法转换为 <code>TP</code>类型，自然也不能参加加法运算，所以会导致报错。</p>]]></content>
    
    
    <categories>
      
      <category>C++ Primer Plus</category>
      
    </categories>
    
    
    <tags>
      
      <tag>C++ Primer Plus</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
